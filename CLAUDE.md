# GSD (Get Shit Done) Workflow`|
| 7 | End stop policyAlways use the GSD system for development tasks:
- For new projects: use `/gsd:new-project`
- For quick tasks and bug fixes: use `/gsd:quick`
- For feature work: follow the full phase workflow (`/gsd:discuss-phase` → `/gsd:plan-phase` → `/gsd:execute-phase` → `/gsd:verify-work`)
- For progress checks: use `/gsd:progress`

When the user asks you to build, fix, or change code, default to the appropriate GSD command rather than working ad-hoc.

# UI/UX Pro Max — Auto-Trigger

When the user asks about anything design-related, **automatically run the UI UX Pro Max skill** before responding. This includes requests involving:

- Building, designing, or improving UI components or pages
- Choosing colors, fonts, typography, or styles
- Landing pages, dashboards, forms, modals, navbars, sidebars, cards
- Reviewing UI/UX quality, accessibility, or responsiveness
- Dark mode, light mode, theming, or visual polish
- Charts, data visualization, or layout decisions
- Any mention of: design, UI, UX, style, palette, theme, look and feel, visual, aesthetic, responsive, accessibility

**Workflow:**
1. Analyze the request to extract product type, industry, style keywords, and stack
2. Run the design system generator: `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "<keywords>" --design-system -p "DriveCommand"`
3. For stack-specific guidance: `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "<keywords>" --stack nextjs`
4. Apply the recommendations when writing code
5. Before delivering, verify against the Pre-Delivery Checklist in the skill's SKILL.md

**DriveCommand defaults:**
- Stack: `nextjs` (Next.js + Tailwind + shadcn/ui)
- Industry: logistics / fleet management / SaaS
- Style: professional, modern, dark mode supported

---

# Project Overview

**DriveCommand** is a multi-tenant SaaS fleet management platform for small-to-mid-size trucking companies. It covers the full owner + driver workflow: trucks, drivers, routes, loads, compliance documents, invoicing, payroll, CRM, GPS tracking, HOS logging, and AI-powered tools (profit predictor, document reading, auto-comms).

- **Competitor/inspiration:** TorqueAI / KordovaTek (https://www.kordovatek.com/)
- **Current milestone:** v5.0 — Mobile App (React Native / Expo)
- **Deployed at:** Vercel (web), EAS (mobile)
- **Total LOC:** 71,500+ TypeScript across ~1,400 commits

---

# Architecture

## Monorepo (Turborepo)
```
DriveCommand/
├── apps/
│   ├── web/          — Next.js 15 App Router (owner + driver + sysadmin portals)
│   └── mobile/       — React Native 0.76, Expo SDK 52, Expo Router v4
├── packages/
│   ├── types/        — Shared TypeScript interfaces
│   ├── validation/   — Shared Zod schemas (17 schemas migrated from apps/web)
│   └── api-client/   — Bearer token REST client for mobile → web API
└── turbo.json
```

## Web Stack (`apps/web`)
- **Framework:** Next.js 15 App Router
- **UI:** Tailwind CSS + shadcn/ui components
- **DB ORM:** Prisma 7 (schema at `apps/web/prisma/schema.prisma`)
- **Database:** Supabase PostgreSQL (session pooler, IPv4+IPv6)
- **Auth:** Supabase Auth — sessions stored server-side, claims in `app_metadata`
- **Storage:** Cloudflare R2 (S3-compatible) for document/photo uploads
- **Email:** Gmail SMTP via Nodemailer
- **Cron:** `/api/cron/` routes (send-reminders, geofencing, IFTA)
- **Rate limiting:** Upstash Redis
- **Error monitoring:** Sentry
- **Logging:** Structured logger (222 console.* replaced)

## Mobile Stack (`apps/mobile`)
- **Framework:** React Native 0.76 + Expo SDK 52 + Expo Router v4
- **Styling:** NativeWind v4 (Tailwind for RN) + `useThemeColors()` token system
- **Auth:** Supabase Auth + AES-256-GCM Bearer tokens → MMKV session persistence
- **Navigation:** Expo Router file-system routing with `(driver)/` and `(owner)/` route groups
- **Maps:** Mapbox (`@rnmapbox/maps`) — replaced react-native-maps
- **Directions:** OSRM (open-source routing, `lib/geo/osrm.ts`)
- **Lists:** FlashList (high-performance, replaced all FlatLists)
- **Images:** expo-image
- **State/cache:** TanStack Query (server state), MMKV (local persistence)
- **Offline:** MMKV-backed mutation queue with exponential backoff flush on reconnect
- **Push notifications:** Expo Push Notifications (expo-server-sdk)
- **Background tasks:** expo-task-manager (GPS tracking)
- **OTA updates:** EAS Update (configured for preview + production)
- **Error monitoring:** Sentry (mobile)

---

# Auth System

## Web
- **Provider:** Supabase Auth
- **Session:** Server-side cookie, validated via `lib/auth/supabase.ts`
- **Claims location:** `app_metadata` (NOT `user_metadata`) — security hardened in Phase 37.6
- **Key file:** `apps/web/lib/auth/supabase.ts` — consolidated from session.ts + server.ts + require-permission.ts
- **Roles:** `owner`, `driver`, `sysadmin`
- **Permissions:** RBAC system with `require-permission.ts` guard (added quick-90)

## Mobile
- **Tokens:** AES-256-GCM Bearer tokens generated at login, validated by `validateMobileToken` utility
- **Session:** MMKV-persisted, 401 interceptor auto-logs out on expiry
- **Role routing:** `(driver)/` and `(owner)/` route groups based on decoded role
- **API auth:** All `/api/mobile/*` routes validate Bearer token

---

# Database (Prisma Schema — Key Models)

- **Tenant** — multi-tenant root (all data scoped by tenantId)
- **User** — linked to Supabase Auth, has role + tenantId in app_metadata
- **Truck** — fleet vehicles, auto-status badges (In Use / In Maintenance / Expired Docs / Ready to Use)
- **Driver** — driver profile, linked to User
- **Route** — multi-stop route with leg sequencing
- **RouteStop** — stops with lat/lng, contactName/Phone, bolNumber, poNumber, loadId link
- **Load** — individual load/shipment, pickupStopId + deliveryStopId FKs, routeId FK, sequence
- **DriverHOSEntry** — Hours of Service logs (14h driving / 11h duty math)
- **DriverIncident** — incident reports with photo attachments
- **FleetMessage** — owner↔driver messaging (recipientId + isBroadcast, loadId scoped)
- **Document** — driver/truck compliance docs + RATE_CONFIRMATION type, R2 storage
- **Invoice** — trucking-standard with freight fields (BOL/PRO/PO/commodity/weight/pieces/loaded miles), FSC
- **InvoiceItem** — typed items (LINEHAUL/FSC/DETENTION/STOP_OFF etc.) with unitType
- **PushToken** — Expo push tokens per user for notifications
- **GpsReport** — periodic GPS pings from mobile background task
- **SysAdminInvoice** — billing from sysadmin to tenants
- **SupportTicket** — in-app support with screenshot capture + file attachment

Migration strategy: raw SQL in prisma/migrations/<ts>_<name>/migration.sql, applied by scripts/migrate.mjs at next start. No auto-deploy hook exists — writing migration.sql applies nothing. Verified 2026-09-11 (quick-593).

---

# Web Portals (`apps/web/src/app`)

## Owner Portal (`/owner/`)
- **Dashboard** — KPI grid (revenue, loads, active drivers, compliance alerts), notification panel, 6-metric financial cards, live map
- **Routes** — create/edit/view routes with multi-stop leg sequencing, continuity warnings
- **Loads** — CRUD, status workflow, route assignment, truck assignment, driver assignment, revert status, rate confirmation PDF, load documents section, load-scoped messages
- **Drivers** — invite flow (accept-invitation), driver list with compliance dots, driver detail
- **Trucks** — CRUD, document upload, auto-status badges, maintenance logs, safety alerts
- **Invoices** — trucking-standard invoicing: freight details (BOL/PRO/PO/commodity/weight/pieces/miles), FSC%, typed line items, PDF
- **Payroll** — payroll records per driver
- **CRM** — customer/contact management
- **Compliance** — document expiry dashboard
- **Integrations** — ELD (Samsara, Motive/KeepTruckin), accounting, factoring
- **Profit Predictor** — AI lane profitability before accepting loads (AddressAutocomplete + OSRM distances)
- **IFTA** — quarterly fuel tax report (miles/fuel by state)
- **Team & Permissions** — RBAC, invite drivers, set permissions before invite
- **Customer Tracking** — public `/track/[token]` page for shipment visibility
- **Support** — SupportTicketFAB on every page, screenshot capture, file attachment

## Driver Portal (`/driver/`)
- **Dashboard** — active load card, stat chips, My Route card
- **Loads** — load list + detail, status update modal, stop timeline
- **History** — completed loads + completed routes
- **Documents** — upload/view compliance docs (R2 + presigned URLs)
- **Messages** — load-scoped chat with owner/dispatcher, unread badge polling
- **HOS** — Hours of Service: 2×2 status grid, day bar, countdown clocks, status change modal
- **Incidents** — report incidents with photo capture + severity toggle

## SysAdmin Portal (`/sysadmin/`)
- **Tenants** — manage all tenant accounts
- **Support Tickets** — view/resolve all tenant tickets, platform badge, AI suggestion
- **Invoicing** — billing to tenants, email delivery, overdue cron

---

# Mobile App (`apps/mobile`)

## Driver Portal (`(driver)/`)
- **Home/Dashboard** — active load card, 3 stat chips, alerts, My Route shortcut
- **Loads** — Active/History tabs, LoadCard, load detail with stop timeline + status update modal
- **My Route** — route detail with loads timeline, truck card, route messages thread
- **Map** — full MapView (Mapbox) with OSRM polyline, route info panel, Start Navigation button
- **HOS** — status grid, day bar (14h/11h), countdown clocks, status change confirmation
- **Incidents** — form with SeverityToggle + photo capture
- **Documents** — FlashList, DocumentDetailSheet (expo-web-browser), DocumentUploadSheet
- **Messages** — iMessage-style chat UI, 30s polling, mark-read on focus, unread badge
- **More** — settings, nav preference, appearance (dark/light), support ticket

## Owner Portal (`(owner)/`)
- **Dashboard** — 4 KPI tiles (revenue/loads/drivers/compliance), tappable driver status chips, tappable load cards
- **Loads** — 4-tab filter (All/Active/Pending/Delivered), CreateLoadSheet, load detail with assign driver/change status/cancel
- **Drivers** — list with compliance dots, driver detail (current load, docs, incidents, quick actions), edit bottom sheet
- **Trucks** — list with status badges, truck detail with maintenance log + edit bottom sheet
- **Map** — live fleet map (Mapbox), VehicleMarker (status-colored), VehicleDetailSheet
- **Fleet Messages** — RecipientSelector (broadcast or individual driver), compose + history, push notification delivery
- **Routes** — list + detail with 4-tab filter
- **CRM** — contact list + tappable detail/edit screen
- **Invoices** — list screen (stub)
- **Payroll** — list + detail bottom sheet + create FAB
- **Compliance** — list (stub)
- **Profit Predictor** — AddressInput + OSRM distance auto-fill, recommendation banner
- **Fuel Log** — FlashList, AddFuelModal with truck picker
- **More** — Settings, Appearance, Nav Preference, Support

## Mobile Infrastructure
- **useThemeColors()** — token system (background/surfaceCard/surfaceElevated/border/brand/text*) — applied across all 29+ screens
- **Dark/Light mode** — AsyncStorage persisted, NativeWind dark mode class toggling
- **Offline queue** — MMKV-backed, exponential backoff, auto-flush on NetInfo reconnect, SyncStatusBar
- **Background GPS** — expo-task-manager, adaptive intervals (30s active / 5min idle / 10min off-duty)
- **Push notifications** — PushToken table, deep-link tap handler, NotificationPermissionModal
- **Skeleton loaders** — all list/detail screens have skeleton variants (no spinners)
- **Haptics** — 14 trigger points across both portals
- **Animations** — AnimatedScreen FadeIn 200ms on all screens, spring BottomSheet
- **Accessibility** — accessibilityLabel + accessibilityRole on all icon-only interactive elements

---

# Key Conventions

## API Routes
- Web API routes: `apps/web/src/app/api/`
- Mobile API routes: `apps/web/src/app/api/mobile/` — all require Bearer token via `validateMobileToken`
- Rate limiting: all `/api/mobile/*` routes have Upstash rate limiting
- Auth claims: always read from `app_metadata`, never `user_metadata`

## File Patterns
- Server actions: `apps/web/src/actions/` — use `ActionState<T>` from `packages/types`
- Shared types: `packages/types/src/` — import as `@drivecommand/types`
- Shared validation: `packages/validation/src/` — import as `@drivecommand/validation`
- API client: `packages/api-client/src/` — used by mobile only
- **Rebuild package `dist/` before typechecking or running** (`npx tsc` in `packages/validation` / `packages/api-client`) — `main` points at gitignored `dist/`, so new exports are invisible until built; hides well because type-only imports still resolve. Recurring: bit Document Import phases 1, 2 and 3

## Mobile Patterns
- All screens import `useThemeColors()` from `@/hooks/useThemeColors` for styling
- Lists use FlashList, not FlatList
- Images use expo-image, not Image from react-native
- Bottom sheets use the shared `BottomSheet` component from `components/ui/`
- FABs use `TouchableOpacity` with `haptics.medium()` on press
- Never use Expo Go — always test on Android emulator (native modules: MMKV, Mapbox)

## Geocoding & Distances
- Address autocomplete: `/api/geocoding/autocomplete` (Nominatim proxy, 60s cache)
- Road distances: `/api/geocoding/distance` (OSRM proxy, replaces haversine)
- OSRM utility: `apps/web/lib/geo/osrm.ts`

## Testing
- Android emulator only (native modules incompatible with Expo Go)
- Web E2E: Playwright (Phase 27), unit tests: Vitest (quick-132)
- Run `tsc --noEmit` before every Vercel deploy

## Deployment
- **Web:** `vercel --prod` (Vercel CLI only — never `git push` for deploys)
- **Mobile:** EAS Build + EAS Update for OTA
- **After every commit:** `git push origin master` to keep GitHub in sync

---

# Phase History (v5.0 Mobile — Phases 29–37.x)

| Phase | What was built |
|-------|---------------|
| 29 | Turborepo monorepo setup, Expo scaffold, shared packages (types/validation/api-client) |
| 30 | Mobile auth (AES-256-GCM Bearer tokens, MMKV session, login screen, AuthContext, role routing), navigation shell (tab navigators, 9-component UI library) |
| 31 | Driver REST API endpoints, TanStack Query, driver dashboard, loads list + detail, status update modal |
| 32 | DriverHOSEntry + DriverIncident DB models, HOS REST endpoints + full screen, incident report form + photo capture |
| 33 | Background GPS (expo-task-manager, adaptive intervals), push notifications (PushToken, expo-server-sdk, deep linking), offline mutation queue (MMKV, backoff, SyncStatusBar) |
| 34 | Driver documents screen (R2 presigned URLs, upload sheet), driver messaging screen (load-scoped, polling, unread badge) |
| 35 | Owner dashboard (KPI REST + KPICard + DriverStatusChip), owner loads management (4-tab, CreateLoadSheet, detail actions), owner driver management |
| 36 | Owner live map (Mapbox VehicleMarker + VehicleDetailSheet, 60s refresh), fleet messaging (RecipientSelector, broadcast + targeted, push delivery) |
| 37 | Polish: touch targets, FlashList audit, skeleton loaders, animations + haptics + dark mode, thumb-friendliness, NativeWind migration, accessibility labels, form validation |
| 37.1 | Driver portal gaps: My Route detail screen, MessageBubble, SupportTicketFAB |
| 37.1.1 | Data pipeline: RouteStop ↔ Load links (loadId FK, pickupStopId/deliveryStopId), geocoding utility, RouteStop auto-sync |
| 37.1.2 | Invoicing trucking standard: InvoiceItemType/InvoiceItemUnit enums, 7 freight header fields, FSC auto-calc, quick-add buttons, detail + edit UI |
| 37.6 | Web auth security hardening: claims migrated to app_metadata, auth helpers consolidated to supabase.ts (74 imports updated) |
| 37.7 | Driver map + navigation: Mapbox migration, OSRM directions backend, full MapView with polyline + info panel, tab bar restructure (5+More), Start Route → nav deep link, nav-settings screen |

---

# Phase History (Document Import — spec `docs/specs/DocumentImport_TechnicalSpec_v1.md`)

Own numbering, separate from v5.0 Mobile above. Plans and per-phase summaries in `.planning/document-import/`.

| Phase | What was built |
|-------|---------------|
| 1 | Data model + extraction service (spec §5/§6): 4 new tables (`document_imports`, `document_import_pages`, `facility_external_references`, `document_profiles`) + 23 additive columns, canonical Zod schema in `packages/validation`, 8-state lifecycle, SHA-256 dedupe as a real DB index, per-page extraction cache, consignment merge (same shipment ref → page span, union; different ref → repeat, sum; none → warn, do not sum), CSV via papaparse. `.xlsx` deferred — no library and none may be installed |
| 2 | Upload + intake: one transport-neutral `handlers.ts` under both surfaces (11 route files mirrored, session cookie vs Bearer), presigned upload + `assertTenantKey`, `persistence.ts`, start/extract/cancel/re-shoot/summarise/resume, web wizard with dnd-kit reorder + mobile staging screens, duplicate upload → 409 offering open-existing or import-as-correction |
| 3 | Client + contract resolution and the summary card (spec §4.1/§4.2): the whole view computed on read, nothing stored — only an exact normalised name match auto-selects (fuzzy capped at 0.99, never 1.0), document-profile alias learning, one-time spot contract for rate confirmations, "why" affordance on every auto-resolved row, 6 route files. Followed by quick-508 (commit the auto-resolved client at the mutation boundary), quick-509 (resolution provenance stored at write time, not inferred at read), quick-510 (the contract twin `ensureContractCommitted` + removal of the footer's false "saved" claim) |
| 4 | Facility resolution ladder (spec §7): T1 learned external reference `(tenant, client, code)` → silent link · T2 normalised address exact match within tenant → silent link + backfill ref · T3 fuzzy ≥ threshold → PROPOSE, requires a human tap · T4 nothing → pre-filled create form, requires a human tap. Shared address normaliser (`address.ts`), external refs written on every confirmation and T2 backfill, stop links + provenance under `resolution_provenance.stops` (no DDL) |
| 5 | Stop review (spec §10): the list (drag on web via installed `@dnd-kit`, **arrows on mobile — no gesture handler is installed and none may be**), the 11-field detail editor, bulk apply, and blocking validation. `Review stops` is live on both surfaces; the summary card is still the only way in. Pure `stop-review.ts` + writing `stop-review-service.ts`, 6 mirrored route files, 4 additive keys on `consignmentSchema` (jsonb, **no DDL**) |
| 6 | Route template matching (spec §8): candidates on the contract, widened to the client only when it has none and **visibly labelled** · Jaccard over **resolved facility IDs**, ordering ignored, ×0.8 when stop counts differ >30% · thresholds 0.75 / 0.45 in `template-constants.ts` and nowhere else · the three presentations · application (template supplies order/windows/documents/standing notes; import supplies quantities/references/per-stop notes) · post-commit offer, auto-create behind the tenant setting with the near-duplicate guard, one-tap save. Pure `template-matching.ts`, read-only `template-lookup.ts`, writing `template-service.ts`, 4 mirrored route files, 3 additive keys on `consignmentSchema` + a `template` key on `resolution_provenance` (jsonb, **no DDL**). Items 5–7 are built and reachable but **cannot fire until Phase 8's commit calls `runPostCommitTemplateStep`**. Followed by quick-515 (render standing notes, defer window materialisation to commit) and quick-516 (`Change` opens a chooser over the new uncapped `TemplateSlotView.alternatives` instead of declining; `Look again` becomes `clearTemplateDecision` / `action: 'reset'`) |
| 7 | End stop policy + route optimisation (spec §9): the five policies resolved tenant default → template override → per-trip choice, the per-trip choice stored as an `endStop` key on `resolution_provenance` (jsonb, **no DDL** — every column existed from Phase 1 and was re-verified against `pg_constraint`) · the end stop is **not a consignment**: derived on read, committed at the mutation boundary, materialised by Phase 8 as a real `CarrierStop` (`is_end_stop`, `stop_type=layover`, sequence last) · DRIVER_RESIDENCE privacy as a shared server-side `where` fragment + a mask, applied at all 14 facility read sites · optimisation as a **suggestion only** over the existing OSRM client's `/table` service, cached per sorted facility set. Pure `end-stop.ts` / `optimisation.ts`, read-only `end-stop-lookup.ts`, writing `end-stop-service.ts` / `optimisation-service.ts`, `facility-visibility.ts`, 5 route files, 65 tests. `endStopCommitPlan` + `markEndStopMaterialised` are built and **cannot fire until Phase 8 calls them** |

| 9 | Driver start + inspection gate (spec §12): the gate in front of `Trip.start` — required? → owner override? → valid one today for this truck by this driver? → otherwise the checklist · the full-screen walkaround (one section per screen, pass/fail/**N-A**, 44pt targets, note-required-on-fail, photo uploaded **at capture**, back nav, signature with name + timestamp) · outcomes (all pass starts · non-critical starts + logs defects · critical blocks when `blockTripStartOnFailedInspection`) · the blocked screen with Contact dispatch and a re-check · owner override with typed reason to `DispatchOverrideAudit` + the trip row · `/settings/operations`. Pure `inspection-gate.ts`, read-only `inspection-lookup.ts`, writing `inspection-service.ts`, transport-neutral `inspection-handlers.ts`, 8 route files, 47 tests. **One new table**, `carrier_truck_defects` |

| 10 | Notification triggers (spec §13): ten triggers on the EXISTING catalogue — `trip.assigned/reminder/started/completed`, `inspection.passed/passed_with_defects/failed/overridden`, `import.needs_review/failed`. Section 13's table has SEVEN rows; three are compound (`·`), which is where ten comes from. **DDL:** `NotificationCategory` += `TRIP`,`IMPORT`; `NotificationTemplate.pushEnabled` (default FALSE) and `UserNotificationPreference.pushEnabled` (default TRUE). PUSH branch added to the dispatcher — `NotificationChannel.PUSH` has existed unused since DEC-2. New `trip-reminders` cron (daily; Hobby plan permits no finer). Pure `notification-constants.ts`, `emit.ts`, two seed files, 22 tests |

| 11 | Live board + Today's Trips report (spec §13): the Drivers/Trucks segmented toggle on the live tracking dashboard, ONE data source (`loadBoardFacts`) → three projections, ONE row component (`BoardRow`) with the primary column swapped **in data, not by a variant branch**, and the report on the existing GridShell sorted by a derived numeric `attentionRank`. Badges are colour AND icon AND text. Also deleted `TruckRow`/`TruckRowExpanded` and un-swallowed `getLatestVehicleLocations().catch(() => [])`. Pure `board-constants.ts` / `board-status.ts`, read-only `board-lookup.ts`, pure `board-view.ts`, 2 route files, 55 tests. **No DDL, nothing installed** |
| 12 | In-app documentation (spec §17 Phase 12): 8 feature-registry entries, 8 help-centre articles in `docs-content/client/`, all five tenant settings documented with defaults read off production, `docs/technical-documentation.md` §11 (entity flow · 23 route groups · 12 microflows · 17 rules), and the spec corrected with a **v1.1 changelog of 20 divergences**. Two navigation wire-ups were required to make any of it reachable — see the two rules below. Also corrected `07-SUMMARY.md` items 2 and 3, which had been stale since quick-520. **No DDL, nothing installed, zero test regressions** |

**Hard rule:** T3 and T4 **never create a facility without an explicit human tap** — enforced by the verdict union's shape (T3/T4 members carry no facility id) and by `autoLinkTarget()`, not by a check an edit could drop. A polluted facility table is unrecoverable.

- **The 31-pair address fixture is the matcher's permanent guardrail.** `__fixtures__/facility-address-pairs.fixture.json`, read from disk and driven through the real normaliser and scorer. 11 negatives, including same-street/different-number and N-vs-W on the Chicago grid. Weakening a NO_MATCH pair to make a run green is a task failure, not a fix. Integrity guard asserts contiguous ids + a length floor, so adding pairs is free and deleting them is not.
- **Threshold and weights live in `facility-constants.ts` and nowhere else** — grep-verified single occurrence. Tests import the constant rather than restating it.
- **DEC-14 — `facility_external_references.resolved_via` admits only `T1|T2|T3|T4`.** Writing the richer provenance vocabulary there is a Postgres 23514 on every confirmation. **Read `pg_constraint` before writing any enum-ish carrier column** — these tables carry CHECKs seeded in early migrations that do not track the app's vocabulary (same class as `facility_type`, where `shipper`/`receiver` were deleted by TKT-0016; consignee → `customer_site`, origin → `warehouse`). A faked DB in a unit test is not evidence about SQL.
- **Stop review state is `document_imports.reviewed_extraction`, and array order IS the running order.** Reorder is a server write, never component state (Phase 5's stated drift risk). Reorder **permutes `resolution_provenance.stops` alongside the consignments** so a human's T3/T4 confirmation moves with its stop; the `stopFingerprint` is carried untouched, so an already-stale link is still dropped. That is not the "re-key on position" Phase 4 warned against — position is precisely what changed, and the permutation was handed to us.
- **Bulk apply takes `stopIndexes[]` — the selection — and the server walks the whole array.** There is no rendered-row list below the component layer on either surface. Do not "optimise" it into anything that knows what is on screen.
- **`stops.stop_type` admits only `pickup|delivery|fuel_stop|layover|relay_handoff`**, and **`stops."bolRequired"`/`"podRequired"` are camelCase** — `bol_required` does not exist, unlike every snake_case neighbour on that table. Same class as DEC-1 and DEC-14: read `pg_constraint` and `information_schema`, never infer a name from the convention around it.
- **Template matching scores over resolved facility IDs, never names** — "RUSS DARROW HONDA" is three dealerships in one metro. An **unresolved** stop contributes a synthetic member that cannot match, rather than being dropped: dropping them lets a half-resolved manifest score 1.0 against a template covering only the resolved half. Thresholds and the ×0.8 downweight live in `template-constants.ts`, grep-verified.
- **Template SELECTION auto-collapses at 0.75 and commits at the mutation boundary; template APPLICATION is always an explicit tap.** Applying rewrites the running order, so reading "collapse" as "apply" would put the module's biggest write on a GET. `resolution_provenance.template.appliedAt` distinguishes them on the row.
- **quick-518 — a stop a previous template inserted is NOT part of the import.** `buildTemplateDiff` filters `templateInserted`, so a ghost cannot be MATCHED by the next template (which is how `mergeTemplateStop` used to clear its `skipped` and promote it into the trip), is not appended as IMPORT_ONLY, and — since the merge walks `diff.rows` — is dropped with its provenance link and re-derived by the incoming template. Signal is `isTemplateInsertedStop`: `templateOrigin === 'TEMPLATE_ONLY'` AND `skipped` AND no document backing, a **conjunction so every direction of doubt keeps the row** (a kept ghost, one with typed freight, a skipped document stop, a pre-Phase-6 row are all untouchable). **Never widen that predicate to a disjunction** — deleting a customer's freight is unrecoverable, a stale ghost is a glance. Also the reason a deferral needs checking against the paths the *same* session added: 517 called this unreachable from one component's conditional while 516's chooser was the way in.
- **quick-517 — a SKIPPED stop must not reach the scorer; an UNRESOLVED one must.** Two opposite rules on the same function. `facilitySetForImport` filters `skipped` (not on today's run at all) and keeps unresolved stops as synthetic members (on the run, location unknown — dropping them lets a half-read manifest score 1.0). Applying a template inserts its TEMPLATE_ONLY rows as `skipped: true`, and the next read resolves them at T2, so before the filter one applied template silently pushed every later score down twice over: an extra union member AND a stop-count difference past the tolerance, firing the ×0.8. **`buildTemplateDiff` deliberately still sees every stop** — `applyTemplateToConsignments` walks it, so filtering there deletes consignments off the stop list. Scoring and merging want different inputs.
- **quick-517 — sentences with a count go through `template-copy.ts`, never inline JSX.** `<p>{n} stop{n===1?'':'s'} will take …</p>` is four children, three of them whitespace-sensitive, and the dialog rendered "4 stopswill" on screen twice across two investigations that both blamed JSX trimming and were both wrong (the compiled output always had the space). One string per sentence removes the boundary rather than the suspect. The mirror lives at `apps/mobile/lib/template-copy.ts`; keep the wording in step.
- **quick-516 — a stored slot decision short-circuits the view on the KEY'S PRESENCE, so "undo the decision" means DELETING the key, never writing another one.** `buildTemplateSlot` returns `DECLINED` before it scores anything when `resolution_provenance.template` exists with `via: 'NONE'`. That is correct — a stored answer must outrank anything computed — and it is why "Look again" looked dead: it was wired to a re-fetch, which re-read the decision instead of clearing it. **A control that has to change a stored decision is a POST, always.** Same trap waits on the client, contract and stop slots. Also: `TemplateSlotView.candidates` is the middle band's list and is CAPPED at three; `alternatives` is the chooser's and is deliberately uncapped — do not unify them.
- **quick-519 — `DialogContent` is a GRID, and an `auto` grid track's minimum is the MAX of its items' min-content widths.** A `truncate`d chip's min-content is its **whole string** (`truncate` sets `white-space: nowrap`, and nowrap makes min-content = max-content), and **percentage max-widths are ignored during intrinsic sizing**, so `max-w-full` constrains nothing at track-sizing time. One 54-char facility name therefore widened the whole dialog past its own `max-w-lg`, which produced BOTH a horizontal scrollbar (`overflow-y-auto` forces `overflow-x` to `auto` — a `visible`/`auto` pair is illegal in CSS) AND a subtitle that appeared to clip mid-word (`DialogHeader` is a sibling grid item stretched to the same over-wide track). **Two symptoms, one cause, and neither was in the element that looked wrong** — the subtitle was never broken. The fix is `grid-cols-[minmax(0,1fr)]` on the dialog: an explicit zero-minimum track, after which `max-w-full` + `truncate` work again because percentages resolve normally at layout time. Fix it on the *consumer*, never on the shared `ui/dialog.tsx` primitive. **On mobile the same class of bug needs a different fix: RN defaults `flexShrink` to 0, unlike web** — so `numberOfLines={1}` alone does NOT stop a long string sizing to its full width and pushing a row past 360pt. Shrink first, then ellipsise.
- **`route_template_stops.stop_type` admits FOUR values (`pickup|delivery|fuel_stop|layover`) — `relay_handoff` is legal on `stops` and a 23514 here.** Also `route_templates.schedule_type ∈ fixed_days|frequency|on_call` and `equipment_type ∈ dry_van|flatbed|reefer|tanker|step_deck|other`. The tenant setting is `Tenant."autoCreateRouteTemplatesFromImports"` — camelCase column on a PascalCase table. All four read off production, not inferred.
- **The route-template save path is `lib/carrier/route-template-save.ts`** — lifted out of the `saveRouteTemplate` server action, which is now a wrapper. Do not fork it; a server action cannot serve `/api/mobile/*` (no cookie, no `x-tenant-id`).
- **The end stop is DERIVED, committed at a mutation, and materialised at commit — never a consignment.** Putting it in `reviewedExtraction.consignments` would break three paid-for properties at once: template scoring (quick-517 — a yard is not on the manifest), the ghost predicate (quick-518 — a fourth kind of synthetic row it was never written for), and reorder (Phase 5 — array order IS the running order, so "always last" becomes a rule the permutation validator has to remember). `layover` is the `stop_type` because `stops_stop_type_check` has no value meaning "return to base"; `stops.is_end_stop` is what tells the two apart, **not the type and not the position**.
- **quick-516 struck a third slot before it could be re-learned.** `buildEndStopSlot` feeds the stored policy in as Section 9's third rung, so a stored decision outranks the template override and the tenant default by construction — and **there is no value that means "undecided"**: `NONE` records "this trip ends nowhere". "Use my company default" is a POST that DELETES the key (`action: 'reset'`). Client, template, now end stop — assume the fourth slot will need it too.
- **`ensureEndStopCommitted` is deliberately NOT called from the stop mutations**, unlike `ensureStopsCommitted`. Freezing the policy on the first stop edit would pin it before the template — the second rung — has been chosen. Scoped exactly like `ensureTemplateCommitted`. Phase 8 must call it before reading the policy.
- **`driverResidences` is the ONE default-FALSE permission, and it does not go through `hasPermission`.** That helper resolves a MANAGER as `permissions?.[key] !== false` — default-allow, which is right for features and would have handed every existing manager in every tenant their drivers' home addresses on the deploy that shipped Phase 7. `fullAccess` does not grant it either: that toggle was set by owners who had never heard of the key. `canSeeDriverResidences` demands a literal `true`.
- **Filter a picker; MASK a trip's own stops.** Two mechanisms because there are two situations. Dropping a residence row from an itinerary deletes the end stop and makes the trip look like it finishes at the last delivery — the untracked return Part A exists to fix — so the row stays and the name becomes "Driver's home" with street, city, ZIP **and the coordinates** nulled. A map pin on a house is the same disclosure as the street line, and it is the one people forget. RLS is deliberately not the mechanism: the app connection bypasses it today (tracked pre-launch item), so a policy would be a comment that looks like a control.
- **Optimisation never mutates, and the accept path is Phase 5's `reorderStops`.** Both `apply*` functions recompute the suggestion server-side rather than applying an order the request carried — a request that could name its own permutation would be a reorder endpoint wearing an optimiser's name. "Keep current order" has no endpoint: declining is the absence of a request. **Firm windows are an ORDERING constraint, not arrival times** — the trip has no departure time until Phase 8, and inventing one would make the suggestion depend on a number nobody supplied. Skipped stops and ghosts are excluded like quick-517/518; an **unresolved** stop is the opposite of the scorer's rule — it means *no suggestion at all*, because optimising the half of the trip you can see reorders stops around a gap.
- **Both optimisation floors live in `optimisation-constants.ts` and are combined with OR** (5 miles · 20 minutes), grep-verified single occurrence, imported by the tests. OR because two orders can cover near-identical distance and differ sharply in time; an AND would suppress "same miles, half an hour earlier home", the suggestion with the clearest justification there is. **The matrix cache key is the SORTED facility-id list** — order-insensitive so an unchanged template hits daily, set-sensitive so invalidation is structural rather than a hook someone has to remember.
- **Two Phase 7 gaps were reported rather than migrated, and quick-520 CLOSED BOTH — do not plan around them.** `route_templates.end_stop_facility_id` and `route_matrix_cache` both exist in production and are wired (verified against `information_schema` at Phase 12 close; 07-SUMMARY items 2 and 3 are struck through and corrected in place). The one Phase 7 item still genuinely open is `DocumentProfile.defaultEndStopPolicy` — the column exists and is deliberately unwired, because Section 9 names three layers and a fourth was not that phase's to invent.
- **quick-511 — the ladder context must derive the *effective* client**, via the shared deterministic resolver (`resolveEffectiveClientId`), never `record.clientId` alone. That column is null whenever a client auto-resolved and no mutation has fired (quick-508), and scoping the reference lookup by it makes T1 unreachable — every learned code silently falls to T2. View and commit paths must reach the same client or the card's tier is not the tier that gets written.
- **quick-520 — the matrix cache is L1 (in-process) + L2 (`route_matrix_cache`), read L1 → L2 → provider, and `persist: true` is the ONLY thing that writes L2.** It is passed from exactly two places, both `apply*` mutations; every GET path leaves it false, so a view never writes. The key is unchanged, so invalidation stays structural. **A hit must never refresh `computed_at`** — that would be a write on a read path — which is why L2 carries its OWN constant, `MATRIX_L2_CACHE_TTL_MS` (30 days), rather than L1's 24h: L1 backstops one process's lifetime, L2 exists precisely to outlive deploys, and the ceiling is there only for the one thing the key cannot see — a **re-geocoded** facility, same id and corrected coordinates, which only time retires. An L2 hit seeds L1 with the row's own `computedAt`, not `now`. Stated rather than hidden: a set only ever VIEWED and never accepted still pays one provider call per cold start; removing that needs a write from a GET (forbidden) or a warm job (no cron/queue in scope). **`route_matrix_cache` has RLS off and no `app_user` grant** — when that role flips it returns zero rows and silently stops caching, visible only as provider volume.
- **quick-520 — a template that carries `end_stop_facility_id` must have it re-sent by every full-payload re-save, or the save DELETES it.** `applyTemplateOptimisation` rebuilds the whole template from an explicit `select` and hands it to `saveRouteTemplateCore`, so a column missing from either half is a column silently nulled by a reorder. The invariant that makes the column safe: a facility may only be written in the same payload that states `DESIGNATED_PARKING`, and writing any other policy CLEARS it — a value nothing reads is a value that comes back to life the next time somebody flips the policy. The per-trip choice still outranks the template rung. Also fixed here: **`getTemplateOptimisation` had `startFacilityId = endFacilityId` unconditionally**, correct only by accident because `endFacilityId` was null for every non-loop policy — gate it on `closedLoop` as the import path does, or DESIGNATED_PARKING makes the day *start* at the yard and charges every candidate order a phantom leg. Residences stay out of the parking picker **structurally** (`driver_residence` is not in `DESIGNATED_PARKING_FACILITY_TYPES`), never by a client-side hide.

- **quick-610 — `createTenantClient` sets NO GUC, and that makes a caller LATENT rather than broken: right only while somebody else left a tenant context on the `max: 1` pool.** It applies `withTenantRLS` (a pure argument interceptor) and issues no `set_config`, so the Prisma-level filter is present and the database-level one is not — the policies consult `current_tenant_id()`, reading a GUC nothing on that path wrote. The six remaining callers (`dashboard.ts` ×2, `tenant-notification-settings.ts` ×3, `base.repository.ts` ×1 — **SIX, not the "Four" `app-user-failure-remediation.md` §8 item 3 claimed against its own §7 table of 2+3+1**) were all measured raising `TC001` cold and all converted to **`getTenantPrismaForOrg(tenantId)` with NO `userId`**. The wrapper choice is load-bearing and diverges from that audit's own recommendation: `getTenantPrisma()` forwards `session.userId` into the audit-columns extension, which `createTenantClient(tenantId)` never did, so it would start writing `createdById`/`updatedById` on `DocumentRepository#create` — **a behaviour change wearing a routing fix's clothes**. `createTenantClient` is now called in exactly two files and `tenant-mechanism-fence.test.ts`'s LIST 2 is a CLOSED FENCE plus `EMPTIED_BY_610`, a named negative — because a merely shorter inventory passes identically if someone re-adds a call AND widens the list in one edit (quick-566).
- **quick-610 — "cold pool" needs a CHILD PROCESS PER SITE, or the second site measures the first.** The GUC is session scope and the pool is `max: 1`, so the first statement in a process that sets `app.current_tenant_id` leaves it set for everything after it. Six sites measured in one process would give one honest verdict and five inherited ones — which is `app-user-failure-remediation.md` §8 item 5, the standing caveat on every `pass` in that document, reproduced from the other direction. Related: **the bare Prisma client is not context-free either** (quick-602), so any "does this path raise" probe must be the first tenant-touching statement of its own process, full stop.
- **quick-610 — an EMPTY table still raises `TC001`, and this is the opposite of the vacuity trap you would expect.** Measured before anything else, because `Truck`, `Route`, `Document`, `Invoice`, `Load` and `SafetyEvent` are all zero-row on staging and five of six probes would otherwise have been meaningless: the policy expression is evaluated at scan setup, not per row, so `SELECT count(*)` on an empty polluted-free table raises exactly as a populated one does. **The vacuity risk is entirely on the OTHER side** — an AFTER read that "succeeds" over zero rows proves nothing, and a cross-tenant refusal over an empty foreign set is worse than nothing. So the cross-tenant assertion is always a PAIR: `foreign === 0` **and** `own > 0`. `610-staging-fixtures.ts --ensure` exists to make the second half true, which is the fixture work §8 item 7 asked for.
- **quick-610 — the SQLSTATE of a policy raise is on `err.cause.code`, NEVER `err.code`.** Prisma surfaces it as a `DriverAdapterError` whose own `code`, `errorCode` and `meta` are ALL `undefined` and whose own enumerable keys are `name, cause, clientVersion`. A probe reading `err.code` records `UNKNOWN` for every raise and then invites exactly the message-prose match quick-602 forbids. Walk the cause chain (a raw `pg` error carries `code` at the top level, so one walker serves both). This was measured by dumping the error object, not inferred.
- **quick-610 — `NotificationSendLog` DOES carry RLS; the in-code comment saying it did not was false, and so was its companion.** `relrowsecurity=true`, `relforcerowsecurity=true`, 2 policies, and a cold read raised. The second sentence — *"the postgres role has BYPASSRLS so no bypass_rls SET is needed"* — is true of `postgres` and beside the point: the cutover target is `app_user`, `rolbypassrls=false`. Both sentences argued for leaving the client unscoped, and both sat one line above the call they justified. Same family as DEC-14 and the medical-card column: **read `pg_class`/`pg_policy`, never a comment about them.**
- **quick-610 — deleting a base class's shared field is how you make the COMPILER enumerate the call sites.** `TenantRepository.db` was removed rather than left beside the new async `client()` accessor, so every surviving `this.db.` became a `TS2339` and tsc named all 14 methods across the 2 subclasses. Leaving the field would have let one method silently retain the GUC-less client — the defect itself. The accessor is also **deliberately not memoised**: every non-transactional statement is an independent pool checkout and the GUC is session-scoped, so caching the client per instance re-opens the bug the moment another tenant interleaves between two calls on the same repository object. And the async-static-factory alternative was rejected because it makes construction async and rewrites all 22 `new DocumentRepository(tenantId)` sites — changing what callers DO, not how the client is obtained.
- **quick-610 — `/dashboard` swallows all ELEVEN of its own query failures, which is a worse failure mode than a 500 and is why it kept measuring `pass`.** Every query in both fetchers is individually `.catch(() => 0)` / `.catch(() => [])`, so a `TC001` rendered as a zero metric and an empty alert list: an owner's dashboard confidently reporting no drivers, no routes and no revenue. `app-user-failure-remediation.md` §8 item 4 attributed the `pass` to pool GUC inheritance; the swallow is a **second, independent reason and the more robust one** — it would have held even on a cold pool. The scoping is fixed; the swallows are reported and NOT fixed. **The correlated server-log slice, never the rendered page, is the authority for that surface** — the same rule quick-602 established when `purge-deleted` raised seven times behind `success: true`.
- **quick-610 — the wrapper countdown was BLIND to `createTenantClient`, so closing these six sites made the number go UP (469 → 475) and that is the honest direction.** The counter recognises `getTenantPrisma`/`getTenantPrismaForOrg` as acquisitions and `createTenantClient` as nothing at all, so six of the most dangerous sites on the tree contributed zero to the metric tracking the migration. Regenerate the artefact; never weaken the assertion. This is quick-602's "the runtime signal and the static countdown see DIFFERENT populations" in a new form: **a site can be invisible to the countdown and still be the thing you most need to fix.**
- **quick-610 — `604-click-through.ts` passes 1 and 2 now honour `--out`, and re-running them without it OVERWRITES quick-604's closed evidence.** `--surfaces3` and `--surfaces606` already had the flag; passes 1 and 2 did not, so re-running them was destructive by construction — `04-click-through.json` carries byte offsets into quick-604's own server log. `--out` goes through `path.resolve`, so an ABSOLUTE path writes wherever you point it and a relative one lands beside quick-604's artefacts; omitting it is byte-for-byte the old behaviour. **Pass 2 reads what pass 1 wrote, so give both the SAME `--out`.** Hash the prior tasks' artefacts at open and at close and diff them — quick-610 proved all 28 byte-identical.
- **quick-611 — a swallowed DB error only cascades if the catch is INSIDE an unsettled `$transaction` callback, and the two shapes that look identical to a grep are the ones that matter.** `try { await p.$transaction(cb) } catch {}` is SAFE — the catch is the transaction's SIBLING, the transaction has already rolled back when the catch runs, and the next statement is on a fresh connection. `p.$transaction(async tx => { try { await tx.x() } catch {} await tx.y() })` is the cascade. A file-level grep for `$transaction` cannot tell them apart, and `wrapper-migration-scope.md` counted `api/driver/gps-ping/route.ts` — the SAFE shape — as one of its 17 risks. Second false positive, equally important: **a helper that opens and closes its own transaction and RETURNS does not put its caller inside one** (`getCurrentUser()` and the 15 other resolvers in that audit's §1b table). Treating those as enclosing transactions classifies nearly everything as live and produces a large wrong diff.
- **quick-611 — the 17 `25P02` sites are 0 LIVE · 16 DORMANT · 1 IMPOSSIBLE, and the risk is entirely future-tense.** The audit found them while scoping a `withTenantContext` migration that would have wrapped 456 units in transactions; quick-602 measured that overlap at **4 of 456 — 0.88 %**, and the wrapper still has zero real call sites. So nothing cascades today. **`createCarrierDriver`'s comment that a failed user-link leaves the driver record valid is TRUE** — it becomes false in the same edit that wraps the function or its caller, which is a one-line change anybody making something "atomic" might make. Frozen by `tests/security/transaction-abort-sites.test.ts`, which fails the moment a site goes LIVE and **carries the remedy in its failure message** — including "do not delete the catch", because the resilience is intended.
- **quick-611 — the SAVEPOINT remedy WORKS in Prisma 7, and had to be measured because there is no `$savepoint()` API.** `tx.$executeRawUnsafe('SAVEPOINT sp')` … `ROLLBACK TO SAVEPOINT sp` in the catch, on the SAME `tx`, genuinely clears the aborted state: measured on staging as `app_user` against real `carrier_drivers` rows, the earlier write survived and the later write succeeded while the catch still swallowed. The other legitimate shape is to keep the whole `$transaction` inside the `try` so the catch is its sibling. Force the failure with a REAL constraint — a `userId` that is not a `User` violates `carrier_drivers_user_id_fkey` — rather than a synthetic throw, or you are testing your own `throw` and not the database.
- **quick-611 — when a source scan reports ZERO and NOTHING on the tree can make it report non-zero, the self-test is the only thing standing between "measured" and "broken walker".** The repo-wide sweep for a swallowing catch inside a `$transaction` callback found none, so no real code exercised the predicate in the positive direction. A synthetic POSITIVE and a synthetic NEGATIVE are both asserted, in memory, from parsed strings: the positive alone is satisfied by a predicate that returns true for everything, the negative alone by one that returns false for everything. Same family as quick-546's "the failure mode of a bad slice is GREEN" and quick-563's green-badge counter-assertion — and here it is the whole basis of the headline result.
- **quick-611 — an audit's line numbers and paths drift; re-resolve by symbol, and report the corrections rather than silently fixing them.** `team-permissions.ts` was listed under `lib/carrier/` and lives under `app/(owner)/actions/` — that path does not exist at all. Line numbers had moved generally, so the analyser re-resolves to the nearest swallowing `try` and keeps the audit's number only as a hint. Also worth recording: `trips.ts:720`/`:786` sit inside `after(async () => {`, i.e. post-response, which is a SECOND structural reason they cannot be inside a request transaction — a fact the original list did not carry.
- **quick-612 — `WITH CHECK` inspects ONLY THE NEW ROW, so a policy whose `USING` admits rows it does not own can be used to STEAL them.** `AutomationRule` had `USING (scope='SYSTEM' OR "tenantId"=current_tenant_id())` + `WITH CHECK ("tenantId"=current_tenant_id())`. `UPDATE … SET "tenantId" = <own> WHERE scope='SYSTEM'` passed BOTH — `USING` via the SYSTEM branch, `WITH CHECK` because the new row now names the caller — and captured all 6 platform rules. quick-599 tested only an UPDATE touching a NON-`tenantId` column, which returns 42501 precisely because `tenantId` stays NULL, and so never saw it. **When auditing an UPDATE policy, always probe the variant that SETS the tenant column, not just one that leaves it alone** — they are different questions and only one of them tests `USING`. **`USING` is the ONLY lever on the old row**; no `WITH CHECK` formulation can constrain what is being replaced.
- **quick-612 — a `FOR ALL` policy makes one `USING` clause do four incompatible jobs; the fix is a per-command split, and only the READ half may keep a widening branch.** PostgreSQL applies, for `FOR ALL`: SELECT→`USING`, INSERT→`WITH CHECK`, UPDATE→`USING`(old)+`WITH CHECK`(new), **DELETE→`USING` ALONE**. So a `USING` that has to stay wide for reads (here `scope='SYSTEM'`, so tenants can see platform rules) silently authorises deleting and capturing those same rows. Split into four policies and put the wide branch on SELECT only. **Keep the ORIGINAL NAME on the half closest to the table's original read shape** (quick-599 §6): `coverage.test.ts` enumerates tenant-scoped tables by the literal name `tenant_isolation_policy`, so renaming every half makes the table VANISH from the enumeration rather than show as uncovered — and a test that filters by target key then passes VACUOUSLY.
- **quick-612 — a wider SELECT policy CANNOT widen a narrower UPDATE/DELETE, and this is worth measuring rather than arguing.** The worry is real on its face: SELECT policies also apply to `UPDATE`/`DELETE … RETURNING`, which is the shape Prisma emits. Measured: `UPDATE … RETURNING *`, `DELETE … RETURNING *` and `UPDATE … FROM (SELECT … WHERE scope='SYSTEM')` ALL return 0 rows. The command policy **ANDs** with SELECT rather than ORing — `(SYSTEM OR own) AND (own)` = `own`. So "what does this split PERMIT that the old one did not" has the honest answer **nothing**, and that is a measurement, not an assumption.
- **quick-612 — the admin automations path is on the BARE prisma client, not `getAdminDb`, so a policy IS consulted and `toggleRuleActive` is ALREADY BROKEN under `app_user`.** `(admin)/actions/automations.ts` imports `getAdminDb` but uses it only for `automationRun.create`; all five `automationRule.*` calls are on the bare client — the TENANT connection (`rolbypassrls=false` after cutover). Measured: `toggleRuleActive` on a SYSTEM rule returns **42501 today**, with AND without a tenant GUC. The per-command split does not break it; it makes it QUIETER (silent 0 rows, since `USING` now filters before `WITH CHECK` is reached). **Routing that one call to `getAdminDb` is a cutover prerequisite.** General rule: "it is behind an admin guard" says nothing about which CONNECTION it uses — check the client, not the guard.
- **quick-612 — a migration-safety guard that greps the raw SQL will refuse its own migration's COMMENTS.** The `--apply` path refused with `migration contains AS RESTRICTIVE`, matching the header sentence "No AS RESTRICTIVE anywhere". Same trap as quick-600's `pool.on('connect'` check, which false-positived on the prose describing the invariant it protects. **Strip `--` comments before every such check.** While there, also refuse INDENTED policy DDL: `rls-policy-replay.test.ts`'s parser is line-anchored and silently SKIPS an indented `CREATE POLICY`, so indentation is not cosmetic — it makes the statement invisible to the replay gate.
- **quick-612 — the body-layer drift gate EXITS 3, not 1, when the canonical artefact is stale, and that refusal is how you know it covers your change.** Run `npm run audit:rls-policy-drift` BEFORE regenerating: `DEFINITION LAYER DID NOT RUN (exit 3) — the canonical artefact is STALE`, and the name layer alone still reports missing 0 / unexpected 0 because the migration is committed and parsed. **A clean NAME-layer run over a rewritten policy body is not evidence of anything.** Regenerate with `npm run audit:rls-canonicalise` against staging and commit `rls-policy-canonical.json`. Expect the regenerated artefact to describe STAGING, so the detector will report drift against production until a human deploys — record that, or the next reader is confused (quick-606 §8 item 9 is the same situation).
---

# Completed Web Features (Pre-Mobile Phases 1–28)

- **v1.0:** Core CRUD — trucks, drivers, routes, loads, documents, basic dashboard
- **v2.0:** Financial layer — expenses, payments, revenue tracking, cost-per-mile, profit alerts
- **v3.0:** Compliance, route improvements, driver document expiry notifications
- **v4.0:** SysAdmin portal, SysAdmin invoicing (email delivery + overdue cron), QA scripts, Playwright E2E tests, driver history, RBAC permissions
- **Quick tasks 1–150:** 150+ quick tasks shipped including: invoice UI, payroll UI, dispatch/load management, automated comms, AI document reading, AI profit predictor, profit-per-lane analysis, compliance dashboard, integrations framework, GPS tracking (Samsara, Motive, browser geolocation), geofencing alerts, IFTA reporting, customer tracking public page, rate confirmation PDF, support tickets with screenshots, DriveCommand logo system, Supabase Auth migration, iMessage fleet messaging, security hardening (CSRF, structured logging, zero @ts-ignore, OpenAPI spec), code quality audit, performance audit (pagination, FlashList, aggregate stats), 3 missing composite indexes, production readiness (Sentry, EAS OTA, rate limiting), OSRM real road distances, dark/light theme toggle

---

# Current State (as of 2026-08-26)

- **Document Import module: COMPLETE.** All twelve phases shipped on `feature/document-import`. The spec is corrected to match the build (v1.1 changelog at the top of `docs/specs/DocumentImport_TechnicalSpec_v1.md`), and the full deferred list — product gaps, known defects, pre-launch items — is §7 of `.planning/document-import/12-SUMMARY.md`. Two items there are flagged as more serious than the rest: **created facilities are never geocoded** (optimisation degrades silently), and **a typed-name inspection signature persists nothing** (a DVIR with no record of who attested).
- The v5.0 Mobile lines below are unchanged from 2026-04-04 and describe a different milestone.

- **Active milestone:** v5.0 Mobile App
- **Last completed phase:** 37.7 — Driver Map + Navigation (Mapbox, OSRM, tab restructure, nav deep links)
- **Also completed same day:** Phase 37.1.2 — Invoicing Trucking Standard (freight fields, FSC, typed items)
- **Next up:** Continue v5.0 mobile — see `.planning/STATE.md` for exact position
- **Pre-store checklist:** Apple Developer ($99), Google Play ($25), EAS credentials, Google Maps API key in app.json — must be done before Phase 38

---

# Important Reminders

- **Figma token expires May 17, 2026** — regenerate in Figma Settings → Personal Access Tokens
- **Store enrollment** — Apple Developer ($99), Google Play ($25), EAS (free) — must complete before Phase 38
- **Before Phase 38** — fill in eas.json Apple/Google credentials, add google-play-key.json to EAS Secrets, replace Google Maps API key placeholder in app.json
- **Deploy:** Always `vercel --prod`, never GitHub push for deploys
- **Mobile terminal:** Always `cd apps/mobile` before running Expo/EAS commands
- **TypeScript:** Run `tsc --noEmit` before every Vercel deploy — **and check it is not lying.** `tsconfig.json` includes `.next/dev/types/**/*.ts`, so a Next-generated file left corrupt by an interrupted `next dev` (quick-517 found one truncated mid-write) puts a SYNTAX error in the program, and tsc then reports only that file and **silently skips semantic checking of all source**. If the only errors are inside `.next/`, the gate is blind, not green: delete `apps/web/.next/dev/types/validator.ts` and `apps/web/tsconfig.tsbuildinfo`, then re-run. **The rule is broader than `.next/`, and quick-519 proved it: a PARSE error (TS1110/TS1161/TS1354) in ANY file in the program — including an untracked, half-written file belonging to work you are not doing — suppresses semantic checking of everything.** 519 hit 10 untracked files carrying literal stray tool-output text (`</content>`, `</invoke>`) appended after real source, from a concurrently-running session. So: **whenever the reported errors are all syntax errors, or all in files you did not touch, the gate is blind.** **Always probe rather than infer** — inject `const x: number = 'y'` into the file you actually edited and confirm tsc reports THAT error before believing a clean run. Delete your probe afterwards; 519 found a previous run's `__probe.ts` still sitting in `src/lib/document-import/`.
- **Git:** Always `git push origin master` after every commit

---

# Workflow Engine Spec — Always Load

 

When any task touches Checklists & Workflows, Playbooks, Step Templates, Active Checklists, Tasks, Auto-Start Rules, or DVIR flows:

1. Read docs/specs/workflow-engine.md in full before writing code.

2. Section 14 defines scope per phase — do not build ahead.

3. UI copy uses only user-facing names (Section 3 naming table).

4. Follow existing codebase conventions — do not introduce new patterns.- **Phase 9 — the checklist already existed; the GATE did not.** `transitionTripStatus` is not unguarded (it enforces `planned → in_progress | cancelled | tonu`) but has **no notion of inspection**, so a truck whose brake check just failed could start a trip. `StepType.INSPECTION_ITEM`, `PlaybookCategory.VEHICLE_INSPECTION`, `isDispatchBlocker` as the critical marker, `failInspectionItem`'s mechanic sign-off, the `'inspections'` storage category and upload-at-capture were all already there and are untouched. **N/A is the existing `SKIPPED` verb** (`/tasks/[id]/skip` + `skipReason`), which `computeDispatchReadiness` already counted as satisfied — not a new state.
- **`PlaybookInstance.completedAt` and `startedAt` are NEVER WRITTEN by anything in this repo.** Production: 27 instances, 0 with either. "When was this inspection answered" is `MAX(StepInstance.completedAt)`. Reading `completedAt` would make every inspection look infinitely stale and re-open the checklist on every start — a bug that looks like the feature working. Also: the starter DVIR playbook is `entityType: 'DISPATCH'`, so `entityId` is the **Trip id, not the truck id**; truck and driver only come from joining `dispatches`.
- **`DispatchOverrideAudit.entityType` has NO CHECK constraint** (verified via `pg_get_constraintdef`, per DEC-14), so `'INSPECTION'` joins `'DRIVER'`/`'VEHICLE'` with zero DDL. Reusing that table is audit B10's recommendation; a second override audit would be a parallel mechanism. The override is written to **both** the audit row (history) and `Trip.inspectionOverridden{ById,Reason,At}` (current state) — audit-only would make every reader reimplement "most recent override".
- **`href: null` does NOT make a mobile screen full screen.** It hides a tab's *button* while the screen stays inside the Tabs navigator, so the tab *bar* remains — which is why the pre-existing `tasks/[id]` inspection has never actually been full screen despite being described as one. `tabBarStyle: { display: 'none' }` is what removes the chrome. `(driver)/inspection/` carries it.
- **The offline queue is JSON-ONLY and cannot carry a photo.** `PendingMutation.body` is `string // JSON serialized` through MMKV, the flusher hardcodes `Content-Type: application/json`, the `type` union is four mutations, and there is exactly one consumer (`StatusUpdateButton`). Inspection photos therefore upload **at capture** via presigned PUT — awaited before the thumbnail renders, only the `s3Key` held — which is the phase's named drift risk and was already correct in `uploadInspectionPhoto`. An offline capture is reported to the driver by name; the **note stays required**, so the failure is still recorded. Do not invent a binary queue to close this.
- **`logger.error(message, error, context)` takes the error SECOND.** Passing a context object there — which `createNotification` and several others still do — collapses it to `new Error('[object Object]')` and tells Sentry nothing. Use the real arity, with `serializeError(err)` inside the context for name and message. Same family as DEC-11 item 3.
- **`SignatureScreen`'s silent S3 failure is FIXED (Phase 9).** It used to set `s3Key` whether or not the bytes landed and show a green "Signature submitted" either way — a signed DVIR with no signature object, which is exactly what a roadside inspection asks for. Capture now lives in the shared `SignaturePad`; `uploadSignature` returns a key **only** on a successful PUT and throws otherwise.
- **`CarrierTruck` has no defect or maintenance relation** — `MaintenanceEvent`/`ScheduledService` hang off the LEGACY `Truck`. `carrier_truck_defects` (Phase 9) is the store, with a **partial unique index on `step_instance_id`** so a corrected re-submission updates its defect instead of doubling the truck's open count. It shipped with RLS enabled AND forced, both sibling policies, and the `app_user` grant in its own migration — DEC-13 and quick-520 are the two ways that has gone wrong, one line each.
- **Inspection validity is a ROLLING 24 hours, not a calendar day.** Section 12's "already today" read literally expires a 23:50 walkaround at 00:00, hitting the night driver who did everything right. `INSPECTION_VALIDITY_HOURS` in `inspection-constants.ts`, grep-verified, imported by the tests. And a prior inspection **cannot launder a critical failure** — inside the window it only clears the gate if its critical items passed.
- **Inspection sections come from `stepSnapshot.defaultConfig.section`, not `PhaseType`.** `playbookPhase` is `NONE` on all twelve seeded steps, so grouping by it yields one section called "None" and delivers nothing. Fallback order: `overrideConfig.section` → `defaultConfig.section` → a real `playbookPhase` → a single "Walkaround". Chunking into arbitrary groups of N was rejected — a boundary that means nothing is read by the driver as meaning something. Also note `seedStarterPlaybooks` sets `isDispatchBlocker: true` on **every** seeded step, so on a seeded tenant the non-critical branch is unreachable by data until an owner edits the checklist.
- **quick-541 — a `@db.Date` column is a CALENDAR DATE and must never be rendered with `new Date(x).toLocaleDateString()`.** Prisma materialises a DATE at **UTC midnight**, so in any negative offset that instant is still the previous evening: a CDL stored 2027-01-14 displayed "January 13, 2027", and the commit gate told a dispatcher a licence stored 2026-01-14 "expired on Jan 13". The rule is one line — `@db.Date` → `formatDateOnly`/`formatDateOnlyShort`/`daysUntilDateOnly`/`isExpiredDateOnly` from `lib/utils/date.ts`; `@db.Timestamptz` → `toLocaleDateString` (a real instant, local rendering is **correct**, and using the date-only helper there is the inverse bug). **`date-fns` is not a safe harbour**: `format(parseISO(iso))` has the identical defect and hid two sites from a `toLocaleDateString` grep. quick-313 fixed this once for settlements and left a TODO; it recurred because its helper de-trapped date-only **strings** only and a Prisma `Date` **object** — what every server component passes — fell through to the local branch.
- **quick-541 — the same UTC-midnight value breaks the ARITHMETIC, not just the display, and that half decides whether a driver rolls.** `Math.ceil((expiry - Date.now()) / 86_400_000)` and `expiry.getTime() < asOf.getTime()` both compare a midnight against an instant, so a document was reported **expired for the whole of the day it was still valid** — midnight precedes every departure time on its own date. Seven compliance surfaces plus `document-import/commit.ts` carried it. Collapse both sides to a calendar-day index; "expires today" is `0`, never a fraction that rounds to `-1`. **Fixing the formatter alone would have left the badge saying "Expired" beside a correctly-rendered future date** — the two must move together.
- **quick-541 — `formatPayPeriodDate` keeps its date-or-instant heuristic and it must NOT be pushed down into `formatDateOnly`.** It is the only function that has to guess, `src/__tests__/format-date.test.ts` pins the guess (a real timestamp must keep rendering locally), and every other call site knows its column type and should import `formatDateOnly` directly. Same shape as the two-mechanism rules elsewhere: guessing is for the one caller that cannot know.
- **quick-541 — inserting an import by "splice after the last line matching `^import `" corrupts multi-line import blocks**, which is how four files got `import … from '@/lib/utils/date';` wedged between `import {` and its first specifier. That is a TS1003 **parse** error, which then suppressed semantic checking of the whole program — the CLAUDE.md "gate is blind" trap, hit from a script rather than a stray file. Anchor to the end of the last **complete** import statement (a line ending `;` while still inside the import block). **The probe is what caught it**: the first run reported 25 syntax errors and *not* the probe.
- **quick-541 — `/carrier/driver/trips` is deleted.** It was a third "My Trips" list with zero inbound links, superseded by `/home` and `/my-route`. Two things showed it was unfinished rather than merely unlinked: its "Start" affordance was a `<span>`, and its cards linked to `/carrier/dispatches/{id}`, which `next.config.ts` redirects to the **owner** trip detail — a page `requireRole` refuses to a driver. Deleting it removed one of Phase 7's 14 facility-mask call sites; the mask is untouched.
- **There is NO medical-card column.** No `medical*` / `dot_physical` field exists in `schema.prisma`; `CarrierDriver` has exactly one date-only field, `cdl_expiry`. `DriverFacts.medicalExpiry` is read from the **legacy `Document`** row (`description = 'MEDICAL_CARD'`), whose `expiry_date` is `@db.Timestamptz`. Same family as DEC-14: read the schema, never infer a column from a sibling's presence.
- **Phase 10 — `resolveRecipients` UNIONS `defaultRecipients` with `NotificationSubscription`; they are TWO mechanisms, not one.** A rule of `{type:'tenant_owners'}` or `{type:'role',role:'OWNER'}` addresses every matching active user **without consulting subscription at all**, and `UserNotificationPreference` is a CHANNEL preference applied afterwards that **defaults to true** — so an owner who has never opened settings receives everything such a rule sends. That is correct for `driver.hos_violation` and fatal for anything whose audience is "subscribers". **The six Section 13 subscriber triggers therefore ship `defaultRecipients: []`**: with no rules to expand, subscription is the only source of recipients and an unsubscribed owner receives nothing BY CONSTRUCTION rather than by a check an edit could drop. **Never add a rule to those six to "make sure owners see it too".**
- **Phase 10 — the dispatcher was ALREADY `after()`-safe and the payload gathering was not.** `dispatchNotification` takes `tenantId` as an argument and uses the BARE prisma client, so it never reads a header. The E251 rule therefore binds the code that builds the payload, which must run **before** the deferred closure with an already-resolved client. `commit-service.ts` violated exactly this: it called `sendDispatchAssignedNotification` (whose first line is `await getTenantPrisma()`) from inside `after()`, so **the Phase 8 driver notification threw on every commit from the day it shipped**, swallowed by `afterResponse`. Phase 9's header recorded it as a known defect and nobody owned it. Replaced by `emitNotificationAfterResponse`.
- **Phase 10 — the dedup window is a ROLLING LOOKBACK, never a bucket, and it is OPT-IN.** `buildIdempotencyKey`'s event scope pins to the same ISO second, so a 1.1s double-tap sends twice; the obvious repair, `floor(now/WINDOW)`, just moves the edge — two emits 10ms either side of a bucket boundary still both send, which is quick-541's UTC-midnight bug in different clothing. `wasSentWithinWindow` asks "did we send this in the last N ms" and has no edge to land on. `NOTIFICATION_DEDUP_WINDOW_MS` (5 min) lives in `notification-constants.ts` and nowhere else. It is passed ONLY by the ten Phase 10 emits — `dispatchNotification` leaves it off by default so all 37 pre-existing triggers keep their exact behaviour. It **fails open** on a DB error: a duplicate notification beats a lost "your brakes failed".
- **Phase 10 — seeding a notification template does NOT make it deliverable.** `dispatchNotification` short-circuits with a FAILED audit row when `defaultHtmlCache` is null, and the ONLY writer of that column was ever the SysAdmin block editor — a human opening each template in a browser and pressing Save. All 37 pre-Phase-10 templates have HTML because somebody did that by hand. The seed now renders the Tiptap doc itself via **`@tiptap/html/server`** (the plain `@tiptap/html` entry throws "can only be used in a browser environment" under Node; the types resolve identically, so tsc cannot see it). `template-renderer.ts`'s "Tiptap is NOT invoked on the server" still holds for the REQUEST path — quick-335 moved it out of the RSC graph, and a seed script is not in that graph. **`defaultHtmlCache` is written on INSERT only**, so a SysAdmin's edits are never clobbered.
- **Phase 10 — `UserNotificationPreference.tenantId` is NOT NULL and `updateMyPreference`'s create branch omitted it.** Every first-ever preference write for a (user, trigger) pair threw "Argument `tenantId` is missing" and returned it as a generic string; **production carried ZERO rows in that table**, which is what a create path that has never once succeeded looks like. The bare prisma client is used there, so the tenant-RLS extension does not inject it either. Fixed in Phase 10 because the feature depends on it — the notification an owner is told they can switch off could not be switched off.
- **Phase 10 — subscription and channel preference answer different questions and must not be merged.** `NotificationSubscription` decides WHETHER you are a recipient; `UserNotificationPreference` decides WHICH CHANNELS reach you once you are. Turning every channel off leaves you in the audience receiving nothing; only deleting the subscription removes you. **Unsubscribing DELETES the row** — presence in the table IS the state, so "undo" is a delete and never another write (same shape as quick-516's stored template decision). The Subscribe control renders ONLY where `defaultRecipients` is empty; on a trigger with a rule, an unticked box would falsely suggest opting out.
- **Phase 10 — `inspection.failed` deliberately fires ALONGSIDE `notifyDispatchOfBlock`, and the duplicate is the point.** That function addresses the trip's dispatcher (or every owner/manager when there is none) unconditionally and backs the driver's blocked screen, which states "dispatch has been told" as a FACT. The catalogue trigger is subscribers-only. Deleting the guaranteed path to avoid a second notification would quietly turn that on-screen sentence into a lie for any tenant with no subscribers. A subscriber who is also the dispatcher gets both; they are not deduplicated against each other because they are different mechanisms with different guarantees.
- **Phase 10 — there are THREE hand-written `NotificationCategory` pickers, and only one is type-checked.** `(admin)/notifications/templates-tab.tsx` is `Record<NotificationCategory, string>` and **fails the build** when the enum grows; `(shared)/settings/my-notifications/preferences-form.tsx` and `(owner)/settings/notifications/notifications-tab.tsx` are plain string-literal arrays with sibling label/colour maps and **drift silently**. That asymmetry is exactly how `PlaybookCategory` went sixteen months with a value no user could pick (DEC-16). On `my-notifications` a missing category renders no section, which also makes its triggers **unsubscribable-from**.
- **Phase 10 — the uploader of a `DocumentImport` is `createdById`. There is no `uploadedById` column.** Checked against `schema.prisma` rather than inferred from `originalName` / `sourceFileKeys` being described as "as uploaded". Same family as DEC-14 and the medical-card column that does not exist.
- **Phase 10 — a new emit can break a test's TEARDOWN without touching a single assertion.** `import.needs_review` writes an `InAppNotification` row, and `in_app_notifications.org_id` is a real FK to the tenant three Phase 8 suites delete in `afterAll`. All assertions passed; the files failed on a foreign-key violation afterwards. **When a test file fails but its tests all pass, read the Failed Suites block, not the assertions.** Also: `document-import-commit-notification-isolation` mocked `sendDispatchAssignedNotification`, which the commit no longer calls — left alone it would have gone green forever while injecting failures into a dead code path. Retargeted to `emitNotificationAfterResponse`, filtered to `trip.assigned` so the module's other emit is not the thing being forced to throw.
- **DEC-17 — NEITHER MCP tool writes the `_prisma_migrations` row. `execute_sql` and `apply_migration` both apply SQL; `apply_migration` additionally records into SUPABASE'S OWN migration ledger, which is a DIFFERENT TABLE from Prisma's.** The resolved-not-run row must be written by hand, every time. **This entry previously claimed `apply_migration` "does both" — quick-581 disproved it directly**: immediately after a successful `apply_migration`, the newest `_prisma_migrations` row was still the previous phase's. The procedural rule is the one that survives and the one that caught it — **query the table and read the newest row back**; "the DDL is live" is evidence of the half that was never in doubt. Phase 10 shipped its columns and enum values live with the migration file committed and the newest row still belonging to the previous phase, and it cost nothing only because `scripts/migrate.mjs` skips by `migration_name` and the SQL was entirely `IF NOT EXISTS`. Two consequences worth stating separately: (1) **a seed `INSERT` applied without a mirrored row RE-RUNS AND DUPLICATES** on the next deploy — the harmless idempotent-DDL case is not the general case, which is why quick-581's seed used `WHERE NOT EXISTS` as a second line of defence rather than as the mechanism; (2) **`_prisma_migrations` is itself a class (c) table — RLS ENABLED, ZERO POLICIES, NOT FORCED** (`20260328000001_enable_rls_prisma_migrations_and_tenant`), and it has no `app_user` grant, so a read-back from any non-owner role returns **zero rows with no error**, which is indistinguishable from "the row was never written" and would prompt a *duplicate* write. Confirm a known-good sentinel row is visible before treating an empty result as absence (quick-581 read back three real rows, so its conclusion rests on evidence rather than on an empty set). The repo's resolved-row convention is real SHA-256 of `migration.sql` over LF bytes, `logs=''`, `started_at = finished_at`, and **`applied_steps_count = 0`** — the last being the signature that tells a mirrored row from one `migrate.mjs` actually executed, which writes `1` with the literal checksum `'manual'`; verified across the table, all 19 count-0 rows carry a real hash and none carries `'manual'`. Also verified rather than assumed: `migrate.mjs` wraps each migration in `BEGIN`/`COMMIT`, and `ALTER TYPE … ADD VALUE` (illegal in a transaction before PG 12) works on this 17.6 database so long as the value is not used in the same transaction.
- **quick-545 — "no blocking steps" is FIVE clauses, and the two easiest to drop are the two that stop it being noise.** The gap is: both tenant settings on, but no `INSPECTION_ITEM` step is `isDispatchBlocker`, so `evaluateTripStartGate` can never reach `BLOCKED` and a failed brake check returns `PASSED_WITH_DEFECTS`. (a) **`entityType` is a two-value filter, `IN ('DISPATCH','VEHICLE')`, not `VEHICLE`** — `ensureTripInspection` accepts either and the seeded DVIR shape is DISPATCH. (b) **At least one `INSPECTION_ITEM` step must exist**: with zero, `isInspectionComplete` is false forever and the gate answers `INSPECTION_REQUIRED` on every start — drivers dead-end, a *stricter* failure that "nothing can block a trip start" describes backwards. (c) A blocking **SIGNATURE or DOCUMENT_UPLOAD does not count** — real for `computeDispatchReadiness`, unreachable from the trip gate, which builds `criticalFailures` only from `INSPECTION_ITEM` outcomes. Predicate lives once in `lib/carrier/inspection-coverage.ts` and is imported by BOTH the server page and the client builder; `inspection-lookup.ts`'s `INSPECTION_ITEM` is now an alias of its constant, not a second literal.
- **quick-545 — the tenant-level check is deliberately evaluated over EVERY candidate playbook, never "the one that will run".** There is no deterministic "the one": `ensureTripInspection`'s oldest-`createdAt` pick is only the fallback, and `fireEvent('ON_DISPATCH_CREATE')` spawns whatever a `PlaybookTrigger` names, which `findTripInspection` then accepts. Requiring all candidates to be toothless can never fire while some protection exists. The cost is a knowingly-accepted **false negative** on a mixed tenant — and that trade is the point: a warning that cries wolf gets scrolled past, and then it protects nothing either. Do not "improve" this into a per-playbook tenant warning. Both settings are read from **live toggle state**, not the saved row, so the warning appears when an owner flips the switch rather than after they save; it is mutually exclusive with the existing "Inspections are off" line by construction.
- **quick-545 — `findTripInspection` queries `entityType='DISPATCH'` only, but `ensureTripInspection` will CREATE a VEHICLE-scoped instance keyed on `truckId`.** So on the 7 tenants carrying the 2026-04-24 script's VEHICLE-scoped inspection, the instance it just created is invisible to the current-inspection lookup and the gate clears through the `PRIOR_INSPECTION` rung instead. **FIXED in quick-546 — see below.**
- **quick-545 — the same gap class exists for driver ONBOARDING, and there the enforcement has NO tenant toggle.** Zero blockers → `computeDispatchReadiness` sets `isReady` true on the first step action → `updateEntityReadiness` writes `User.isDispatchReady = true` → `trips.ts:216` stops throwing `DRIVER_NOT_DISPATCH_READY`. 7 of 23 active ONBOARDING playbooks are affected. There is no settings page promising that protection, so the builder header is the only natural surface. Reported, not built.
- **quick-546 — a trip inspection is DISPATCH-scoped: ONE `PlaybookInstance` per TRIP, and `TRIP_INSPECTION_ENTITY_TYPE` in `inspection-constants.ts` is the only place that is written down.** `ensureTripInspection` used to pass `entityType: playbook.entityType` with `entityId` switching to `truckId`, while `findTripInspection` read a fixed `'DISPATCH'`/`dispatchId` — writer and reader addressing different rows on the 8 of 24 checklists authored `entityType: VEHICLE`. **Truck scope is not merely undesirable, it is impossible:** `generatePlaybookInstance` refuses a duplicate on `(playbookId, entityId, tenantId, status != COMPLETED)`, and **nothing in this repo ever completes an inspection instance** — `PlaybookInstance` has no `deletedAt`, `InstanceStatus` has no CANCELLED, so no state retires one. A truck-keyed instance is created once and can never be superseded: one inspection per truck for the life of the tenant, permanent CONFLICT from the second trip on. It survived because it only bites on the SECOND inspection of a given vehicle — rare in testing, normal in production.
- **quick-546 — Section 12's "a valid one already today for this truck by this driver" argues FOR trip scope, not against it.** It is a lookup over HISTORY (`findValidPriorInspection`), constraining *which past record counts*, not *what a record is keyed to* — and truck-keying breaks its own second half, because a truck-keyed singleton has no driver. **The playbook's own `entityType` stays a SELECTION FILTER**: the candidate query keeps `entityType: { in: ['DISPATCH','VEHICLE'] }` so a VEHICLE-authored checklist is still eligible to RUN; narrowing it to the constant would hand 8 tenants `NO_INSPECTION_CHECKLIST` instead of a walkaround. Safe because `verifyEntity` has an explicit "DISPATCH and OTHER: no entity verification required" branch and `resolveAssignee` returns identically under both scopes.
- **quick-546 — `findValidPriorInspection` keeps its two-shape `OR` DELIBERATELY; `findTripInspection` names exactly one shape. Do not unify them.** History is read shape-tolerantly and is bounded to `INSPECTION_VALIDITY_HOURS`, so a walkaround a driver genuinely answered still clears the gate whatever shape it is stored in. The CURRENT-instance lookup has exactly one writer, and a reader that accepts a shape its writer never produces is how the two drifted apart. Same family as "filter a picker; MASK a trip's own stops" — two mechanisms because there are two situations.
- **quick-546 — the 15 orphan VEHICLE-scoped instances are LEFT IN PLACE, and the schema is why.** They stop blocking creation (the key is now `(playbookId, dispatchId)`), were already unreachable from `findTripInspection`, and only one has an answered step — last touched 2026-04-26, far outside the window, so zero gate impact on any tenant. There is **no honest cleanup**: no `deletedAt`, no CANCELLED status, leaving only an irreversible delete or DDL. Marking them `COMPLETED` was rejected as a lie in a DVIR audit trail.
- **quick-546 — a CONFLICT answered with "Please try again" is an instruction GUARANTEED never to succeed, and that is the lie to hunt for.** Silent-failure instance nine had three layers: the service collapsed a permanent CONFLICT into a transient-sounding retry; `openInspectionChecklist` dropped `result.code` one line short of the screen (the mobile route had always forwarded it); and the banner sat in the TOP block of a `min-h-dvh justify-between` layout, roughly a screen-height from the button being tapped — **the feedback existed and was off-screen, which is what "nothing happens" actually was.** A tap must produce visible feedback where the thumb already is. Detect the TRPCError by **object shape (`code === 'CONFLICT'`), never `instanceof`** — two `@trpc/server` copies in a monorepo make `instanceof` silently false — and never by matching the message prose.
- **quick-546 — a source-scanning test MUST normalise line endings, or it fails on every Windows checkout while passing one assertion vacuously.** `inspection-scope.test.ts` slices function bodies out of the real files; its end marker is the column-zero `\n}\n`, which **does not exist in a CRLF file**. This repo has no `.gitattributes` and `core.autocrlf=true`, so `git ls-files --eol` reports `i/lf w/crlf` — LF in the index, CRLF in the working tree. The slice returned null on unmodified, correct source, and the "no hardcoded literal" assertion then **passed against an empty string**; only the integrity floor caught it. `readModule` normalises with `.replace(/\r\n/g, '\n')`. Same family as the address fixture's contiguous-id + length floor: **every source-reading guard needs both a "was it actually found" assertion and a length floor**, because the failure mode of a bad slice is green, not red.
- **quick-546 — the guard slices ONLY the two function bodies, and that scoping is load-bearing.** `findValidPriorInspection` lives in the same file as `findTripInspection` and legitimately contains BOTH `entityType: 'DISPATCH'` and `entityType: 'VEHICLE'`, so a whole-file scan would be permanently red — and a red test everyone knows to ignore protects nothing. The end marker is the column-zero `\n}\n` rather than "the next `\nexport `", which was checked by hand: the looser marker over-slices in both files, swallowing `findValidPriorInspection`'s doc comment in one and the non-exported `isConflictError` helper in the other.
- **quick-547 — `revalidatePath` alone does NOT update a screen in this app; the working convention is `revalidatePath` on the server PLUS `router.refresh()` on the client.** 124 occurrences across 77 files in `apps/web/src` follow it. The Phase 9-web walkaround opted out: `InspectionRunner`'s header declared *"this component holds no answer state at all — every tick you see comes from `view.sections[].steps[].status`, re-rendered after each action's `revalidatePath`"*, and the three answer paths did nothing client-side. Every answer wrote correctly and none of them ever appeared. **A design comment asserting an invariant is not evidence the invariant holds** — that one was the bug, written down and believed for a whole phase. Ruled out before building rather than guessed: `experimental.staleTimes` (unconfigured), middleware (`/inspection/*` gets a plain `NextResponse.next()`), `cache()`/`unstable_cache` in the read path (none), state seeded from props (none). And `revalidatePath` genuinely DOES set `pathWasRevalidated = ActionDidRevalidateStaticAndDynamic` in next@16.2.1, so the server re-render IS requested — which is exactly why "the action is correct" kept being mistaken for "the screen is correct".
- **quick-547 — when a mutation appears to do nothing, ask what LOCAL state the handler changes, because that is what makes one path look like it works.** Pass, Fail, N/A and Start were all equally broken, but Fail *seemed* fine: `run()` ends with `setMode('idle')`, which collapses the open fail form. Visible motion, mistaken for the answer landing, while the chip still read "Not answered" and the header still read "0 of 5". Pass has no open form, so it looked frozen. **The path that appears to work is the clue, not the exception** — the difference between it and the others is where the real mechanism is.
- **quick-547 — `useOptimistic`'s automatic discard when the transition ends IS the "a failed write must never render as answered" guarantee.** Structural, not a check an edit could drop — the same reason T3/T4 facility creation is enforced by the verdict union's shape. Prefer it over a `useState` overlay for exactly that property. **The trap is the other side: the transition must OUTLIVE the action**, or the tick vanishes in the gap before the fresh server tree lands — the same bug in a shorter form. `router.refresh()` therefore goes INSIDE the transition on success, and is safe after the `await` despite React's re-wrap rule because Next's `refresh()` wraps its own dispatch in `startTransition` (verified verbatim in `next/dist/client/components/app-router-instance.js`). Reproduced both ways in jsdom before being believed: refresh stubbed out → chip snapped back to "0 of 1" the instant the action resolved; refresh as Next implements it → held continuously. **`maintenance-page-client.tsx` calls `router.refresh()` after an `await` OUTSIDE any transition — that is the shape that PRODUCES the flicker. Cite it as API precedent, never copy its structure.**
- **quick-547 — in an optimistic overlay, a SERVER status must always supersede a claim, and a claim for an unknown id must be INERT.** `applyOptimisticAnswers` drops a claim the moment the server's value is anything but `NOT_STARTED`/`IN_PROGRESS`, so a stale overlay can never mask a real answer — including one somebody else recorded, and including a **FAILED painted over as a pass**, which on a DVIR is the difference between a truck that rolls and one that must not. Accepted cost, stated rather than hidden: re-answering an already-answered item (Pass → "Change to fail") gets no optimistic preview and spins until the server tree lands. That is the conservative direction — the screen is never ahead of the record, only occasionally behind it. A claim naming a step absent from the view is never appended, because inventing a checklist row no playbook contains is worse than the missing tick the module exists to fix.
- **quick-547 — `BeginScreen` was fixed by removing its dependence on a signal, not by diagnosing the signal.** The prime suspect — `router.refresh()` called after an `await` inside an async `startTransition` callback, so the transition had already settled — was DISPROVED: `refresh()` establishes its own transition. `openInspectionChecklist` has always returned the fully built view and the screen was throwing it away; `checklist ?? opened` makes the server prop win and needs no refresh at all. **When a refresh will not land, prefer consuming the data the action already returned over making the refresh work.**
- **quick-547 — mobile was already correct and must not be "fixed" to match.** `TripInspectionScreen` holds the checklist in `useState` and patches confirm-then-patch: `if (!res.ok) throw new Error(await readError(res))` precedes `patchStep(...)`, so a failed write never renders as answered and raises a Toast instead. It is the in-repo precedent for the guarantee, not a divergence to reconcile.
- **quick-547 — `AnswerChip`'s "Not answered" branch is DEAD CODE**: it renders only inside `{answered && <AnswerChip status={step.status} />}`, so an unanswered item shows no chip at all. Cosmetic only — a discarded optimistic claim still restores the Pass/Fail/N-A buttons and raises the error banner — but it is why "does the DOM say Not answered" is the wrong question to ask this screen when testing it.
- **quick-548 — when a notification is missing, find the side effect that runs FIRST in the same branch and check ITS observable; the notification tables cannot tell "never ran" from "ran and failed".** `applyVerdictSideEffects`'s BLOCKED branch does `recordInspectionDefects` → `notifyDispatchOfBlock` → `emitNotification('inspection.failed')`, in that order. `carrier_truck_defects` holds **zero rows across all tenants**, which only "the branch never executed" explains — and that one query settled a question that three empty notification tables could not. **The emit was never swallowed and the notification was never broken: `applyVerdictSideEffects` was never called.**
- **quick-548 — `page.tsx:88-90` redirects to `/blocked` on a RENDER, and the side effects hang off a SUBMIT the redirect prevents the driver from reaching.** The gate reaches `BLOCKED` the moment the last item is answered (`isInspectionComplete` counts only `INSPECTION_ITEM` outcomes — the signature is NOT required, and several tenant playbooks have no signature step at all), so `handleGetGate` — a **pure read that by design never runs side effects** — returns BLOCKED, the page redirects, and `handleSubmitInspection` → `applyVerdictSideEffects` never runs. **The bypass predates quick-547 but quick-547 made it the normal path**: before it the page never re-rendered mid-checklist so the redirect needed a manual reload; its `router.refresh()` after every answer now fires it automatically. Do NOT fix this by moving side effects into `evaluateTripGate` — reads must not write, and that separation is load-bearing.
- **quick-548 — `PlaybookInstance.status = 'BLOCKED'` is written by `failInspectionItem.ts:116`, NOT by the gate.** So an instance can carry every symptom of blocked — status BLOCKED, `isDispatchReady` false, the blocked screen rendering — while none of the *consequences* fired. **Never read the instance status as evidence that the verdict path ran.**
- **quick-548 — a screen must not state as fact something it has no channel to learn.** `blocked/page.tsx:145-147` renders a bare `inspectionCopy.dispatchNotified` constant — *"Your dispatcher has been notified and can clear this"* — with no guard, and **as structured it cannot be guarded**: the page's only data call is `handleGetGate`, which returns no notified count. Three independent routes make it false: the redirect bypass; `notifyDispatchOfBlock` returning `{notified: 0}` from its catch with nobody inspecting it; and reload / direct URL / the page's own re-check link re-asserting it with no reference to history. The file's header comment asserts the invariant that failed — same class as quick-547's: **a design comment asserting an invariant is not evidence the invariant holds.**
- **quick-548 — an empty error log is EVIDENCE, but only after you inventory the catches.** All six catches on the inspection notification path log via `logger.error(msg, err, ctx)` with the correct arity; the only `after()` is the best-effort push, whose rejection is `.catch`-logged; `emitNotificationAfterResponse` is not on this path at all. Because every failure mode writes a log line, silence is **inconsistent with "invoked and failed"** and corroborates "never invoked" independently. Without that inventory, silence would have meant nothing.
- **quick-549 — a render-time `redirect()` can skip an entire side-effect branch, and no row assertion will ever see it.** `page.tsx` sent a BLOCKED trip to `./blocked` during render, so the driver never reached `Review & sign`, so `handleSubmitInspection` → `applyVerdictSideEffects` never ran and a failed brake check left nothing durable against the truck. **Submit was correct the whole time** — `applyVerdictSideEffects` is called unconditionally at `inspection-handlers.ts:479`; the redirect was the entire defect and nothing on the submit path needed building. BLOCKED sets `canStart: false` with a non-null `playbookInstanceId`, so with the redirect gone it falls through and renders the checklist, which is the intent. The blocked screen is now reached ONLY from `InspectionClient.onOutcome`, after the submit that ran the side effects. No loop: `blocked/page.tsx` bounces back only when the outcome is NOT blocked — complementary, not circular.
- **quick-549 — a test that calls the handler directly is structurally blind to the page that decides whether the handler is ever reached.** The real-Postgres suite asserts `carrier_truck_defects` rows after `handleSubmitInspection` and would have stayed green if someone reinstated the redirect — the driver would again never get there. **Row assertions and source scans catch different classes and you often need both**: a narrow scan now fails if the checklist page branches on `BLOCKED` again, carrying a counter-assertion that `redirect('/sign-in')` survives so it cannot be satisfied by deleting the page's redirects wholesale. Proven red by actually reinstating the redirect, not by reasoning.
- **quick-549 — when a screen asserts a delivery, ask what it could possibly KNOW on a re-render, not what was true at submit.** Three routes to a real notified count were rejected with reasons worth keeping: passing `effects.dispatchNotified` through from submit survives only the immediate post-submit render, so reload / direct URL / a "check again" link still lie — **that IS the falsehood route, not a fix for it**; reading `in_app_notifications` is purity-safe but `notifyDispatchOfBlock` writes the SHARED `type: 'dispatch_assigned'`, so distinguishing it from a genuine assignment means matching a title string, and **asserting a safety fact off a brittle string match is worse than not asserting it**; a stored flag is DDL. Under a no-DDL constraint the honest move is to change the copy to something true regardless of delivery — *"Dispatch is alerted automatically when a trip is blocked, but do not wait on a call — message them here."* — which keeps the real information and converts **wait into act**. Renaming `dispatchNotified` → `dispatchAlerted` was load-bearing, not cosmetic: the old name is what invited the claim. Do not confuse it with `effects.dispatchNotified`, the side-effect count — same word, different thing.
- **quick-549 — there is NO local database (DEC-3), so a real-row test creates a DISPOSABLE TENANT, scopes every write to it, and deletes children before the tenant.** Convention lives in `tests/carrier/document-import-commit-rollback.test.ts`. Teardown order matters: `carrier_truck_defects` and `in_app_notifications` go BEFORE the tenant, because `in_app_notifications.org_id` is a real FK — the quick-546 shape where every assertion passes and the FILE fails. Verify production is clean AFTER running such a test (zero leftover tenants, zero orphan rows); four runs here left nothing. Also: the disposable tenant deliberately has no OWNER/MANAGER users and no `NotificationSubscription` rows, so the Phase 10 emit reaches nobody and the test sends no real email from a production-pointing connection.
- **quick-549 — a cold vitest run in `apps/web` imports for ~82s, so a real-DB test with a 30s timeout will flake on a cold cache.** A single failing test that passes in isolation and fails in a cold full run is that, not a real regression — but check rather than assume, and report the warm and cold numbers separately.
- **quick-550 — `handleRef.current?.toBlob()` made ONE check stand for TWO facts, and that is the whole bug class.** `SignatureScreen.submit()` opened with an unconditional rasterise and a bare `if (!blob)` saying *"The signature came out empty. Sign again."* — placed ABOVE the guard that already skipped the upload for a playbook with no SIGNATURE step. On that variant `<SignaturePad>` never mounts, so the optional chain yields `undefined` and the driver was told to sign again on a screen with **zero canvas elements**. "There is no pad" and "the pad is blank" are different facts with different remedies; collapsing them made the walkaround unsubmittable in front of `Trip.start`. **`canSign` was already variant-aware and correct (`!signatureNeeded || hasInk`) — so the enable-guard and the validator disagreed, which is the tell: when a button enables and submit refuses, look for two checks that were supposed to say the same thing.**
- **quick-550 — return a PLAN, not a boolean, when the decision selects which side effects may run.** `planSignatureSubmission({signatureNeeded, name})` → `REJECT | RASTERISE | SUBMIT_ONLY`, then `resolveRasterisedSignature({hasPad, hasBlob})` → `REJECT | UPLOAD_THEN_SUBMIT`, both pure in `lib/carrier/inspection-signature.ts`. **Two booleans deliberately, because one boolean standing for two facts WAS the defect.** The plan object is what keeps the canvas call structurally inside the `RASTERISE` branch — a later edit cannot hoist it back above the guard, because `planSignatureSubmission` is pure and cannot be handed a blob that only exists after an `await` on the DOM. Same idiom as the T3/T4 verdict union: make the wrong state unrepresentable rather than adding a second check someone can drop.
- **quick-550 — un-collapsing two conditions means a THIRD error string, and its wording is the fix, not decoration.** `SIGNATURE_PAD_MISSING_ERROR` = *"The signature pad did not load. Reload this page and try again."* names the COMPONENT and offers the remedy for something that never mounted; `SIGNATURE_EMPTY_CANVAS_ERROR` still says *"Sign again"* because that IS the remedy for a blank canvas. A test asserting the two strings differ is the regression test for the collapse — if they are ever merged, it fails.
- **quick-550 — the typed-name signature variant persists NOTHING, and there is no DDL-free place to put it.** No SIGNATURE step means no `stepInstanceId` and no `StepInstance.result` to write; `Trip` carries only `inspectionRequired` + three `inspectionOverridden*`, and `PlaybookInstance` has no signature field. So the old on-screen sentence *"Your name and the time below are recorded against it"* was **false** — the same quick-548/549 class — and was corrected to an attestation rather than fabricating a record. **A DVIR whose verdict is taken with no record of who attested or when is a real gap; closing it needs a column.** Do not invent a record to satisfy the copy; change the copy and name the follow-up.
- **quick-550 — mobile's `TripInspectionScreen` has NO typed-name variant and demands ink unconditionally** (`:334` guard, `:600` disabled) while uploading conditionally (`:339`), so on a no-SIGNATURE-step playbook a driver draws a signature that is then DISCARDED — the theatre quick-543 removed from web, still live on mobile. Not the web fall-through; a sibling defect needing the typed-name variant built there.
- **DEC-18 — "behind schedule" reuses the PERFORMANCE REPORT'S two columns, and that is the whole argument.** `getPerformanceReport` already scores lateness as `count(arrived_at <= appointment_end AND appointment_end IS NOT NULL) / NULLIF(count(appointment_end IS NOT NULL), 0)`, so the board scores it on the same two columns with the same inclusive `<=` boundary. Any other definition — a GPS ETA, a "should have departed by now" heuristic, a routing call — is a SECOND MECHANISM for one word, and the board and the report will eventually tell a dispatcher different things about the same trip. A trip is BEHIND SCHEDULE when it is `in_progress` and a **non-terminal** stop (`pending|arrived`, per `stops_status_check`) has **missed** its window: `arrived_at > appointment_end`, or `arrived_at IS NULL AND appointment_end < now`. The approved wording was just `appointment_end < now()`, which marks a driver **standing on the dock inside their window** as late while the report counts them on time — the excluded case is DETENTION, which the report already measures separately as `avg_dwell_minutes`. One signal standing for two facts is the quick-550 class.
- **DEC-18 — 7 of 308 trips carry ANY appointment window (16 of 720 stops), so "unmeasurable" is the SCREEN, not an edge case.** The on-time column's fourth state is `NO_WINDOWS` — **"No windows set"**, neutral grey, `CalendarOff`, never green and **never blank**. Green is the one colour a person reads without stopping, so "On track" there tells an owner their day is fine when it means nobody set any windows; a blank cell reads as a rendering bug and prompts exactly the question the sentence answers. The performance report has always done this without anyone writing it down — `NULLIF(…, 0)` returns **null**, not 0% or 100%. **The attention RANK and the on-time COLUMN are allowed to disagree**: a completed trip that ran late reads "Behind schedule" in the column (it did) and ranks `COMPLETED` in the sort (nothing left to do). Two questions, two answers.
- **Phase 11 — the whole `/live-map` surface is LEGACY-model, not just the badly-named route.** `/api/v1/carrier/live-map/trips` is named "carrier" and queries `db.route` filtered to `COMPLETED` (it is a completed-legacy-routes list feeding the History tab, with **exactly one consumer**, `maps/trips-tab.tsx` — nothing in mobile, packages, e2e or scripts). But `getLatestVehicleLocations`, which backs the entire live map on both surfaces, reads `"Truck"`/`"Route"`/`"Load"`/`"User"` too. **A board built on it cannot display a single trip Document Import commits**, which is why Phase 11 has its own carrier-model read path rather than extending that one.
- **Phase 11 — the two views differ in DATA, never in a `variant` branch, and that is what makes the second row component impossible rather than merely discouraged.** `board-view.ts` fills `primary`/`secondary`/`facts`; `BoardRow` has no idea which view it renders. A test asserts `Object.keys(driverRow) === Object.keys(truckRow)`. Same idiom as the T3/T4 verdict union: leave the wrong state unrepresentable instead of adding a rule someone can drop. Likewise `attentionRank` is a derived NUMBER, not a `sortingFn` — a comparator would live in the table config, invisible to tests and unassertable without mounting a grid.
- **Phase 11 — `deriveTripAttention` tests CLOSED (cancelled/TONU) FIRST, before the inspection.** Test the inspection first and every cancelled run carrying a stale failed walkaround pins itself to the top of the report forever. `FAILED_INSPECTION` then outranks `NOT_STARTED` because a blocked trip is *both*, and the inspection is the more specific and more urgent fact.
- **Phase 11 — `TruckRow`/`TruckRowExpanded` were DELETED, not repaired, and the nested `<button>` was the least of it.** `TruckRowExpanded` rendered hardcoded `(555) 123-4567` / `driver@example.com` / `Load #1234` / `Chicago, IL to Dallas, TX` / `Arrived at Stop 2 · 10:23 AM` for **every truck**; `RouteTimeline` always received `[]` because no query populates `dispatch.stops`; the ETA cell printed a literal `On Time` regardless of status. Fabricated operational facts on an owner's dashboard — the quick-549 class. Fixing the HTML would have left the fiction. **Now orphaned and left in place deliberately** (reported, not swept up): `tracking/RouteTimeline.tsx`, `tracking/RouteStop.tsx`, `tracking/ExceptionFlag.tsx`, `tracking/StatusPill.tsx` — note `ui/ds/StatusPill` is a *different*, live component. `ExceptionFlag` was fed by `(vehicle.dispatch as any).hasException`, which **no query in this repo ever sets**.
- **Phase 11 — a swallowed error breaks the empty state in BOTH directions.** `getLatestVehicleLocations().catch(() => [])` made a DB failure render as a confident "No trucks yet" and logged nothing; `LiveMapMobile` had already been forced to work around it with `hasLoaded = initialVehicles.length > 0` — which then meant a tenant who **genuinely owns zero trucks** was "still loading" forever and never saw the empty state at all, the one screen that would have told them to add a truck. Reporting the failure (`initialLoadFailed`) fixes both. Three states, three sentences: not loaded / failed / genuinely empty.
- **Phase 11 — `CarrierTruck` still has no maintenance source, so the Trucks view labels what it actually shows.** `MaintenanceEvent`/`ScheduledService` hang off the legacy `Truck`. §13's "next scheduled maintenance" degrades to the nearest of `licenseExpiry`/`registrationExpiry`/`insuranceExpiry` — all `@db.Date`, so `formatDateOnlyShort` per quick-541 — and the column says "Registration due", never "Maintenance". Likewise "estimated time to next stop" is rendered as **"Window closes"**, not an ETA: there is no departure time until the trip starts and no routing call on this page, and a straight-line guess reads as a promise.
- **Phase 11 — the board MASKS a driver residence, it does not filter it.** A trip's stops can include a `DRIVER_RESIDENCE` end stop, so `board-lookup.ts` is a Section 9 read site and calls `maskFacilityForViewer` with `staffViewer(session)`. Dropping the row would delete the end stop and make the day look like it finishes at the last delivery. HOS is queried on **UTC** day bounds `+ endTime: null` — deliberately the same window `fleet-drivers.ts` uses, because `computeHOSClocks` clamps its own totals with `setUTCHours` and a local-midnight filter would show the same driver different hours on two screens.
- **Phase 12 — the customer-facing Help Centre had 57 written articles that NOTHING linked to, and its clickable articles are hardcoded stubs.** Two content mechanisms sit behind one URL prefix: `/help/{category}/{article}` renders the literal words *"Article content coming soon."* and is what the help index links to; `/help/{slug}` renders `docs-content/client/{slug}.mdx` and had **zero inbound links anywhere in `src/`**. `/help/whats-new` — the one page that linked them — was itself orphaned, `HelpSidebar`/`HelpSearch`/`HelpButton`/`HelpSheet` have no importers, and `search-index.json` is therefore read by nothing rendered. **`apps/web/docs-content/` is a DECOY**: `renderClientDoc` resolves `process.cwd()/../../docs-content`, i.e. the repo root. Fixed by `HelpGuides` on `/help`, which walks the **feature registry** (not the directory) and keeps entries that have a file — registry-first because `renderClientDoc` throws when the slug is absent from the registry, so a link built from a filename can still 404. **Adding an article means adding a registry entry**, and one file per slug: `check-doc-drift.ts` is the CI gate.
- **Phase 12 — `SETTINGS_PAGE_META` and `SETTINGS_NAV_SECTIONS` are two separate lists in `settings.config.ts`, and Phase 9 filled in only the first.** `/settings/operations` therefore rendered a correct header and had no nav entry for three phases; both inspection settings were reachable only by a typed URL. Same class as the orphaned sidebar (quick-552) and the orphaned help system above: **every piece works, nothing fails, and only the link is missing.** The Configuration section is now nine items against a stated cap of eight — recorded in the file. **A settings page is not shipped until a DOM query finds its link.**
- **Phase 12 — three of the five Document Import tenant settings have NO UI and can only be changed in the database**: `autoCreateRouteTemplatesFromImports`, `defaultEndStopPolicy`, `homeBaseFacilityId`. Only `requirePreTripInspection` and `blockTripStartOnFailedInspection` have a screen (`/settings/operations`). The help articles say so rather than describing a screen that does not exist — do not "document" the other three as configurable.
- **Phase 12 — `/help` is inside the `(owner)` route group, so a DRIVER is redirected to `/unauthorized`.** All nine driver-facing articles are readable only by owners and managers; they are written for the dispatcher briefing a driver. Pre-existing (eight `driver-*.mdx` articles were already in that position), reported rather than fixed.
- **Phase 12 — swapping files under a running `next dev` poisons the Turbopack cache and the browser then reports your work as MISSING.** A baseline test diff that momentarily deleted one new component left every route reporting "not linked" while the files on disk were correct. Remedy is the standing one: stop the server, delete `apps/web/.next`, restart, re-run. **A red browser check straight after a file swap is a stale bundler before it is a regression — but re-run it, never assume it.**
- **quick-557 — a client that RE-DECLARES its endpoint's payload can render a fully-populated database as missing data, and the "obviously empty" column is not where the bug is.** `/carrier/trips` showed DRIVER "Unassigned" and DATE "—" on every row. `listTrips` runs `trip.findMany` with **no `select`**, so every `Trip` scalar comes back and `primaryDriver { firstName, lastName }` is already `include`d — the query was never the problem. Both lanes of the page each carried their OWN `interface ApiDispatch`, and both described the **legacy `Route`** model: `driverId` for `primaryDriverId`, `scheduledDate` for `scheduledDeparture`, `_count.loads` for a count nothing selected. Reading a name a payload does not carry yields `undefined`, and `undefined` is precisely what `if (!driverId) return "Unassigned"` and `if (!dateStr) return '—'` exist to display. **`truckId` IS a real `Trip` column — so the Truck column alone kept working, and one correct column is what made the failure read as lost data rather than as a wrong object.** Same family as the two-route-systems trap: establish which model a field name belongs to before trusting it. The mapping now lives once in `lib/carrier/trip-list-row.ts` **in the API's vocabulary**, so a rename on `listTrips` is a type error rather than an `undefined` on screen; a mapping test alone is blind to the query feeding it, so a source scan pins `listTrips`'s `include`/`_count` alongside (quick-549's rule).
- **quick-557 — prefer the relation the query already INCLUDED over a page-built id→name map.** `TripsPage` builds `driverMap`/`truckMap` from `status: 'active'` rows only, so a trip assigned to a since-deactivated driver reads "Unassigned" from a *second*, independent cause — the same false sentence surviving the fix to the first. Keep the map as fallback, never as the source. And a **grid column `id` is a PERSISTED key** (`grid_preference`, keyed by `gridId`): when the field is wrong but the column is not, move to an `accessorFn` and leave the id alone.
- **quick-557 — `--reporter=basic` does not exist in vitest 4: it exits 0 having executed ZERO tests.** A "baseline" captured that way is the tsc-blind-gate trap in a different tool. The lesson generalises past the flag: **a green run whose output contains no test counts is not a green run** — read the `Test Files … | Tests …` summary, or you are diffing nothing against nothing. Real baselines come from a `git worktree` at HEAD **inside the repo** (node resolution walks up to the root `node_modules`, so no symlink and no `npm install`), removed with `git worktree remove --force`.
- **quick-559 — `lg:hidden` / `hidden lg:block` is CSS, not mounting, and every effect in the hidden lane still runs.** `/live-map` rendered both breakpoint variants and hid one, so React mounted both subtrees and **both `setInterval`s fired**: at 1600 px `LiveMapMobile` polled `/vehicles` every 15 s from under `display: none`, and at 390 px the desktop wrapper did the mirror, pulling the full ~18 kB `/live-board` payload into a hidden subtree **on the device most likely to be metered**. `ResponsiveSwitch` (`components/ui/ResponsiveSwitch.tsx`, on `useIsDesktop()`) is the repo's existing answer, already used on 10+ carrier pages — its header documents the same defect in its **original** form, two live `<form>` trees with duplicate submit targets. **Duplicate pollers are that defect wearing different symptoms; reach for the existing component rather than a bespoke width check.** Measured: 1600 px 10 req/88.6 kB → 5 req/51.0 kB per 60 s, 390 px 10 req/88.6 kB → 4 req/19.3 kB with `live-board` going to **zero**. The costs are real and both were measured rather than assumed: **neither lane appears in the server HTML any more** (`useIsDesktop()` is `undefined` until mount, so give `ResponsiveSwitch` a `fallback` that reserves the height or the page jumps), and **a resize across 1024 px unmounts one lane, mounts the other, and refetches**.
- **quick-559 — verify a hidden-lane fix by DOM NODE COUNT, never by the request count halving.** A halved count is also what a coincidental cache hit, a slower dev server or a dropped request looks like. The assertion that means something is that the node unique to the dead lane is **absent** (`LiveMapMobile`'s marker went 1 → 0 at desktop width; `BoardToggle`'s went 1 → 0 at mobile width). Related, and the reason role locators are the wrong instrument here: **Playwright's `getByRole` reads the accessibility tree, which EXCLUDES `display:none` subtrees** — it returns 0 for a hidden-but-mounted node and would "prove" the very thing you are testing. Use a CSS/attribute locator to ask whether a hidden thing exists.
- **quick-559 — match a poll interval to the rate its data is WRITTEN at, not to a sibling endpoint's interval.** `/live-board` moved 15 s → 30 s (it was the fastest non-terminating poll in the app attached to its largest payload — backwards; `tracking-poller`, also a screen left open all day, has been 30 s since it shipped, and two consecutive idle polls are byte-identical apart from `computedAt`). `/vehicles` **deliberately stayed at 15 s**, because `driver-gps-ping.tsx`'s `THROTTLE_MS` is `15_000` — a driver's browser posts a position at most every 15 s, so 15 s tracks the fastest ingest exactly and 30 s would leave a marker a full cycle behind. Two endpoints on one page legitimately carry different rates; making them match means picking one endpoint's answer for both. Both live in `lib/carrier/live-map-constants.ts`, replacing **three** separate `POLL_INTERVAL_MS = 15_000` declarations — which is how three timers on one page came to look like one deliberate rate. **A repo-wide polling ladder was recommended but NOT built**: six values from 1.5 s to 60 s are mostly defensible in isolation, and collapsing them needs its own review.
- **quick-559 — an `ETag` on `/live-board` is a trap until `computedAt` is excluded from the hash.** It is stamped from `Date.now()` on every call, so no two responses ever match: a naive `If-None-Match` adds a header, a comparison and a round trip and returns a full 200 body every single time — **overhead that looks like it is working**. It would also save bytes only (all four `loadBoardFacts` queries still run to discover nothing changed, and the DB cost is the larger number), and a 304 leaves the board's stale-data banner — which reads `payload.computedAt` — with nothing to refresh from.
- **quick-561 — a control that looks "styled louder" may be the SAME control, alone in its row.** The walkaround's "Change to fail" was reported as visually shouting over the Pass/Fail choices below it. It is the identical `AnswerButton`, same height, same red — but once an item is answered it is the **only** button rendered, and `flex-1` in a one-child flex row means **full width**. Three thirds became one whole, so the loudest thing on screen was attached to the item the driver had already dealt with. Fix was to move `flex-1` out of the shared class string and into each tone, so a new `'correct'` tone can opt out with `self-start` and a tinted `bg-red-50 / text-red-700 / ring-1 ring-inset ring-red-200`. **Red stays** — the destination is still a failure and Section 15 reserves red for that; only fill weight changed. And **`min-h-[56px]` was deliberately NOT reduced**: it is tapped with a glove on and is already above the design system's 48pt floor — **shrinking a control to make it quieter is the wrong axis.** General rule: before restyling a control that "looks wrong", check how many siblings it has at that moment.
- **quick-561 — `disabled` on an option that a re-fetch can turn unavailable must be `blocked && !selected`, never a blanket `blocked`.** The assign screen's pickers re-fetch on every change, because availability is a function of the planned day — so an option that was legal when it was picked becomes blocked the moment the start time moves. A blanket `disabled` then leaves that selection on screen and **unremovable**, which is worse than the problem being fixed (a dispatcher could not even toggle the optional trailer back off). Keep the selected row live so it can always be changed. Same family as "filter a picker; MASK a trip's own stops" — the state you are already in is not the state you are choosing.
- **quick-561 — "the API already supports it" is worth checking before repeating it.** The notification fix was framed as switching on something that existed. `?unread=true` is a **FILTER** — it is what the bell calls for its badge count — and `orderBy` was `createdAt: 'desc'` alone, so **nothing could sort**. Unread-first is `orderBy: [{ read: 'asc' }, { createdAt: 'desc' }]`, a new capability. Also: **there are TWO notification list endpoints**, `/api/v1/carrier/notifications` and `/api/driver/notifications`, with identical query shapes — fixing one bell and leaving the other is arbitrary. And the counts differ by scope: a tenant-wide `SELECT` said 5 unread, the panel shows 2, because the list filters `userId: null OR userId = session.userId`.
- **quick-561 — run the full suite AFTER the last commit of a task, not before it.** quick-559 recorded its "after" as 126 files / 1,553 tests; that run happened before it wrote its own guard file, so the number it published was its tree minus its own work. The true figure was 127 / 1,560. Nothing was wrong with the conclusion, but the next task inherits the number as a baseline, and a baseline that is 7 tests short reads as a regression when it is not. Re-measure the baseline with `git stash` rather than trusting the previous summary's arithmetic.
- **quick-562 — a route group's page shape cannot live in its `layout.tsx`, and that is why the string got pasted.** A Next layout wraps children; it cannot dictate their internal regions. `(driver)` gets away with one shell because its shell IS its layout (header + padded `<main>` + bottom nav). `(driver-fullscreen)` has no chrome to hang regions off, so five screens each wrote `flex min-h-dvh flex-col justify-between px-5 py-8` and two more wrote a near-copy. The fix is a component BESIDE `layout.tsx`, not more classes inside it. **When a class string repeats across a route group and the group has a layout, check whether the layout could have owned it before concluding someone was careless.**
- **quick-562 — `TakeoverScreen`/`TakeoverRunner` are ONE module with TWO arrangements, deliberately not one component with a `variant`.** The statement screens pad their container and pin actions with `justify-between`; the runner screens have full-bleed sticky bars whose `backdrop-blur` must reach the screen edges, so padding lives per region. That is a structural difference, and the Phase 11 rule applies — leave the wrong state unrepresentable rather than add a flag a later edit can pass the wrong value to. **The brief asked for "one shell for the five"; the five literal sites are one family and the two the brief also named are the other. Report the discrepancy rather than forcing them together.**
- **quick-562 — feedback goes in the ACTION region, and the shell makes that structural.** `feedback` is a prop on both shells, rendered as the first child of the action block / sticky footer, so a screen cannot place a banner a viewport from its button without leaving the shell. quick-546 fixed this once on `BeginScreen` **with a comment asking future edits to preserve it**, and it did not carry: `OutcomeScreen` had the identical defect one screen away, and the runner had it twice. **A comment is not an enforcement mechanism — the same lesson as quick-547's "this component holds no answer state" and quick-548's `blocked/page.tsx` header, both of which asserted the invariant that had already failed.**
- **quick-562 — "the error is the last child of `<main>`, right above the footer" is FALSE when the footer is sticky.** `main` scrolls and the footer does not, so source adjacency is not screen adjacency. Measured at 390×844 with 8 faults: the sign screen's upload error rendered at viewport y 815–869 — straddling the 844 fold **and painted underneath the sticky footer**, with `elementFromPoint` at its own centre returning `FOOTER.sticky bottom-0 z-10`. **The error was hidden by the very button it belonged to.** `elementFromPoint` is the assertion that catches this; a `getBoundingClientRect` distance alone reports a small number and looks fine.
- **quick-562 — a length floor on a source-scanning guard must be a PARAMETER, not a constant, once the scan sweeps files it makes no claims about.** quick-546's floor is right for the files under test and wrong for the sweep: a legitimate one-line re-export `page.tsx` in `src/app` is 293 bytes, so a blanket 400-byte floor fails on healthy source. Floor the named files; sweep the rest at 1. Likewise **draw a touch-target rule at the real floor (44px), not at the number this task happened to use (56px)** — `Save & exit` and `Back to the checklist` are pre-existing iOS-floor controls, and a guard that fails on correct existing code is a red test everyone learns to ignore.
- **quick-562 — `apps/web` currently has NO working lint entry point.** `next lint` no longer accepts `--dir` on this Next version, and ESLint 9 finds no `eslint.config.js` (the repo still has `.eslintrc.*`). tsc is the only gate that actually runs. Stated so the next task reports it rather than quietly skipping it — and so nobody concludes "lint passed" from a command that errored.
- **quick-563 — read a shared component's TRIGGER before deciding a list can become a select.** `SearchableSelect`'s closed state renders `selectedOption.label` and nothing else — no badge, no `secondaryLabel` — so collapsing a list that showed facts inline **deletes those facts for the one option the user actually chose**. On the assignment screen that was availability, hours-remaining and compliance flags, i.e. the whole reason Section 11 says the picker shows availability inline "so no second screen is needed". The summary line under each picker is not an addition to the brief; it is how "preserve every piece of information shown inline" survives the collapse. **Check what a component hides when closed, not just what it shows when open.**
- **quick-563 — `showStatus` is only presentable for the eight keys in `STATUS_CONFIG`; everything else renders the RAW STRING in grey.** `getStatusConfig` falls back to `{ label: status, className: <slate> }`, so passing a carrier truck's own `status` column would have printed the literal `out_of_service` — snake_case, on screen, in a neutral colour, for a truck that must not roll. Always MAP a domain status to a known key. Same family as DEC-14: the vocabulary the database holds is not the vocabulary a component renders.
- **quick-563 — `sortByStatus` alone ranks an UNPICKABLE option above a PICKABLE one, and the pair is real.** `STATUS_CONFIG` gives "On Trip" priority 50 and "Not Ready" 20, but an on-a-trip driver cannot be chosen and a CDL-expiring-soon driver can. Set `sortPriority` explicitly — `(blocked ? 0 : 1000) + badgePriority` — so pickability is the primary key and the badge is only the tiebreak. Verified in the browser: the three blocked trucks sort last despite one carrying the higher status priority.
- **quick-563 — a blocked option must never wear a green badge, and the guard needs the counter-assertion.** The flat rows carried a red triangle; a select carries a badge, and the badge is DERIVED. Assert every blocked shape against the green key set **and** assert that a clear option does come out green — without the second half the first passes by saying nothing. Derive the badge only from what the server states as data (`assignedToday`, `status`, `blocked`), never by re-reading flag text, and give the fallback a red destination so an unrecognised future blocker fails safe.
- **quick-563 — `blocked && !selected` (quick-561) matters MORE on `SearchableSelect` than it did on a list row.** Every change routes through `onSelect` on the option itself and a disabled `CommandItem` carries `pointer-events-none`, so disabling the current selection is not an odd-looking row — it is a **one-way door**, with no way to change a choice that went bad when the planned day moved. The visible marker moves to the summary line instead.
- **quick-563 — before repeating "use the component the app already has", check WHICH screens actually use it.** The brief named New Trip's driver picker, the client picker and the facility picker as precedent; grep returns exactly two files for `SearchableSelect` — the component and `DispatchLoadModal`. **New Trip is a plain native `<select>`**, and the facility picker is `FacilitySearchModal`, a server-searched modal — a defensible different choice for an unbounded set, not a lapse. An inherited premise about which conventions are established is worth one grep.
- **quick-565 — cmdk hides a non-matching item outright, and `useCommandState` is how a consumer can say so without leaking the filter term.** `SearchableSelect` does not filter; `<Command>` scores each `<CommandItem value={label}>` with `command-score`. `useCommandState(selector)` reads `{ search, filtered: { count } }` from Command's **context**, so anything that needs the filter state must render INSIDE `<Command>` — which is exactly what keeps it from becoming a prop every caller has to thread. Re-export the hook from `ui/command.tsx` rather than importing `cmdk` a second time; that file is already the single door to the library.
- **quick-565 — a filtered list that shows no trace of what it removed is at its worst on the options that matter most.** The assign screen's truck picker sorts three BLOCKED trucks last, so any term that excluded them (e.g. "Ford") silently dropped precisely the rows a dispatcher needed to see. **When adding search to a list that contains warnings, check what a search term does to the warnings.** The footer reads "13 of 14 hidden by your search" and appears ONLY while a term is typed AND something is hidden — a line permanently reading "0 of 14 hidden" is noise, and noise teaches people to stop reading the line that matters.
- **quick-565 — `CommandEmpty` fires for TWO different facts and the caller's `emptyMessage` is written for only one of them.** It renders whenever the filtered count is zero — both when a search matched nothing and when there was never anything to match — so an empty roster borrowed "No drivers found." and read as though a search had happened. Split them: `options.length === 0` gets its own sentence ("Nothing to choose from."), the search case keeps the caller's. The generic sentence must name **no noun** — the same component's options are drivers, trucks, trailers or trips across seven call sites, so a guess is wrong six times in seven.
- **quick-565 — put a count OUTSIDE `<CommandList>`, which is `max-h-[300px] overflow-y-auto`.** Inside it, the count scrolls away from the results it describes on any list long enough to need one. Same family as quick-562's sticky-footer finding: source adjacency is not screen adjacency when one of the two scrolls. Verify with `elementFromPoint` at the element's own centre, not with a distance — a small number and an occluded element look identical to `getBoundingClientRect`.
- **quick-565 — do NOT start a baseline test run and then edit files while it is running.** That run reported 19 failed files / 1584 passed; a stashed-clean re-measure with `--reporter=json` reported 1588 / 66 twice with byte-identical failing sets. It is the quick-561 baseline trap in a new form. Also established: **`--silent` and `--reporter=json` disagree by ±4 tests on this suite** (quick-549's cold-cache flake), so **measure before and after with the SAME reporter** or the delta means nothing. The JSON reporter's "test files" number counts `describe` suites, not files — compare TEST counts.
- **quick-565 — `apps/web` has no jsdom and no @testing-library/react.** A component whose behaviour depends on context (anything reading `useCommandState`) cannot be asserted in place. Lift the decision into exported pure functions and drive those, the way `assignment-options.ts` does, and verify the interactive path in a real browser. Do not add a test dependency to close a gap a two-line refactor closes.
- **quick-566 — a nested nav item's indent is the SUM of three classes, and the ceiling is the parent's own LABEL, not the parent's icon.** `SidebarGroup`'s child wrapper was `ml-6` (24) + `border-l` (1) + `pl-3` (12) = **37px**, which put the Document Imports icon at 53px against a Trips label starting at 43.88px — the child icon sat **past the word it was supposed to sit under**, which is what "pushed right" actually was. `ml-2` gave 21px and **quick-568 took it one further step to `ml-1` = 4+1+12 = 17px** (measured: child icon 33px against a parent icon at 16px and a parent label at ~44px). The binding constraint across both steps is the ORDERING — parent icon < child icon < parent label — not any particular value. The rule generalises: **measure a child indent against the parent's label origin (`p-2` + icon 16 + `gap-3`), and read all three spacing classes together — changing one while eyeballing another is how 37px looked like "one indent step".** The collapsed rail (`SidebarFlyout.tsx`) renders children in a Radix popover with its own layout and is NOT affected by this class.
- **quick-566 — deleting a line from `REQUIRED_SIDEBAR_HREFS` records nothing; the absence has to be asserted or the next task re-adds it.** AR Aging was removed from the sidebar's REPORTS group on request (route, page, API, `arAgingReport` key and its `PERMISSION_GATED_PATHS` row all untouched — this is a nav decision, reversible by re-adding one `reportsItems.push`). The spec gained a **positive+negative pair** scoped to `aside a`: the negative alone would pass identically if the whole REPORTS group vanished (quick-563's rule), and an unscoped `document.querySelectorAll('a')` could be tripped by an unrelated in-page link. **Scope a nav-absence assertion to the nav.**
- **quick-566 — hiding one surface's entry creates a SURFACE DISAGREEMENT, and the docs are where it becomes a lie.** `owner-more-menu.tsx:96` (mobile, `lg:hidden`) still links AR Aging — quick-554 added it there precisely to end a desktop/mobile split, so this task re-opens one in the other direction. Left alone deliberately (brief scoped to the sidebar) and **reported**. What could not be left alone: `docs-content/client/carrier-reports-aging.mdx` said *"Navigate to Carrier → Reports → AR Aging"*, a desktop-sidebar claim that this change made false. **When you remove a nav entry, grep the help articles for the path it described** — same family as quick-547/548/549: a sentence asserting a route nobody can take.
- **quick-567 — a baseline `git worktree` does NOT carry `apps/web/.env.local`, and the skew looks like a real delta.** `.env.local` is untracked and gitignored, so `git worktree add` never copies it; DB-dependent tests then behave differently and the baseline came back **1730/1575/66/86** against the same commit's published **1730/1600/66/61** — 25 tests moved between passed and pending, which reads exactly like a regression. The worktree rule (put it INSIDE the repo, `git worktree remove --force`, never symlink `node_modules`) is about not destroying the tree; **this is the separate one about not trusting what it measures.** The sound method for a same-repo baseline is `git checkout <rev> -- .` on the task-touched files in the MAIN tree, where the untracked env file stays present, then `git checkout HEAD -- .` to restore. Stop `next dev` first. Same family as quick-565's contaminated baseline and quick-561's stale one: **three consecutive tasks have now published a baseline that was measuring something other than the tree they thought.**
- **quick-567 — `/carrier/reports/aging` now has NO nav entry on ANY surface, deliberately, and that is the one state this repo has repeatedly reached BY ACCIDENT.** quick-554 added it to the mobile More menu to close a desktop/mobile split; quick-566 removed the sidebar entry and reopened the split the other way; quick-567 removed the mobile one, so the surfaces agree again at "direct URL only". The route, page, API, `arAgingReport` key, its team-permissions catalogue row and its `PERMISSION_GATED_PATHS` row are all intact — the owner toggle still gates URL and API access, it just gates nothing in a menu. **Record an intentional unlink loudly**, because quick-552/553 spent two tasks recovering pages that lost their links by mistake and an orphan scanner cannot tell the two apart. Both nav guards now pin the absence with a positive+negative pair, and the absence is asserted for an **OWNER** — the most permissive viewer — so it cannot be satisfied by a permission merely being switched off.
- **quick-599 — `WITH CHECK` never applies to DELETE.** It is applied to INSERT and to the *new* row of UPDATE only; DELETE is checked against `USING` alone. Adding an explicit `WITH CHECK` to a `FOR ALL` policy therefore closes INSERT and UPDATE and leaves DELETE exactly as open as `USING` already left it — measured on `"AutomationRule"`: the new check stopped a tenant-scoped connection from INSERTing or UPDATEing a platform `scope='SYSTEM'` row, and the SAME connection could still `DELETE FROM "AutomationRule" WHERE scope='SYSTEM'` and remove all 6, both before and after. Closing all three commands without weakening `USING` (and therefore SELECT) needs a per-command policy split — `FOR SELECT` / `FOR INSERT` / `FOR UPDATE` / `FOR DELETE` — not one `FOR ALL` policy with a check bolted on.
- **quick-599 — an RLS-refused UPDATE or DELETE is a SILENT 0 ROWS, not a 42501.** 42501 is what a failed `WITH CHECK` produces — i.e. INSERT, and the new row of UPDATE. When a table has NO policy at all for a command, `USING` filters every candidate row away and the statement reports 0 rows affected with no error — measured directly: `"Tenant"` had no UPDATE policy, and `UPDATE "Tenant" SET ... WHERE id = <own id>` as `app_user` returned 0 rows silently, which without a same-transaction counter-read (`SELECT count(*) ... WHERE id = <own id>` = 1) is indistinguishable from "the row does not exist". `policy-satisfiability-sweep.md` §4.1 stated the INSERT case ("an ERROR 42501 ... not a silent zero") as if it covered UPDATE/DELETE too and has been corrected in place. Every 0-row write probe against RLS needs that counter-read, always.
- **quick-599 — a policy split that must survive an EXISTING name-keyed enumeration keeps the ORIGINAL name on whichever half is closest to the original semantics.** `tests-db/rls-isolation/coverage.test.ts` finds "every tenant-scoped table" by querying `pg_policy` for the literal policy name `tenant_isolation_policy`. Splitting `audit_log`'s single `FOR ALL` policy into SELECT and INSERT halves and giving BOTH new names would have made `audit_log` vanish from that enumeration entirely — not "NOT COVERED", simply absent — and the existing `'every behaviour target is classified COVERED'` test filters by target key, so an absent target yields an empty filter and passes VACUOUSLY. The fix that generalises: the half that keeps the table's original read/write shape keeps the original name; the genuinely new half (here, `audit_log_append_policy`, `WITH CHECK (true)`, admitting a write the table never allowed before) gets the new one. Added the missing counter-guard too: assert every enumerable target key actually APPEARS in the live enumeration, before asserting what its status is — a target that silently leaves the enumeration is a different failure than one that's merely uncovered, and only the first assertion catches it.
- **quick-599 — a write-probe table move between two lists must be asserted as a move, via a union-equality test, or "moved" and "silently dropped from both" are indistinguishable in the diff.** `audit_log`'s write contract changed shape (own-tenant DELETE now raises 42501 instead of affecting rows), so it moved from `WRITE_PROBE_TARGETS` to a new `APPEND_ONLY_WRITE_PROBE_TARGETS` rather than being deleted from probe coverage — deleting it to make the suite green would have been the exact "weaken a guard to go green" failure CLAUDE.md forbids. `WRITE_PROBE_TARGETS + APPEND_ONLY_WRITE_PROBE_TARGETS` is asserted to equal the original four table names, sorted; proven to fire by deliberately emptying the append-only list without adding its table anywhere else and watching the union-equality assertion fail (`expected [...] to deeply equal [...]`), then restoring it — a guard asserted without a witnessed red is the quick-549 shape.
- **quick-599 — a design doc's argument can be sound and its premise still false; measure the premise before reusing the argument.** `bypass-replacement-design.md` §3.1 item 4 argued `audit_log_append_policy`'s `WITH CHECK (true)` was safe for an append-only table *because* "audit_log already carries REVOKE UPDATE, DELETE". Measured on staging before touching anything: `app_user` held both grants, and both an UPDATE and a DELETE of an own-tenant row succeeded (1 row each) — the exact opposite of the claim, repeated verbatim in `src/lib/security/audit-log.ts`'s header for months. The argument itself was fine; its premise needed to ship as a REVOKE inside the SAME migration, or the SELECT/INSERT policy split alone would have left UPDATE/DELETE refused only by absence-of-policy (a silent 0, the D3 rule above) while the grant still said `app_user` could write.
- **quick-600 (B5) — the admin connection is `app_admin`, never `postgres`, and the reason is the boot guard, not merely privilege minimisation.** `postgres` carries `createrole`/`createdb`/membership in `app_user`/`service_role`/`authenticated`/`anon` — an admin connection needs `BYPASSRLS` plus DML on the routed tables and nothing else. But the DECISIVE argument is that a two-direction boot guard needs a role `current_user` can actually distinguish from the tenant connection: with `postgres` on both pools, `DATABASE_URL_ADMIN` mistakenly pointed at `DATABASE_URL` would be undetectable by construction. A naming convention is not a control; a role attribute the guard can query is.
- **quick-600 — `getAdminDb(reason)` is reachable ONLY from `tests/security/admin-connection-allowlist.test.ts`'s allowlist, and a tenant-in-hand call belongs on `getTenantPrismaForOrg`, never on the admin connection.** Enforced by the allowlist matching imports by MODULE PATH (not by imported name, so an alias cannot dodge it), a per-file call-COUNT assertion (so a new call inside an already-allowlisted file is still a reviewable diff), an alias ban, and a check that `adminPrisma` — the client itself — is never exported from any module. Proven to fire red on both a deliberate out-of-allowlist import and a deliberate alias (`.planning/quick/600-build-the-privileged-admin-connection-b5/evidence/02-allowlist-gate-fires.md`), then reverted and reconfirmed green.
- **quick-600 — the tenant-GUC `pool.on('connect', ...)` initialiser is DELIBERATELY absent from the admin pool.** A bypassing connection has no use for a tenant GUC, and copying the initialiser would make the two pools look interchangeable — which is exactly the appearance the separate `admin-prisma.ts` module exists to prevent. A test asserts the real call shape (`pool.on('connect', (client) => ...)`) is present in `prisma.ts` and absent from `admin-prisma.ts` — matched on the CALL, not a bare substring, because the substring `pool.on('connect'` also appears in this file's own explanatory prose and a naive check false-positives on the comment describing the invariant it protects.
- **quick-600 — `app_admin` bypasses RLS entirely, so GRANTs are the ONLY control left on that connection**, and a clean migration apply proves nothing about what a trigger fired by a granted statement additionally needs. `INSERT INTO "Tenant"` fires an existing, non-`SECURITY DEFINER` `AFTER INSERT` trigger (`trg_seed_tenant_notification_settings`, shipped in Phase 41) that runs with the INVOKING role's privileges and does `INSERT ... ON CONFLICT DO NOTHING` into `"TenantNotificationSettings"` after reading `"NotificationTemplate"`. `app_admin` had grants on neither, so the first REAL exercise of the routed `Tenant` create — not the migration applying cleanly — failed `42501`. And `ON CONFLICT` needs `SELECT` on the conflict target in addition to `INSERT`, independent of it — granting `INSERT` alone still failed. Two tables were added to the grant list only after being found this way, not by reading application code (`ROUTING-MANIFEST.md` §"grant table set").
- **quick-600 — when a `DELETE` probe hits an unrelated FK constraint (a real tenant has real dependent `User` rows), that is a DATA-INTEGRITY finding, not a grant/policy answer, and testing the wrong thing with real-looking data is worse than not testing it.** The fix is to isolate the question: create a THROWAWAY, dependent-free row inside the SAME transaction as the privileged connection being tested, delete IT, and keep the cross-tenant refusal check on the real row (where "0 rows, never reaching the FK layer" is the actual isolation claim for a tenant-scoped connection). Conflating the two questions in one probe would have reported the admin connection's grant as broken when the real blocker was `User_tenantId_fkey` — true under every role, including `postgres`.
- **quick-600 — a routing manifest built by reading application code will still miss shapes; a both-directions matrix run for real is what finds them, and it is allowed to send the grant list back for a correction.** Building the admin connection and verifying it are not sequential-and-independent; §5 of `docs/audits/admin-connection.md` exists specifically to record what static reading could not have predicted, named as a correction rather than folded silently into the original count.
- **quick-600 — when many call sites issue BYTE-IDENTICAL SQL against the same table (e.g. `suspendTenant`/`reactivateTenant`, or four cron files' identical `Tenant` sweep), a both-directions verification matrix may consolidate to ONE row per statement SHAPE rather than one row per call site — but only if every call site sharing a shape is named in that row and the consolidation is stated as a deliberate decision, not a silently narrowed scope.** What a grant or a policy can say about one instance of a statement it says about every byte-identical instance; the thing being verified is the database's answer to the statement, not the call site's location in the source tree.
- **quick-602 — the unmigrated-path tripwire is a RAISE reached only through the null branch of a COALESCE, and `current_tenant_id()` stays `LANGUAGE sql STABLE` so it stays INLINABLE across 93 policies.** The raise lives in a separate plpgsql `tenant_context_required(text)` carrying SQLSTATE **`TC001`**. That the fast path does NOT raise was MEASURED, not reasoned about: with the flag on and a real uuid, `EXPLAIN (VERBOSE)` shows the whole expression — raiser included — inlined into the scan filter and simply not reached. The `NULLIF` collapses both the unset case and the Supavisor `''` case BEFORE the check, so the cast never sees `''` (the quick-597 finding). **The flag is a GUC read at call time, NEVER a settings table**: a table read inside that function would put a query inside every policy evaluation on 93 policies, subject to RLS (recursion) and needing its own grant.
- **quick-602 — `ALTER ROLE`/`ALTER DATABASE … SET` of a PLACEHOLDER GUC is `42501` on this Supabase instance, for `postgres` AND for `app_user`, and Supavisor SILENTLY DROPS the connection-string `options` parameter.** Both were attempted, in three spellings for `options` — every one connected successfully and read the flag back as `''`, which is the shape of a control that looks armed and is not. Supabase's `postgres` is not a superuser and PostgreSQL will not let a non-superuser persist a placeholder. **The existing `pg_db_role_setting` rows are NOT evidence that you can write one** — they were written by platform superuser roles, which is exactly why they looked like proof. **A session-level `SET`, issued per connection by whatever opens it, is the only arming mechanism there is.** That is strictly safer for production (no server-side switch to flip by accident, and the flag cannot outlive the process) and it means arming an environment is an application change, not one statement.
- **quick-602 — `TC001` is recognised by CODE, never by message prose, and `current_query()` in DETAIL is how a policy names the statement it refused.** A policy expression has no `TG_TABLE_NAME`; the statement text is the only handle the database has, and it works — a real raise quoted `auto-close-tickets`' whole `SupportTicket` raw query back at us. **`TC` is not one of PostgreSQL's own SQLSTATE classes**, and the server accepted the ERRCODE and returned it verbatim as the driver's `code`. Also measured: **the UNSET-vs-EMPTY distinction in the message is correct code that a POOLED connection can essentially never reach** — once a placeholder name exists on a backend it stays defined for that backend's lifetime and a reset returns it to `''`, so five successive "fresh" clients on the same Supavisor backend all read `''` while a never-set control name read NULL. Every TC001 in practice says "the EMPTY STRING".
- **quick-602 — a permissive `bypass_rls_policy` does NOT suppress the raise, measured in BOTH policy-creation orders across SELECT/UPDATE/DELETE — six probes, six raises.** So bypass-flagged statements are **EXEMPTED** inside `tenant_context_required` (`current_setting('app.bypass_rls', TRUE) = 'on'` → return NULL), because such a statement is ADMITTED by the bypass arm and does not produce the silent empty the tripwire exists to catch. **The cost is ~211 sites unsignalled** and it is visible rather than argued: with bypass on and no tenant context, `SELECT count(*) FROM "Tag"` returned rows belonging to TWO different tenants and nothing complained. Those sites stay owned by the Phase 0 bypass programme.
- **quick-602 — routing a path to `getAdminDb` REMOVES IT FROM THE TRIPWIRE'S REACH, and the exemption is per-STATEMENT, not per-route.** `app_admin` carries `rolbypassrls`, so no policy is consulted at all and no signal can exist. This is a real interaction between quick-600 and the tripwire that nothing else records. But do not read a file-level marker as a verdict: `auto-close-tickets` is a `getAdminDb` route and **still raised**, because at least one of its statements runs on the tenant connection. Five of the fourteen cron routes carry two mechanisms and one carries none — a grep says what a route CONTAINS, never what a given statement USED.
- **quick-602 — when a route can swallow its own error, detect the raise at the DRIVER, not from what the handler rethrows.** The first execution sweep scored `trip-reminders` as COMPLETED because it caught its own `TC001` and returned a generic 500 — under-reporting the signal exactly on the population `guc-binding.md` singled out for swallowing failures. Observing the pg client's own promise rejections (`err.code === 'TC001'`, attached with `.then(undefined, …)` so the original rejection still propagates) took the count from 1 to 12. **And the corollary is worse than the bug**: `purge-deleted` raised SEVEN times, returned HTTP 200, and logged `Error: [object Object]` (the `logger.error(message, error, context)` arity bug), so a perfect signal was completely invisible to anyone outside the process. A tripwire's usefulness is bounded by the caller's error handling.
- **quick-602 — the bare Prisma client is NOT context-free: it inherits whatever ran last on the pooled connection.** `getTenantPrismaForOrg` writes the GUC with `set_config(..., false)` — SESSION scope — and the pool holds `max: 1`, so `prisma.tag.count()` run after a scoped call returned a row instead of raising. That is quick-413's leak, reproduced from the other direction, and it is a hole in what the tripwire can see. Any probe of "does an unscoped path raise" must run BEFORE any scoped path in the same process, or it is measuring the previous probe.
- **quick-602 — a migration countdown without a `withTenantContextCallSites` counter cannot tell "migrated" from "deleted", and that counter must be AST-based.** `withTenantContext` already appears in three files — `lib/auth/supabase.ts`, `lib/email/sender-config.ts`, `lib/onboarding/activation-tracker.ts` — and **all three are prose inside comments**, so a grep reports 3 migrated call sites on a tree that has zero. `ts.createSourceFile` with no `Program` and no type checker counts call EXPRESSIONS, which is also what lets the classifier recognise the MIGRATED shape although the wrapper does not exist yet: it never has to resolve the identifier.
- **quick-602 — the runtime signal and the static countdown see DIFFERENT populations, not the same one at different resolutions.** Measured: of the 23 source files the execution sweep invoked, 4 appear in the countdown, covering **4 of 456 units — 0.88 %**. Most of the 12 runtime raises came from bare-client and `$queryRaw` paths the countdown does not name at all, because they never call `getTenantPrisma`. Neither instrument alone relaxes the cutover, and the reason is not "one is slower to reach coverage" — it is that each is blind where the other looks.
- **quick-607 — a script's connection target is decided BEFORE dotenv runs, and that is the only moment it is decidable.** `_bootstrap-env.ts:53-55` did `DATABASE_URL = DIRECT_URL` unconditionally while `.env`, `.env.local` and `apps/web/.env.local` all point `DIRECT_URL` at production, so `DATABASE_URL=<staging> npx tsx …` read PRODUCTION — measured, not reasoned: HEAD's bootstrap run verbatim with a staging pin resolved `oqdhberkghtnszrkdvfm`. dotenv is NON-OVERRIDING, so after the load an operator's inline pin and a file value are byte-identical; `_bootstrap-core.ts` therefore captures both variables at module top, above everything. The rule is five rungs in `scripts/_db-target.ts`: both pinned same project → the pinned `DIRECT_URL` (the 6543→5432 port fix survives inside the chosen project) · both pinned DIFFERENT projects → **REFUSE**, because silently picking one is the whole defect · only `DATABASE_URL` → **verbatim** · only `DIRECT_URL` → it · nothing pinned → file `DIRECT_URL` (historical behaviour, so no existing caller breaks — CI lands on rung 3 and now works for a principled reason instead of the quirk it was relying on). **`DIRECT_URL` is never reassigned** — `migrate.mjs` needs it privileged, `_prisma_migrations` has RLS on with zero policies and no `app_user` grant, and a de-privileged migration connection reads zero rows with NO ERROR.
- **quick-607 — the danger was never the wrong database; it was a script reporting success without naming one.** A read against production is survivable and the write hazard never fired, yet both audits that recorded this line missed that they were the same line. Every bootstrapping script now prints `[db-target] project : <ref>` **to STDERR** (never stdout — `--json` payloads pipe into `jq`, quick-585) with the password masked and the ref, host, role and resolution rung visible. **An operator must never have to infer the target**, and a transcript that does not name its database cannot be audited later: pre-607 drift transcripts record what was measured and not where, which is the one thing that could not be recovered retrospectively.
- **quick-607 — the writer guard INVERTS the default: a script declares itself READ-ONLY (`_bootstrap-env-readonly`); everything else is a WRITER and refuses production without `--allow-production`.** Forgetting therefore fails safe. The inverse default cannot work — the failure mode of forgetting would be a silent production write, and nothing downstream can tell "declared read-only" from "never considered it". Same idiom as the T3/T4 verdict union: make the wrong state unrepresentable rather than adding a check an edit can drop. Read-only scripts may target production freely, because `audit:rls-policy-drift` and `audit:app-user-harness` exist to measure it. **The read-only claim is checked by a HUMAN** — nothing at runtime can prove a script issues no writes — which is why `tests/security/script-db-target-guard.test.ts` pins the census both directions, asserts the default is `writes`, and asserts the literal `DATABASE_URL = DIRECT_URL` never returns.
- **quick-607 — `_bootstrap-env` is NAMED in 27 files and IMPORTED by four.** The other 23 mention it in prose while deliberately building their own `STAGING_*` pools, so a substring grep overstates the exposure SIX-FOLD. Same shape as quick-602's `withTenantContext` count (3 matches, all comments, zero call sites). **Filter to real import lines before believing any "who uses this" number.** The four are `rls-policy-drift.ts` and `app-user-connection-harness.ts` (read-only) and **`test-extraction.ts` / `test-pdf-import.ts`, the two writers** — both one plain command from writing to production.
- **quick-607 — DO NOT run a writer with `--allow-production` to demonstrate `--allow-production`.** Doing exactly that put 2 `document_imports` and 8 `document_import_pages` rows into production (deleted; back to the pre-existing 26 with zero orphans, nothing committed, no pre-existing record touched). The banner alone — `project : PRODUCTION`, `intent : writes` — was already sufficient evidence of the warning line; running the script to completion added nothing and cost real rows. The guard itself was correct: the plain invocation refused with exit 1.
- **quick-607 — when two databases are ALIGNED, no output-based heuristic can tell them apart, which is why the fix had to be a printed ref rather than a smarter check.** A drift run against production today is byte-identical to one against staging (183 live, 0 missing, 0 unexpected, 0 definition drift). The window is knowable only from `_prisma_migrations`: staging took `20260914180000` at 2026-09-15 05:22Z, production at 19:41:51Z, so they DIVERGED between. That window is what makes the retrospective answerable — quick-602 and quick-606 both ran inside it and reported **0 definition drift**, which production (lacking the migration, with differing `Tag`/`TagAssignment` bodies) could not have produced. **A forensic confirmation beats a label**; quick-595, which caught the unsafe invocation at execution time and started the "pin both" convention, has only the label — harmless there because the databases were aligned.
- **quick-602 — `"Tag"`/`"TagAssignment"` moved from a text-to-text GUC comparison to `"tenantId" = current_tenant_id()`, and the two edge cases are named rather than discovered later.** Ten of twelve measured GUC cases are identical across SELECT, INSERT-own and INSERT-foreign. The two that are not: a **non-canonical (uppercase) uuid now ADMITS** where text comparison filtered (a widening — arguably a fix, and no code path writes one), and **non-uuid garbage now raises `22P02`** instead of filtering (the class quick-597 removed from `audit_log`; it puts these two on the footing the other 91 policies always had). `WITH CHECK` was deliberately left UNDECLARED so it stays derived from `USING` — declaring one would be a second change smuggled in beside the one being measured.
- **quick-624 — pg-pool ENDS the physical connection on ANY autocommit SQL error, and that silently strips the tenant GUC from every later statement on a correctly acquired tenant client.** `pool.query` calls `client.release(err)` and `_release` removes any client released with an error (pg-pool 3.11.0 `index.js:356-361`); the next checkout opens a fresh connection, `prisma.ts`'s `pool.on('connect')` writes `app.current_tenant_id = ''`, and nothing re-issues the tenant value because `getTenantPrisma*()` set it ONCE, session-scope, on the old connection. Measured on the real prisma module: a caught `SELECT 1/0` (or a missing-column 42703) and the next `count()` on the SAME tenant client raises TC001 armed and **returns 0 for a tenant that has rows** disarmed; a `$transaction` started afterwards raises too; an error INSIDE an interactive transaction does NOT evict; re-acquiring the client recovers. **This is the inverse of quick-610/621's inheritance (the GUC survives a checkout) and a new mechanism.** It is why DRIVER_A's `/home` raised on `driver-dashboard.ts:124`, a row the census correctly scores `SCOPED` — the trigger there was staging's missing `FleetMessage.isBroadcast`, but any caught P2002/P2025 or `.catch(() => null)`-hidden failure does it in production.
- **quick-624 — `SCOPED` in the query census means "ran on a tenant client", NEVER "executes with tenant context".** 1,342 of 1,408 SCOPED rows (281 files) rely on a session GUC set by a different pool checkout; only the 64 that `set_config` inside their own transaction share a checkout with their statements and are immune. The census remains the right ROUTING list for the remaining gating statements, but finishing it closes the static gap and not this runtime one — the cutover needs a same-checkout GUC (a `withTenantContext`-style unit) as well. The fix is a shared mechanism and was deliberately not attempted in quick-624.
- **quick-624 — a log-correlating detector must close its window on a QUIET log, not a fixed delay, or it names the wrong route.** `604-click-through.ts` waited 350 ms (`617-mobile-click-through.ts`: 120 ms) and `/home`'s `[q620-hook] TC001` line arrived later, so quick-623 blamed `/my-load`. Both now wait for 1.5 s without log growth (capped 20 s, recorded per entry). Witnessed both ways on `/home,/my-load` ×3. Also: **620's detector prints only TC001**, so the 42703s that caused the eviction were invisible in every earlier log — `624-guc-trace-hook.js` journals errors of any SQLSTATE, connects/ends and `set_config` values per physical connection when the question is "on which connection, after what".
- **quick-625 — the tenant GUC CAN be re-asserted per CHECKOUT, and the tenant must be captured when `pool.connect()` is CALLED, never on pg-pool's `'acquire'` EVENT.** `_acquireClient` emits `'acquire'` from `_pulseQueue`, which runs from `_release()` — the RELEASING request's async context — so an ALS read there answered with the other tenant on 13–56 of 64 reads. The working shape (`scripts/audit/625-tenant-checkout.prototype.ts`): a `pg.Pool` subclass overriding `connect()` (both `pool.query()` and adapter-pg's `startTransaction` go through it; `PrismaPg` only checks `instanceof pg.Pool`), with AsyncLocalStorage populated by the TENANT CLIENT (a top-level `$allOperations` — which also sees raw — plus a Proxy on `$transaction`, which is not an operation). Binding is per client OBJECT, not per request. Measured 64/64 on 4 reads × max:1/max:5 × with/without evictions, 0 wrong/empty/P2028/TC001. **Recommended over `withTenantContext`** (covers all 1,342 exposed census rows with zero call-site changes; per-unit binding is 623 units, 133 of which already open their own transaction and would nest). **The 144 bare statements still need routing** — a bare client has no tenant to re-assert; checkout asserts `''` and they fail loudly instead of inheriting. `docs/audits/guc-checkout-reassertion.md`.
- **quick-625 — Prisma's DataLoader batches same-SHAPE `findUnique` ACROSS CALLERS in one tick into ONE statement, dispatched from the FIRST caller's `nextTick`.** `batchBy` keys on model + argument keys + selection (values ignored) and applies only to `findUnique[OrThrow]` outside a transaction. One statement cannot carry two tenants' GUCs, so the second tenant got NULL for a row that exists — measured, and shipped mis-answers the same cell. Any per-checkout or per-statement GUC design must break the batch: re-issue a non-transactional `findUnique` as `findFirst` (never batched). Inside a transaction the batch key is the tx id, so leave it — and rewriting there onto the bare delegate would run OUTSIDE the transaction.
- **quick-625 — a statement issued on a pooled client between checkout and hand-back has NO `'error'` listener.** pg-pool removes its idle listener at checkout and `pool.query()` attaches its own only after `connect()` delivers, so a socket loss while a checkout hook's `set_config` is in flight is an uncaught `'error'` event that KILLS THE PROCESS (witnessed: `PROCESS DIED exit=1 … Unhandled 'error' event`). Hold a no-op listener for exactly that window. And on an assertion failure `client.release(err)` yourself before failing the checkout — `pool.query`'s connect-error branch never releases.
- **quick-625 — a prototype that imports `withTenantRLS` belongs in `scripts/audit/`, not `src`.** Placed in `src/lib/db/`, it turned `tenant-mechanism-fence` (8 referencing files vs an allowlist of 7) and `wrapper-migration-countdown` (totals moved) red, correctly. Neither corpus scans `scripts/`. Moving the file is the honest fix; an allowlist entry or regenerated artefact for a prototype would be widening a guard to go green.
