---
phase: 27-automated-playwright-tests
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - e2e/owner/dashboard.spec.ts
  - e2e/owner/trucks.spec.ts
  - e2e/owner/drivers.spec.ts
  - e2e/owner/loads.spec.ts
  - e2e/owner/routes.spec.ts
  - e2e/owner/finance.spec.ts
autonomous: true

must_haves:
  truths:
    - "Owner dashboard loads with fleet summary stats"
    - "Trucks can be created, viewed, edited, and deleted via the UI"
    - "Drivers can be invited and managed via the UI"
    - "Load lifecycle PENDING to DISPATCHED to DELIVERED to INVOICED is tested end-to-end"
    - "Route finance section displays expenses and profit data"
  artifacts:
    - path: "e2e/owner/dashboard.spec.ts"
      provides: "Dashboard rendering and navigation tests"
    - path: "e2e/owner/trucks.spec.ts"
      provides: "Truck CRUD lifecycle tests"
    - path: "e2e/owner/drivers.spec.ts"
      provides: "Driver invite and management tests"
    - path: "e2e/owner/loads.spec.ts"
      provides: "Full load dispatch lifecycle tests"
      min_lines: 60
    - path: "e2e/owner/routes.spec.ts"
      provides: "Route creation and multi-stop tests"
    - path: "e2e/owner/finance.spec.ts"
      provides: "Route finance, invoicing, and payroll page tests"
  key_links:
    - from: "e2e/owner/*.spec.ts"
      to: ".playwright/auth/owner.json"
      via: "test.use({ storageState })"
      pattern: "storageState.*owner\\.json"
    - from: "e2e/owner/loads.spec.ts"
      to: "/loads"
      via: "page.goto and status advancement clicks"
      pattern: "page\\.goto.*loads"
---

<objective>
Implement all Owner/Manager portal E2E tests covering dashboard, trucks, drivers, loads/dispatch lifecycle, routes, and finance.

Purpose: The owner portal is the largest and most complex portal with ~106 QA test cases. This plan automates the critical flows: CRUD operations for all entities and the full load lifecycle (PENDING -> DISPATCHED -> DELIVERED -> INVOICED). This is the core business value of the application.

Output: 6 spec files in e2e/owner/ covering all major owner portal features.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-automated-playwright-tests/27-RESEARCH.md
@.planning/phases/27-automated-playwright-tests/27-01-SUMMARY.md
@docs/qa/owner-tests.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner dashboard, trucks CRUD, and drivers management tests</name>
  <files>
    e2e/owner/dashboard.spec.ts
    e2e/owner/trucks.spec.ts
    e2e/owner/drivers.spec.ts
  </files>
  <action>
    Create 3 spec files in `e2e/owner/`. Each file MUST start with:
    ```
    import { test, expect } from '@playwright/test';
    import path from 'path';
    test.use({ storageState: path.join(__dirname, '../../.playwright/auth/owner.json') });
    ```

    Reference `docs/qa/owner-tests.md` for exact flows.

    **e2e/owner/dashboard.spec.ts** (~5 tests):
    - `@smoke dashboard loads with fleet summary cards` — goto `/dashboard`, expect stat cards (Total Trucks, Active Drivers, Active Loads, etc.)
    - `dashboard shows recent activity or loads table` — expect some content below stat cards
    - `sidebar navigation links work` — click Trucks in sidebar, expect URL `/trucks`; click Drivers, expect `/drivers`; click Loads, expect `/loads`
    - `dashboard filters by tag if tag filter exists` — if tag filter dropdown present, select a tag, expect content updates
    - `dashboard responsive on mobile viewport` — set viewport to 375px width, reload, expect content still visible and usable

    **e2e/owner/trucks.spec.ts** (~7 tests):
    - `@smoke truck list page loads` — goto `/trucks`, expect heading and table/list
    - `truck list shows columns` — expect headers: Make/Model, Year, VIN, Status, etc.
    - `@smoke create truck — happy path` — click Add/New Truck button, fill form (Make: `Test Make ${Date.now()}`, Model: `TestModel`, Year: `2024`, VIN: generate unique, Status: Active), submit, expect success
    - `view truck detail` — click a truck from the list, expect detail page with truck info
    - `edit truck` — on detail page, click Edit, change a field, save, expect updated value
    - `delete truck` — find the test truck created earlier (or create one), delete it, confirm, expect it removed from list
    - `truck form validation` — submit empty form, expect validation errors on required fields

    **e2e/owner/drivers.spec.ts** (~5 tests):
    - `@smoke driver list page loads` — goto `/drivers`, expect heading and table/list
    - `@smoke invite driver` — click Invite/Add button, fill email (`testdriver+${Date.now()}@example.com`), fill name, submit, expect success message or new entry in list
    - `view driver detail` — click a driver from list, expect detail page
    - `driver detail shows assigned routes/loads` — on detail page, expect section for assignments
    - `driver form validation` — submit invite form with invalid email, expect error

    **General patterns:**
    - Use `Date.now()` for all created entity names to avoid collision between test runs
    - Use `getByRole`, `getByLabel`, `getByText` selectors — fall back to `getByTestId` only when necessary
    - If `getByTestId` is needed, add `data-testid` to the source component and document which file was modified
    - Do NOT use `page.waitForTimeout()` — use `waitForLoadState('domcontentloaded')` or element visibility waits
    - For delete tests, clean up after yourself or delete only records created in the test
  </action>
  <verify>
    Run `npx playwright test e2e/owner/dashboard.spec.ts e2e/owner/trucks.spec.ts e2e/owner/drivers.spec.ts --project=chromium` — all tests pass.
  </verify>
  <done>
    Dashboard, trucks CRUD, and driver management tests all pass. Smoke-tagged tests run quickly. No regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Load lifecycle, routes, and finance tests</name>
  <files>
    e2e/owner/loads.spec.ts
    e2e/owner/routes.spec.ts
    e2e/owner/finance.spec.ts
  </files>
  <action>
    Create 3 spec files in `e2e/owner/`. Same storageState pattern as Task 1.

    Reference `docs/qa/owner-tests.md` sections on Loads/Dispatch, Routes, and Finance.

    **e2e/owner/loads.spec.ts** (~8 tests — this is the most important spec):
    - `@smoke load list page loads` — goto `/loads`, expect heading and table
    - `@smoke create load — happy path` — click New Load, fill origin, destination, rate, select truck and driver, submit, expect new load in list with PENDING status
    - `load detail page shows info` — click a load, expect detail page with origin, destination, rate, status
    - `@smoke dispatch load (PENDING to DISPATCHED)` — on a PENDING load detail, click Dispatch, expect status changes to DISPATCHED
    - `advance load IN_TRANSIT` — on DISPATCHED load, advance to IN_TRANSIT if applicable
    - `@smoke deliver load (to DELIVERED)` — advance load to DELIVERED status, expect status badge updates
    - `create invoice from delivered load` — on DELIVERED load, click Create Invoice or navigate to invoice creation, expect INVOICED status
    - `load form validation` — submit empty load form, expect required field errors

    IMPORTANT: The load lifecycle tests should ideally work on a single load created at the start of the describe block. Use `test.describe.serial()` to ensure they run in order if Playwright supports it, OR create a fresh load in each test that needs a specific status. The serial approach is preferred to test the actual status progression.

    **e2e/owner/routes.spec.ts** (~5 tests):
    - `@smoke route list page loads` — goto `/routes`, expect heading and table/list
    - `create route` — click New Route, fill origin, destination, select truck and driver, submit, expect success
    - `route detail page loads` — click a route, expect detail page
    - `multi-stop route editor` — on route form, add multiple stops using add stop button, fill addresses, expect stops appear in order
    - `route shows stop timeline` — on a route with stops, expect stop timeline/list rendering on detail page

    **e2e/owner/finance.spec.ts** (~5 tests):
    - `@smoke invoices page loads` — goto `/invoices`, expect heading and table
    - `payroll page loads` — goto `/payroll`, expect heading and content
    - `CRM page loads` — goto `/crm`, expect heading and customer list/table
    - `route finance section visible` — navigate to a route detail, expect finance section (expenses, profit, etc.)
    - `compliance page loads` — goto `/compliance`, expect heading and content (document expiry tracking)

    **Patterns specific to load lifecycle:**
    - If using `test.describe.serial`, the describe block should create a load in the first test and subsequent tests operate on it
    - Use `page.getByRole('button', { name: /dispatch/i })` type selectors for action buttons
    - After status changes, wait for the page to reflect the new status before asserting
    - For invoice creation from delivered load, follow the actual UI flow (may be a button on load detail or navigate to /invoices/new)
  </action>
  <verify>
    Run `npx playwright test e2e/owner/ --project=chromium` — all owner tests pass.
    Run `npx playwright test --grep @smoke e2e/owner/ --project=chromium` — smoke subset passes.
    Run full suite `npx playwright test --project=chromium` to confirm no regressions.
  </verify>
  <done>
    All ~30 owner portal E2E tests pass including the full load lifecycle. Smoke subset identifies the critical path. No regressions in existing specs or sysadmin tests.
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test e2e/owner/ --project=chromium` — all owner tests pass
2. `npx playwright test --grep @smoke --project=chromium` — all smoke tests (sysadmin + owner) pass
3. `npx playwright test --project=chromium` — full suite passes (existing + sysadmin + owner)
4. Load lifecycle test covers PENDING -> DISPATCHED -> DELIVERED -> INVOICED progression
5. CRUD operations tested for trucks, drivers, loads, routes
</verification>

<success_criteria>
- Owner dashboard, trucks, drivers, loads, routes, and finance pages all have passing E2E tests
- Load lifecycle fully tested from creation through invoicing
- All smoke-tagged tests identifiable and fast
- No test data collisions (Date.now() uniqueness pattern used throughout)
- No regressions in Plan 01 specs
</success_criteria>

<output>
After completion, create `.planning/phases/27-automated-playwright-tests/27-02-SUMMARY.md`
</output>
