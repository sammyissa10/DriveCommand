---
phase: 27-automated-playwright-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - e2e/auth.setup.ts
  - e2e/fixtures/auth-helpers.ts
  - e2e/sysadmin/auth.spec.ts
  - e2e/sysadmin/dashboard.spec.ts
  - e2e/sysadmin/tenants.spec.ts
  - e2e/sysadmin/support.spec.ts
  - e2e/sysadmin/invoicing.spec.ts
  - e2e/tkt-fixes.spec.ts
  - e2e/dashboard-filtering.spec.ts
  - e2e/management-flows.spec.ts
  - e2e/gps-tracking.spec.ts
  - e2e/tags.spec.ts
  - e2e/responsive.spec.ts
autonomous: true

must_haves:
  truths:
    - "Auth setup authenticates all 3 roles (sysadmin, owner, driver) via API before any test runs"
    - "Existing e2e specs continue to pass without modification to their test logic"
    - "SysAdmin portal login, dashboard, tenant CRUD, support tickets, and invoicing lifecycle are tested"
    - "Running `npx playwright test` executes the full suite with HTML report"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Multi-role project config with setup dependency"
      contains: "storageState"
    - path: "e2e/auth.setup.ts"
      provides: "API-based auth for owner, sysadmin, and driver roles"
      contains: "authenticate as sysadmin"
    - path: "e2e/fixtures/auth-helpers.ts"
      provides: "Shared auth path constants and helper exports"
      exports: ["OWNER_AUTH", "SYSADMIN_AUTH", "DRIVER_AUTH"]
    - path: "e2e/sysadmin/auth.spec.ts"
      provides: "SysAdmin login/logout tests"
    - path: "e2e/sysadmin/tenants.spec.ts"
      provides: "Tenant CRUD and suspend/reactivate tests"
    - path: "e2e/sysadmin/invoicing.spec.ts"
      provides: "Invoice lifecycle DRAFT to SENT to PAID tests"
  key_links:
    - from: "e2e/auth.setup.ts"
      to: "/api/auth/login"
      via: "request.post API call"
      pattern: "request\\.post.*api/auth/login"
    - from: "e2e/sysadmin/*.spec.ts"
      to: ".playwright/auth/sysadmin.json"
      via: "test.use({ storageState })"
      pattern: "storageState.*sysadmin\\.json"
---

<objective>
Set up multi-role Playwright auth infrastructure and implement all SysAdmin portal E2E tests.

Purpose: Establish the auth fixture foundation that Plans 02 and 03 depend on, then cover the entire SysAdmin portal (auth, dashboard, tenants, support, invoicing). The sysadmin portal is the simplest of the three, making it ideal for validating the infrastructure before tackling larger portals.

Output: Updated playwright.config.ts, multi-role auth.setup.ts, auth helper constants, 5 sysadmin spec files, all 6 existing specs updated with explicit storageState declarations.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-automated-playwright-tests/27-RESEARCH.md
@playwright.config.ts
@e2e/auth.setup.ts
@docs/qa/sysadmin-tests.md
@src/app/(admin)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-role auth infrastructure — config, auth setup, fixtures, and existing spec migration</name>
  <files>
    playwright.config.ts
    e2e/auth.setup.ts
    e2e/fixtures/auth-helpers.ts
    e2e/tkt-fixes.spec.ts
    e2e/dashboard-filtering.spec.ts
    e2e/management-flows.spec.ts
    e2e/gps-tracking.spec.ts
    e2e/tags.spec.ts
    e2e/responsive.spec.ts
  </files>
  <action>
    1. Create `e2e/fixtures/auth-helpers.ts` exporting path constants for all 3 auth state files:
       - `OWNER_AUTH = path.join(__dirname, '../../.playwright/auth/owner.json')`
       - `SYSADMIN_AUTH = path.join(__dirname, '../../.playwright/auth/sysadmin.json')`
       - `DRIVER_AUTH = path.join(__dirname, '../../.playwright/auth/driver.json')`
       - Also export `LEGACY_AUTH` pointing to `.playwright/auth.json` for backward compat

    2. Rewrite `e2e/auth.setup.ts` to authenticate all 3 roles via API (not UI):
       - Use `setup.beforeAll` to create `.playwright/auth/` directory with `fs.mkdirSync(AUTH_DIR, { recursive: true })`
       - Three `setup()` blocks: "authenticate as owner", "authenticate as sysadmin", "authenticate as driver"
       - Each does `request.post('/api/auth/login', { data: { email, password } })` and asserts `res.status() === 200`
       - Owner uses `process.env.TEST_OWNER_EMAIL ?? 'demo@drivecommand.com'` and `process.env.TEST_OWNER_PASSWORD ?? 'demo1234'`
       - Sysadmin uses `process.env.TEST_SYSADMIN_EMAIL!` and `process.env.TEST_SYSADMIN_PASSWORD!` — these MUST be set in env
       - Driver uses `process.env.TEST_DRIVER_EMAIL!` and `process.env.TEST_DRIVER_PASSWORD!`
       - Owner setup also writes to legacy `.playwright/auth.json` for backward compat with existing specs
       - IMPORTANT: The sysadmin authenticates via the SAME `/api/auth/login` endpoint as owner/driver (the sysadmin account simply has `isSystemAdmin: true` in the DB). There is NO separate `/admin/login` API — the `/admin/login` page in the QA docs refers to a UI page that uses the same auth endpoint. The auth setup should use `/api/auth/login` for all 3 roles.

    3. Update `playwright.config.ts`:
       - Remove global `storageState: authFile` from chromium and mobile projects
       - Keep the `setup` project matching `auth.setup.ts`
       - Keep `chromium` and `mobile` projects depending on `setup` but WITHOUT global storageState
       - Keep `workers: process.env.CI ? 1 : 3` (DB connection pool constraint)
       - Change reporter to `[['html', { open: 'never' }]]` so reports generate without auto-opening
       - Keep all other settings (baseURL, trace, retries, forbidOnly)

    4. Update ALL 6 existing spec files to add explicit `test.use({ storageState })` at the top:
       - Import path from 'path' and add `test.use({ storageState: path.join(__dirname, '../.playwright/auth/owner.json') })` to each
       - This is necessary because we removed global storageState from the config
       - Files: tkt-fixes.spec.ts, dashboard-filtering.spec.ts, management-flows.spec.ts, gps-tracking.spec.ts, tags.spec.ts, responsive.spec.ts
       - Do NOT change any existing test logic — only add the storageState declaration
  </action>
  <verify>
    Run `npx playwright test e2e/auth.setup.ts --project=setup` to confirm all 3 auth setups succeed (3 passed).
    Then run `npx playwright test e2e/tags.spec.ts --project=chromium` to confirm existing specs still work with the new per-file storageState.
    Check that `.playwright/auth/owner.json`, `.playwright/auth/sysadmin.json`, `.playwright/auth/driver.json` all exist.
  </verify>
  <done>
    Auth setup authenticates 3 roles via API. All 6 existing specs pass with explicit storageState. Three auth state JSON files exist in .playwright/auth/.
  </done>
</task>

<task type="auto">
  <name>Task 2: SysAdmin portal E2E tests — auth, dashboard, tenants, support tickets, invoicing</name>
  <files>
    e2e/sysadmin/auth.spec.ts
    e2e/sysadmin/dashboard.spec.ts
    e2e/sysadmin/tenants.spec.ts
    e2e/sysadmin/support.spec.ts
    e2e/sysadmin/invoicing.spec.ts
  </files>
  <action>
    Create 5 spec files in `e2e/sysadmin/`. Each file MUST start with:
    ```
    import { test, expect } from '@playwright/test';
    import path from 'path';
    test.use({ storageState: path.join(__dirname, '../../.playwright/auth/sysadmin.json') });
    ```

    Reference `docs/qa/sysadmin-tests.md` for the exact flows to automate. Use `@smoke` tag prefix on critical tests for fast CI subset.

    **e2e/sysadmin/auth.spec.ts** (~3 tests):
    - `@smoke sysadmin can access admin dashboard` — goto `/admin-dashboard`, expect heading visible
    - `unauthenticated user redirected from admin routes` — use fresh context with no storageState `{ storageState: { cookies: [], origins: [] } }`, goto `/admin-dashboard`, expect redirect to `/sign-in`
    - `non-admin user redirected from admin routes` — use owner storageState, goto `/admin-dashboard`, expect redirect to `/sign-in` (owner is not isSystemAdmin)

    **e2e/sysadmin/dashboard.spec.ts** (~3 tests):
    - `@smoke dashboard shows metric cards` — goto `/admin-dashboard`, expect stat cards visible (Total Tenants, Active Loads, Open Tickets, etc.)
    - `dashboard shows recent activity` — expect some list/table content on dashboard
    - `dashboard navigation links work` — click Tenants link, expect URL change to `/tenants`

    **e2e/sysadmin/tenants.spec.ts** (~6 tests):
    - `@smoke tenant list loads with table` — goto `/tenants`, expect table with headers (Company, Owner, Status, etc.)
    - `tenant detail page loads` — click first tenant row link, expect detail page with company info
    - `@smoke create tenant — happy path` — goto `/tenants/new`, fill company name (use `Test Tenant ${Date.now()}`), fill owner email, submit, expect success redirect or toast
    - `suspend active tenant` — on tenant detail, click Suspend, confirm, expect status changes to Suspended
    - `reactivate suspended tenant` — find or use previously suspended tenant, click Reactivate, expect status changes back to Active
    - `tenant detail shows billing history` — on tenant detail page, scroll to billing section, expect it renders (even if empty)

    **e2e/sysadmin/support.spec.ts** (~4 tests):
    - `support ticket list loads` — goto `/admin-support`, expect heading and ticket list/table
    - `@smoke reply to support ticket` — click first ticket, type reply in reply form, submit, expect reply appears in thread
    - `change ticket status` — on ticket detail, change status dropdown, expect status updates
    - `filter tickets by status` — if filter exists, select a status filter, expect list updates

    **e2e/sysadmin/invoicing.spec.ts** (~6 tests):
    - `invoice list page loads` — goto `/billing`, expect heading and invoice list
    - `@smoke create invoice — happy path` — goto `/billing/new`, select tenant, add line item (description + amount), set due date, submit, expect redirect to invoice detail or list
    - `invoice detail page shows line items` — navigate to an invoice, expect line items table visible
    - `@smoke send invoice (DRAFT to SENT)` — on a DRAFT invoice detail, click Send, expect status changes to SENT
    - `mark invoice as paid (SENT to PAID)` — on a SENT invoice, click Mark Paid, expect status changes to PAID
    - `void invoice` — on an invoice, click Void, confirm, expect status changes to VOID

    **General patterns for ALL sysadmin specs:**
    - Use `page.waitForLoadState('domcontentloaded')` after navigation, NOT `networkidle`
    - Use `getByRole`, `getByLabel`, `getByText`, `getByTestId` — avoid CSS selectors
    - For CRUD tests that create records, use `Date.now()` in names for uniqueness
    - Do NOT use `page.waitForTimeout()` — use element waits instead
    - Add `data-testid` attributes to source components ONLY if `getByRole`/`getByText`/`getByLabel` selectors are insufficient. When adding `data-testid`, document which component file was modified.
  </action>
  <verify>
    Run `npx playwright test e2e/sysadmin/ --project=chromium` — all sysadmin tests pass.
    Run `npx playwright test --grep @smoke e2e/sysadmin/ --project=chromium` — smoke subset runs in under 60 seconds.
    Then run the full suite `npx playwright test --project=chromium` to confirm sysadmin tests don't break existing specs.
  </verify>
  <done>
    All ~22 sysadmin E2E tests pass. Smoke-tagged subset completes under 60 seconds. No regressions in existing specs. HTML report generates cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test --project=setup` — 3 auth setups pass (owner, sysadmin, driver)
2. `npx playwright test --project=chromium` — all tests pass (existing 6 specs + new sysadmin specs)
3. `.playwright/auth/` contains owner.json, sysadmin.json, driver.json
4. `npx playwright test --grep @smoke --project=chromium` — smoke subset completes quickly
5. HTML report exists at `playwright-report/index.html`
</verification>

<success_criteria>
- Multi-role auth infrastructure works for all 3 roles via API-based login
- All 6 existing spec files pass with per-file storageState (no regressions)
- SysAdmin portal fully covered: auth boundaries, dashboard, tenant CRUD, support tickets, invoicing lifecycle
- HTML report generates without manual intervention
- Smoke-tagged tests identifiable via `--grep @smoke`
</success_criteria>

<output>
After completion, create `.planning/phases/27-automated-playwright-tests/27-01-SUMMARY.md`
</output>
