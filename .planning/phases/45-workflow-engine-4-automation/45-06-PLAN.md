---
phase: 45-workflow-engine-4-automation
plan: "06"
type: execute
wave: 4
depends_on: ["45-01", "45-02", "45-03", "45-04", "45-05"]
files_modified:
  - apps/web/src/__tests__/workflows-trigger-router.test.ts
  - apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts
autonomous: true

must_haves:
  truths:
    - "Phase 4 DoD test 1 (fireEvent match+skip) is covered — delegated to Plan 02's existing test file"
    - "Phase 4 DoD test 2 (disableRecipe stops future spawns, leaves existing instances untouched) is covered by an integration test"
    - "Phase 4 DoD test 3 (override audit record written with reason + userId) is covered by an integration test"
    - "Phase 4 DoD test 4 (dispatch creation blocked for non-ready driver without override) is covered by an integration test"
    - "All tests pass locally and in CI"
  artifacts:
    - path: "apps/web/src/__tests__/workflows-trigger-router.test.ts"
      provides: "Integration tests for disableRecipe instance-preservation (DoD test 2)"
      contains: "disableRecipe"
      min_lines: 80
    - path: "apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts"
      provides: "Integration tests for dispatch blocking + override audit (DoD tests 3 + 4)"
      contains: "DispatchOverrideAudit"
      min_lines: 120
  key_links:
    - from: "apps/web/src/__tests__/workflows-trigger-router.test.ts"
      to: "PlaybookTrigger + PlaybookInstance"
      via: "asserts instance count unchanged after disableRecipe"
      pattern: "playbookInstance.*count"
    - from: "apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts"
      to: "DispatchOverrideAudit"
      via: "asserts audit row created with correct reason + userId + entityType"
      pattern: "dispatchOverrideAudit"
---

<objective>
Land the required Phase 4 DoD tests from spec Section 15 to close out the phase. Phase 4 requires exactly 4 tests: (1) fireEvent match/skip, (2) disableRecipe preserves instances, (3) override audit row written correctly, (4) dispatch blocked without override.

Test 1 is satisfied by Plan 02's unit tests. This plan adds Tests 2, 3, and 4 as integration tests. The goal is to prove Phase 4 DoD coverage, not achieve full coverage — that's Phase 46's job.

Purpose: The checker will compare this phase's test file list against spec Section 15 Phase 4. Missing any of the 4 tests blocks phase sign-off.

Output: 2 new test files + confirmation that Plan 02's fireEvent test already covers Test 1. Total test count for phase: 3 files covering 4 DoD scenarios.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-workflow-engine-4-automation/45-RESEARCH.md
@docs/specs/DriveCommand_Workflow_Engine_v2.md
@.planning/phases/45-workflow-engine-4-automation/45-02-SUMMARY.md
@.planning/phases/45-workflow-engine-4-automation/45-04-SUMMARY.md
@.planning/phases/45-workflow-engine-4-automation/45-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test — disableRecipe preserves existing instances (DoD test 2)</name>
  <files>
    apps/web/src/__tests__/workflows-trigger-router.test.ts
  </files>
  <action>
Create `apps/web/src/__tests__/workflows-trigger-router.test.ts`. Before writing, read an existing workflows integration test (e.g., `apps/web/src/__tests__/workflows-generate-instance.test.ts` or similar) for the established pattern of mocking Prisma + constructing test context.

**Test coverage (Phase 4 DoD test 2):**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { appRouter } from '@/server/api/root';

// Mocks — match the convention used by other workflow tests
vi.mock('@/lib/db/prisma', () => ({
  prisma: {
    playbookTrigger: {
      findFirst: vi.fn(),
      findMany: vi.fn(),
      updateMany: vi.fn(),
      update: vi.fn(),
      create: vi.fn(),
    },
    playbook: { findFirst: vi.fn() },
    playbookInstance: { count: vi.fn(), findMany: vi.fn() },
  },
}));
```

Tests:

1. **enableRecipe happy path**
   - Given a recipe key + valid playbookId
   - When `trigger.enableRecipe` is called by an admin
   - Then `playbookTrigger.create` (or update if existing) is called with `isActive: true`, correct `triggerEvent`, correct `conditions`

2. **enableRecipe rejects unknown recipe key** → `TRPCError NOT_FOUND`

3. **enableRecipe rejects playbook that doesn't belong to tenant** → `TRPCError NOT_FOUND` (tenant scoping)

4. **disableRecipe preserves existing PlaybookInstance rows (DoD test 2 — critical):**
   - Seed mock: 1 `PlaybookTrigger` row active + 3 `PlaybookInstance` rows with `playbookId` matching that trigger
   - Act: call `trigger.disableRecipe({ recipeKey: 'cdl_driver_onboarding' })`
   - Assert 1: `playbookTrigger.updateMany` called with `{ data: { isActive: false } }`
   - Assert 2: `playbookInstance.count({ where: { playbookId: <same> } })` before and after equals 3 (unchanged)
   - Assert 3: no mutation methods called on `playbookInstance`, `stepInstance`, or any related table

5. **disableRecipe on already-inactive trigger is idempotent** — second call returns `{ disabled: 0 }` without error.

6. **All procedures reject non-admin callers** — mock a tenant member (not admin) context and assert `TRPCError FORBIDDEN` is thrown.

**Running:**
```
cd apps/web && npx vitest run src/__tests__/workflows-trigger-router.test.ts
```
All tests must pass.
  </action>
  <verify>
1. File exists and has ≥6 `it(...)` blocks: `grep -c "^\s*it(" apps/web/src/__tests__/workflows-trigger-router.test.ts` returns ≥6.
2. At least one test exercises Phase 4 DoD test 2: `grep "unchanged" apps/web/src/__tests__/workflows-trigger-router.test.ts` returns ≥1 match.
3. `npx vitest run src/__tests__/workflows-trigger-router.test.ts` — all tests pass.
  </verify>
  <done>
Trigger router has integration test coverage. Phase 4 DoD test 2 (disableRecipe preserves instances) is proven with pre/post instance count assertions + no-mutation assertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test — dispatch enforcement + override audit (DoD tests 3 + 4)</name>
  <files>
    apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts
  </files>
  <action>
Create `apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts`.

**Test coverage (Phase 4 DoD tests 3 + 4):**

Mock `prisma` with the same pattern as Task 1 but include:
- `user.findUnique` (for readiness + role lookups)
- `carrierDispatch.create`
- `dispatchOverrideAudit.create`

Tests:

1. **DoD test 4 — dispatch blocked for non-ready driver without override:**
   - Seed: `User.isDispatchReady = false` for the driver
   - Act: call `createDispatch({ ..., overrideReason: undefined })`
   - Assert: throws `DRIVER_NOT_DISPATCH_READY`
   - Assert: `carrierDispatch.create` was NOT called
   - Assert: `dispatchOverrideAudit.create` was NOT called

2. **DoD test 3 — override path writes audit row:**
   - Seed: `User.isDispatchReady = false` for driver, current user has role `owner`
   - Act: call `createDispatch({ ..., overrideReason: 'customer emergency', overrideForEntityType: 'DRIVER', overrideForEntityId: <driver userId>, currentUserId: <admin id> })`
   - Assert: `carrierDispatch.create` called exactly once
   - Assert: `dispatchOverrideAudit.create` called exactly once with:
     - `reason: 'customer emergency'`
     - `userId: <admin id>`
     - `entityType: 'DRIVER'`
     - `entityId: <driver userId>`
     - `tenantId: <orgId>`
     - `dispatchId: <returned dispatch id>`

3. **Override rejected for non-admin:**
   - Seed: `User.isDispatchReady = false`, current user has role `driver`
   - Act: call `createDispatch` with `overrideReason`
   - Assert: throws `OVERRIDE_REQUIRES_ADMIN`
   - Assert: no audit row created

4. **Ready driver creates dispatch normally (no override needed, no audit):**
   - Seed: `User.isDispatchReady = true`
   - Act: call `createDispatch({ ..., overrideReason: undefined })`
   - Assert: `carrierDispatch.create` called, `dispatchOverrideAudit.create` NOT called

5. **Audit row includes tenantId matching the dispatch's orgId:**
   - Explicit check to catch cross-tenant audit bugs: assert `audit.tenantId === dispatch.orgId` in the override case.

**Running:**
```
cd apps/web && npx vitest run src/__tests__/workflows-dispatch-enforcement.test.ts
```

All tests must pass.
  </action>
  <verify>
1. File exists and has ≥5 `it(...)` blocks: `grep -c "^\s*it(" apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts` returns ≥5.
2. At least one test exercises DoD test 3 (audit write): `grep "dispatchOverrideAudit.*create" apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts` returns ≥2 matches (seed + assertion).
3. At least one test exercises DoD test 4 (block without override): `grep "DRIVER_NOT_DISPATCH_READY" apps/web/src/__tests__/workflows-dispatch-enforcement.test.ts` returns ≥1 match.
4. `npx vitest run src/__tests__/workflows-dispatch-enforcement.test.ts` — all tests pass.
5. Full test suite still passes: `cd apps/web && npx vitest run` — all green (no regressions in Phase 42/43/44 tests).
  </verify>
  <done>
Dispatch enforcement has integration test coverage. Phase 4 DoD test 3 (override audit with reason + userId) and test 4 (block non-ready without override) both proven. Admin-vs-non-admin guard tested. Ready-driver happy path tested.
  </done>
</task>

</tasks>

<verification>
Confirmation that all 4 Phase 4 DoD tests from spec Section 15 are covered across the phase:

| DoD Test | Plan | File |
|----------|------|------|
| 1. fireEvent match+skip | 45-02 | `workflows-fire-event.test.ts` |
| 2. disableRecipe preserves instances | 45-06 | `workflows-trigger-router.test.ts` |
| 3. override audit written | 45-06 | `workflows-dispatch-enforcement.test.ts` |
| 4. dispatch blocked without override | 45-06 | `workflows-dispatch-enforcement.test.ts` |

Run full test suite at phase end:
```
cd apps/web && npx vitest run
```
All green.
</verification>

<success_criteria>
- All 4 Phase 4 DoD tests covered via ≥3 distinct test files
- No regressions: Phase 42/43/44 test suites still pass
- Override audit assertions include all 5 required fields (reason, userId, entityType, entityId, tenantId)
- Block-without-override assertion proves no carrierDispatch.create call was made (not just that an error was thrown)
- disableRecipe instance-preservation asserts the exact count pre/post with no mutation methods called
</success_criteria>

<output>
After completion, create `.planning/phases/45-workflow-engine-4-automation/45-06-SUMMARY.md` listing all test files in the phase + which DoD test each covers + final suite pass confirmation. Flag Phase 45 as complete and ready for `/gsd:verify-work`.
</output>
