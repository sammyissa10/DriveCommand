---
phase: 10-dashboard-system-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(owner)/actions/dashboard.ts
  - src/components/dashboard/stat-card.tsx
  - src/app/(owner)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "Owner sees stat cards showing total trucks, active drivers, active routes, and maintenance alerts"
    - "Stat cards link to their respective detail pages (/trucks, /drivers, /routes)"
    - "Dashboard loads fast with parallel data fetching (Promise.all)"
    - "Phase 9 widgets (maintenance, documents) still display below stat cards"
  artifacts:
    - path: "src/app/(owner)/actions/dashboard.ts"
      provides: "Fleet stats aggregation server action"
      exports: ["getFleetStats"]
    - path: "src/components/dashboard/stat-card.tsx"
      provides: "Reusable stat card component with variant support"
      exports: ["StatCard"]
    - path: "src/app/(owner)/dashboard/page.tsx"
      provides: "Updated dashboard with stat cards + widgets"
      contains: "StatCard"
  key_links:
    - from: "src/app/(owner)/dashboard/page.tsx"
      to: "src/app/(owner)/actions/dashboard.ts"
      via: "getFleetStats() server action call"
      pattern: "getFleetStats"
    - from: "src/app/(owner)/dashboard/page.tsx"
      to: "src/app/(owner)/actions/notifications.ts"
      via: "getUpcomingMaintenance() and getExpiringDocuments() calls"
      pattern: "Promise\\.all"
    - from: "src/components/dashboard/stat-card.tsx"
      to: "detail pages"
      via: "Next.js Link component wrapping card"
      pattern: "Link.*href"
---

<objective>
Add fleet overview stat cards to the owner dashboard showing total trucks, active drivers, active routes, and maintenance alerts. Stat cards are clickable links to detail pages.

Purpose: Owners need at-a-glance fleet metrics (DASH-01: fleet overview dashboard). Stat cards provide immediate totals, while Phase 9 widgets below show actionable alerts.
Output: Updated dashboard page with stat cards grid above existing widgets, parallel data fetching for performance.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dashboard-system-admin/10-RESEARCH.md
@.planning/phases/09-notifications-reminders/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fleet stats server action and stat card component</name>
  <files>
    src/app/(owner)/actions/dashboard.ts
    src/components/dashboard/stat-card.tsx
  </files>
  <action>
    Create `src/app/(owner)/actions/dashboard.ts` with a `getFleetStats()` server action:
    - Add 'use server' directive at top
    - Import requireRole from '@/lib/auth/server', UserRole from '@/lib/auth/roles', getTenantPrisma from '@/lib/context/tenant-context'
    - Enforce OWNER/MANAGER role with `await requireRole([UserRole.OWNER, UserRole.MANAGER])`
    - Get tenant-scoped Prisma: `const db = await getTenantPrisma()`
    - Use Promise.all() for 4 parallel count queries:
      1. `db.truck.count()` for totalTrucks
      2. `db.user.count({ where: { role: 'DRIVER', isActive: true } })` for activeDrivers (note: role stored as string in DB, use literal 'DRIVER')
      3. `db.route.count({ where: { status: { in: ['PLANNED', 'IN_PROGRESS'] } } })` for activeRoutes (use string literals matching RouteStatus enum)
      4. `db.scheduledService.count({ where: { isCompleted: false } })` for maintenanceAlerts
    - Use @ts-ignore before each Prisma call (follows established pattern for Prisma 7 withTenantRLS extension type issues)
    - Return object: `{ totalTrucks, activeDrivers, activeRoutes, maintenanceAlerts }`
    - Export the return type as `FleetStats` interface

    Create `src/components/dashboard/stat-card.tsx` as a client component:
    - Add 'use client' directive
    - Import Link from 'next/link'
    - Props interface `StatCardProps`: label (string), value (number), href (string), variant ('default' | 'warning', default 'default')
    - Render a Next.js Link wrapping the card div (entire card is clickable)
    - Card styling: rounded-lg border p-6 shadow-sm with hover:shadow-md transition-shadow for interactivity feedback
    - Variant styling: 'warning' uses border-yellow-300 bg-yellow-50, 'default' uses border-gray-200 bg-white
    - Label: text-sm font-medium text-gray-600
    - Value: mt-2 text-3xl font-bold text-gray-900
    - Export StatCard component
  </action>
  <verify>
    Verify files exist and have correct exports:
    - `grep -c "getFleetStats" src/app/(owner)/actions/dashboard.ts` should be >= 1
    - `grep -c "StatCard" src/components/dashboard/stat-card.tsx` should be >= 1
    - `grep -c "use server" src/app/(owner)/actions/dashboard.ts` should be 1
    - `grep -c "use client" src/components/dashboard/stat-card.tsx` should be 1
    - `grep -c "Link" src/components/dashboard/stat-card.tsx` should be >= 1
    - `npx tsc --noEmit` should pass (or show only pre-existing errors)
  </verify>
  <done>
    getFleetStats server action returns { totalTrucks, activeDrivers, activeRoutes, maintenanceAlerts } with OWNER/MANAGER authorization. StatCard component renders clickable card with label, value, href, and variant styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard page with stat cards and parallel fetching</name>
  <files>
    src/app/(owner)/dashboard/page.tsx
  </files>
  <action>
    Update `src/app/(owner)/dashboard/page.tsx` to add stat cards above existing Phase 9 widgets:

    - Add imports: `getFleetStats` from '@/app/(owner)/actions/dashboard', `StatCard` from '@/components/dashboard/stat-card'
    - Keep existing imports: requireRole, UserRole, getUpcomingMaintenance, getExpiringDocuments, UpcomingMaintenanceWidget, ExpiringDocumentsWidget

    - Replace sequential data fetching with Promise.all for parallel fetching:
      ```
      const [stats, upcomingMaintenance, expiringDocuments] = await Promise.all([
        getFleetStats(),
        getUpcomingMaintenance(),
        getExpiringDocuments(),
      ]);
      ```

    - Add stat cards grid ABOVE the widget grid (between heading and widgets):
      ```
      <div className="mb-6 grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4">
        <StatCard label="Total Trucks" value={stats.totalTrucks} href="/trucks" />
        <StatCard label="Active Drivers" value={stats.activeDrivers} href="/drivers" />
        <StatCard label="Active Routes" value={stats.activeRoutes} href="/routes" />
        <StatCard
          label="Maintenance Alerts"
          value={stats.maintenanceAlerts}
          href="/trucks"
          variant={stats.maintenanceAlerts > 0 ? 'warning' : 'default'}
        />
      </div>
      ```
    - Stat card links: "Total Trucks" -> /trucks, "Active Drivers" -> /drivers, "Active Routes" -> /routes, "Maintenance Alerts" -> /trucks (same as maintenance widget)
    - Keep existing Phase 9 widget grid unchanged below stat cards
    - Update page comment to mention stat cards in addition to widgets
  </action>
  <verify>
    Verify dashboard page includes stat cards and parallel fetching:
    - `grep -c "StatCard" src/app/(owner)/dashboard/page.tsx` should be >= 4
    - `grep -c "Promise.all" src/app/(owner)/dashboard/page.tsx` should be 1
    - `grep -c "getFleetStats" src/app/(owner)/dashboard/page.tsx` should be >= 1
    - `grep -c "UpcomingMaintenanceWidget" src/app/(owner)/dashboard/page.tsx` should be >= 1 (Phase 9 widgets preserved)
    - `npx tsc --noEmit` should pass (or show only pre-existing errors)
  </verify>
  <done>
    Dashboard page shows 4 stat cards (Total Trucks, Active Drivers, Active Routes, Maintenance Alerts) in responsive 4-column grid above Phase 9 widgets. All data fetched in parallel via Promise.all. Each stat card links to its detail page.
  </done>
</task>

</tasks>

<verification>
1. Dashboard page renders stat cards grid (4 cols on lg, 2 on md, 1 on sm) above Phase 9 widgets
2. getFleetStats uses tenant-scoped Prisma with OWNER/MANAGER authorization
3. Stat cards are clickable links to /trucks, /drivers, /routes
4. Maintenance Alerts card shows 'warning' variant when count > 0
5. Promise.all fetches all dashboard data in parallel
6. Phase 9 widgets (upcoming maintenance, expiring documents) still display correctly below stat cards
</verification>

<success_criteria>
- Owner sees 4 stat cards on dashboard with real fleet data
- Each stat card links to its corresponding list page
- Maintenance Alerts card highlights in yellow when alerts exist
- Dashboard loads all data in parallel (no waterfall)
- Phase 9 widgets remain functional below stat cards
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-system-admin/10-01-SUMMARY.md`
</output>
