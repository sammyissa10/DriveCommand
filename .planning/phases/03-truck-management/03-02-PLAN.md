---
phase: 03-truck-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/trucks/truck-form.tsx
  - src/components/trucks/truck-list.tsx
  - src/app/(owner)/trucks/page.tsx
  - src/app/(owner)/trucks/new/page.tsx
  - src/app/(owner)/trucks/[id]/page.tsx
  - src/app/(owner)/trucks/[id]/edit/page.tsx
autonomous: true

must_haves:
  truths:
    - "Owner can navigate to /trucks and see a list of all their trucks"
    - "Owner can click 'Add Truck' and fill out a form to create a new truck"
    - "Owner can click on a truck to see its details including document metadata"
    - "Owner can edit any truck's details from the detail page"
    - "Owner can delete a truck from the list with immediate UI feedback"
    - "Truck list supports sorting by column headers and filtering via search input"
    - "Form shows validation errors inline when submission fails"
    - "Document metadata fields (registration number, insurance expiry) are editable in the form"
  artifacts:
    - path: "src/components/trucks/truck-form.tsx"
      provides: "Reusable truck form for create and edit"
      min_lines: 50
    - path: "src/components/trucks/truck-list.tsx"
      provides: "TanStack Table with sorting and filtering"
      min_lines: 50
    - path: "src/app/(owner)/trucks/page.tsx"
      provides: "Truck list page with add button and delete actions"
      min_lines: 20
    - path: "src/app/(owner)/trucks/new/page.tsx"
      provides: "New truck form page"
      min_lines: 10
    - path: "src/app/(owner)/trucks/[id]/page.tsx"
      provides: "Truck detail view with document metadata"
      min_lines: 20
    - path: "src/app/(owner)/trucks/[id]/edit/page.tsx"
      provides: "Edit truck form page with pre-filled values"
      min_lines: 15
  key_links:
    - from: "src/app/(owner)/trucks/new/page.tsx"
      to: "src/app/(owner)/actions/trucks.ts"
      via: "useActionState(createTruck)"
      pattern: "useActionState.*createTruck"
    - from: "src/app/(owner)/trucks/[id]/edit/page.tsx"
      to: "src/app/(owner)/actions/trucks.ts"
      via: "useActionState(updateTruck.bind(null, id))"
      pattern: "useActionState.*updateTruck"
    - from: "src/app/(owner)/trucks/page.tsx"
      to: "src/app/(owner)/actions/trucks.ts"
      via: "listTrucks() for data, deleteTruck for removal"
      pattern: "listTrucks|deleteTruck"
    - from: "src/components/trucks/truck-list.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable with sorting and filtering"
      pattern: "useReactTable"
---

<objective>
Build the complete truck management UI: list page with TanStack Table (sorting/filtering/delete), create/edit forms using React 19 useActionState, and detail view with document metadata display.

Purpose: Give owners a full interface to manage their fleet inventory, completing all TRUK-01 through TRUK-04 requirements.
Output: Six files -- two reusable components (truck-form, truck-list) and four pages (list, new, detail, edit).
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-truck-management/03-RESEARCH.md
@.planning/phases/03-truck-management/03-01-SUMMARY.md
@src/app/(owner)/layout.tsx
@src/app/(owner)/actions/trucks.ts
@src/lib/validations/truck.schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Table and create truck-form and truck-list components</name>
  <files>src/components/trucks/truck-form.tsx, src/components/trucks/truck-list.tsx</files>
  <action>
1. Install TanStack Table: `npm install @tanstack/react-table@latest`

2. Create src/components/trucks/truck-form.tsx:
   - 'use client' component
   - Props: { action: (prevState: any, formData: FormData) => Promise<any>, initialData?: Partial truck fields, submitLabel: string }
   - Use useActionState(action, null) for form state management
   - Fields: make (text input), model (text input), year (number input, min 1900 max current+1), vin (text input, maxLength 17, uppercase pattern), licensePlate (text input, maxLength 20), odometer (number input, min 0, step 1)
   - Document metadata section (collapsible or grouped under "Documents" heading): registrationNumber (text), registrationExpiry (date input), insuranceNumber (text), insuranceExpiry (date input)
   - For date fields: use type="date" input, the form will send ISO date strings
   - Show field-level validation errors from state.error (keyed by field name)
   - Show general error from state.error if it's a string
   - Disable all inputs and button while isPending
   - Button text shows submitLabel, changes to "Saving..." while pending
   - Pre-fill all fields from initialData when provided (for edit mode)
   - Tailwind styling: consistent with owner portal (white bg, gray borders, blue primary button), max-w-2xl, space-y-4 between fields, labels with block font-medium mb-1
   - Each input: w-full border border-gray-300 rounded-md px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500

3. Create src/components/trucks/truck-list.tsx:
   - 'use client' component
   - Props: { trucks: Truck[], onDelete: (id: string) => void }
   - Import useReactTable, getCoreRowModel, getSortedRowModel, getFilteredRowModel, flexRender from @tanstack/react-table
   - Columns: make, model, year, vin, licensePlate, odometer (formatted with toLocaleString()), actions column with Edit link (/trucks/{id}/edit) and Delete button
   - State: sorting (useState<SortingState>([])), globalFilter (useState(''))
   - Search input above table: placeholder "Search trucks...", filters across all columns
   - Table headers clickable for sorting, show sort direction indicator (up/down arrow characters, NOT emoji)
   - Table styling: w-full, border-collapse, header bg-gray-50 text-left text-sm font-medium text-gray-500 uppercase, rows with border-b hover:bg-gray-50, cells px-6 py-4
   - Delete button: red text, confirms with window.confirm("Are you sure you want to delete this truck?") before calling onDelete
   - Empty state: "No trucks found. Add your first truck to get started." centered message
   - Use Truck type from Prisma generated client for type safety
  </action>
  <verify>
- `npm ls @tanstack/react-table` confirms installation
- src/components/trucks/truck-form.tsx exists with useActionState, all core fields, document metadata fields, and validation error display
- src/components/trucks/truck-list.tsx exists with useReactTable, sorting, filtering, delete handler, and empty state
- Both files have 'use client' directive
- `npx tsc --noEmit` passes (or at minimum no errors in truck component files)
  </verify>
  <done>Two reusable truck components: TruckForm handles create/edit with useActionState and inline validation errors for all fields including document metadata; TruckList renders a sortable/filterable table with TanStack Table including search, column sorting, edit links, and delete with confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Create truck pages (list, new, detail, edit) wired to server actions</name>
  <files>src/app/(owner)/trucks/page.tsx, src/app/(owner)/trucks/new/page.tsx, src/app/(owner)/trucks/[id]/page.tsx, src/app/(owner)/trucks/[id]/edit/page.tsx</files>
  <action>
1. Create src/app/(owner)/trucks/page.tsx (truck list page):
   - Server component (no 'use client')
   - Import listTrucks from actions, call it to fetch trucks
   - Render page header: "Trucks" title with "Add Truck" link button (href="/trucks/new") styled as blue primary button
   - Create a client wrapper component (inline or separate) that handles delete:
     - Uses useOptimistic to optimistically remove deleted trucks from UI
     - Uses useTransition + startTransition to wrap deleteTruck call
     - Passes optimistic trucks and handleDelete to TruckList component
   - If the inline client wrapper approach is used, it can be a small 'use client' component within the same directory or embedded
   - Actually, create a small client component TruckListWrapper inside the page file or a separate file (src/app/(owner)/trucks/truck-list-wrapper.tsx) that:
     - Takes initialTrucks as prop
     - Uses useOptimistic for delete
     - Uses useTransition for deleteTruck server action
     - Renders TruckList with optimistic data and delete handler
   - The server component page fetches data and passes to the client wrapper

2. Create src/app/(owner)/trucks/new/page.tsx:
   - 'use client' component
   - Import TruckForm and createTruck action
   - Render page heading "Add New Truck" with back link to /trucks
   - Render TruckForm with action={createTruck} and submitLabel="Create Truck"

3. Create src/app/(owner)/trucks/[id]/page.tsx (detail view):
   - Server component
   - Extract id from params (await params pattern for Next.js 16)
   - Call getTruck(id) from actions
   - If not found, call notFound() from next/navigation
   - Display truck details in a clean layout:
     - Header: "{year} {make} {model}" as title, with Edit button (link to /trucks/{id}/edit) and Back link to /trucks
     - Details grid (2 columns on lg): VIN, License Plate, Odometer (formatted with commas + " miles"), Created date, Updated date
     - Document Metadata section (if documentMetadata exists and is non-null):
       - Parse with documentMetadataSchema.safeParse for type safety
       - Show registration number, registration expiry (formatted date), insurance number, insurance expiry (formatted date)
       - Only show fields that have values
       - If no metadata, show "No document information recorded"
   - Tailwind: max-w-4xl, card-style sections with bg-white rounded-lg shadow-sm p-6

4. Create src/app/(owner)/trucks/[id]/edit/page.tsx:
   - Server component that fetches truck data, then renders client form
   - Extract id from params (await params)
   - Call getTruck(id), if not found call notFound()
   - Create a client wrapper or use TruckForm directly:
     - The updateTruck action needs the truck ID bound: updateTruck.bind(null, id)
     - Render TruckForm with action={updateTruck.bind(null, id)}, initialData from truck, submitLabel="Update Truck"
   - Page heading: "Edit Truck" with back link to /trucks/{id}
   - Actually, since TruckForm is a client component and we need to bind the id, create a small client wrapper component (EditTruckForm) in the same file or inline that:
     - Receives truckId and initialData as props
     - Binds updateTruck with the id: useActionState(updateTruck.bind(null, truckId), null)
     - Renders TruckForm
   - OR simpler: make the edit page itself a client component that receives serialized truck data from a server component wrapper. The cleanest pattern: the page.tsx is a server component that fetches data, then renders a client EditTruckClient component.

   For the edit page, the cleanest approach:
   - page.tsx is an async server component that calls getTruck(id)
   - It passes truck data to a 'use client' child component
   - The child component uses TruckForm with updateTruck.bind(null, id) as the action
   - The child can be defined in the same file above the default export, or in a separate file

   For document metadata in initialData: parse the truck.documentMetadata JSON and spread the fields into the initial form data so date inputs get pre-filled correctly.
  </action>
  <verify>
- All four page files exist in the correct Next.js App Router directory structure
- `npx tsc --noEmit` passes
- /trucks page renders TruckList with data from listTrucks()
- /trucks/new page uses TruckForm with createTruck action
- /trucks/[id] page shows truck details including document metadata
- /trucks/[id]/edit page pre-fills TruckForm with existing truck data
- Delete flow: TruckList -> confirm -> deleteTruck -> optimistic removal from list
- Navigation links: list -> new, list -> detail, detail -> edit, edit -> detail, all -> back to list
  </verify>
  <done>Complete truck management UI: list page with search/sort/filter/delete (optimistic), create page with validated form, detail page showing all fields including document metadata, and edit page with pre-filled form. All pages are wired to server actions and live under the owner portal layout with role-based access.</done>
</task>

</tasks>

<verification>
- Owner can navigate to /trucks and see all their trucks in a sortable/filterable table (TRUK-03)
- Owner can click "Add Truck" and create a truck with all core fields (TRUK-01)
- Owner can edit any truck's details from the detail page (TRUK-02)
- Owner can delete a truck with confirmation and immediate UI feedback (TRUK-02)
- Owner can view and edit document metadata (registration number, insurance expiry) per truck (TRUK-04)
- Validation errors show inline on the form when input is invalid
- Empty state shows helpful message when no trucks exist
- All pages inherit owner portal layout (auth enforced at layout + server action level)
</verification>

<success_criteria>
- TanStack Table installed and used in truck-list component
- All four pages render within the (owner) layout
- Forms use React 19 useActionState for state management
- Delete uses useOptimistic for instant feedback
- Document metadata fields are editable in create/edit forms and displayed in detail view
- Type safety maintained throughout (Prisma types, Zod inferred types)
</success_criteria>

<output>
After completion, create `.planning/phases/03-truck-management/03-02-SUMMARY.md`
</output>
