---
phase: 37.1-driver-portal-gaps
plan: 02
type: execute
wave: 2
depends_on: ["37.1-01"]
files_modified:
  - apps/mobile/app/(driver)/loads/my-route.tsx
  - apps/mobile/components/driver/MessageBubble.tsx
  - apps/mobile/app/(driver)/messages.tsx
autonomous: true

must_haves:
  truths:
    - "Driver can view full route detail screen with loads timeline, route/truck details, and route messages"
    - "Loads timeline shows vertical connector lines with colored nodes per load status"
    - "Tapping a load in the timeline navigates to that load's detail screen"
    - "Route messages section shows route-scoped conversation with send capability"
    - "Route status badge is prominently displayed in the header area"
  artifacts:
    - path: "apps/mobile/app/(driver)/loads/my-route.tsx"
      provides: "Full-screen My Route detail screen"
      min_lines: 100
    - path: "apps/mobile/components/driver/MessageBubble.tsx"
      provides: "Shared message bubble component"
  key_links:
    - from: "apps/mobile/app/(driver)/loads/my-route.tsx"
      to: "/api/mobile/driver/route"
      via: "driverApi.getMyRoute() in useQuery"
      pattern: "driverApi\\.getMyRoute"
    - from: "apps/mobile/app/(driver)/loads/my-route.tsx"
      to: "/api/mobile/driver/messages/route-thread"
      via: "driverApi.getRouteMessages() and sendRouteMessage()"
      pattern: "driverApi\\.(getRouteMessages|sendRouteMessage)"
    - from: "apps/mobile/app/(driver)/loads/my-route.tsx"
      to: "apps/mobile/app/(driver)/loads/[id].tsx"
      via: "router.push on timeline item press"
      pattern: "router\\.push.*loads/"
---

<objective>
Build the full-screen My Route detail screen with three sections: loads timeline, route/truck details, and route messages. Also extract MessageBubble to a shared component.

Purpose: Give drivers a comprehensive view of their assigned route — all linked loads in a visual timeline, truck/scheduling details, and a route-scoped messaging thread — all accessible from the RouteCard built in plan 01.
Output: My Route screen, shared MessageBubble component.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.1-driver-portal-gaps/37.1-CONTEXT.md
@.planning/phases/37.1-driver-portal-gaps/37.1-RESEARCH.md
@.planning/phases/37.1-driver-portal-gaps/37.1-01-SUMMARY.md
@apps/mobile/app/(driver)/loads/[id].tsx
@apps/mobile/app/(driver)/messages.tsx
@apps/mobile/components/driver/StopTimelineItem.tsx
@apps/mobile/components/ui/AnimatedScreen.tsx
@apps/mobile/components/ui/Skeleton.tsx
@apps/mobile/components/ui/Badge.tsx
@apps/mobile/components/driver/RouteLoadTimelineItem.tsx
@apps/web/src/app/(driver)/my-route/page.tsx
@packages/api-client/src/driver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MessageBubble to shared component</name>
  <files>
    apps/mobile/components/driver/MessageBubble.tsx
    apps/mobile/app/(driver)/messages.tsx
  </files>
  <action>
**Extract MessageBubble** from `apps/mobile/app/(driver)/messages.tsx`:
- Find the inline MessageBubble component/function defined within messages.tsx
- Move it to `apps/mobile/components/driver/MessageBubble.tsx` as a named export
- Keep the same props interface and styling — do not change any visual behavior
- Props should include: message object (with body, senderRole, createdAt, sender info), isOwnMessage boolean
- Layout: right-aligned bubble for own messages (bg-sky-600), left-aligned for others (bg-slate-700), sender name, timestamp, message body
- Update `messages.tsx` to import MessageBubble from the new file instead of using the inline definition
- Verify messages.tsx still works after the extraction (no removed imports, no broken references)
  </action>
  <verify>
Run `npx tsc --noEmit -p apps/mobile/tsconfig.json` to verify no type errors. Messages tab should render identically after extraction.
  </verify>
  <done>
MessageBubble is a standalone exported component. messages.tsx imports it from the shared location. No visual changes to the Messages tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the My Route detail screen</name>
  <files>
    apps/mobile/app/(driver)/loads/my-route.tsx
  </files>
  <action>
Create the full My Route detail screen at `apps/mobile/app/(driver)/loads/my-route.tsx`.

**Screen structure** (wrap in AnimatedScreen, use ScrollView for the full page):

**Header area:**
- Back button (ArrowLeft, same pattern as loads/[id].tsx)
- Title: "My Route" (text-xl, font-bold, text-white)
- Subtitle: `{origin} -> {destination}` (text-sm, text-slate-400)
- Route status Badge displayed prominently next to the title (per user decision)
- Use the same header pattern as the existing load detail screen

**Section 1: Loads on this Route**
- Section heading: "Loads on this Route" (text-lg, font-semibold, text-white, mb-3)
- Map over `route.loads[]` and render `RouteLoadTimelineItem` for each load (import from plan 01)
- Pass `isLast` prop based on array index
- `onPress` navigates to `/(driver)/loads/${load.id}` via `router.push` — this is where drivers update load status (per user decision: timeline is read-only)
- If no loads: show "No loads assigned to this route" empty state text

**Section 2: Route & Truck Details**
- Section heading: "Route Details" (text-lg, font-semibold, text-white, mb-3)
- Display in a card (bg-slate-800, rounded-xl, p-4):
  - Scheduled date (format with date-fns or simple locale string)
  - Route status
  - Route name (if present)
- If truck exists, show "Truck Details" sub-section:
  - `{year} {make} {model}` (e.g. "2024 Peterbilt 579")
  - VIN (text-slate-400, text-xs)
  - License plate (text-slate-400, text-xs)
- If no truck: "No truck assigned" muted text

**Section 3: Route Messages**
- Section heading: "Route Messages" (text-lg, font-semibold, text-white, mb-3)
- Fetch route messages using `useQuery` with key `['driver-route-messages', routeId]` calling `driverApi.getRouteMessages(routeId)`
- Display messages using the extracted `MessageBubble` component
- Add a message input row at the bottom of the section: TextInput + Send button (same pattern as Messages tab)
- On send: call `driverApi.sendRouteMessage(routeId, body)` via `useMutation`, invalidate the messages query on success
- If no messages: "No messages yet" empty state

**Data fetching:**
- Use `useQuery` with key `['driver-route']` calling `driverApi.getMyRoute()` — shares cache with RouteCard from plan 01
- Show Skeleton loading state while fetching (per existing screen patterns)
- Handle error state: show error message with retry button

**Navigation:**
- Screen is accessed via push from RouteCard (`router.push('/(driver)/loads/my-route')`)
- Back button returns to loads tab
  </action>
  <verify>
Run `npx tsc --noEmit -p apps/mobile/tsconfig.json`. On Android emulator: navigate to Loads tab, tap RouteCard, verify My Route screen shows loads timeline, route/truck details, and route messages section. Tap a load in timeline to verify navigation to load detail. Send a route message and verify it appears.
  </verify>
  <done>
My Route screen displays all three sections: loads timeline with colored nodes and connector lines, route and truck details card, route messages thread with send capability. Tapping a load navigates to load detail. Status badge is prominent in header. Screen uses shared cache with RouteCard.
  </done>
</task>

</tasks>

<verification>
- My Route screen renders with all three sections when navigated from RouteCard
- Loads timeline shows vertical connector lines, colored dot nodes (green for delivered, sky for in-transit, muted for others)
- Tapping a load in timeline pushes load detail screen
- Route details show scheduled date, status, truck info
- Route messages load and display correctly
- Sending a route message works and updates the thread
- MessageBubble is reused between Messages tab and route messages
- Back button returns to Loads tab
</verification>

<success_criteria>
Complete My Route detail screen with loads timeline, route/truck details, and route messaging. MessageBubble extracted and shared. All navigation flows work. TypeScript compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/37.1-driver-portal-gaps/37.1-02-SUMMARY.md`
</output>
