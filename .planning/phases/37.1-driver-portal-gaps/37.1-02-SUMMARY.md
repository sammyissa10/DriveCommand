---
phase: 37.1-driver-portal-gaps
plan: 02
subsystem: mobile
tags: [react-native, expo, nativewind, tanstack-query, messaging]

requires:
  - phase: 37.1-driver-portal-gaps
    plan: 01
    provides: RouteCard, RouteLoadTimelineItem, getMyRoute, getRouteMessages, sendRouteMessage

provides:
  - My Route full-screen detail screen with loads timeline, route/truck details, route messages
  - MessageBubble shared component (extracted from messages.tsx, reused in route messages)
  - Navigation target for RouteCard tap: /(driver)/loads/my-route

affects:
  - messages.tsx — imports MessageBubble from shared location (no visual change)

tech-stack:
  added: []
  patterns:
    - "My Route shares ['driver-route'] query cache key with RouteCard — single fetch, shared state"
    - "Route messages use separate ['driver-route-messages', routeId] cache key"
    - "MessageBubble extracted to components/driver/ for reuse across Messages tab and route messages"

key-files:
  created:
    - apps/mobile/app/(driver)/loads/my-route.tsx
    - apps/mobile/components/driver/MessageBubble.tsx
  modified:
    - apps/mobile/app/(driver)/messages.tsx

key-decisions:
  - "MessageBubble extracted with formatMessageTime and getSenderLabel helpers — both exported for potential future reuse"
  - "Route messages inline in ScrollView (not FlashList) — route messages are low-volume, ScrollView simpler and avoids FlashList ref complexity"
  - "Send button shows ActivityIndicator spinner during mutation pending state"
  - "KeyboardAvoidingView wraps ScrollView for message input to avoid keyboard overlap"

metrics:
  duration: 128s
  tasks: 2
  files_created: 2
  files_modified: 1
  completed: 2026-03-29
---

# Phase 37.1 Plan 02: My Route Detail Screen + MessageBubble Summary

**My Route full-screen detail screen with loads timeline, route/truck details, and route-scoped messaging — MessageBubble shared component extracted from Messages tab**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-03-29T04:49:05Z
- **Completed:** 2026-03-29T04:51:13Z
- **Tasks:** 2
- **Files:** 2 created, 1 modified

## Accomplishments

- Extracted inline `MessageBubble` from `messages.tsx` to `components/driver/MessageBubble.tsx` with `formatMessageTime` and `getSenderLabel` helpers exported
- Updated `messages.tsx` to import from shared location — zero visual/behavior change to Messages tab
- Built `my-route.tsx` full-screen detail screen with three sections:
  - **Loads timeline**: `RouteLoadTimelineItem` per load, tapping navigates to `/(driver)/loads/{id}` for status updates
  - **Route & Truck Details**: scheduled date, status, route name, truck year/make/model, VIN, license plate
  - **Route Messages**: fetched via `driverApi.getRouteMessages`, send via `driverApi.sendRouteMessage`, cache invalidation on success, shared `MessageBubble` rendering
- Route status badge in header, origin → destination subtitle
- Shared `['driver-route']` cache key with `RouteCard` — no duplicate API calls
- Skeleton loading, error state with retry, no-route-assigned state, pull-to-refresh

## Task Commits

1. **Task 1: Extract MessageBubble to shared component** — `8045fea` (feat)
2. **Task 2: Build the My Route detail screen** — `ae0420a` (feat)

## Files Created/Modified

- `apps/mobile/components/driver/MessageBubble.tsx` — standalone exported component with helpers
- `apps/mobile/app/(driver)/loads/my-route.tsx` — full-screen route detail screen (382 lines)
- `apps/mobile/app/(driver)/messages.tsx` — import updated to use shared MessageBubble

## Decisions Made

- MessageBubble extracted with both `formatMessageTime` and `getSenderLabel` exported — available for any future messaging component
- Route messages rendered in a ScrollView inline section (not FlashList) — route messages are low-volume per-route threads; simpler and avoids pre-existing FlashList ref type issues
- `KeyboardAvoidingView` wraps `ScrollView` so the message input stays visible when the keyboard opens
- Send button shows `ActivityIndicator` while mutation is pending (replaces Send icon) to communicate in-flight state

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- [x] `apps/mobile/components/driver/MessageBubble.tsx` — FOUND
- [x] `apps/mobile/app/(driver)/loads/my-route.tsx` — FOUND
- [x] Commit `8045fea` — FOUND
- [x] Commit `ae0420a` — FOUND
- [x] TypeScript: 0 new errors introduced (10 pre-existing FlashList/ExternalLink errors unchanged)

## Self-Check: PASSED
