---
phase: 50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables
plan: "06"
type: execute
wave: 6
depends_on: ["50-05"]
files_modified:
  - apps/web/src/components/carrier/trucks/CarrierTruckList.tsx
  - apps/web/src/components/carrier/drivers/CarrierDriverList.tsx
  - apps/web/src/components/carrier/loads/LoadList.tsx
  - apps/web/src/components/carrier/clients/ClientList.tsx
  - apps/web/src/app/api/v1/carrier/loads/route.ts
  - apps/web/src/app/(owner)/carrier/fleet/trucks/page.tsx
  - apps/web/src/app/(owner)/carrier/fleet/drivers/page.tsx
  - apps/web/src/app/(owner)/carrier/clients/page.tsx
  - apps/web/src/app/(owner)/carrier/dashboard/page.tsx
  - apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts
autonomous: true

must_haves:
  truths:
    - "Sample carrier trucks show a SAMPLE pill badge in the Unit # cell of CarrierTruckList"
    - "Sample carrier drivers show a SAMPLE pill badge in the Driver cell of CarrierDriverList"
    - "Sample loads show a SAMPLE pill badge in the Load # cell of LoadList"
    - "Sample clients show a SAMPLE pill badge in the Name cell of ClientList"
    - "The carrier dashboard banner (SampleDataBanner) appears when hasSampleRecords is true (queries carrier tables, not PascalCase tables)"
    - "KPI counts on the carrier dashboard exclude sample records (isSample: false filter)"
    - "Phase 49 PascalCase SamplePill/Banner placements remain untouched (Option Z)"
  artifacts:
    - path: "apps/web/src/components/carrier/trucks/CarrierTruckList.tsx"
      provides: "SamplePill in truck list rows"
      contains: "SamplePill"
    - path: "apps/web/src/components/carrier/drivers/CarrierDriverList.tsx"
      provides: "SamplePill in driver list rows"
      contains: "SamplePill"
    - path: "apps/web/src/components/carrier/loads/LoadList.tsx"
      provides: "SamplePill in load list rows"
      contains: "SamplePill"
    - path: "apps/web/src/components/carrier/clients/ClientList.tsx"
      provides: "SamplePill in client list rows"
      contains: "SamplePill"
    - path: "apps/web/src/app/api/v1/carrier/loads/route.ts"
      provides: "isSample field in loads API response"
      contains: "isSample"
    - path: "apps/web/src/app/(owner)/carrier/dashboard/page.tsx"
      provides: "hasSampleRecords queries carrier tables instead of PascalCase tables"
      contains: "carrierTruck.count"
    - path: "apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts"
      provides: "KPI counts with isSample: false filter"
      contains: "isSample: false"
  key_links:
    - from: "apps/web/src/components/carrier/loads/LoadList.tsx"
      to: "apps/web/src/app/api/v1/carrier/loads/route.ts"
      via: "fetch('/api/v1/carrier/loads') — LoadList is client-side, isSample must be in API response"
      pattern: "isSample"
    - from: "apps/web/src/app/(owner)/carrier/dashboard/page.tsx"
      to: "carrierTruck, carrierDriver, carrierLoad, carrierClient"
      via: "tx.carrierTruck.count, tx.carrierDriver.count, tx.carrierLoad.count, tx.carrierClient.count with isSample: true filter"
      pattern: "carrierTruck\\.count.*isSample"
    - from: "apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts"
      to: "carrierLoad queries"
      via: "isSample: false added to all where clauses"
      pattern: "isSample.*false"
---

<objective>
Integrate SamplePill into the four carrier list components and fix the dashboard's hasSampleRecords check to query carrier snake_case tables. Also add isSample exclusion to the KPI route.

Purpose: The SamplePill badges tell users which rows are sample data. The dashboard banner correctly shows when sample data exists in the carrier tables (not the now-irrelevant PascalCase tables). KPI counts are accurate because they exclude sample rows.
Output: Four list components with SamplePill, three parent page files verified/updated for isSample field propagation, updated dashboard page, updated KPI route, updated loads API route.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/components/onboarding/sample-pill.tsx
@apps/web/src/components/onboarding/sample-data-banner.tsx
@apps/web/src/components/carrier/trucks/CarrierTruckList.tsx
@apps/web/src/components/carrier/drivers/CarrierDriverList.tsx
@apps/web/src/components/carrier/loads/LoadList.tsx
@apps/web/src/components/carrier/clients/ClientList.tsx
@apps/web/src/app/api/v1/carrier/loads/route.ts
@apps/web/src/app/(owner)/carrier/fleet/trucks/page.tsx
@apps/web/src/app/(owner)/carrier/fleet/drivers/page.tsx
@apps/web/src/app/(owner)/carrier/clients/page.tsx
@apps/web/src/app/(owner)/carrier/dashboard/page.tsx
@apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts
@.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SamplePill to four carrier list components + add isSample to loads API response + verify parent pages pass isSample</name>
  <files>
    apps/web/src/components/carrier/trucks/CarrierTruckList.tsx
    apps/web/src/components/carrier/drivers/CarrierDriverList.tsx
    apps/web/src/components/carrier/loads/LoadList.tsx
    apps/web/src/components/carrier/clients/ClientList.tsx
    apps/web/src/app/api/v1/carrier/loads/route.ts
    apps/web/src/app/(owner)/carrier/fleet/trucks/page.tsx
    apps/web/src/app/(owner)/carrier/fleet/drivers/page.tsx
    apps/web/src/app/(owner)/carrier/clients/page.tsx
  </files>
  <action>
    Read ALL eight files before editing. The SamplePill component is a zero-prop component at `apps/web/src/components/onboarding/sample-pill.tsx`.

    --- CarrierTruckList.tsx ---
    1. Add import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
    2. In the `CarrierTruckItem` interface, add: `isSample: boolean;`
    3. In the Unit # cell `<td>` (where the truck name `<Link>` is rendered), add after the name link:
       `{t.isSample && <SamplePill />}`
    4. Ensure `isSample` is passed through from the parent page's Prisma query (the parent page queries carrierTruck and should now include isSample — verify this is included or the page will need updating too; check the page file that renders CarrierTruckList).

    --- CarrierDriverList.tsx ---
    1. Add import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
    2. In the `CarrierDriverItem` interface, add: `isSample: boolean;`
    3. In the Driver cell `<td>` (where the driver name `<Link>` is rendered), add after the name link:
       `{d.isSample && <SamplePill />}`
    4. Same parent page check as above.

    --- ClientList.tsx ---
    1. Add import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
    2. In the `ClientItem` interface, add: `isSample: boolean;`
    3. In the Name cell `<td>` (where the client name `<Link>` is rendered), add after the name link:
       `{c.isSample && <SamplePill />}`

    --- LoadList.tsx ---
    LoadList fetches data client-side via `fetch('/api/v1/carrier/loads')`. The API response must include isSample.
    1. Add import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
    2. In the `LoadItem` interface (defined inline in LoadList.tsx), add: `isSample: boolean;`
    3. In the Load # cell `<td>` (where referenceNumber `<Link>` is rendered), add after the link:
       `{load.isSample && <SamplePill />}`

    --- apps/web/src/app/api/v1/carrier/loads/route.ts ---
    This is the GET endpoint LoadList fetches from. Read the file and locate the Prisma select/findMany query. Add `isSample: true` to the `select` clause (or ensure `isSample` is included if using `select: { ... }`). If the route uses `findMany` without an explicit select (returns all fields), isSample will already be included after Plan 50-01's schema update — but verify by checking whether the route has an explicit `select:` clause. If it has an explicit select, add `isSample: true` to it.

    --- apps/web/src/app/(owner)/carrier/fleet/trucks/page.tsx ---
    Read this file. Locate the Prisma query that fetches carrier trucks (likely `prisma.carrierTruck.findMany`).
    - If the query has an explicit `select:` clause, add `isSample: true` to it so the field flows through to CarrierTruckList.
    - If the query uses `findMany` WITHOUT an explicit `select:` (fetches all fields), isSample is already included — document that no change is needed in a code comment if desired.
    Make the appropriate change based on what you find.

    --- apps/web/src/app/(owner)/carrier/fleet/drivers/page.tsx ---
    Same approach as trucks page: Read the file, find the `prisma.carrierDriver.findMany` query, add `isSample: true` to the `select:` clause if one exists, or confirm all-fields fetch already includes it.

    --- apps/web/src/app/(owner)/carrier/clients/page.tsx ---
    Same approach: Read the file, find the `prisma.carrierClient.findMany` query, add `isSample: true` to the `select:` clause if one exists, or confirm all-fields fetch already includes it.

    After all edits, run:
    ```bash
    cd apps/web && npx tsc --noEmit
    ```
    Fix any type errors before committing.
  </action>
  <verify>
    1. `grep "SamplePill" apps/web/src/components/carrier/trucks/CarrierTruckList.tsx` — must show import and conditional render.
    2. `grep "SamplePill" apps/web/src/components/carrier/drivers/CarrierDriverList.tsx` — must show import and conditional render.
    3. `grep "SamplePill" apps/web/src/components/carrier/loads/LoadList.tsx` — must show import and conditional render.
    4. `grep "SamplePill" apps/web/src/components/carrier/clients/ClientList.tsx` — must show import and conditional render.
    5. `grep "isSample" apps/web/src/app/api/v1/carrier/loads/route.ts` — must show isSample in the response.
    6. `grep "isSample" apps/web/src/app/(owner)/carrier/fleet/trucks/page.tsx` — must show `isSample` in a `select:` clause, OR confirm the query has no explicit `select:` (meaning all fields are returned and `isSample` is included automatically).
    7. `grep "isSample" apps/web/src/app/(owner)/carrier/fleet/drivers/page.tsx` — must show `isSample` in a `select:` clause, OR confirm the query has no explicit `select:` (meaning all fields are returned and `isSample` is included automatically).
    8. `grep "isSample" apps/web/src/app/(owner)/carrier/clients/page.tsx` — must show `isSample` in a `select:` clause, OR confirm the query has no explicit `select:` (meaning all fields are returned and `isSample` is included automatically).
    9. `cd apps/web && npx tsc --noEmit` — must pass with no errors.
  </verify>
  <done>
    All four list components import SamplePill and render it conditionally. isSample interface field added to all four item types. Loads API route includes isSample in response. All three parent page files either include isSample in their select clause or confirmed to fetch all fields (no explicit select, so all fields including isSample are returned). tsc --noEmit passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix dashboard hasSampleRecords to query carrier tables + add isSample:false filter to KPI route</name>
  <files>
    apps/web/src/app/(owner)/carrier/dashboard/page.tsx
    apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts
  </files>
  <action>
    Read both files before editing.

    --- apps/web/src/app/(owner)/carrier/dashboard/page.tsx ---

    Locate the `hasSampleRecords` block. It currently queries PascalCase models:
    ```typescript
    const [truckCount, driverCount, loadCount, customerCount] = await Promise.all([
      tx.truck.count({ where: { tenantId: orgId, isSample: true } }),
      tx.user.count({ where: { tenantId: orgId, role: 'DRIVER', isSample: true } }),
      tx.load.count({ where: { tenantId: orgId, isSample: true } }),
      tx.customer.count({ where: { tenantId: orgId, isSample: true } }),
    ]);
    ```

    Replace with queries to carrier snake_case models using `orgId` as the tenant FK:
    ```typescript
    const [truckCount, driverCount, loadCount, clientCount] = await Promise.all([
      tx.carrierTruck.count({ where: { orgId, isSample: true } }),
      tx.carrierDriver.count({ where: { orgId, isSample: true } }),
      tx.carrierLoad.count({ where: { orgId, isSample: true } }),
      tx.carrierClient.count({ where: { orgId, isSample: true } }),
    ]);
    return truckCount + driverCount + loadCount + clientCount > 0;
    ```

    Update the destructured variable name from `customerCount` to `clientCount` for accuracy (and update the return sum accordingly).

    The banner render condition below the hasSampleRecords block remains unchanged:
    ```typescript
    {tenant?.sampleDataSeeded && hasSampleRecords && (
      <SampleDataBanner tenantId={orgId} />
    )}
    ```

    Do NOT touch Phase 49's PascalCase SampleDataBanner placements elsewhere in the codebase (Option Z).

    --- apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts ---

    Read the file and locate all `prisma.carrierLoad.count(...)` calls (or `prisma.carrierLoad.findMany` / aggregate calls). There are likely 3 calls (e.g., for weekly/monthly revenue counts or load status counts).

    For each `carrierLoad` query that has a `where:` clause, add `isSample: false` to the where clause:
    ```typescript
    // Before:
    prisma.carrierLoad.count({ where: { orgId, createdAt: { gte: monday } } })
    // After:
    prisma.carrierLoad.count({ where: { orgId, isSample: false, createdAt: { gte: monday } } })
    ```

    Apply this filter to ALL carrier model queries in this KPI route (carrierLoad, carrierTruck, carrierDriver — whatever models are queried). The goal is that KPIs reflect real production data, not sample seeds.

    After all edits, run:
    ```bash
    cd apps/web && npx tsc --noEmit
    ```
    Fix any type errors before committing.
  </action>
  <verify>
    1. `grep "carrierTruck.count\|carrierDriver.count\|carrierLoad.count\|carrierClient.count" apps/web/src/app/(owner)/carrier/dashboard/page.tsx` — must show all four carrier queries.
    2. `grep "tx.truck\|tx.user\|tx.load\|tx.customer" apps/web/src/app/(owner)/carrier/dashboard/page.tsx` — must return NO matches in the hasSampleRecords block (old PascalCase queries fully replaced).
    3. `grep "isSample: false" apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts` — must show at least one match per carrier model queried.
    4. `cd apps/web && npx tsc --noEmit` — must pass with no errors.
    5. Live verification:
       a. Sign into the test tenant (postscript-test-co) — the dashboard should show the SampleDataBanner (since sample data was seeded and is now in carrier tables).
       b. KPI numbers should reflect only real data (sample loads/trucks excluded).
       c. Sample rows in the truck, driver, load, and client list pages should show the SAMPLE pill badge.
       d. Dismiss the SampleDataBanner — it should disappear and stay dismissed within the same session.
  </verify>
  <done>
    dashboard/page.tsx hasSampleRecords queries carrierTruck/carrierDriver/carrierLoad/carrierClient using orgId. KPI route has isSample:false on all carrier queries. tsc --noEmit passes. Live: SampleDataBanner shows for tenant with sample data, KPIs exclude sample records, SAMPLE pills appear on sample rows.
  </done>
</task>

</tasks>

<verification>
Full end-to-end check after both tasks:
1. `grep -r "SamplePill" apps/web/src/components/carrier/` — shows SamplePill in all four list component files.
2. `grep "carrierTruck.count" apps/web/src/app/(owner)/carrier/dashboard/page.tsx` — shows carrier query in hasSampleRecords.
3. `grep "isSample: false" apps/web/src/app/api/v1/carrier/dashboard/kpi/route.ts` — shows filter on KPI queries.
4. `grep "isSample" apps/web/src/app/(owner)/carrier/fleet/trucks/page.tsx` — must show `isSample` in a `select:` clause, OR confirm the query has no explicit `select:` (meaning all fields are returned and `isSample` is included automatically).
5. `grep "isSample" apps/web/src/app/(owner)/carrier/fleet/drivers/page.tsx` — must show `isSample` in a `select:` clause, OR confirm the query has no explicit `select:` (meaning all fields are returned and `isSample` is included automatically).
6. `grep "isSample" apps/web/src/app/(owner)/carrier/clients/page.tsx` — must show `isSample` in a `select:` clause, OR confirm the query has no explicit `select:` (meaning all fields are returned and `isSample` is included automatically).
7. `cd apps/web && npx tsc --noEmit` exits 0.
8. Live: Sign into postscript-test-co. SampleDataBanner appears on /carrier/dashboard. Sample rows show SAMPLE pill in all four list pages. KPI numbers exclude sample data. Banner dismisses and stays dismissed in session.
</verification>

<success_criteria>
- SamplePill renders in truck, driver, load, and client list rows for isSample=true records.
- isSample field added to all four item interfaces.
- Loads API route includes isSample in GET response so LoadList (client-side fetch) has the field.
- Trucks, drivers, and clients page files verified: isSample included in Prisma select clause (or confirmed all-fields fetch already includes it — no explicit select means all fields returned).
- dashboard/page.tsx hasSampleRecords queries carrierTruck, carrierDriver, carrierLoad, carrierClient — NOT the PascalCase models.
- KPI route has isSample: false on all carrier model queries.
- Phase 49 PascalCase SamplePill/Banner placements not touched (Option Z).
- tsc --noEmit passes.
- Live: banner visible, pills visible on sample rows, KPIs accurate.
</success_criteria>

<output>
After completion, commit with: `feat(50-06): integrate SamplePill and SampleDataBanner into carrier list pages`

Then create `.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-06-SUMMARY.md`
</output>
