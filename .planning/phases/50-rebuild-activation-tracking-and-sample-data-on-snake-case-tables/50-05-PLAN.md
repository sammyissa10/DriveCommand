---
phase: 50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables
plan: "05"
type: execute
wave: 4
depends_on: ["50-03", "50-04"]
files_modified:
  - apps/web/src/lib/carrier/dispatches.ts
autonomous: true

must_haves:
  truths:
    - "Transitioning a dispatch from planned to in_progress fires recordActivationEvent(orgId, 'first_load_in_transit')"
    - "The tracker call is in a non-blocking after() block wrapped in try/catch"
    - "After the first in_transit dispatch for the test tenant, firstLoadInTransitAt is set, isActivated flips true, completionPct reaches 100"
    - "A tenant.activated AppEvent is written to the AppEvent table"
    - "No isSample guard is needed — dispatches are always real operations"
    - "The existing ON_DISPATCH_DEPART fireEvent after() block and push notification after() blocks are preserved untouched"
  artifacts:
    - path: "apps/web/src/lib/carrier/dispatches.ts"
      provides: "Activation tracker hook in transitionDispatchStatus planned→in_progress branch"
      contains: "recordActivationEvent"
  key_links:
    - from: "apps/web/src/lib/carrier/dispatches.ts"
      to: "apps/web/src/lib/onboarding/activation-tracker.ts"
      via: "import { recordActivationEvent } from '@/lib/onboarding/activation-tracker'"
      pattern: "recordActivationEvent.*first_load_in_transit"
    - from: "planned→in_progress branch"
      to: "recordActivationEvent call"
      via: "after() block inserted after ON_DISPATCH_DEPART fireEvent block, before return"
      pattern: "first_load_in_transit"
---

<objective>
Wire the activation tracker into `transitionDispatchStatus` in dispatches.ts. When a dispatch transitions from `planned` to `in_progress`, fire `recordActivationEvent(orgId, 'first_load_in_transit')` in a non-blocking after() block. This is the final tracker event — it sets isActivated=true and writes the tenant.activated AppEvent.

Purpose: Completing activation tracking. When a tenant moves their first load to in_transit, they have demonstrated core product usage and are considered fully activated.
Output: Updated dispatches.ts with activation tracker wiring in the planned→in_progress branch.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/lib/carrier/dispatches.ts
@apps/web/src/lib/onboarding/activation-tracker.ts
@.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recordActivationEvent import and after() block in transitionDispatchStatus planned→in_progress branch</name>
  <files>
    apps/web/src/lib/carrier/dispatches.ts
  </files>
  <action>
    Read the FULL file `apps/web/src/lib/carrier/dispatches.ts` before editing. This is important — the file is complex and the insertion point must be precise.

    STEP 1 — Add import at the top of the file:
    ```typescript
    import { recordActivationEvent } from '@/lib/onboarding/activation-tracker';
    ```
    Check if `after` is already imported (it is likely used for fireEvent/sendPushToUser calls). If not already imported, add it to the 'next/server' import. If it is already imported, do not add a duplicate.

    STEP 2 — Locate the `planned → in_progress` branch inside `transitionDispatchStatus`. It contains:
    - A prisma.carrierDispatch.update call setting status to 'in_progress' and actualDeparture
    - Push notification after() call (sendPushToUser)
    - Notification after() call (createNotification)
    - ON_DISPATCH_DEPART fireEvent after() call
    - A `return { id: updated.id, status: updated.status, notes: updated.notes };` statement

    STEP 3 — Insert a new after() block AFTER the ON_DISPATCH_DEPART fireEvent block and BEFORE the `return` statement:
    ```typescript
    // Activation tracker: first load moved to in_transit
    after(async () => {
      try {
        await recordActivationEvent(orgId, 'first_load_in_transit');
      } catch (err) {
        logger.error('[transitionDispatchStatus] activation tracker failed', { dispatchId: id, err });
      }
    });

    return { id: updated.id, status: updated.status, notes: updated.notes };
    ```

    No `isSample` guard is needed. Dispatches cannot be seeded as sample data (there is no sample dispatch creation in the seeder). The tracker itself is idempotent — calling it again after isActivated=true is a no-op.

    NOTE: The `first_real_driver` event is already handled by `accept-invitation/route.ts` (lines 252–258). Do NOT add any driver-related tracker call here.

    After editing, run:
    ```bash
    cd apps/web && npx tsc --noEmit
    ```
    Fix any type errors before committing.
  </action>
  <verify>
    1. `grep "recordActivationEvent" apps/web/src/lib/carrier/dispatches.ts` — must show import + call.
    2. `grep "first_load_in_transit" apps/web/src/lib/carrier/dispatches.ts` — must show the event string in the after() block.
    3. `cd apps/web && npx tsc --noEmit` — must pass with no errors.
    4. Verify insertion order (grep context): the activation tracker after() block must appear AFTER ON_DISPATCH_DEPART and BEFORE the return statement in the planned→in_progress branch.
    5. Live verification (end-to-end, use test tenant e1999e87-0f5b-4b27-8d53-7aa89055384c):
       a. Confirm ActivationProgress state: firstRealTruckAt set (from 50-03), firstRealDriverAt set (from accept-invitation), firstRealClientAt set (from 50-04), firstLoadInTransitAt IS NULL, isActivated=false.
       b. Navigate to /carrier/dispatches. Create a real load, then transition it to IN_PROGRESS (in_transit).
       c. Re-query ActivationProgress: firstLoadInTransitAt must have timestamp, isActivated must be true, completionPct must be 100.
       d. Query AppEvent: `SELECT * FROM "AppEvent" WHERE "tenantId"='e1999e87-0f5b-4b27-8d53-7aa89055384c' AND "eventType"='tenant.activated'` — must return one row.
  </verify>
  <done>
    dispatches.ts has recordActivationEvent import and after() block in the planned→in_progress branch. tsc --noEmit passes. Live test: transitioning first dispatch to in_progress sets firstLoadInTransitAt, isActivated=true, completionPct=100, and writes tenant.activated AppEvent.
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `grep "recordActivationEvent" apps/web/src/lib/carrier/dispatches.ts` shows both import and call.
2. `grep "first_load_in_transit" apps/web/src/lib/carrier/dispatches.ts` shows the event string.
3. Existing ON_DISPATCH_DEPART after() block is still present and unmodified.
4. `cd apps/web && npx tsc --noEmit` exits 0.
5. Live: full activation sequence completes — 20% → 40% → 60% → 80% → 100% with tenant.activated AppEvent written.
</verification>

<success_criteria>
- after() block added after ON_DISPATCH_DEPART and before return in the planned→in_progress branch.
- No isSample guard (dispatches cannot be sample).
- try/catch prevents tracker failure from affecting the dispatch transition response.
- tsc --noEmit passes.
- Live verification: isActivated flips true, completionPct=100, tenant.activated AppEvent written on first in_transit dispatch.
</success_criteria>

<output>
After completion, commit with: `feat(50-05): wire activation tracker into dispatch IN_TRANSIT`

Then create `.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-05-SUMMARY.md`
</output>
