---
phase: 50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables
plan: "04"
type: execute
wave: 3
depends_on: ["50-01", "50-02"]
files_modified:
  - apps/web/src/app/api/v1/carrier/clients/route.ts
autonomous: true

must_haves:
  truths:
    - "Creating a real (non-sample) carrier client fires recordActivationEvent(orgId, 'first_real_client') in a non-blocking after() block"
    - "Creating a sample client (isSample=true) does NOT fire the activation tracker"
    - "The activation tracker call is wrapped in try/catch and never blocks the HTTP response"
    - "After creating the test tenant's first real client, firstRealClientAt is set and completionPct is 60"
  artifacts:
    - path: "apps/web/src/app/api/v1/carrier/clients/route.ts"
      provides: "Activation tracker hook on real client creation"
      contains: "recordActivationEvent"
  key_links:
    - from: "apps/web/src/app/api/v1/carrier/clients/route.ts"
      to: "apps/web/src/lib/onboarding/activation-tracker.ts"
      via: "import { recordActivationEvent } from '@/lib/onboarding/activation-tracker'"
      pattern: "recordActivationEvent.*first_real_client"
    - from: "client.isSample"
      to: "after() block guard"
      via: "if (!client.isSample) check before calling recordActivationEvent"
      pattern: "client\\.isSample"
---

<objective>
Wire the activation tracker into the carrier clients POST route. When a real (non-sample) client is created, fire `recordActivationEvent(orgId, 'first_real_client')` in a non-blocking after() block. This file currently has no `after()` usage — both the `after` and `recordActivationEvent` imports must be added.

Purpose: Tracks when a tenant adds their first real client, advancing the activation progress from 40% to 60%.
Output: Updated clients route.ts with after import and activation tracker wiring.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/app/api/v1/carrier/clients/route.ts
@apps/web/src/lib/onboarding/activation-tracker.ts
@.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add after import, recordActivationEvent import, and tracker after() block to clients route POST handler</name>
  <files>
    apps/web/src/app/api/v1/carrier/clients/route.ts
  </files>
  <action>
    Read the full file `apps/web/src/app/api/v1/carrier/clients/route.ts` before editing.

    STEP 1 — Add two imports at the top of the file. The `after` function from 'next/server' is NOT currently imported in this file — it must be added. The `recordActivationEvent` import is also new.

    Add to the 'next/server' import line (or add a new import if it currently only imports NextResponse):
    ```typescript
    import { after, NextResponse } from 'next/server';
    ```
    If the file currently has `import { NextResponse } from 'next/server'`, change it to `import { after, NextResponse } from 'next/server'`.

    Add the activation tracker import:
    ```typescript
    import { recordActivationEvent } from '@/lib/onboarding/activation-tracker';
    ```

    STEP 2 — Locate the POST handler success path. It currently looks like:
    ```typescript
    const client = await createClient(orgId, parsed.data);
    return NextResponse.json({ data: client }, { status: 201 });
    ```

    STEP 3 — Insert the after() block between the createClient call and the return:
    ```typescript
    const client = await createClient(orgId, parsed.data);

    after(async () => {
      if (!client.isSample) {
        try {
          await recordActivationEvent(orgId, 'first_real_client');
        } catch (err) {
          logger.error('[carrier/clients] activation tracker failed', { clientId: client.id, err });
        }
      }
    });

    return NextResponse.json({ data: client }, { status: 201 });
    ```

    Note: Confirm that `logger` is already imported in this file. If not, use `console.error` as fallback (but check first — most carrier routes use the structured logger).

    After editing, run:
    ```bash
    cd apps/web && npx tsc --noEmit
    ```
    Fix any type errors before committing.
  </action>
  <verify>
    1. `grep "after" apps/web/src/app/api/v1/carrier/clients/route.ts` — must show `after` in the 'next/server' import AND in the after() call.
    2. `grep "recordActivationEvent" apps/web/src/app/api/v1/carrier/clients/route.ts` — must show import + call.
    3. `grep "isSample" apps/web/src/app/api/v1/carrier/clients/route.ts` — must show `!client.isSample` guard.
    4. `cd apps/web && npx tsc --noEmit` — must pass with no errors.
    5. Live verification:
       a. Ensure firstRealClientAt IS NULL for tenant e1999e87-0f5b-4b27-8d53-7aa89055384c (or run the reset SQL from phase context).
       b. Add a real client via /carrier/clients/new.
       c. Query ActivationProgress — firstRealClientAt must have a timestamp, completionPct must be 60.
  </verify>
  <done>
    clients/route.ts has `after` imported from 'next/server', recordActivationEvent imported and called with 'first_real_client', isSample guard in place. tsc --noEmit passes. Live test confirms firstRealClientAt populates and completionPct=60.
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `grep "after" apps/web/src/app/api/v1/carrier/clients/route.ts` shows after in import and in after() call.
2. `grep "recordActivationEvent" apps/web/src/app/api/v1/carrier/clients/route.ts` shows import and call.
3. `cd apps/web && npx tsc --noEmit` exits 0.
4. Live: creating a real client advances completionPct to 60 and sets firstRealClientAt.
</verification>

<success_criteria>
- `after` added to the 'next/server' import (was not present before).
- after() block inserted between createClient() call and return statement.
- isSample guard prevents sample clients from triggering tracker.
- try/catch prevents tracker failure from affecting HTTP response.
- tsc --noEmit passes.
- Live verification: firstRealClientAt set, completionPct=60 after first real client creation.
</success_criteria>

<output>
After completion, commit with: `feat(50-04): wire activation tracker into clients create`

Then create `.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-04-SUMMARY.md`
</output>
