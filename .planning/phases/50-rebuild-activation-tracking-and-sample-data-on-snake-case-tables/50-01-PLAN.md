---
phase: 50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/prisma/migrations/20260501000001_phase50_01_carrier_trucks_is_sample/migration.sql
  - apps/web/prisma/migrations/20260501000002_phase50_02_clients_is_sample/migration.sql
  - apps/web/prisma/migrations/20260501000003_phase50_03_loads_is_sample/migration.sql
  - apps/web/prisma/migrations/20260501000004_phase50_04_carrier_drivers_is_sample/migration.sql
  - apps/web/prisma/schema.prisma
autonomous: true

must_haves:
  truths:
    - "carrier_trucks table has an is_sample column (BOOLEAN NOT NULL DEFAULT false)"
    - "clients table has an is_sample column (BOOLEAN NOT NULL DEFAULT false)"
    - "loads table has an is_sample column (BOOLEAN NOT NULL DEFAULT false)"
    - "carrier_drivers table has an is_sample column (BOOLEAN NOT NULL DEFAULT false)"
    - "Prisma client recognizes isSample on CarrierTruck, CarrierClient, CarrierLoad, CarrierDriver"
    - "npx prisma generate completes without errors"
  artifacts:
    - path: "apps/web/prisma/migrations/20260501000001_phase50_01_carrier_trucks_is_sample/migration.sql"
      provides: "DDL for carrier_trucks.is_sample"
      contains: "ALTER TABLE carrier_trucks ADD COLUMN IF NOT EXISTS is_sample"
    - path: "apps/web/prisma/migrations/20260501000002_phase50_02_clients_is_sample/migration.sql"
      provides: "DDL for clients.is_sample"
      contains: "ALTER TABLE clients ADD COLUMN IF NOT EXISTS is_sample"
    - path: "apps/web/prisma/migrations/20260501000003_phase50_03_loads_is_sample/migration.sql"
      provides: "DDL for loads.is_sample"
      contains: "ALTER TABLE loads ADD COLUMN IF NOT EXISTS is_sample"
    - path: "apps/web/prisma/migrations/20260501000004_phase50_04_carrier_drivers_is_sample/migration.sql"
      provides: "DDL for carrier_drivers.is_sample"
      contains: "ALTER TABLE carrier_drivers ADD COLUMN IF NOT EXISTS is_sample"
    - path: "apps/web/prisma/schema.prisma"
      provides: "Prisma model fields for isSample"
      contains: "isSample Boolean @default(false) @map(\"is_sample\")"
  key_links:
    - from: "apps/web/prisma/migrations/20260501000001_phase50_01_carrier_trucks_is_sample/migration.sql"
      to: "carrier_trucks table"
      via: "migrate.mjs wraps in BEGIN/COMMIT — SQL must NOT include BEGIN/COMMIT"
      pattern: "ALTER TABLE carrier_trucks ADD COLUMN IF NOT EXISTS is_sample"
    - from: "apps/web/prisma/schema.prisma"
      to: "Prisma generated client"
      via: "npx prisma generate in apps/web/"
      pattern: "isSample.*@map.*is_sample"
---

<objective>
Add the `is_sample` boolean column to four carrier snake_case tables via raw SQL migrations, then update the Prisma schema and regenerate the client.

Purpose: Plans 50-02 through 50-06 all depend on `isSample` existing on the Prisma models. This plan creates the schema foundation everything else builds on.
Output: Four migration.sql files, an updated schema.prisma, and a regenerated Prisma client.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/prisma/schema.prisma
@.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write four migration SQL files</name>
  <files>
    apps/web/prisma/migrations/20260501000001_phase50_01_carrier_trucks_is_sample/migration.sql
    apps/web/prisma/migrations/20260501000002_phase50_02_clients_is_sample/migration.sql
    apps/web/prisma/migrations/20260501000003_phase50_03_loads_is_sample/migration.sql
    apps/web/prisma/migrations/20260501000004_phase50_04_carrier_drivers_is_sample/migration.sql
  </files>
  <action>
    Create four migration directories under `apps/web/prisma/migrations/` (the correct location — NOT `apps/web/migrations/`). Each directory contains exactly one `migration.sql` file.

    CRITICAL: migrate.mjs wraps each migration in its own BEGIN/COMMIT. The SQL files must NOT include BEGIN or COMMIT themselves.

    File 1 — `apps/web/prisma/migrations/20260501000001_phase50_01_carrier_trucks_is_sample/migration.sql`:
    ```sql
    ALTER TABLE carrier_trucks ADD COLUMN IF NOT EXISTS is_sample BOOLEAN NOT NULL DEFAULT false;
    ```

    File 2 — `apps/web/prisma/migrations/20260501000002_phase50_02_clients_is_sample/migration.sql`:
    ```sql
    ALTER TABLE clients ADD COLUMN IF NOT EXISTS is_sample BOOLEAN NOT NULL DEFAULT false;
    ```

    File 3 — `apps/web/prisma/migrations/20260501000003_phase50_03_loads_is_sample/migration.sql`:
    ```sql
    ALTER TABLE loads ADD COLUMN IF NOT EXISTS is_sample BOOLEAN NOT NULL DEFAULT false;
    ```

    File 4 — `apps/web/prisma/migrations/20260501000004_phase50_04_carrier_drivers_is_sample/migration.sql`:
    ```sql
    ALTER TABLE carrier_drivers ADD COLUMN IF NOT EXISTS is_sample BOOLEAN NOT NULL DEFAULT false;
    ```

    Do NOT run `prisma migrate dev`. These are raw SQL files that migrate.mjs applies.
  </action>
  <verify>
    Run: `ls apps/web/prisma/migrations/ | grep phase50`
    Expected: four directories listed (20260501000001_phase50_01_carrier_trucks_is_sample, 20260501000002_phase50_02_clients_is_sample, 20260501000003_phase50_03_loads_is_sample, 20260501000004_phase50_04_carrier_drivers_is_sample).
    
    Check each file has content: `cat apps/web/prisma/migrations/20260501000001_phase50_01_carrier_trucks_is_sample/migration.sql`
    Expected: single ALTER TABLE statement, no BEGIN/COMMIT.
  </verify>
  <done>Four migration.sql files exist with correct ALTER TABLE statements and no BEGIN/COMMIT wrappers.</done>
</task>

<task type="auto">
  <name>Task 2: Update schema.prisma and regenerate Prisma client</name>
  <files>
    apps/web/prisma/schema.prisma
  </files>
  <action>
    Read `apps/web/prisma/schema.prisma` first (required before editing).

    Locate the four carrier models and add the `isSample` field to each. Place the new field after the `status` field in each model (or at a logical grouping point near other boolean flags). The exact field declaration is identical for all four models:

    ```prisma
    isSample Boolean @default(false) @map("is_sample")
    ```

    Models to update:
    1. `CarrierTruck` (maps to `@@map("carrier_trucks")`) — add `isSample Boolean @default(false) @map("is_sample")`
    2. `CarrierClient` (maps to `@@map("clients")`) — add `isSample Boolean @default(false) @map("is_sample")`
    3. `CarrierLoad` (maps to `@@map("loads")`) — add `isSample Boolean @default(false) @map("is_sample")`
    4. `CarrierDriver` (maps to `@@map("carrier_drivers")`) — add `isSample Boolean @default(false) @map("is_sample")`

    After editing schema.prisma, run Prisma client generation:
    ```bash
    cd apps/web && npx prisma generate
    ```

    Do NOT run `prisma migrate dev` or `prisma db push` — the migrations are raw SQL files, not Prisma-managed.
  </action>
  <verify>
    1. Run `grep -n "isSample" apps/web/prisma/schema.prisma` — must show 4 matches, one in each carrier model.
    2. Run `cd apps/web && npx prisma generate` — must complete with "Generated Prisma Client" message and no errors.
    3. Run `grep -n "isSample" apps/web/node_modules/.prisma/client/index.d.ts | head -20` — must show isSample fields in generated types.
  </verify>
  <done>schema.prisma has isSample on all four carrier models. `npx prisma generate` exits 0. Generated client types include isSample.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls apps/web/prisma/migrations/ | grep phase50` shows exactly 4 directories.
2. `grep -c "isSample" apps/web/prisma/schema.prisma` returns 4.
3. `cd apps/web && npx prisma generate` exits 0.
4. `npx tsc --noEmit` in `apps/web/` passes (no new type errors introduced by schema additions).
</verification>

<success_criteria>
- Four migration SQL files exist with correct IF NOT EXISTS ALTER TABLE statements and no BEGIN/COMMIT.
- schema.prisma has `isSample Boolean @default(false) @map("is_sample")` in CarrierTruck, CarrierClient, CarrierLoad, CarrierDriver.
- `npx prisma generate` completes without errors.
- `npx tsc --noEmit` passes in apps/web/.
</success_criteria>

<output>
After completion, commit with: `feat(50-01): add is_sample to snake_case tables`

Then create `.planning/phases/50-rebuild-activation-tracking-and-sample-data-on-snake-case-tables/50-01-SUMMARY.md`
</output>
