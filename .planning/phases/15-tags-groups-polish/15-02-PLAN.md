---
phase: 15-tags-groups-polish
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/tags/tag-filter.tsx
  - src/app/(owner)/live-map/page.tsx
  - src/app/(owner)/live-map/actions.ts
  - src/app/(owner)/safety/page.tsx
  - src/app/(owner)/safety/actions.ts
  - src/app/(owner)/fuel/page.tsx
  - src/app/(owner)/fuel/actions.ts
  - src/app/(owner)/live-map/loading.tsx
  - src/app/(owner)/safety/loading.tsx
  - src/app/(owner)/fuel/loading.tsx
  - src/app/(owner)/dashboard/loading.tsx
  - src/app/(owner)/tags/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Owner can filter Live Map by tag to show only matching vehicles"
    - "Owner can filter Safety Dashboard by tag to show only matching vehicles"
    - "Owner can filter Fuel Dashboard by tag to show only matching vehicles"
    - "All dashboard pages show skeleton loading states while data fetches"
    - "Dashboard pages handle empty state gracefully when no data exists"
  artifacts:
    - path: "src/components/tags/tag-filter.tsx"
      provides: "Reusable tag filter dropdown component"
    - path: "src/app/(owner)/live-map/loading.tsx"
      provides: "Loading skeleton for live map page"
    - path: "src/app/(owner)/safety/loading.tsx"
      provides: "Loading skeleton for safety dashboard"
    - path: "src/app/(owner)/fuel/loading.tsx"
      provides: "Loading skeleton for fuel dashboard"
    - path: "src/app/(owner)/dashboard/loading.tsx"
      provides: "Loading skeleton for main dashboard"
  key_links:
    - from: "src/components/tags/tag-filter.tsx"
      to: "URL searchParams"
      via: "router.push with tagId query parameter"
      pattern: "searchParams.*tagId"
    - from: "src/app/(owner)/safety/page.tsx"
      to: "src/app/(owner)/safety/actions.ts"
      via: "passes tagId from searchParams to server actions"
      pattern: "getFleetSafetyScore.*tagId"
    - from: "src/app/(owner)/safety/actions.ts"
      to: "TagAssignment table"
      via: "SQL subquery filter on truckId"
      pattern: "TagAssignment.*tagId"
---

<objective>
Add tag-based filtering to all Fleet Intelligence dashboards (Map, Safety, Fuel) and add loading/error states to all dashboard pages.

Purpose: Complete the organization system by enabling cross-dashboard filtering (NAVI-03 requirement). Loading states provide production-ready UX with skeleton placeholders during data fetch. Tags become functional filters, not just labels.
Output: Tag filter dropdown on all dashboards, filtered server actions, loading.tsx files for Suspense boundaries on all dashboard routes.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-groups-polish/15-01-SUMMARY.md
@src/app/(owner)/safety/page.tsx
@src/app/(owner)/safety/actions.ts
@src/app/(owner)/fuel/page.tsx
@src/app/(owner)/fuel/actions.ts
@src/app/(owner)/live-map/page.tsx
@src/app/(owner)/live-map/actions.ts
@src/app/(owner)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag filter component and dashboard filtering via server actions</name>
  <files>
    src/components/tags/tag-filter.tsx
    src/app/(owner)/live-map/page.tsx
    src/app/(owner)/live-map/actions.ts
    src/app/(owner)/safety/page.tsx
    src/app/(owner)/safety/actions.ts
    src/app/(owner)/fuel/page.tsx
    src/app/(owner)/fuel/actions.ts
  </files>
  <action>
1. Create src/components/tags/tag-filter.tsx ('use client'):
- Props: `tags: Array<{ id: string; name: string; color: string }>`, `selectedTagId: string | null`
- Uses the existing shadcn Select component (already at src/components/ui/select.tsx — do NOT use Popover here, use Select for single-selection dropdown)
- Select options: "All Vehicles" (value="" to clear filter) + one option per tag, each showing a colored dot (inline div with backgroundColor) + tag name
- On value change, update URL searchParams: use `useRouter()` and `usePathname()` from next/navigation. If tag selected, push `${pathname}?tagId=${id}`; if "All" selected, push `${pathname}` (no query param)
- Compact design to fit in page header row: wrap in a div with `w-[200px]`

2. Update server actions to accept optional `tagId` parameter for filtering:

In src/app/(owner)/live-map/actions.ts — update `getLatestVehicleLocations`:
- Add parameter: `(tagId?: string)`
- When tagId is provided, add to the raw SQL query an INNER JOIN:
  ```sql
  ${tagId ? Prisma.sql`INNER JOIN "TagAssignment" ta ON ta."truckId" = t.id AND ta."tagId" = ${tagId}::uuid` : Prisma.empty}
  ```
  This filters the results to only trucks with the specified tag. Use Prisma.sql and Prisma.empty for safe SQL interpolation (import from '@prisma/client' or use tagged template conditionals).
  If tagId is undefined/null, the query remains unchanged (Prisma.empty produces no SQL).

In src/app/(owner)/safety/actions.ts — update all 4 exported functions:
- Add optional `tagId?: string` parameter to: `getFleetSafetyScore`, `getEventDistribution`, `getSafetyScoreTrend`, `getDriverRankings`
- When tagId is provided, add a WHERE clause filter using a subquery:
  ```sql
  ${tagId ? Prisma.sql`AND se."truckId" IN (SELECT "truckId" FROM "TagAssignment" WHERE "tagId" = ${tagId}::uuid AND "truckId" IS NOT NULL)` : Prisma.empty}
  ```
- For getDriverRankings (which joins Truck), filter the Truck set with the same subquery on t.id

In src/app/(owner)/fuel/actions.ts — update all 5 exported functions:
- Add optional `tagId?: string` parameter to: `getFleetFuelSummary`, `getFuelEfficiencyTrend`, `getCO2Emissions`, `getIdleTimeAnalysis`, `getFuelEfficiencyRankings`
- When tagId provided, add same subquery filter pattern on fr."truckId" or t.id depending on which alias the query uses
- Use the same Prisma.sql/Prisma.empty conditional pattern

3. Update dashboard pages to read searchParams and pass tagId to server actions:

IMPORTANT: In Next.js 16 App Router, `searchParams` is a Promise and MUST be awaited:
```tsx
export default async function PageName({
  searchParams,
}: {
  searchParams: Promise<{ tagId?: string }>;
}) {
  const { tagId } = await searchParams;
  // ...
}
```

In src/app/(owner)/live-map/page.tsx:
- Accept `searchParams` prop (Promise<{ tagId?: string }>), await it
- Import and call listTags() from '@/app/(owner)/actions/tags' to get available tags
- Pass tagId to getLatestVehicleLocations(tagId || undefined)
- Add TagFilter component between title and status legend:
  ```tsx
  <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <h1 ...>Live Fleet Map</h1>
      <p ...>{vehicles.length} vehicle{vehicles.length !== 1 ? 's' : ''} tracked</p>
    </div>
    <TagFilter tags={tags} selectedTagId={tagId || null} />
  </div>
  ```

In src/app/(owner)/safety/page.tsx:
- Accept and await `searchParams` prop
- Import and call listTags()
- Pass tagId to all 4 server action calls: getFleetSafetyScore(30, tagId), getEventDistribution(tagId), etc.
- Add TagFilter in header row next to title using same flex layout pattern

In src/app/(owner)/fuel/page.tsx:
- Accept and await `searchParams` prop
- Import and call listTags()
- Pass tagId to all 5 server action calls
- Add TagFilter in header row next to title
  </action>
  <verify>
Run `npm run build` to verify all pages compile. Navigate to /safety?tagId=some-uuid and verify server action receives the tagId. Navigate to /safety (no tagId) and verify all data shows. Check that TagFilter dropdown renders on all three dashboard pages.
  </verify>
  <done>
All three dashboards (Map, Safety, Fuel) accept tagId searchParam for filtering. TagFilter dropdown appears on all dashboard pages. Selecting a tag filters data to only vehicles with that tag. "All Vehicles" shows unfiltered data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Loading skeletons for all dashboard pages</name>
  <files>
    src/app/(owner)/live-map/loading.tsx
    src/app/(owner)/safety/loading.tsx
    src/app/(owner)/fuel/loading.tsx
    src/app/(owner)/dashboard/loading.tsx
    src/app/(owner)/tags/loading.tsx
  </files>
  <action>
Create loading.tsx files for each dashboard route. These are Next.js App Router Suspense boundaries that show skeleton UI while the page server component fetches data. All loading.tsx must be default exports.

1. src/app/(owner)/dashboard/loading.tsx:
- Import Skeleton from '@/components/ui/skeleton', Card + CardContent + CardHeader from '@/components/ui/card'
- Structure matching the actual dashboard page layout (from src/app/(owner)/dashboard/page.tsx):
  - Header skeleton: Skeleton h-8 w-48 for title, Skeleton h-4 w-64 for subtitle
  - Grid of 4 stat card skeletons: `grid grid-cols-2 lg:grid-cols-4 gap-4` with Card containing Skeleton h-6 w-20 (label) + Skeleton h-10 w-32 (value)
  - Two widget skeletons: `grid grid-cols-1 lg:grid-cols-2 gap-6` with Card h-64 each

2. src/app/(owner)/safety/loading.tsx:
- Structure matching safety/page.tsx layout:
  - Header skeleton (h-8 w-64 title + h-4 w-96 subtitle + h-10 w-[200px] for filter dropdown)
  - Top row: `grid grid-cols-1 lg:grid-cols-3 gap-6` — Card skeleton h-[350px] (score) + Card skeleton h-[350px] lg:col-span-2 (distribution)
  - Full-width Card skeleton h-[350px] (trend chart)
  - Bottom row: `grid grid-cols-1 lg:grid-cols-2 gap-6` — two Card skeletons h-[400px] each

3. src/app/(owner)/fuel/loading.tsx:
- Structure matching fuel/page.tsx layout:
  - Header skeleton with filter placeholder
  - Top row: `grid grid-cols-1 lg:grid-cols-3 gap-6` — Card h-[350px] (summary) + Card h-[350px] lg:col-span-2 (MPG trend)
  - Middle row: `grid grid-cols-1 lg:grid-cols-2 gap-6` — two Card skeletons h-[350px]
  - Full-width Card skeleton h-[300px] (leaderboard)

4. src/app/(owner)/live-map/loading.tsx:
- Structure matching live-map/page.tsx layout:
  - Header skeleton (title + subtitle + filter)
  - Status legend: 3 small Skeleton circles (w-3 h-3 rounded-full) with text Skeletons
  - Map area: Skeleton with `flex-1 rounded-lg` and min-h-[400px], containing a centered "Loading map..." text in muted-foreground

5. src/app/(owner)/tags/loading.tsx:
- Structure matching tags/page.tsx layout:
  - Header skeleton
  - Card skeleton h-[200px] (tag manager area)
  - Card skeleton h-[400px] (tag assignment area)

All loading.tsx files should:
- Use `import { Skeleton } from '@/components/ui/skeleton'`
- Use `import { Card, CardContent, CardHeader } from '@/components/ui/card'`
- Match the grid layout classes (grid-cols) of their respective pages exactly
- Use `space-y-6` wrapper to match page spacing
- Be default export functions (Next.js convention)
  </action>
  <verify>
Run `npm run build` to verify all loading.tsx files compile. All loading files are at the correct route-level paths for Next.js to detect them as Suspense boundaries.
  </verify>
  <done>
All five dashboard pages (dashboard, live-map, safety, fuel, tags) have loading.tsx files providing skeleton UI via Next.js Suspense boundaries. Skeleton layouts match the visual structure of their respective pages.
  </done>
</task>

</tasks>

<verification>
- Tag filter dropdown appears on Live Map, Safety, and Fuel dashboard pages
- Selecting a tag filters dashboard data to only tagged vehicles
- "All Vehicles" option removes the filter (no tagId in URL)
- Navigating to any dashboard shows loading skeleton before content appears
- `npm run build` succeeds
</verification>

<success_criteria>
Owner can filter all three Fleet Intelligence dashboards by tag. All dashboard pages display loading skeletons during data fetch. Tag filter state is stored in URL searchParams for shareability.
</success_criteria>

<output>
After completion, create `.planning/phases/15-tags-groups-polish/15-02-SUMMARY.md`
</output>
