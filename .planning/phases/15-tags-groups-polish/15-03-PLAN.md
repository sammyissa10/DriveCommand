---
phase: 15-tags-groups-polish
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - playwright.config.ts
  - e2e/tags.spec.ts
  - e2e/dashboard-filtering.spec.ts
  - e2e/responsive.spec.ts
  - src/components/safety/safety-score-card.tsx
  - src/components/safety/event-distribution-chart.tsx
  - src/components/safety/safety-trend-chart.tsx
  - src/components/safety/driver-leaderboard.tsx
  - src/components/safety/threshold-config.tsx
  - src/components/fuel/fuel-summary-card.tsx
  - src/components/fuel/mpg-trend-chart.tsx
  - src/components/fuel/emissions-card.tsx
  - src/components/fuel/idle-time-card.tsx
  - src/components/fuel/fuel-leaderboard.tsx
  - src/components/vehicle/vehicle-detail-sheet.tsx
  - next.config.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Playwright E2E tests pass for tag CRUD operations"
    - "Playwright E2E tests pass for dashboard filtering by tags"
    - "Playwright E2E tests verify mobile responsive layouts at 375px width"
    - "Sidebar collapses to off-canvas drawer on mobile (<768px)"
    - "Charts stack vertically on mobile (single column)"
    - "No horizontal overflow on any dashboard page at 375px width"
    - "First-load JavaScript bundle is under 500KB for all routes"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration for E2E tests"
    - path: "e2e/tags.spec.ts"
      provides: "E2E tests for tag management page"
    - path: "e2e/dashboard-filtering.spec.ts"
      provides: "E2E tests for dashboard tag filtering"
    - path: "e2e/responsive.spec.ts"
      provides: "E2E tests for mobile responsive behavior"
  key_links:
    - from: "e2e/tags.spec.ts"
      to: "src/app/(owner)/tags/page.tsx"
      via: "Playwright page navigation"
      pattern: "page.goto.*tags"
    - from: "e2e/responsive.spec.ts"
      to: "src/components/ui/sidebar.tsx"
      via: "viewport resize verification"
      pattern: "setViewportSize"
---

<objective>
Set up Playwright E2E testing, write tests for tag management and dashboard filtering, ensure mobile responsive layouts, and verify bundle size.

Purpose: Playwright E2E tests validate the full user workflow — creating tags, assigning them to vehicles, filtering dashboards by tags, and verifying mobile responsiveness. Combined with responsive fixes and bundle verification, this completes the production-ready UX requirement.
Output: Playwright config, E2E test suites for tags/filtering/responsive, mobile-responsive chart components, verified bundle size under 500KB.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-groups-polish/15-02-SUMMARY.md
@src/components/navigation/owner-shell.tsx
@src/components/navigation/sidebar.tsx
@src/components/ui/sidebar.tsx
@src/hooks/use-mobile.tsx
@src/app/(owner)/safety/page.tsx
@src/app/(owner)/fuel/page.tsx
@src/app/(owner)/live-map/page.tsx
@src/app/(owner)/tags/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright setup, E2E tests for tags, dashboard filtering, and responsive behavior</name>
  <files>
    playwright.config.ts
    e2e/tags.spec.ts
    e2e/dashboard-filtering.spec.ts
    e2e/responsive.spec.ts
    package.json
  </files>
  <action>
1. Install Playwright as a dev dependency:
```bash
npm install --save-dev @playwright/test
npx playwright install chromium
```

2. Add test scripts to package.json:
- `"test:e2e": "playwright test"`
- `"test:e2e:ui": "playwright test --ui"`

3. Create playwright.config.ts at project root:
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'mobile',
      use: { ...devices['iPhone 14'] },
    },
  ],
});
```

4. Create e2e/tags.spec.ts — Tag management E2E tests:
- Test: Navigate to /tags page and verify it loads (h1 "Tags & Groups" visible)
- Test: Create a new tag — fill name input, select color, click create button, verify new tag Badge appears
- Test: Delete a tag — click delete button on a tag, confirm dialog, verify tag removed
- Test: Assign tag to truck — go to Trucks tab, open Popover on a truck row, check a tag checkbox, verify Badge appears on the truck row
- Test: Unassign tag from truck — uncheck a tag checkbox, verify Badge removed
- Test: Switch between Trucks and Drivers tabs — verify tab content changes
- Test: Empty state — when no tags exist, verify "No tags created yet" message appears
- Use `page.goto('/tags')` and standard Playwright locators (getByRole, getByText, getByTestId)
- Handle Clerk auth: use `page.goto('/tags')` — if auth redirect occurs, either:
  a) Set up a Clerk test user with `clerk.testing` helpers, OR
  b) Use `storageState` to persist authenticated session across tests
  c) SIMPLEST: use Clerk's test mode by setting `CLERK_TESTING_TOKEN` env var and `page.addInitScript` to bypass auth in test environment
- IMPORTANT: If Clerk auth blocks E2E tests, add a comment noting that tests require a running dev server with a seeded test user, and document the setup steps

5. Create e2e/dashboard-filtering.spec.ts — Dashboard tag filtering E2E tests:
- Test: Navigate to /safety and verify TagFilter dropdown renders
- Test: Select a tag from TagFilter dropdown, verify URL updates with `?tagId=` parameter
- Test: Select "All Vehicles" from dropdown, verify tagId param is removed from URL
- Test: Same tests for /fuel page
- Test: Same tests for /live-map page
- Test: Verify loading skeleton appears briefly when navigating between filtered/unfiltered views (check for Skeleton component visibility)

6. Create e2e/responsive.spec.ts — Mobile responsive E2E tests:
- Test (mobile project): Navigate to /safety at iPhone 14 viewport (390x844), verify:
  - No horizontal scrollbar: `await expect(page.locator('body')).toHaveCSS('overflow-x', 'visible')` or check `document.documentElement.scrollWidth <= document.documentElement.clientWidth`
  - Charts are stacked vertically (single column layout)
  - Sidebar is not visible as fixed sidebar (it should be in drawer/Sheet mode)
- Test (mobile project): Navigate to /fuel at mobile viewport, verify same constraints
- Test (mobile project): Navigate to /live-map at mobile viewport, verify map fills viewport
- Test (mobile project): Navigate to /tags at mobile viewport, verify tag management UI is usable
- Test (mobile project): Open sidebar on mobile by clicking hamburger/trigger, verify Sheet overlay appears
- Test (desktop project): Navigate to /safety at desktop viewport, verify multi-column grid layout
  </action>
  <verify>
Run `npx playwright test --reporter=list` to verify all tests pass (or document which tests need a running authenticated dev server). Verify playwright.config.ts is valid. Verify test files are syntactically correct with `npx tsc --noEmit`.
  </verify>
  <done>
Playwright is installed and configured with chromium and mobile projects. E2E test suites cover tag management CRUD, dashboard filtering by tags across all 3 dashboards, and mobile responsive behavior at iPhone 14 viewport. Test scripts added to package.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile responsive fixes, bundle verification, and final polish</name>
  <files>
    src/components/safety/safety-score-card.tsx
    src/components/safety/event-distribution-chart.tsx
    src/components/safety/safety-trend-chart.tsx
    src/components/safety/driver-leaderboard.tsx
    src/components/safety/threshold-config.tsx
    src/components/fuel/fuel-summary-card.tsx
    src/components/fuel/mpg-trend-chart.tsx
    src/components/fuel/emissions-card.tsx
    src/components/fuel/idle-time-card.tsx
    src/components/fuel/fuel-leaderboard.tsx
    src/components/vehicle/vehicle-detail-sheet.tsx
    next.config.ts
  </files>
  <action>
The shadcn sidebar component already handles mobile automatically at <768px — the Sidebar renders inside a Sheet (off-canvas drawer) via SidebarProvider + useIsMobile() hook. This is built into src/components/ui/sidebar.tsx. No changes needed to sidebar or owner-shell.

Audit and fix each component for mobile responsiveness:

**Safety dashboard components:**

1. src/components/safety/safety-score-card.tsx:
- Verify Card content doesn't overflow on narrow screens
- If any stat values are in a horizontal flex row, ensure they use `flex-wrap` or stack: `flex flex-col sm:flex-row`

2. src/components/safety/event-distribution-chart.tsx:
- If internal layout uses multi-column grid, verify it has `grid-cols-1 md:grid-cols-2` (stack on mobile)
- Verify ChartContainer has `min-h-[300px]` for Recharts (prevent 0px collapse)

3. src/components/safety/safety-trend-chart.tsx:
- Verify ChartContainer has min-h-[300px] and chart uses ResponsiveContainer or w-full
- If chart labels overflow on mobile, consider reducing `min-h` on mobile: add className `min-h-[250px] sm:min-h-[300px]` to ChartContainer

4. src/components/safety/driver-leaderboard.tsx:
- If table-based: wrap the table in `<div className="overflow-x-auto">` to enable horizontal scroll on narrow screens
- If table has many columns, consider hiding lower-priority columns on mobile: `hidden sm:table-cell` for less critical columns

5. src/components/safety/threshold-config.tsx:
- If form inputs are in a multi-column grid, ensure it's `grid-cols-1 sm:grid-cols-2` or similar
- If it uses a Dialog/Sheet for config, verify it works at mobile widths

**Fuel dashboard components:**

6. src/components/fuel/fuel-summary-card.tsx:
- If stat metrics are in a horizontal row, add `flex-wrap` or use `grid grid-cols-2 gap-4` for mobile-safe wrapping

7. src/components/fuel/mpg-trend-chart.tsx:
- Same as safety-trend-chart: verify min-h and responsiveness of Recharts container

8. src/components/fuel/emissions-card.tsx:
- If it contains a table/list of truck emissions, wrap in `overflow-x-auto`

9. src/components/fuel/idle-time-card.tsx:
- Verify BarChart container has proper sizing, min-h-[300px]
- If summary stats below chart are in a row, ensure `flex-wrap` or stacking

10. src/components/fuel/fuel-leaderboard.tsx:
- Wrap table in `overflow-x-auto` if not already present
- Consider `hidden sm:table-cell` for lower-priority columns on mobile

**Map components:**

11. src/components/vehicle/vehicle-detail-sheet.tsx (if this file exists):
- If Sheet uses a fixed width like `w-[400px]`, change to `w-full sm:w-[400px] sm:max-w-[400px]`
- Verify Sheet side="right" works on mobile (shadcn Sheet is full-screen on mobile by default, which is correct)

**Key principle:** Only make changes where actual responsive issues exist. Read each file first, then apply fixes. The existing `grid-cols-1 lg:grid-cols-N` layouts on pages already stack on mobile. Focus on:
- Tables that overflow horizontally (add overflow-x-auto wrapper)
- Fixed widths that should be responsive (change w-[Npx] to w-full sm:w-[Npx])
- Flex rows that should wrap on mobile (add flex-wrap)
- Text/numbers that might truncate

**Bundle size verification:**

12. Run the production build and check bundle sizes:
```bash
npx next build
```
After the build completes, Next.js outputs a route-by-route size analysis with "First Load JS" column. The success criteria requires first-load JS under 500KB for ALL routes.

13. Check the build output carefully:
- Look at the "First Load JS shared by all" value (framework + shared chunks)
- Look at each route's individual First Load JS value
- Record ALL values for the summary

14. If any route exceeds 500KB first-load JS, apply these optimizations in priority order:

**Priority 1: Verify dynamic imports are in place**
Check that Leaflet is behind dynamic import boundary. Leaflet + react-leaflet should ONLY appear in src/components/maps/ files, which are behind `dynamic(() => import(...), { ssr: false })`. If any other file imports them directly, wrap in dynamic import.

**Priority 2: Check for @turf/turf in app code**
Turf.js (~500KB) must only be in prisma/seeds/ (seed scripts), NOT in src/ app code. If found in app code, extract the specific function needed and use a lighter alternative.

**Priority 3: Dynamic import chart components if needed**
If safety or fuel routes exceed budget, dynamically import the chart components:
```tsx
import dynamic from 'next/dynamic';
const SafetyScoreCard = dynamic(() => import('@/components/safety/safety-score-card').then(m => ({ default: m.SafetyScoreCard })));
```

**Priority 4: Install @next/bundle-analyzer for detailed analysis (only if needed)**
```bash
npm install --save-dev @next/bundle-analyzer
```
Then wrap next.config.ts with bundleAnalyzer.

15. After any optimizations, re-run `npx next build` and verify ALL routes show First Load JS < 500KB.

16. If bundle is already under 500KB without changes (likely since charts are client components behind page boundaries and Leaflet is already dynamically imported), document this finding.

17. Record final bundle sizes in the summary.

18. Re-run Playwright tests after responsive fixes to confirm they pass:
```bash
npx playwright test
```
  </action>
  <verify>
Run `npm run build` to verify all changes compile. Run `npx playwright test` to verify E2E tests pass after responsive fixes. `npx next build` output shows all routes under 500KB first-load JS.
  </verify>
  <done>
All safety and fuel chart components have mobile-responsive layouts. Tables scroll horizontally, flex rows wrap, charts maintain minimum heights. Production build verified under 500KB first-load JS. Playwright E2E tests pass for all test suites.
  </done>
</task>

</tasks>

<verification>
- Playwright E2E tests pass for tag CRUD, dashboard filtering, and responsive behavior
- All chart components render correctly at 375px viewport width (no horizontal overflow)
- Sidebar renders as drawer on mobile viewports (<768px)
- All chart grids stack to single column on mobile
- Live map fills available viewport on mobile
- `npx next build` shows all routes under 500KB first-load JS
- No horizontal scroll on any dashboard at 375px width
</verification>

<success_criteria>
Playwright E2E test suites pass for tag management, dashboard filtering, and mobile responsiveness. All dashboards are responsive on mobile devices with stacking layouts, horizontal scrolling tables, and drawer sidebar. First-load JavaScript bundle is verified under 500KB across all routes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-tags-groups-polish/15-03-SUMMARY.md`
</output>
</output>
