---
phase: 49-tenant-self-onboarding-onboarding-ux-activation-tracking
plan: 03
type: execute
wave: 2
depends_on:
  - 49-01
  - 49-02
files_modified:
  - apps/web/src/app/(owner)/carrier/dashboard/page.tsx
  - apps/web/src/app/(owner)/trucks/page.tsx
  - apps/web/src/app/(owner)/drivers/page.tsx
  - apps/web/src/app/(owner)/loads/page.tsx
  - apps/web/src/app/(owner)/crm/page.tsx
  - apps/web/src/app/(owner)/actions/trucks.ts
  - apps/web/src/app/(owner)/actions/drivers.ts
  - apps/web/src/app/(owner)/actions/dashboard.ts
  - apps/web/src/components/trucks/truck-list.tsx
  - apps/web/src/components/drivers/driver-list.tsx
  - apps/web/src/components/loads/load-list.tsx
  - apps/web/src/components/crm/customer-list.tsx
autonomous: true

must_haves:
  truths:
    - "SampleDataBanner appears at the top of /carrier/dashboard, trucks list, drivers list, loads list, and CRM list pages when sampleDataSeeded=true AND at least one isSample row exists in the fetched data"
    - "Banner is dismissible per-session via sessionStorage key 'sample-banner-dismissed-{tenantId}'"
    - "SAMPLE pill appears inline in each row where isSample=true in trucks, drivers, loads, and customers list components"
    - "Dashboard KPI activeDrivers count excludes isSample=true users (adds isSample:false filter to db.user.count)"
    - "Dashboard KPI activeLoads count excludes isSample=true loads (adds isSample:false filter to db.load.count)"
    - "Trucks list count for page header excludes isSample=true trucks"
    - "Drivers list count excludes isSample=true drivers"
    - "List rows for all four entity types still SHOW isSample records (just with SAMPLE pill — not filtered out)"
  artifacts:
    - path: "apps/web/src/app/(owner)/carrier/dashboard/page.tsx"
      provides: "Dashboard page with SampleDataBanner"
      contains: "SampleDataBanner"
    - path: "apps/web/src/app/(owner)/trucks/page.tsx"
      provides: "Trucks page with SampleDataBanner + isSample=false count"
      contains: "SampleDataBanner"
    - path: "apps/web/src/app/(owner)/drivers/page.tsx"
      provides: "Drivers page with SampleDataBanner + isSample=false count"
      contains: "SampleDataBanner"
    - path: "apps/web/src/app/(owner)/loads/page.tsx"
      provides: "Loads page with SampleDataBanner + isSample=false total count"
      contains: "SampleDataBanner"
    - path: "apps/web/src/app/(owner)/crm/page.tsx"
      provides: "CRM page with SampleDataBanner + isSample=false customer count"
      contains: "SampleDataBanner"
    - path: "apps/web/src/components/trucks/truck-list.tsx"
      provides: "TruckList with SamplePill on isSample rows"
      contains: "SamplePill"
    - path: "apps/web/src/components/drivers/driver-list.tsx"
      provides: "DriverList with SamplePill on isSample rows"
      contains: "SamplePill"
    - path: "apps/web/src/components/loads/load-list.tsx"
      provides: "LoadList with SamplePill on isSample rows"
      contains: "SamplePill"
    - path: "apps/web/src/components/crm/customer-list.tsx"
      provides: "CustomerList with SamplePill on isSample rows"
      contains: "SamplePill"
  key_links:
    - from: "apps/web/src/app/(owner)/carrier/dashboard/page.tsx"
      to: "apps/web/src/components/onboarding/sample-data-banner.tsx"
      via: "import SampleDataBanner, rendered with tenantId"
      pattern: "SampleDataBanner"
    - from: "apps/web/src/app/(owner)/actions/dashboard.ts"
      to: "apps/web/prisma/schema.prisma User"
      via: "db.user.count({ where: { isSample: false } })"
      pattern: "isSample.*false"
    - from: "apps/web/src/components/trucks/truck-list.tsx"
      to: "apps/web/src/components/onboarding/sample-pill.tsx"
      via: "import SamplePill, rendered when row.isSample === true"
      pattern: "isSample.*SamplePill"
---

<objective>
Wire the SampleDataBanner and SamplePill into all five owner pages, add isSample=false filters to dashboard KPI counts and list page header counts, and propagate isSample field through list component props so SAMPLE pills render.

Purpose: New tenants see sample data clearly labeled and get accurate KPI numbers that reflect only their real data.
Output: All 5 pages show sample banner when appropriate; 4 list components show SAMPLE pill on sample rows; dashboard KPIs and list counts exclude sample records.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-tenant-self-onboarding-onboarding-ux-activation-tracking/49-CONTEXT.md
@.planning/phases/49-tenant-self-onboarding-onboarding-ux-activation-tracking/49-RESEARCH.md
@.planning/phases/49-tenant-self-onboarding-onboarding-ux-activation-tracking/49-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: isSample=false filters for dashboard KPIs and list action counts</name>
  <files>
    apps/web/src/app/(owner)/actions/dashboard.ts
    apps/web/src/app/(owner)/actions/trucks.ts
    apps/web/src/app/(owner)/actions/drivers.ts
  </files>
  <action>
**dashboard.ts — _fetchDashboardMetrics:**

Locate the `activeDriversCount` query (around line 341):
```ts
db.user.count({
  where: { role: 'DRIVER', isActive: true },
})
```
Add `isSample: false` to the where clause:
```ts
db.user.count({
  where: { role: 'DRIVER', isActive: true, isSample: false },
})
```

Locate the `activeLoadsCount` query (around line 377):
```ts
db.load.count({
  where: { status: { in: ['DISPATCHED', 'PICKED_UP', 'IN_TRANSIT'] } },
})
```
Add `isSample: false`:
```ts
db.load.count({
  where: { status: { in: ['DISPATCHED', 'PICKED_UP', 'IN_TRANSIT'] }, isSample: false },
})
```

Do NOT change lateLoadsCount, unpaid/overdue invoice queries, revenue per mile, or route counts — those are financial/compliance figures that don't involve isSample.

---

**trucks.ts — listTrucks action:**

Find the `listTrucks` server action (grep for `export async function listTrucks`). It likely calls `prisma.truck.findMany(...)`. The list itself should still return ALL trucks including isSample ones (so the SAMPLE pill can render). Do NOT add isSample filter to findMany.

However, if `listTrucks` also returns a count or the Trucks page derives counts from the returned array length, that count will be inaccurate. The Trucks page (`trucks/page.tsx`) renders the list via `listTrucks()` and does not show a separate count in its header. If the trucks page uses `trucks.length` anywhere as a count stat, change that specific count to `trucks.filter(t => !t.isSample).length`. Otherwise no change needed to trucks.ts — the list is shown in full.

Ensure `listTrucks` selects `isSample` in its return: add `isSample: true` to the select/include if it's not already there. If `listTrucks` returns full truck objects (no select), isSample is automatically included.

---

**drivers.ts — listDrivers action:**

Find `export async function listDrivers`. It likely calls `prisma.user.findMany(...)` with `role: 'DRIVER'`. The list should still return ALL drivers including isSample ones. Ensure `isSample` field is included in the returned data (add it to select if a select clause exists; if full object is returned it's already included).

If a separate count is derived (a standalone `prisma.user.count(...)` inside listDrivers), add `isSample: false` to that specific count query only.

Make sure the User type returned by listDrivers has isSample field available for the DriverList component.
  </action>
  <verify>
Run `npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -E "(dashboard|trucks|drivers)\.ts"` — no errors.

Run `grep -n "isSample" apps/web/src/app/(owner)/actions/dashboard.ts` — should show at least 2 matches (user.count and load.count).
  </verify>
  <done>dashboard.ts activeDriversCount and activeLoadsCount both have isSample:false filter. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add SamplePill to list components + SampleDataBanner to pages</name>
  <files>
    apps/web/src/components/trucks/truck-list.tsx
    apps/web/src/components/drivers/driver-list.tsx
    apps/web/src/components/loads/load-list.tsx
    apps/web/src/components/crm/customer-list.tsx
    apps/web/src/app/(owner)/carrier/dashboard/page.tsx
    apps/web/src/app/(owner)/trucks/page.tsx
    apps/web/src/app/(owner)/drivers/page.tsx
    apps/web/src/app/(owner)/loads/page.tsx
    apps/web/src/app/(owner)/crm/page.tsx
  </files>
  <action>
**Step A: Add SamplePill to the 4 list components**

For each list component, add `isSample` to the row/item interface and render `<SamplePill />` next to the primary identifier when `isSample === true`.

**truck-list.tsx:**
- Import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
- `TruckWithRelations` type likely comes from `@/lib/trucks/compute-truck-status`. Check if `isSample` is in that type. If yes, use it directly. If not, the Prisma-returned truck object has it — pass it through.
- In the table column for 'Make' (the first column), update the cell renderer to append `{info.row.original.isSample && <SamplePill />}` after the make text.

**driver-list.tsx:**
- Import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
- The `User` type from `@/generated/prisma/client` includes `isSample` (confirmed in schema). The `DriverListProps.drivers: User[]` already has it.
- In the table/list row where the driver's name is rendered, add `{driver.isSample && <SamplePill />}` after the name.

**load-list.tsx:**
- Import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
- Add `isSample?: boolean` to the `LoadItem` interface.
- In each load row's heading (where `loadNumber` is displayed), add `{load.isSample && <SamplePill />}` after the load number.

**customer-list.tsx:**
- Import: `import { SamplePill } from '@/components/onboarding/sample-pill';`
- Add `isSample?: boolean` to the `Customer` interface.
- In the customer card where `companyName` is displayed, add `{customer.isSample && <SamplePill />}` after the company name.

---

**Step B: Add SampleDataBanner to the 5 pages**

For each page, the pattern is:
1. Read `sampleDataSeeded` from the Tenant record (bypass_rls for dashboard page; getTenantPrisma for list pages)
2. Detect `hasSampleRecords` by checking if any fetched row has `isSample === true`
3. If `sampleDataSeeded && hasSampleRecords`, render `<SampleDataBanner tenantId={tenantId} />` at the top of the page JSX (before the header/title section)

**carrier/dashboard/page.tsx:**
- This is a server component with `session.tenantId` available.
- Add a query (after existing session check) to get `sampleDataSeeded` from Tenant:
  ```ts
  const tenant = await prisma.$transaction(async (tx) => {
    await tx.$executeRaw`SELECT set_config('app.bypass_rls', 'on', TRUE)`;
    return tx.tenant.findUnique({ where: { id: orgId }, select: { sampleDataSeeded: true } });
  }, TX_OPTIONS);
  ```
- Import `prisma, TX_OPTIONS` if not already imported.
- For `hasSampleRecords`, do a quick count check (bypass_rls):
  ```ts
  const sampleCount = tenant?.sampleDataSeeded
    ? await prisma.$transaction(async (tx) => {
        await tx.$executeRaw`SELECT set_config('app.bypass_rls', 'on', TRUE)`;
        return tx.truck.count({ where: { tenantId: orgId, isSample: true } });
      }, TX_OPTIONS).catch(() => 0)
    : 0;
  ```
- Render `{tenant?.sampleDataSeeded && sampleCount > 0 && <SampleDataBanner tenantId={orgId} />}` at the top of the returned JSX, above the header `<div>`.
- Import `SampleDataBanner` from `@/components/onboarding/sample-data-banner`.

**trucks/page.tsx, drivers/page.tsx, loads/page.tsx, crm/page.tsx:**

Each of these pages already fetches data via `listTrucks()`, `listDrivers()`, inline `prisma.xxx.findMany()` etc. The pages already have the fetched rows in scope.

For these pages:
- Detect `hasSampleRecords = fetchedRows.some(r => r.isSample)` — use the data already fetched (no extra query needed).
- Read `sampleDataSeeded` from Tenant via `getTenantPrisma()` (already available):
  ```ts
  const prismaClient = await getTenantPrisma();
  const tenant = await prismaClient.tenant.findFirst({ select: { sampleDataSeeded: true } });
  ```
  Note: `getTenantPrisma()` returns a tenant-scoped client so `findFirst` on Tenant scoped to this tenant will work (it's the only row in scope). If `Tenant` is not accessible via the tenant-scoped client, use the bypass_rls pattern instead.
- `tenantId` for the banner: get via `requireTenantId()` or from the session.
- Render `<SampleDataBanner tenantId={tenantId} />` at the top of the page (before the `<div className="space-y-6">` wrapper, or as the first child inside it).

**loads/page.tsx** — `hasSampleRecords` = `loads.some(l => l.isSample)`. Also update the `totalCount` stat: change `prisma.load.count({ where: { archivedAt: null } })` to two counts — keep the existing for display total if desired, or add a separate `realCount` = `prisma.load.count({ where: { archivedAt: null, isSample: false } })` for the header stat.

**crm/page.tsx** — `hasSampleRecords` = `customers.some(c => c.isSample)`. The `totalCount` and `activeCount` stats currently don't exclude isSample — add `isSample: false` to those counts to show real numbers in the stat cards.

**Important:** Do NOT add `isSample=false` to the `findMany` queries that populate the list — sample rows must still appear (with SAMPLE pill).

For pages where `getTenantPrisma()` is a `Promise`, remember to `await` it before calling methods on it.
  </action>
  <verify>
Run `npx tsc --noEmit -p apps/web/tsconfig.json` — no errors across all 9 modified files.

Run `grep -rn "SampleDataBanner" apps/web/src/app/(owner)/` — should appear in 5 page files.

Run `grep -rn "SamplePill" apps/web/src/components/` — should appear in 4 list component files.
  </verify>
  <done>All 4 list components import and conditionally render SamplePill. All 5 pages import and conditionally render SampleDataBanner with tenantId prop. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/web/tsconfig.json` — no errors
2. `grep -rn "SampleDataBanner" apps/web/src/app/(owner)/` — 5 matches (dashboard, trucks, drivers, loads, crm pages)
3. `grep -rn "SamplePill" apps/web/src/components/` — 4 matches (truck-list, driver-list, load-list, customer-list)
4. `grep "isSample: false" apps/web/src/app/(owner)/actions/dashboard.ts` — 2 matches (user.count, load.count)
5. `grep "isSample" apps/web/src/components/loads/load-list.tsx` — shows isSample in LoadItem interface and SamplePill render
6. Banner is never fetching extra data for pages that already have the rows in scope (hasSampleRecords = rows.some(...))
</verification>

<success_criteria>
- SampleDataBanner renders on all 5 owner pages when sampleDataSeeded=true and isSample records exist, dismisses per-session via sessionStorage
- SAMPLE pill appears next to entity names in all 4 list components for isSample=true rows
- Dashboard activeDrivers and activeLoads KPIs count only non-sample records
- List pages show accurate (non-sample) counts in their header stats
- Sample records remain visible in all lists (not filtered out from display)
- TypeScript compiles clean across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/49-tenant-self-onboarding-onboarding-ux-activation-tracking/49-03-SUMMARY.md`
</output>
