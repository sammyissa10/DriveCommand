---
phase: 37-polish-performance
plan: "07"
type: execute
wave: 3
depends_on: ["05"]
files_modified:
  - apps/mobile/components/owner/CreateLoadSheet.tsx
  - apps/mobile/app/(driver)/incidents/new.tsx
  - apps/mobile/components/driver/DocumentUploadSheet.tsx
autonomous: true

must_haves:
  truths:
    - "Validation triggers on submit only — no onBlur or onChange validation errors"
    - "Every required field shows a red border (border-red-500) when its error state is set"
    - "Every field error message uses text-red-500 text-xs (not text-red-400)"
    - "API errors surface as a toast notification, not replacing field-level errors"
    - "CreateLoadSheet, CreateIncidentSheet (incidents/new.tsx), and DocumentUploadSheet all follow the same error pattern"
    - "Login banner error and HOS/fleet forms are untouched — their current patterns are intentionally different"
  artifacts:
    - path: "apps/mobile/components/owner/CreateLoadSheet.tsx"
      provides: "Standardized submit-only validation — border-red-500 + text-red-500 inline errors"
      contains: "text-red-500"
    - path: "apps/mobile/app/(driver)/incidents/new.tsx"
      provides: "Description TextInput gets error-state border — border-red-500 when errors.description set"
      contains: "border-red-500"
    - path: "apps/mobile/components/driver/DocumentUploadSheet.tsx"
      provides: "File field error border migrated from inline style to NativeWind border-red-500 className"
      contains: "border-red-500"
  key_links:
    - from: "apps/mobile/components/owner/CreateLoadSheet.tsx handleSubmit"
      to: "field errors display"
      via: "setErrors(newErrors) on submit → each field reads errors.fieldName for border + text"
      pattern: "errors\\.\\w+ \\? 'border-red-500'"
    - from: "apps/mobile/app/(driver)/incidents/new.tsx handleSubmit"
      to: "description TextInput border"
      via: "errors.description drives className border color: border-red-500 vs border-slate-600"
      pattern: "errors\\.description.*border-red-500"
    - from: "apps/mobile/components/driver/DocumentUploadSheet.tsx"
      to: "file picker border"
      via: "errors.file drives className: border-red-500 vs border-slate-600 (replaces inline style borderColor)"
      pattern: "errors\\.file.*border-red-500"
---

<objective>
Standardize form validation UI across the three forms that have partial or inconsistent implementation. The locked pattern is: submit-only validation, red border (border-red-500) on invalid fields, inline error text (text-red-500 text-xs), API errors as toast.

Purpose: CreateLoadSheet uses text-red-400 (should be text-red-500). incidents/new.tsx description TextInput has no error-state border change. DocumentUploadSheet file field uses an inline style={{ borderColor }} instead of NativeWind className. This plan makes all three consistent with the locked convention.

Out of scope (intentional exceptions per user decisions):
- login.tsx — single banner error is correct for a two-field auth form
- hos.tsx — notes field is optional, status picker has no text inputs requiring validation
- fleet.tsx compose — empty-send is blocked by canSend guard, no field errors needed
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-polish-performance/37-CONTEXT.md
@.planning/phases/37-polish-performance/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize CreateLoadSheet validation to text-red-500 and ensure all required fields have error-state borders</name>
  <files>apps/mobile/components/owner/CreateLoadSheet.tsx</files>
  <action>
    CreateLoadSheet already has submit-only validation with an `errors` state object and per-field error messages. Two issues to fix:

    **Issue 1 — text-red-400 → text-red-500:**
    The required-field asterisks and inline error Text components use `text-red-400`. The locked convention is `text-red-500`. Do a targeted find-and-replace:
    - `className="text-red-400"` on required-field asterisk Text spans → `className="text-red-500"`
    - `className="text-red-400 text-xs mt-1"` on error message Text → `className="text-red-500 text-xs mt-1"`

    Only change text-red-400 usages that are part of the validation pattern (asterisks and error messages). Do not change any other color values.

    **Issue 2 — Verify all required fields have error-state border:**
    The form has these required fields: customerId (customer picker), origin (TextInput), destination (TextInput), pickupDate (date picker), and rate (TextInput). Read the current implementation and confirm each field's border changes to border-red-500 when its error is set.

    Current pattern for text inputs (should look like):
    ```tsx
    className={`bg-slate-800 border rounded-lg px-3 py-2.5 text-white text-sm ${
      errors.fieldName ? 'border-red-500' : 'border-slate-600'
    }`}
    ```

    For picker/selector rows (customerId, pickupDate) that are Pressable-triggered, the outer container View should have the red border:
    ```tsx
    className={`bg-slate-800 border rounded-lg px-3 py-2.5 flex-row items-center justify-between ${
      errors.fieldName ? 'border-red-500' : 'border-slate-600'
    }`}
    ```

    If any required field is missing the error-state border class, add it. Do not add validation to optional fields (deliveryDate, notes, etc.).

    **Issue 3 — API error toast:**
    The mutation's onError handler should show a toast. Check if it already does. If the onError currently calls `Alert.alert(...)` or `setErrors(...)` with an API-level message, change it to:
    ```tsx
    onError: (err) => {
      Toast.show({
        type: 'error',
        text1: 'Failed to create load',
        text2: err instanceof Error ? err.message : 'Please try again',
      })
    }
    ```
    If it already uses Toast, leave it alone.

    Do not change the validation logic (which fields are required, what messages are shown) — only the styling and error surface.
  </action>
  <verify>grep -n "text-red-400" apps/mobile/components/owner/CreateLoadSheet.tsx | wc -l</verify>
  <done>Zero text-red-400 usages remain in CreateLoadSheet.tsx. All required fields have error-state border-red-500 class. API errors go to Toast. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add error-state border to incidents/new.tsx description field and fix DocumentUploadSheet file border to NativeWind className</name>
  <files>
    apps/mobile/app/(driver)/incidents/new.tsx
    apps/mobile/components/driver/DocumentUploadSheet.tsx
  </files>
  <action>
    **incidents/new.tsx — Add error-state border to description TextInput:**

    The description TextInput currently has a hardcoded `className="bg-slate-800 border border-slate-600 rounded-xl px-4 py-3 text-white text-base"`. It does not change color when `errors.description` is set. Fix by making the border conditional:

    ```tsx
    <TextInput
      value={description}
      onChangeText={setDescription}
      multiline
      numberOfLines={4}
      maxLength={500}
      placeholder="Describe the incident..."
      placeholderTextColor="#64748b"
      className={`bg-slate-800 border rounded-xl px-4 py-3 text-white text-base ${
        errors.description ? 'border-red-500' : 'border-slate-600'
      }`}
      style={{ minHeight: 100, textAlignVertical: 'top' }}
    />
    ```

    The existing error message Text below the input already uses `text-red-500 text-xs` — leave it unchanged.

    Also verify the category and severity selector rows (the Pressable-triggered pickers) have error-state border changes when `errors.category` or `errors.severity` are set. These are not TextInputs but selector rows — they should follow the same pattern as the description fix above. If they already have conditional border classes, leave them. If missing, add the same `errors.category ? 'border-red-500' : 'border-slate-600'` conditional.

    **DocumentUploadSheet.tsx — Migrate file field border from inline style to NativeWind className:**

    The file picker container View currently uses:
    ```tsx
    style={{
      borderColor: errors.file ? '#ef4444' : '#334155',
      // ... other style props
    }}
    ```

    Replace the borderColor inline style with a NativeWind className conditional. Find the View that wraps the file picker area (the one with `borderColor: errors.file ? '#ef4444' : '#334155'`) and change it to use className:

    ```tsx
    className={`border rounded-xl overflow-hidden ${
      errors.file ? 'border-red-500' : 'border-slate-700'
    }`}
    ```

    Move any remaining non-border inline style props (padding, backgroundColor, etc.) to className equivalents if they are static. Keep only truly dynamic runtime values as style props.

    Also check the docName and expiryDate TextInput fields in DocumentUploadSheet — if they have a hardcoded border that does not react to their error state, apply the same `errors.docName ? 'border-red-500' : 'border-slate-600'` className conditional. The error message Text components already use `text-red-500 text-xs` — leave those unchanged.

    Run tsc after each file.
  </action>
  <verify>grep -n "border-red-500\|border-slate-600\|border-slate-700" apps/mobile/app/\(driver\)/incidents/new.tsx apps/mobile/components/driver/DocumentUploadSheet.tsx | grep -v "node_modules"</verify>
  <done>description TextInput in incidents/new.tsx shows border-red-500 when errors.description is set. DocumentUploadSheet file field border uses NativeWind className (not inline style borderColor). All error Text components use text-red-500. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- grep -rn "text-red-400" apps/mobile/components/owner/CreateLoadSheet.tsx — should return 0 matches
- grep -n "errors\.description.*border\|border.*errors\.description" apps/mobile/app/\(driver\)/incidents/new.tsx — should show conditional border class
- grep -n "borderColor.*errors\.file" apps/mobile/components/driver/DocumentUploadSheet.tsx — should return 0 matches (migrated to className)
- npx tsc --noEmit --project apps/mobile/tsconfig.json — no errors in touched files
</verification>

<success_criteria>
All three forms follow the locked pattern: submit-only validation, border-red-500 on error fields, text-red-500 text-xs error messages, API errors as Toast. Zero inline borderColor style props for error states. Zero text-red-400 in validation contexts. TypeScript clean. No behavior changes — only styling consistency.
</success_criteria>

<output>
After completion, create `.planning/phases/37-polish-performance/37-07-SUMMARY.md`
</output>
