---
phase: 19-multi-stop-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/migrations/20260226000003_add_route_stops/migration.sql
  - prisma/schema.prisma
  - src/lib/validations/route.schemas.ts
  - src/app/(owner)/actions/routes.ts
autonomous: true

must_haves:
  truths:
    - "RouteStop table exists in PostgreSQL with RLS tenant isolation"
    - "Prisma client can create, read, update, delete RouteStop records"
    - "Creating a route with stops persists stops in correct position order"
    - "Updating a route replaces its stops atomically (delete old + create new)"
    - "Deleting a route cascades to delete its stops"
    - "getRoute returns stops ordered by position"
  artifacts:
    - path: "prisma/migrations/20260226000003_add_route_stops/migration.sql"
      provides: "RouteStop table, enums, indexes, RLS policies"
      contains: "tenant_isolation_policy"
    - path: "prisma/schema.prisma"
      provides: "RouteStop model, RouteStopType enum, RouteStopStatus enum"
      contains: "model RouteStop"
    - path: "src/lib/validations/route.schemas.ts"
      provides: "routeStopSchema Zod validation"
      contains: "routeStopSchema"
    - path: "src/app/(owner)/actions/routes.ts"
      provides: "Stop CRUD in createRoute/updateRoute, stops included in getRoute"
      contains: "stops_${i}_address"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260226000003_add_route_stops/migration.sql"
      via: "schema mirrors migration SQL"
      pattern: "model RouteStop"
    - from: "src/app/(owner)/actions/routes.ts"
      to: "prisma/schema.prisma"
      via: "prisma.routeStop.create"
      pattern: "stops.*create"
---

<objective>
Create the RouteStop data model with migration SQL, Prisma schema, Zod validation, and server action integration for creating/updating routes with stops.

Purpose: Foundation for multi-stop routes -- all subsequent UI and driver integration depends on this model existing and being wirable via server actions.
Output: RouteStop table in PostgreSQL with RLS, Prisma types generated, server actions accepting stop data via flat FormData keys.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-multi-stop-routes/19-RESEARCH.md
@prisma/schema.prisma
@prisma/migrations/20260226000002_add_rls_missing_tables/migration.sql
@src/app/(owner)/actions/routes.ts
@src/lib/validations/route.schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL + Prisma schema for RouteStop model</name>
  <files>
    prisma/migrations/20260226000003_add_route_stops/migration.sql
    prisma/schema.prisma
  </files>
  <action>
1. Create migration file `prisma/migrations/20260226000003_add_route_stops/migration.sql` with:
   - `RouteStopType` enum (PICKUP, DELIVERY) using DO/EXCEPTION block for idempotency
   - `RouteStopStatus` enum (PENDING, ARRIVED, DEPARTED) using DO/EXCEPTION block
   - `CREATE TABLE "RouteStop"` with columns:
     - id UUID PK (gen_random_uuid())
     - routeId UUID NOT NULL
     - tenantId UUID NOT NULL
     - position INTEGER NOT NULL
     - type "RouteStopType" NOT NULL
     - address TEXT NOT NULL
     - lat DECIMAL(10,8) nullable
     - lng DECIMAL(11,8) nullable
     - scheduledAt TIMESTAMPTZ nullable
     - arrivedAt TIMESTAMPTZ nullable
     - departedAt TIMESTAMPTZ nullable
     - notes TEXT nullable
     - status "RouteStopStatus" NOT NULL DEFAULT 'PENDING'
     - geofenceHit BOOLEAN NOT NULL DEFAULT false
     - createdAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
     - updatedAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
   - Indexes: routeId, tenantId, (routeId + position composite), status
   - FK to Route(id) ON DELETE CASCADE ON UPDATE CASCADE
   - FK to Tenant(id) ON DELETE RESTRICT ON UPDATE CASCADE
   - RLS: ENABLE + FORCE ROW LEVEL SECURITY
   - tenant_isolation_policy (USING tenantId = current_tenant_id(), WITH CHECK same)
   - bypass_rls_policy (USING current_setting('app.bypass_rls', TRUE)::text = 'on')

2. Update `prisma/schema.prisma`:
   - Add `enum RouteStopType { PICKUP DELIVERY }`
   - Add `enum RouteStopStatus { PENDING ARRIVED DEPARTED }`
   - Add `model RouteStop` with all fields matching migration, including:
     - `@db.Decimal(10, 8)` for lat, `@db.Decimal(11, 8)` for lng
     - `@db.Timestamptz` on all DateTime fields
     - `@default(dbgenerated("gen_random_uuid()"))` on id
     - `route Route @relation(fields: [routeId], references: [id], onDelete: Cascade)`
     - `tenant Tenant @relation(fields: [tenantId], references: [id])`
     - `@@index([routeId])`, `@@index([tenantId])`, `@@index([routeId, position])`, `@@index([status])`
   - Add `stops RouteStop[]` relation on Route model
   - Add `routeStops RouteStop[]` relation on Tenant model

3. Run `npx prisma generate` to regenerate client with new types.
  </action>
  <verify>
    Run `npx prisma generate` -- must succeed with no errors. Run `npx prisma validate` -- must output "valid". Verify migration SQL file exists and contains both RLS policies (grep for "tenant_isolation_policy" and "bypass_rls_policy").
  </verify>
  <done>
    RouteStop model in schema.prisma with all fields, enums, relations, and indexes. Migration SQL with RLS policies. Prisma client regenerated with RouteStop types available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Zod validation + server actions for stop CRUD</name>
  <files>
    src/lib/validations/route.schemas.ts
    src/app/(owner)/actions/routes.ts
  </files>
  <action>
1. In `src/lib/validations/route.schemas.ts`, add:
   ```typescript
   export const routeStopSchema = z.object({
     type: z.enum(['PICKUP', 'DELIVERY']),
     address: z.string().min(1, 'Address is required').max(500),
     scheduledAt: z.string().optional(),
     notes: z.string().max(1000).optional(),
   });
   ```
   Export the type: `export type RouteStopInput = z.infer<typeof routeStopSchema>;`

2. In `src/app/(owner)/actions/routes.ts`, modify `createRoute`:
   - After existing field parsing, add stop parsing loop:
     ```typescript
     const stops: Array<{ type: string; address: string; scheduledAt?: string; notes?: string }> = [];
     let i = 0;
     while (formData.get(`stops_${i}_address`)) {
       stops.push({
         type: (formData.get(`stops_${i}_type`) as string) || 'PICKUP',
         address: formData.get(`stops_${i}_address`) as string,
         scheduledAt: (formData.get(`stops_${i}_scheduledAt`) as string) || undefined,
         notes: (formData.get(`stops_${i}_notes`) as string) || undefined,
       });
       i++;
     }
     ```
   - Validate each stop with `routeStopSchema.safeParse()` in a loop. If any fail, return `{ error: 'Invalid stop data at position N' }`.
   - In the `prisma.route.create` call, add nested `stops: { create: [...] }` with each stop mapped to `{ tenantId, position: idx + 1, type, address, scheduledAt: scheduledAt ? new Date(scheduledAt) : null, notes: notes || null }`.

3. In `updateRoute`:
   - Parse stops the same way as createRoute (flat FormData keys).
   - Check if `formData.get('stops_0_address')` exists (i.e., stops were submitted). If stops are present in FormData:
     - Inside the existing try block, before the route update, delete all existing stops: `await prisma.routeStop.deleteMany({ where: { routeId: id } })`.
     - After route update, create new stops: `await prisma.routeStop.createMany({ data: stops.map((s, idx) => ({ routeId: id, tenantId, position: idx + 1, type: s.type as any, address: s.address, scheduledAt: s.scheduledAt ? new Date(s.scheduledAt) : null, notes: s.notes || null })) })`.
   - If `formData.has('stops_submitted')` is true but no stops exist (all were removed), just deleteMany existing stops. Use a hidden field `stops_submitted=true` in the form to distinguish "no stops submitted" from "stops section not present in form".

4. In `getRoute`:
   - Add `stops: { orderBy: { position: 'asc' } }` to the include block.

5. In `listRoutes`:
   - Add `_count: { select: { stops: true } }` to include block so the route list can show stop count.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- must pass with no type errors. Verify `getRoute` include has `stops` by reading the file. Verify `createRoute` and `updateRoute` both parse `stops_N_address` fields.
  </verify>
  <done>
    Server actions accept stop data via flat FormData keys (stops_0_address, stops_0_type, etc.), persist them as RouteStop records with correct position ordering, and return stops in getRoute. Zod schema validates stop input.
  </done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes
2. `npx prisma generate` succeeds
3. `npx tsc --noEmit` passes with no errors
4. Migration SQL contains both `tenant_isolation_policy` and `bypass_rls_policy`
5. `getRoute` includes stops ordered by position
6. `createRoute` and `updateRoute` parse flat FormData stop fields
</verification>

<success_criteria>
- RouteStop table defined in migration SQL with all columns, indexes, FKs, and RLS policies
- Prisma schema has RouteStop model with correct relations to Route and Tenant
- Prisma client regenerated successfully
- Server actions create/update/read stops alongside routes
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/19-multi-stop-routes/19-01-SUMMARY.md`
</output>
