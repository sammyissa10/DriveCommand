---
phase: 37.7-driver-map-navigation
plan: "06"
type: execute
wave: 5
depends_on: ["37.7-04", "37.7-05"]
files_modified:
  - apps/mobile/docs/architecture.md
autonomous: false

must_haves:
  truths:
    - "architecture.md has a Navigation Strategy section describing the hybrid map + nav app approach"
    - "architecture.md documents the new 5+More tab bar structure"
    - "architecture.md documents the /api/geocoding/directions endpoint"
    - "A new EAS dev build is triggered and the app loads on the Android emulator without crashing"
    - "Map tab renders without a red screen or native module error"
    - "Driver live location dot appears on the map"
    - "Tapping 'Start Navigation' (with an active load) opens the navigation app"
  artifacts:
    - path: "apps/mobile/docs/architecture.md"
      provides: "Updated documentation with Navigation Strategy, tab bar structure, directions API"
      contains: "Navigation Strategy"
  key_links: []
---

<objective>
Update architecture documentation and verify the full phase works end-to-end via a new EAS dev build on the Android emulator. This plan documents what was built and confirms the native Mapbox module loads correctly.

Purpose: @rnmapbox/maps requires a native dev build — the existing build from before this phase will crash. This checkpoint ensures the driver triggers the build before the orchestrator marks the phase complete.
Output: Updated architecture.md; confirmed working dev build with map rendering.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37.7-driver-map-navigation/37.7-CONTEXT.md
@.planning/phases/37.7-driver-map-navigation/37.7-04-SUMMARY.md
@.planning/phases/37.7-driver-map-navigation/37.7-05-SUMMARY.md
@apps/mobile/docs/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update architecture.md with Navigation Strategy section</name>
  <files>apps/mobile/docs/architecture.md</files>
  <action>
    Add the following sections to apps/mobile/docs/architecture.md. Append or insert near the navigation/routing section — do not remove any existing content.

    **Section to add: Driver Tab Bar Structure (Phase 37.7)**

    ```markdown
    ## Driver Tab Bar Structure (Phase 37.7+)

    The driver tab bar follows a 5+More pattern:

    | Tab | Route | Icon | Notes |
    |-----|-------|------|-------|
    | Dashboard | `(driver)/index` | House | GPSStatusDot overlay |
    | Loads | `(driver)/loads` | Truck | Load list + detail |
    | Map | `(driver)/map` | Navigation | Mapbox full-screen map |
    | Messages | `(driver)/messages` | MessageSquare | Unread badge |
    | More | `(driver)/more` | Grid2X2 | Overflow menu |

    Hidden routes (href: null): `hos`, `documents`, `incidents`
    These are accessible via the More menu (`(driver)/more/index.tsx`).
    ```

    **Section to add: Navigation Strategy**

    ```markdown
    ## Navigation Strategy (Phase 37.7+)

    DriveCommand uses a hybrid approach for driver navigation:

    **In-app map:** `@rnmapbox/maps` renders a full-screen Mapbox Standard map with:
    - Driver live location via `UserLocation` component (expo-location foreground permissions)
    - OSRM road polyline drawn via `ShapeSource` + `LineLayer`
    - Numbered `MarkerView` stop markers; active/next stop highlighted in brand blue

    **Turn-by-turn:** Handled by external navigation apps via deep links:
    - Apple Maps: `maps://?daddr={lat},{lng}` (iOS default)
    - Google Maps: `https://www.google.com/maps/dir/?api=1&destination={lat},{lng}&travelmode=driving`
    - Waze: `waze://?ll={lat},{lng}&navigate=yes`

    Driver preference stored in MMKV under key `nav_app_preference`. Android is locked to Google Maps.
    iOS preference configurable in More → Navigation App.

    **One-tap Start Route:** When driver taps "Start Route" on a load (`DISPATCHED → EN_ROUTE`),
    `StatusUpdateButton` simultaneously:
    1. Calls `router.navigate('/(driver)/map')` to switch to the Map tab
    2. Calls `openNavigation(lat, lng)` (`lib/navigation.ts`) to open the preferred nav app

    Target is always the first stop with `status !== 'DEPARTED'` (next unvisited stop).
    ```

    **Section to add: Directions API**

    ```markdown
    ## Directions API

    `POST /api/geocoding/directions`

    Proxies OSRM (public endpoint, no API key) for mobile clients. CORS and mixed-content
    restrictions on mobile networks require all OSRM calls to go through this backend proxy.

    Request: `{ stops: Array<{ lat: number; lng: number }> }` (minimum 2 stops)
    Response: `{ polyline: [number, number][] | null, distanceMiles: number | null, durationSeconds: number | null }`

    Polyline is in GeoJSON `[lng, lat]` order (Mapbox ShapeSource native format).

    Auth: accepts mobile Bearer token or web session cookie.
    Rate limit: `geocodingLimiter` with key `dir:{userId}`.

    Implementation: `apps/web/src/app/api/geocoding/directions/route.ts`
    OSRM utility: `apps/web/src/lib/geo/osrm.ts` → `getOSRMDirections()`
    api-client: `driverApi.getDirections(token, stops)` → `DirectionsResult`
    Mobile hook: `apps/mobile/hooks/useDriverDirections.ts`
    ```

    **Mapbox token note:**

    ```markdown
    ## Mapbox Configuration

    Token: `EXPO_PUBLIC_MAPBOX_TOKEN` (dev `.env`) or `Constants.expoConfig.extra.mapboxToken` (builds)
    Initialized at: `apps/mobile/app/(driver)/_layout.tsx` module level via `Mapbox.setAccessToken()`
    Must be called before any `MapView` renders — module-level call guarantees this.

    Before Phase 38 EAS production build: replace `MAPBOX_PUBLIC_TOKEN_PLACEHOLDER` in `app.json`
    with the real public token, or configure via EAS Secrets + `app.config.js`.
    ```
  </action>
  <verify>
    - grep "Navigation Strategy" apps/mobile/docs/architecture.md returns a match
    - grep "Directions API" apps/mobile/docs/architecture.md returns a match
    - grep "5+More" apps/mobile/docs/architecture.md returns a match
    - grep "nav_app_preference" apps/mobile/docs/architecture.md returns a match
  </verify>
  <done>architecture.md updated with Navigation Strategy, Directions API, Driver Tab Bar Structure, and Mapbox Configuration sections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Build new dev client and verify end-to-end</name>
  <files>apps/mobile (built via Expo CLI)</files>
  <action>Trigger EAS/Expo dev build and verify end-to-end functionality per how-to-verify steps below.</action>
  <verify>See how-to-verify steps: map renders, tabs correct, nav app opens, TypeScript clean.</verify>
  <done>All verification steps pass; user types "approved".</done>
  <what-built>
    Complete Phase 37.7: Driver Map + Navigation Screen.

    - @rnmapbox/maps installed, react-native-maps removed
    - Mapbox.setAccessToken wired in driver layout
    - POST /api/geocoding/directions endpoint live
    - RouteStop type updated with lat/lng
    - driverApi.getDirections added
    - Driver tab bar restructured: Dashboard · Loads · Map · Messages · More
    - More menu created with HOS, Documents, Incidents, Navigation Settings
    - map.tsx full implementation: UserLocation, OSRM polyline, stop markers, info panel
    - useDriverDirections hook
    - One-tap "Start Route" → Map tab switch + navigation app open
    - nav-settings.tsx for navigation app preference
    - architecture.md updated
  </what-built>
  <how-to-verify>
    **Step 1 — Build new dev client (required — native module changed):**

    From the apps/mobile directory:
    ```
    cd apps/mobile
    npx expo run:android
    ```
    (or `eas build --profile development --platform android` for a full EAS build)

    Wait for the build to complete and install on the Android emulator.

    **Step 2 — Verify Map tab:**
    1. Log in as a driver
    2. Tap the "Map" tab (3rd tab, Navigation icon)
    3. Expected: Full-screen Mapbox map renders (not a red screen, not "Module not found")
    4. Expected: A location permission prompt appears or the live location dot appears on the map
    5. Expected: "No active load" chip shows at the top when no load is active
    6. Expected: "Start Navigation" button is greyed out

    **Step 3 — Verify More menu:**
    1. Tap the "More" tab (5th tab, Grid2X2 icon)
    2. Expected: Scrollable menu with sections: DRIVING (Hours of Service), DOCUMENTS (My Documents), SAFETY (Incidents), SETTINGS (Navigation App)
    3. Tap "Navigation App" — expected: preference screen opens

    **Step 4 — Verify HOS and Documents are accessible via More (not tab bar):**
    1. Confirm HOS and Documents are NOT visible as tab bar tabs
    2. Tap "Hours of Service" in the More menu — expected: HOS screen opens

    **Step 5 — Verify active load flow (requires an active load in DISPATCHED status):**
    1. Navigate to Loads and find a load in DISPATCHED status (or create one in the web app)
    2. Tap "Start Route"
    3. Expected: App simultaneously switches to Map tab AND opens the navigation app (Google Maps on Android)
    4. Expected: Map tab shows polyline if stop coordinates are populated in the DB

    **Step 6 — TypeScript verification:**
    ```
    cd apps/web && npx tsc --noEmit
    cd apps/mobile && npx tsc --noEmit
    ```
    Both must pass with no errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues found (red screens, missing tabs, nav app not opening, TypeScript errors)</resume-signal>
</task>

</tasks>

<verification>
architecture.md has Navigation Strategy, Directions API, Driver Tab Bar Structure, Mapbox Configuration.
New dev build succeeds and map renders on Android emulator.
All 5 driver tabs visible; HOS and Documents accessible via More.
Start Route triggers Map tab + navigation app.
TypeScript clean on both apps/web and apps/mobile.
</verification>

<success_criteria>
Phase 37.7 fully verified: Mapbox map renders, polyline draws for active loads, one-tap navigation works, More menu is functional, documentation updated.
</success_criteria>

<output>
After completion, create `.planning/phases/37.7-driver-map-navigation/37.7-06-SUMMARY.md`
</output>
