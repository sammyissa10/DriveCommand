---
phase: 37.7-driver-map-navigation
plan: "01"
subsystem: mobile/maps
tags: [mapbox, react-native, maps, ios, navigation]
dependency_graph:
  requires: []
  provides:
    - "@rnmapbox/maps installed and configured"
    - "Mapbox token initialized at module level before any MapView renders"
    - "VehicleMarker components migrated to Mapbox"
    - "iOS URL schemes for navigation app deep links"
  affects:
    - apps/mobile/app/(driver)/_layout.tsx
    - apps/mobile/components/driver/VehicleMarker.tsx
    - apps/mobile/components/owner/VehicleMarker.tsx
    - apps/mobile/app.json
tech_stack:
  added:
    - "@rnmapbox/maps ^10.3.0 — native Mapbox SDK for React Native"
  removed:
    - "react-native-maps 1.27.2 — replaced by @rnmapbox/maps"
    - "react-native-map-clustering ^4.0.0 — depended on react-native-maps, unused"
  patterns:
    - "Mapbox.setAccessToken at module level for pre-render initialization"
    - "MarkerView with [longitude, latitude] GeoJSON coordinate order"
    - "Pressable wrapper for touch events on MarkerView children"
key_files:
  created:
    - apps/mobile/components/driver/VehicleMarker.tsx
  modified:
    - apps/mobile/package.json
    - apps/mobile/components/owner/VehicleMarker.tsx
    - apps/mobile/app/(driver)/_layout.tsx
    - apps/mobile/app.json
decisions:
  - "Removed react-native-map-clustering alongside react-native-maps since it peer-depends on it and has no screen-level importers"
  - "Used Pressable wrapper instead of MarkerView onSelected for touch events — onSelected does not exist on MarkerView type"
  - "Module-level Mapbox.setAccessToken (not inside useEffect) ensures token is set before any child screen mounts a MapView"
metrics:
  duration: "198s"
  completed_date: "2026-04-03"
  tasks_completed: 2
  files_modified: 5
---

# Phase 37.7 Plan 01: Mapbox Foundation Summary

**One-liner:** Replaced react-native-maps with @rnmapbox/maps, migrated VehicleMarker components to MarkerView, and wired Mapbox token initialization at driver layout module level.

## Tasks Completed

| # | Task | Commit | Key Files |
|---|------|--------|-----------|
| 1 | Install @rnmapbox/maps, remove react-native-maps | 58c62ce | apps/mobile/package.json, package-lock.json |
| 2 | Migrate VehicleMarker.tsx, init Mapbox token, iOS URL schemes | 7deaf4c | components/driver/VehicleMarker.tsx (new), components/owner/VehicleMarker.tsx, app/(driver)/_layout.tsx, app.json |

## What Was Built

- **@rnmapbox/maps ^10.3.0** installed via `expo install` — native Mapbox SDK, replaces react-native-maps
- **react-native-maps** and **react-native-map-clustering** removed from package.json — no screen-level importers existed
- **components/driver/VehicleMarker.tsx** — new Mapbox-compatible marker with colored circle (32px, #0ea5e9 default) and optional text label
- **Mapbox.setAccessToken** at module level in `(driver)/_layout.tsx` — reads from EXPO_PUBLIC_MAPBOX_TOKEN env var or app.json extra.mapboxToken fallback
- **app.json LSApplicationQueriesSchemes** — `["comgooglemaps", "waze", "maps"]` added to ios.infoPlist for navigation app deep link support

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Migrated owner/VehicleMarker.tsx to @rnmapbox/maps**
- **Found during:** Task 2
- **Issue:** components/owner/VehicleMarker.tsx imported from react-native-maps which was just removed. Left as-is it would crash any screen that uses it.
- **Fix:** Rewrote to use MarkerView from @rnmapbox/maps with Pressable wrapper for touch handling (onSelected does not exist on MarkerView type — used Pressable instead)
- **Files modified:** apps/mobile/components/owner/VehicleMarker.tsx
- **Commit:** 7deaf4c

## TypeScript Status

No new TypeScript errors introduced. Two pre-existing errors in `(driver)/_layout.tsx` (tabBarActiveLabelStyle) and `_layout.tsx` (arg count) were present before this plan and unrelated to Mapbox.

## Notes

- A new dev build is required before testing any map screen — @rnmapbox/maps requires native code linking. The existing dev build will crash on MapView. Rebuild handled in Plan 06.
- Mapbox token initialization happens at module level so it fires before any MapView renders in child screens.

## Self-Check: PASSED

All created/modified files verified on disk. Both commits confirmed in git log.
