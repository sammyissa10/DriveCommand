---
phase: 37.2-owner-route-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/api/mobile/owner/routes/route.ts
  - apps/web/src/app/api/mobile/owner/routes/[id]/route.ts
  - packages/api-client/src/owner.ts
  - apps/mobile/app/(owner)/routes/index.tsx
  - apps/mobile/app/(owner)/routes/[id].tsx
autonomous: true

must_haves:
  truths:
    - "Owner can see routes grouped by status (Active first, then Scheduled, then Completed) in collapsible sections"
    - "Each route card shows route name, status badge, stop count, and assigned truck"
    - "Owner can create a route from the mobile app via POST API"
    - "Route detail header shows assigned driver, truck, and route status"
    - "Owner can reassign truck on active routes via edit sheet"
  artifacts:
    - path: "apps/web/src/app/api/mobile/owner/routes/route.ts"
      provides: "POST handler for creating routes with stops"
      exports: ["GET", "POST"]
    - path: "apps/web/src/app/api/mobile/owner/routes/[id]/route.ts"
      provides: "PATCH handler accepting optional truckId with tenant validation"
      exports: ["GET", "PATCH"]
    - path: "packages/api-client/src/owner.ts"
      provides: "createRoute method and CreateRoutePayload type"
      contains: "createRoute"
    - path: "apps/mobile/app/(owner)/routes/index.tsx"
      provides: "Collapsible status-grouped route list with updated cards"
      min_lines: 100
    - path: "apps/mobile/app/(owner)/routes/[id].tsx"
      provides: "Route detail with truck display in info card and truck picker in edit sheet"
      contains: "truck"
  key_links:
    - from: "apps/mobile/app/(owner)/routes/index.tsx"
      to: "packages/api-client/src/owner.ts"
      via: "ownerApi.getRoutes"
      pattern: "ownerApi\\.getRoutes"
    - from: "packages/api-client/src/owner.ts"
      to: "/api/mobile/owner/routes"
      via: "apiRequest POST"
      pattern: "createRoute.*POST"
    - from: "apps/mobile/app/(owner)/routes/[id].tsx"
      to: "packages/api-client/src/owner.ts"
      via: "ownerApi.updateRoute with truckId"
      pattern: "truckId"
---

<objective>
Add route creation API, redesign route list to collapsible status sections, and add truck display + truck editing to route detail.

Purpose: Enable owners to create routes from mobile, see routes organized by status (Active/Scheduled/Completed) with collapsible sections, and view/reassign trucks on route detail per locked decisions.
Output: POST /api/mobile/owner/routes endpoint, updated PATCH handler with truckId support, updated api-client, redesigned route list screen, route detail with truck info + truck picker in edit sheet.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/app/api/mobile/owner/routes/route.ts
@apps/web/src/app/api/mobile/owner/routes/[id]/route.ts
@packages/api-client/src/owner.ts
@apps/mobile/app/(owner)/routes/index.tsx
@apps/mobile/app/(owner)/routes/[id].tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST route creation API + extend PATCH with truckId + api-client methods</name>
  <files>
    apps/web/src/app/api/mobile/owner/routes/route.ts
    apps/web/src/app/api/mobile/owner/routes/[id]/route.ts
    packages/api-client/src/owner.ts
  </files>
  <action>
1. In `apps/web/src/app/api/mobile/owner/routes/route.ts`, add a POST handler below the existing GET:
   - Validate Bearer token, require OWNER role, apply rate limit (same pattern as GET above it).
   - Accept JSON body: `{ name: string, driverId: string, truckId: string, scheduledDate?: string, stops: Array<{ address: string, type: 'PICKUP' | 'DELIVERY', scheduledAt?: string, notes?: string }> }`.
   - Validate: name required (non-empty), driverId required, truckId required, stops must have at least 2 entries.
   - Inside prisma.$transaction with bypass_rls:
     a. Verify driver belongs to tenant: `tx.driver.findFirst({ where: { id: driverId, tenantId } })` — return 400 if not found.
     b. Verify truck belongs to tenant: `tx.truck.findFirst({ where: { id: truckId, tenantId } })` — return 400 if not found.
     c. Derive origin from first stop address, destination from last stop address.
     d. Create route: `tx.route.create({ data: { tenantId, name, driverId, truckId, status: 'PLANNED', origin, destination, scheduledDate: scheduledDate ? new Date(scheduledDate) : new Date(), stops: { create: stops.map((s, i) => ({ tenantId, position: i + 1, type: s.type, address: s.address, status: 'PENDING', scheduledAt: s.scheduledAt ? new Date(s.scheduledAt) : null, notes: s.notes ?? null })) } } })`.
     e. Return 201 with `{ route: { id, name, status } }`.
   - Wrap in try/catch, log errors with logger.error, return 500 on failure.

2. In `apps/web/src/app/api/mobile/owner/routes/[id]/route.ts`, extend the PATCH handler to accept optional `truckId`:
   - Update the body type to include `truckId?: string`: `{ name?: string; status?: string; scheduledDate?: string; driverId?: string; truckId?: string }`.
   - Add truck validation after the existing driverId validation block (around line 160): if `body.truckId` is provided, verify truck belongs to tenant: `const truck = await tx.truck.findFirst({ where: { id: body.truckId, tenantId }, select: { id: true } }); if (!truck) throw new Error('Truck not found in this tenant');`.
   - Add truckId to the updateData block: `if (body.truckId !== undefined) updateData.truckId = body.truckId;`.
   - The existing include already returns truck data (make/model/licensePlate), so no changes needed to the response shape.

3. In `packages/api-client/src/owner.ts`:
   - Add `CreateRoutePayload` interface: `{ name: string, driverId: string, truckId: string, scheduledDate?: string, stops: Array<{ address: string, type: 'PICKUP' | 'DELIVERY', scheduledAt?: string, notes?: string }> }`.
   - Add `createRoute` method to ownerApi: POST to `/api/mobile/owner/routes` with the payload.
   - Update the `updateRoute` method's payload type to include optional `truckId`: the payload should be `{ name?: string; status?: string; driverId?: string; truckId?: string }`.
   - Add `getTruckOptions` method: GET `/api/mobile/owner/trucks` returning truck list (reuse existing getTrucks if it exists, or alias it).
  </action>
  <verify>Run `cd apps/web && npx tsc --noEmit` — no new type errors. Visually inspect the POST handler has proper auth/validation/error handling matching existing GET pattern. Verify PATCH handler now accepts truckId with tenant validation.</verify>
  <done>POST /api/mobile/owner/routes accepts name + driverId + truckId + stops, validates inputs, creates Route + RouteStops in transaction, returns 201. PATCH /api/mobile/owner/routes/[id] accepts optional truckId with tenant validation. api-client has createRoute method, updated updateRoute payload type, and getTruckOptions method.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign route list + add truck display and truck picker to route detail</name>
  <files>
    apps/mobile/app/(owner)/routes/index.tsx
    apps/mobile/app/(owner)/routes/[id].tsx
  </files>
  <action>
**Part A — Route list redesign (index.tsx):**

Replace the tab-based filter with client-side grouping into collapsible sections. Per user decision: routes grouped by status (Active, Scheduled, Completed), collapsible sections, Active first.

1. Remove the TabType, STATUS_TABS, activeTab state, and the horizontal ScrollView tab bar.
2. Change the query to always fetch status='all': `ownerApi.getRoutes(token!, 'all')` with queryKey `['owner-routes']`.
3. Add a `CollapsibleSection` inline component:
   - Props: `title: string`, `count: number`, `defaultOpen: boolean`, `children: React.ReactNode`.
   - State: `isOpen` (default from prop).
   - Renders: a Pressable header row with title, count badge, and animated chevron (ChevronDown rotating 180deg when open). When open, renders children below.
   - Use `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut)` on toggle for smooth expand/collapse. Import LayoutAnimation from react-native.
4. Update the OwnerRouteCard to also show:
   - Truck info: add a line below driver showing truck make/model/licensePlate (from route.truck). Format as `{make} {model} ({licensePlate})`.
   - Stop count is already shown in the existing card (route._count.stops). Keep it.
5. In the main render, after data loads, group routes:
   ```
   const activeRoutes = (data ?? []).filter(r => r.status === 'IN_PROGRESS')
   const scheduledRoutes = (data ?? []).filter(r => r.status === 'PLANNED')
   const completedRoutes = (data ?? []).filter(r => r.status === 'COMPLETED')
   ```
6. Replace the FlashList with a ScrollView containing 3 CollapsibleSections:
   - "Active" (activeRoutes.length) — defaultOpen: true
   - "Scheduled" (scheduledRoutes.length) — defaultOpen: true
   - "Completed" (completedRoutes.length) — defaultOpen: false (collapsed by default since these are historical)
   Each section maps its routes through OwnerRouteCard.
7. Add a FAB (floating action button) at the bottom-right corner for "Create Route" — a circular brand-colored button with a Plus icon. On press: `haptic.medium(); router.push('/(owner)/routes/new')`. Position absolute, bottom: 24, right: 24, size 56x56, rounded-full, shadow.
8. Keep the pull-to-refresh via RefreshControl on the ScrollView.
9. Update empty state: if all groups empty, show single EmptyState. If only some groups empty, those sections just don't render.
10. Keep skeleton loading state and error state as-is.

**Part B — Route detail truck display ([id].tsx):**

Add truck info to the Route Info card. The API already returns `route.truck` with `{ id, make, model, licensePlate }`.

1. In the Route Info card (around line 426-433), add a new InfoField row for Truck after the existing Driver row:
   ```
   <View className="w-1/2">
     <InfoField label="Truck" value={route.truck ? `${route.truck.make} ${route.truck.model} (${route.truck.licensePlate})` : 'Unassigned'} />
   </View>
   ```
   This satisfies the locked decision: "Route detail header: assigned driver, truck, route status (minimal)". Driver and status are already shown; this adds truck.

**Part C — Route detail truck picker in EditRouteSheet ([id].tsx):**

Add a truck picker to EditRouteSheet so owners can reassign trucks on active routes (locked decision: "Full edit allowed on active routes").

1. Update `EditRouteSheetProps` interface to include `currentTruckId: string | null` and `currentTruckLabel: string`.
2. In EditRouteSheet component, add state: `const [truckPickerVisible, setTruckPickerVisible] = useState(false)`, `const [selectedTruckId, setSelectedTruckId] = useState<string | null>(currentTruckId)`, `const [selectedTruckLabel, setSelectedTruckLabel] = useState(currentTruckLabel)`.
3. Add a TanStack Query for trucks (same pattern as existing driver query): `useQuery({ queryKey: ['owner-trucks'], queryFn: () => ownerApi.getTrucks(token), enabled: !!token && truckPickerVisible, staleTime: 60_000 })`.
4. Add a Truck section in the EditRouteSheet below the Driver section (before the Save button): label "Truck", Pressable showing `selectedTruckLabel` with ChevronDown, opens truck picker on press.
5. Create inline `TruckPickerSheet` component (or add it below `DriverPickerSheet`): a BottomSheet listing trucks from the query. Each row shows `{make} {model} — {licensePlate}` with a status dot. On select: `setSelectedTruckId(truckId); setSelectedTruckLabel(label); setTruckPickerVisible(false)`.
6. Update `handleSave` to include truckId in payload: `if (selectedTruckId && selectedTruckId !== currentTruckId) payload.truckId = selectedTruckId`.
7. Update the `onSave` prop type to include `truckId?: string` in the payload: `onSave: (payload: { name?: string; status?: string; driverId?: string; truckId?: string }) => void`.
8. Update the `updateRoute` mutation in the main screen to match the updated payload type.
9. Pass `currentTruckId={route.truck?.id ?? null}` and `currentTruckLabel={route.truck ? \`${route.truck.make} ${route.truck.model} (${route.truck.licensePlate})\` : 'No Truck'}` to EditRouteSheet.
  </action>
  <verify>Run `cd apps/mobile && npx tsc --noEmit` — no type errors. The route list screen compiles and shows collapsible sections with Active first, includes FAB for route creation. Route detail shows truck in info card. EditRouteSheet has truck picker.</verify>
  <done>Route list shows collapsible status sections (Active/Scheduled/Completed) instead of tabs. Each card shows truck info. FAB navigates to route creation. Active routes appear first, Completed collapsed by default. Route detail info card shows assigned truck. EditRouteSheet includes truck picker for reassignment. PATCH API accepts truckId.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes
- `cd apps/mobile && npx tsc --noEmit` passes
- POST API handler follows same auth/rls/rate-limit pattern as existing GET
- PATCH API handler accepts optional truckId with tenant validation
- Route list no longer has tab filter, shows collapsible sections
- Route detail info card shows driver, truck, and status (per locked decision)
- EditRouteSheet has truck picker (per locked decision: full edit on active routes)
- FAB visible on route list for creating new routes
</verification>

<success_criteria>
- POST /api/mobile/owner/routes creates routes with stops, validated and tenant-scoped
- PATCH /api/mobile/owner/routes/[id] accepts optional truckId with tenant validation
- Route list redesigned with collapsible status groups (Active first)
- Route cards show truck info alongside driver/stop count
- Route detail info card displays assigned driver, truck, and route status
- EditRouteSheet includes truck picker for truck reassignment
- CreateRoute FAB navigates to /(owner)/routes/new
</success_criteria>

<output>
After completion, create `.planning/phases/37.2-owner-route-maintenance/37.2-01-SUMMARY.md`
</output>
