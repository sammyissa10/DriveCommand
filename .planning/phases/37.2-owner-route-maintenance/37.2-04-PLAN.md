---
phase: 37.2-owner-route-maintenance
plan: 04
type: execute
wave: 2
depends_on: ["37.2-02"]
files_modified:
  - apps/mobile/app/(owner)/more/maintenance.tsx
  - apps/mobile/app/(owner)/more/trucks/[id].tsx
  - apps/mobile/app/(owner)/more/trucks/index.tsx
  - apps/mobile/app/(owner)/more/index.tsx
  - apps/mobile/components/owner/ScheduleServiceSheet.tsx
  - apps/mobile/components/owner/MaintenanceServicePicker.tsx
autonomous: true

must_haves:
  truths:
    - "Owner can view all scheduled services across trucks from a top-level maintenance screen"
    - "Due Soon section at top of maintenance screen shows overdue and due-soon items"
    - "Owner can schedule a new service for a truck with due date and/or mileage"
    - "Owner can mark a scheduled service complete with actual date and odometer"
    - "Service type selection uses a predefined picker, not free text"
    - "Truck list shows warning badge on trucks with due-soon or overdue services"
    - "Truck detail shows full service history (not capped at 5) and scheduled services section"
  artifacts:
    - path: "apps/mobile/app/(owner)/more/maintenance.tsx"
      provides: "Top-level maintenance screen with Due Soon section"
      min_lines: 100
    - path: "apps/mobile/components/owner/ScheduleServiceSheet.tsx"
      provides: "Bottom sheet form for scheduling a service"
      min_lines: 50
    - path: "apps/mobile/components/owner/MaintenanceServicePicker.tsx"
      provides: "Predefined service type picker bottom sheet"
      min_lines: 40
  key_links:
    - from: "apps/mobile/app/(owner)/more/maintenance.tsx"
      to: "packages/api-client/src/owner.ts"
      via: "ownerApi.getAllScheduledServices"
      pattern: "getAllScheduledServices"
    - from: "apps/mobile/components/owner/ScheduleServiceSheet.tsx"
      to: "packages/api-client/src/owner.ts"
      via: "ownerApi.createScheduledService"
      pattern: "createScheduledService"
    - from: "apps/mobile/app/(owner)/more/trucks/[id].tsx"
      to: "packages/api-client/src/owner.ts"
      via: "ownerApi.getScheduledServices"
      pattern: "getScheduledServices"
---

<objective>
Build maintenance UI: top-level screen, scheduled service forms, truck detail enhancements, truck list badges.

Purpose: Give owners full maintenance management on mobile — schedule services, see what's due soon across the fleet, and complete services with audit trail. Replace free-text service type with predefined picker.
Output: New maintenance screen, 2 new components, enhanced truck detail and truck list.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.2-owner-route-maintenance/37.2-02-SUMMARY.md
@apps/mobile/app/(owner)/more/trucks/[id].tsx
@apps/mobile/app/(owner)/more/trucks/index.tsx
@apps/mobile/app/(owner)/more/index.tsx
@apps/mobile/constants/maintenance.ts
@packages/api-client/src/owner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MaintenanceServicePicker, ScheduleServiceSheet, and top-level maintenance screen</name>
  <files>
    apps/mobile/components/owner/MaintenanceServicePicker.tsx
    apps/mobile/components/owner/ScheduleServiceSheet.tsx
    apps/mobile/app/(owner)/more/maintenance.tsx
    apps/mobile/app/(owner)/more/index.tsx
  </files>
  <action>
1. **MaintenanceServicePicker.tsx** — a reusable bottom sheet for selecting a service type:
   - Props: `visible: boolean, onClose: () => void, onSelect: (type: string) => void, selectedType?: string`.
   - Import `MAINTENANCE_SERVICE_TYPES` from `@/constants/maintenance`.
   - Render as a BottomSheet (snapPoint="70%") with title "Service Type".
   - Inside: a ScrollView with FlashList-like layout of Pressable rows, each showing the service type name. Selected row highlighted with brand color + checkmark. On press: `haptic.light(); onSelect(type); onClose()`.
   - Use `useThemeColors()` for all styling.

2. **ScheduleServiceSheet.tsx** — bottom sheet form for scheduling a future service:
   - Props: `visible: boolean, onClose: () => void, truckId: string, token: string, onSuccess: () => void`.
   - State: serviceType (string, empty), dueDate (string, empty — YYYY-MM-DD format), dueMileage (string, empty), notes (string, empty).
   - Service type field: Pressable that opens MaintenanceServicePicker. Shows selected type or "Select service type".
   - Due Date field: TextInput with YYYY-MM-DD placeholder. Label: "Due Date (optional if mileage set)".
   - Due Mileage field: TextInput numeric. Label: "Due at Mileage (optional if date set)".
   - Notes field: TextInput multiline.
   - "Schedule Service" button. Validates: serviceType required, at least one of dueDate or dueMileage required. On submit: useMutation calling `ownerApi.createScheduledService(token, truckId, { serviceType, dueDate: dueDate || undefined, dueMileage: dueMileage ? parseInt(dueMileage) : undefined, notes: notes || undefined })`.
   - onSuccess: haptic.success(), Toast success, call props.onSuccess(), close sheet. onError: haptic.error(), Toast error.
   - Wrap in KeyboardAvoidingView. Use same inputStyle/labelStyle patterns as LogMaintenanceSheet in trucks/[id].tsx.

3. **maintenance.tsx** — top-level maintenance screen (accessible from More menu):
   - SafeAreaView + AnimatedScreen. Header: back arrow + "Maintenance" title.
   - Fetch all scheduled services: `useQuery({ queryKey: ['all-scheduled-services'], queryFn: () => ownerApi.getAllScheduledServices(token!) })`.
   - **Due Soon section** at top — card with amber/red background accent:
     - Filter services where status === 'overdue' or status === 'due_soon'.
     - If none: show a green "All Clear" message.
     - If items exist: render each as a row showing: truck name (make model licensePlate), service type, status badge (red for overdue, amber for due_soon), due date or due mileage.
     - Each row is a Pressable that navigates to the truck detail: `router.push('/(owner)/more/trucks/{truckId}')`.
   - **All Scheduled** section below — all non-completed services, grouped by truck.
     - Each truck group: truck name header, then list of scheduled services with type + due info + status badge.
   - FAB: "Schedule Service" button — but since scheduling requires a truck context, this FAB opens a truck picker first (simple BottomSheet listing trucks from `ownerApi.getTrucks`), then on selection opens ScheduleServiceSheet with that truckId.
   - Skeleton loader while loading. Pull-to-refresh.

4. **More menu update** — in `apps/mobile/app/(owner)/more/index.tsx`:
   - Add a "Maintenance" row to the FLEET section, positioned after "Trucks":
     ```
     {
       label: 'Maintenance',
       subtitle: 'Service scheduling',
       Icon: Wrench,
       iconBg: 'rgba(245,158,11,0.15)',
       iconColor: '#f59e0b',
       route: '/(owner)/more/maintenance',
     }
     ```
   - Import `Wrench` from lucide-react-native (already imported in trucks/[id].tsx, but verify it's imported here).
  </action>
  <verify>Run `cd apps/mobile && npx tsc --noEmit` — no type errors. Maintenance screen file exists. More menu has Maintenance entry. Both new components compile.</verify>
  <done>Top-level maintenance screen shows Due Soon alerts + all scheduled services across trucks. ScheduleServiceSheet allows scheduling with date/mileage. MaintenanceServicePicker provides predefined type selection. More menu has Maintenance entry.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance truck detail and truck list with maintenance features</name>
  <files>
    apps/mobile/app/(owner)/more/trucks/[id].tsx
    apps/mobile/app/(owner)/more/trucks/index.tsx
  </files>
  <action>
1. **Truck detail (`[id].tsx`) enhancements:**

   a. **Replace free-text service type with MaintenanceServicePicker in LogMaintenanceSheet:**
      - Import MaintenanceServicePicker from `@/components/owner/MaintenanceServicePicker`.
      - Replace the TextInput for serviceType with a Pressable that shows selected type or "Select service type". On press: set `pickerVisible = true`.
      - Add `const [pickerVisible, setPickerVisible] = useState(false)` state.
      - Render MaintenanceServicePicker below the BottomSheet (same nesting pattern as DriverPickerSheet in routes/[id].tsx).
      - When a type is selected, `setServiceType(type)`.

   b. **Remove the 5-item cap on service history:**
      - Change `maintenance.slice(0, 5)` to just `maintenance` — show all records.
      - If there are many records (>10), consider wrapping in a collapsible section or just letting ScrollView handle it. Keep it simple — show all, the ScrollView already handles scroll.

   c. **Add Scheduled Services section** between Service History and Record History:
      - New query: `useQuery({ queryKey: ['truck-scheduled-services', id], queryFn: () => ownerApi.getScheduledServices(token!, id!) })`.
      - Render a card titled "Scheduled Services" with Wrench icon (same style as Service History card).
      - Header right: "Schedule" link (like the "Log" link on Service History) — on press opens ScheduleServiceSheet.
      - Import ScheduleServiceSheet from `@/components/owner/ScheduleServiceSheet`.
      - List each scheduled service: service type, status badge (overdue=red, due_soon=amber, ok=green), due date and/or due mileage.
      - Each service row has a "Mark Complete" Pressable. On press:
        - Open a small confirmation BottomSheet (inline, like other sheets in this file) asking for actual service date (pre-filled today) and actual odometer (pre-filled truck.odometer).
        - On confirm: call `ownerApi.completeScheduledService(token!, id!, { serviceId, actualDate, actualOdometer })` via useMutation.
        - onSuccess: invalidate both scheduled-services and maintenance queries, haptic.success(), Toast success.
      - If no scheduled services: "No scheduled services" text.
      - Add `const [scheduleSheetVisible, setScheduleSheetVisible] = useState(false)` state.
      - onSuccess callback for ScheduleServiceSheet: invalidate `['truck-scheduled-services', id]`.

   d. **Update onRefresh** to also refetch scheduled services.

2. **Truck list (`index.tsx`) — add due-soon badge:**
   - Fetch all scheduled services once: `useQuery({ queryKey: ['all-scheduled-services'], queryFn: () => ownerApi.getAllScheduledServices(token!) })`.
   - Create a Set of truckIds that have at least one service with status 'overdue' or 'due_soon'.
   - On each truck card, if the truck's id is in the due-soon set, show a small warning indicator:
     - An amber/red dot or a small `AlertTriangle` icon (from lucide) next to the truck status badge.
     - Use amber for due_soon, red for overdue. If truck has both, show red.
   - This is a lightweight overlay — do NOT restructure the entire truck card. Just add a small indicator element.
  </action>
  <verify>Run `cd apps/mobile && npx tsc --noEmit` — no type errors. Truck detail has scheduled services section with mark-complete flow. LogMaintenanceSheet uses picker not free text. Service history no longer capped at 5. Truck list shows warning indicators.</verify>
  <done>Truck detail: full service history (no cap), predefined service type picker in LogMaintenanceSheet, scheduled services section with schedule/complete flows. Truck list: warning badge on trucks with due-soon/overdue services.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` passes
- More menu has Maintenance entry navigating to maintenance screen
- Top-level maintenance screen shows Due Soon alerts + all scheduled services
- Truck detail has Scheduled Services section with mark-complete flow
- LogMaintenanceSheet uses predefined service type picker
- Service history shows all records (not capped at 5)
- Truck list cards show warning indicator for due-soon/overdue trucks
</verification>

<success_criteria>
- Two entry points to maintenance: truck detail screen AND top-level maintenance screen (per user decision)
- Due Soon alerts surface in both: top of maintenance screen AND warning badge on truck list
- Scheduling supports date, mileage, or both triggers (per user decision)
- Completion creates MaintenanceEvent audit trail
- Service type selection is predefined picker everywhere (no free text)
- Full service history displayed on truck detail
</success_criteria>

<output>
After completion, create `.planning/phases/37.2-owner-route-maintenance/37.2-04-SUMMARY.md`
</output>
