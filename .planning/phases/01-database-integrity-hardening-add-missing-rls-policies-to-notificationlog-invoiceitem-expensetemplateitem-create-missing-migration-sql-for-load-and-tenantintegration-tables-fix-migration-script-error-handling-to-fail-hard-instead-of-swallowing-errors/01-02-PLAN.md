---
phase: 01-database-integrity-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/migrate.mjs
autonomous: true

must_haves:
  truths:
    - "A migration failure causes migrate.mjs to exit with code 1 (not 0)"
    - "The process does not log 'Starting app anyway...' and continue after a migration error"
    - "The finally block still runs (client.end() is still called) before process.exit(1)"
    - "TypeScript compilation produces no new errors after Plan 01's schema changes"
  artifacts:
    - path: "scripts/migrate.mjs"
      provides: "Migration runner that fails hard on error"
      contains: "process.exit(1)"
  key_links:
    - from: "catch block in scripts/migrate.mjs"
      to: "process.exit(1)"
      via: "direct call after console.error"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Fix the one-line bug in scripts/migrate.mjs where migration failures are swallowed, then run a TypeScript type check to confirm Plan 01's schema changes introduce no compiler errors.

Purpose: Currently if any migration fails, migrate.mjs logs the error and then continues starting the app — meaning the app boots against a broken schema and silently serves corrupted data or crashes at runtime. The fix makes deployment fail fast and visibly at the infrastructure level, so broken deploys are caught before serving traffic.

Output: Updated migrate.mjs that exits with code 1 on failure, confirmed TypeScript clean build.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/migrate.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix migrate.mjs to exit with code 1 on migration failure</name>
  <files>scripts/migrate.mjs</files>
  <action>
Read `scripts/migrate.mjs`. Find the outer catch block near lines 79-82:

```javascript
} catch (e) {
  console.error('Migration error:', e.message);
  console.log('Starting app anyway...');
} finally {
```

Replace ONLY that catch block with:

```javascript
} catch (e) {
  console.error('Migration error:', e.message);
  process.exit(1);
} finally {
```

The `finally { await client.end(); }` block that follows must remain unchanged — Node.js executes finally before process.exit(1) takes effect, so the pg client connection is still properly closed.

Do not change any other part of the file. The inner catch block (around lines 71-75) that handles per-migration failures and rethrows is correct and must stay as-is.
  </action>
  <verify>
1. `grep -n "Starting app anyway" scripts/migrate.mjs` — must return no results (line removed)
2. `grep -n "process.exit" scripts/migrate.mjs` — must show `process.exit(1)` in the outer catch block
3. `grep -n "client.end" scripts/migrate.mjs` — must still show `await client.end()` in the finally block
4. `grep -c "catch" scripts/migrate.mjs` — must return 2 (inner catch for individual migrations + outer catch)
  </verify>
  <done>
scripts/migrate.mjs outer catch block contains `process.exit(1)` instead of `console.log('Starting app anyway...')`. The finally block with `await client.end()` is unchanged. The file has exactly 2 catch blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: TypeScript type check to confirm no new errors</name>
  <files></files>
  <action>
Run the TypeScript compiler in no-emit mode to verify Plan 01's schema.prisma changes (tenantId added to InvoiceItem and ExpenseTemplateItem) do not cause type errors elsewhere in the codebase.

```bash
npx tsc --noEmit 2>&1
```

If there are errors:
- Errors referencing `InvoiceItem` or `ExpenseTemplateItem` that require the new `tenantId` field — fix the call sites to include `tenantId` when creating these records. Look for `prisma.invoiceItem.create` and `prisma.expenseTemplateItem.create` calls and add `tenantId` from the parent record's context.
- Errors in other files unrelated to this phase — document them in the SUMMARY but do not fix them (they are pre-existing).

The passing condition is: zero errors in files that this phase touched (`prisma/schema.prisma`, `scripts/migrate.mjs`), and no new errors in files that use `InvoiceItem` or `ExpenseTemplateItem` Prisma types.
  </action>
  <verify>
`npx tsc --noEmit 2>&1 | grep -E "error TS" | grep -v "node_modules"` — the output should either be empty or show only pre-existing errors unrelated to InvoiceItem/ExpenseTemplateItem. If InvoiceItem or ExpenseTemplateItem errors appear, they must be fixed before this task is done.
  </verify>
  <done>
TypeScript compilation shows no errors in files modified or affected by this phase. Any pre-existing errors in unrelated files are documented in the SUMMARY. The app can be built without type errors from the schema changes.
  </done>
</task>

</tasks>

<verification>
Full phase verification:

1. `grep "process.exit(1)" scripts/migrate.mjs` — exits 0 with match
2. `grep "Starting app anyway" scripts/migrate.mjs` — exits 1 (no match — line is gone)
3. `npx tsc --noEmit 2>&1 | grep -c "error TS"` — count is 0 or matches pre-existing baseline
4. Review `scripts/migrate.mjs` lines 78-85 manually to confirm outer catch → process.exit(1) → finally → client.end() structure is correct
</verification>

<success_criteria>
- migrate.mjs outer catch calls `process.exit(1)` — any migration failure terminates the process with a non-zero exit code
- The string "Starting app anyway" no longer exists anywhere in migrate.mjs
- `npx tsc --noEmit` produces no new errors introduced by this phase's changes
- Phase 01 is fully complete: 5 tables have RLS, 2 tables have migration SQL, migrate.mjs fails hard
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-integrity-hardening-add-missing-rls-policies-to-notificationlog-invoiceitem-expensetemplateitem-create-missing-migration-sql-for-load-and-tenantintegration-tables-fix-migration-script-error-handling-to-fail-hard-instead-of-swallowing-errors/01-02-SUMMARY.md`
</output>
