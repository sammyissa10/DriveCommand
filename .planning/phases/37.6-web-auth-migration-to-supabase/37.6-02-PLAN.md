---
phase: 37.6-web-auth-migration-to-supabase
plan: 02
type: execute
wave: 2
depends_on: ["37.6-01"]
files_modified:
  - apps/web/src/lib/auth/supabase.ts
  - apps/web/src/lib/auth/session.ts
  - apps/web/src/lib/auth/server.ts
  - apps/web/src/lib/auth/require-permission.ts
autonomous: true

must_haves:
  truths:
    - "All auth helper imports resolve to src/lib/auth/supabase.ts"
    - "All three portals (Owner, Driver, SysAdmin) load without import errors"
    - "Function signatures are unchanged -- no downstream breakage"
  artifacts:
    - path: "apps/web/src/lib/auth/supabase.ts"
      provides: "Consolidated auth helpers: getSession, requireAuth, requireRole, requireTenantId, isSystemAdmin, getCurrentUser, getRole, requirePermission"
      min_lines: 50
  key_links:
    - from: "70+ action/page files"
      to: "src/lib/auth/supabase.ts"
      via: "import { requireAuth, requireRole, ... } from '@/lib/auth/supabase'"
      pattern: "@/lib/auth/supabase"
---

<objective>
Consolidate auth helpers (session.ts + server.ts + require-permission.ts) into a single src/lib/auth/supabase.ts and update all ~70 import paths across the codebase.

Purpose: Per CONTEXT.md decision, "a single src/lib/auth/supabase.ts should replace the entire old src/lib/auth/ directory" (for server-only helpers). This eliminates the fragmented auth module structure left from the migration.

Output: Single consolidated auth module, all imports updated, old files removed.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.6-web-auth-migration-to-supabase/37.6-01-SUMMARY.md
@.planning/phases/37.6-web-auth-migration-to-supabase/37.6-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create consolidated supabase.ts and update all import paths</name>
  <files>
    apps/web/src/lib/auth/supabase.ts
    apps/web/src/lib/auth/session.ts
    apps/web/src/lib/auth/server.ts
    apps/web/src/lib/auth/require-permission.ts
  </files>
  <action>
    **Create apps/web/src/lib/auth/supabase.ts:**
    - Merge contents of `session.ts`, `server.ts`, and `require-permission.ts` into one file
    - Keep all exported functions with IDENTICAL signatures: `getSession`, `requireAuth`, `requireRole`, `requireTenantId`, `isSystemAdmin`, `getCurrentUser`, `getRole`, `requirePermission`
    - Keep all type exports (SessionData, etc.)
    - Deduplicate any shared imports (e.g., both session.ts and server.ts import from supabase/server)
    - server.ts currently imports from session.ts -- inline those calls since they are now in the same file

    **Update all ~70 import paths:**
    - Find all files importing from `@/lib/auth/session` -- change to `@/lib/auth/supabase`
    - Find all files importing from `@/lib/auth/server` -- change to `@/lib/auth/supabase`
    - Find all files importing from `@/lib/auth/require-permission` -- change to `@/lib/auth/supabase`
    - This is a mechanical find-and-replace. Function names and signatures are unchanged.
    - Use sed/grep or similar to do this efficiently across all files

    **Remove old files:**
    - Delete `session.ts`, `server.ts`, `require-permission.ts`
    - Keep `auth-context.tsx` (client component, "use client"), `guards.tsx` (client component), `mobile-auth.ts` (mobile-specific), `permissions.ts` (type-only), `roles.ts` (type-only)

    **DO NOT touch:**
    - `auth-context.tsx` -- uses "use client", cannot merge with server module
    - `guards.tsx` -- uses "use client"
    - `mobile-auth.ts` -- separate concern (mobile JWT validation)
    - `permissions.ts`, `roles.ts` -- pure type files, fine as-is
  </action>
  <verify>
    Run `npx tsc --noEmit` from apps/web -- must pass with zero errors.
    Run `grep -rn "from.*@/lib/auth/session" apps/web/src/` -- should return zero results.
    Run `grep -rn "from.*@/lib/auth/server" apps/web/src/` -- should return zero results (except mobile-auth.ts if it imports from server).
    Run `grep -rn "from.*@/lib/auth/require-permission" apps/web/src/` -- should return zero results.
    Confirm `apps/web/src/lib/auth/session.ts`, `apps/web/src/lib/auth/server.ts`, `apps/web/src/lib/auth/require-permission.ts` no longer exist.
    Confirm `apps/web/src/lib/auth/supabase.ts` exists and exports all expected functions.
  </verify>
  <done>
    Three auth helper files consolidated into supabase.ts. All ~70 import paths updated. Old files deleted. TypeScript compiles cleanly. All three portals resolve imports correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end verification across all three portals</name>
  <files></files>
  <action>
    **Build verification:**
    - Run `npx next build` from apps/web to confirm full production build succeeds
    - This catches any import errors, missing exports, or type issues across all 70+ files

    **Manual smoke test checklist (document for user):**
    - After build passes, note in SUMMARY that user should verify:
      1. Owner portal: login, dashboard loads, can view routes/loads
      2. Driver portal: login, driver dashboard loads
      3. SysAdmin portal: login, admin dashboard loads, can view tenants
      4. Create a new invitation, accept it -- new user should have app_metadata claims
  </action>
  <verify>
    `npx next build` from apps/web exits with code 0 (no errors).
  </verify>
  <done>
    Production build succeeds. All imports resolve. Phase 37.6 migration is complete: security claims in app_metadata, auth helpers consolidated, AUTH_SECRET removed, stale comments cleaned.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx next build` passes
- No imports from `@/lib/auth/session`, `@/lib/auth/server`, or `@/lib/auth/require-permission` remain
- `src/lib/auth/supabase.ts` exports all required functions
- Old files (session.ts, server.ts, require-permission.ts) deleted
</verification>

<success_criteria>
Auth helpers consolidated into single supabase.ts. All import paths updated. Production build succeeds. The auth module is clean: supabase.ts (server helpers), auth-context.tsx (client provider), guards.tsx (client guards), mobile-auth.ts (mobile JWT), permissions.ts (types), roles.ts (types).
</success_criteria>

<output>
After completion, create `.planning/phases/37.6-web-auth-migration-to-supabase/37.6-02-SUMMARY.md`
</output>
