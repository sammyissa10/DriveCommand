---
phase: 43-workflow-engine-2-execution
plan: "04"
type: execute
wave: 3
depends_on:
  - 43-03
files_modified:
  - apps/web/src/app/(owner)/checklists/page.tsx
  - apps/web/src/app/(owner)/checklists/_components/DashboardClient.tsx
  - apps/web/src/app/(owner)/checklists/instances/[id]/page.tsx
  - apps/web/src/app/(owner)/checklists/instances/[id]/_components/ChecklistDetailClient.tsx
autonomous: true

must_haves:
  truths:
    - "Active Work Board swimlanes (Needs Attention / In Progress / Completed Today) appear at the top of /checklists when instances exist"
    - "No swimlanes rendered if zero instances exist — empty state shown instead"
    - "Start Checklist button opens a modal with playbook selector + entity picker, calls instance.generate on submit"
    - "Active Checklist Detail page at /checklists/instances/[id] shows entity name, playbook name, status badge, completion percent, dispatch readiness banner"
    - "Step rows show status icon, step name, assignee, due date (red if overdue), and contextual action button"
    - "No internal names (PlaybookInstance, StepInstance) appear in JSX text — naming lint must pass"
  artifacts:
    - path: "apps/web/src/app/(owner)/checklists/page.tsx"
      provides: "Dashboard with Active Work Board swimlanes above existing Playbook grid"
    - path: "apps/web/src/app/(owner)/checklists/instances/[id]/page.tsx"
      provides: "Active Checklist Detail server component"
      contains: "ChecklistDetailClient"
    - path: "apps/web/src/app/(owner)/checklists/instances/[id]/_components/ChecklistDetailClient.tsx"
      provides: "Client component rendering step rows with contextual action buttons"
  key_links:
    - from: "apps/web/src/app/(owner)/checklists/page.tsx"
      to: "trpc.workflows.instance.list"
      via: "useQuery in DashboardClient"
      pattern: "instance.list"
    - from: "apps/web/src/app/(owner)/checklists/instances/[id]/page.tsx"
      to: "trpc.workflows.instance.get"
      via: "server-side prefetch or client query"
      pattern: "instance.get"
    - from: "ChecklistDetailClient.tsx"
      to: "trpc.workflows.stepInstance.skip"
      via: "overflow menu Skip action"
      pattern: "stepInstance.skip"
---

<objective>
Build the two web UI deliverables for Phase 43: (1) Active Work Board swimlanes added to the existing /checklists dashboard, and (2) the new Active Checklist Detail screen at /checklists/instances/[id]. Both use user-facing naming exclusively.

Purpose: Dispatchers need a visual board to see which drivers/vehicles are blocked and a detail screen to track individual step completion. The naming lint (from Phase 42) will automatically cover new files here.
Output: Updated dashboard page + new detail page with client components.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-workflow-engine-2-execution/43-RESEARCH.md
@apps/web/src/app/(owner)/checklists/page.tsx
@apps/web/src/app/(owner)/checklists/_components/DashboardClient.tsx
@docs/specs/DriveCommand_Workflow_Engine_v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Active Work Board swimlanes + Start Checklist modal to /checklists dashboard</name>
  <files>
    apps/web/src/app/(owner)/checklists/page.tsx
    apps/web/src/app/(owner)/checklists/_components/DashboardClient.tsx
  </files>
  <action>
Read `apps/web/src/app/(owner)/checklists/page.tsx` and `apps/web/src/app/(owner)/checklists/_components/DashboardClient.tsx` in full first. Understand what already exists so you add the Work Board above the existing Playbook grid without removing anything.

Also run the UI/UX Pro Max search script before implementing:
```
python .claude/skills/ui-ux-pro-max/scripts/search.py "kanban swimlane board dispatch checklist" --design-system -p "DriveCommand"
```

**Additions to make:**

**1. In DashboardClient.tsx (or create a new `WorkBoardSection` component file referenced from it):**

Add the Active Work Board above the Playbook grid. The Work Board shows three swimlane columns:
- **Needs Attention** — red left border (`border-l-4 border-red-500`) — instances where `status === 'BLOCKED'` OR `status === 'IN_PROGRESS'` with any overdue step
- **In Progress** — yellow left border (`border-l-4 border-yellow-500`) — `status === 'IN_PROGRESS'` with no overdue blockers
- **Completed Today** — green left border (`border-l-4 border-green-500`) — `status === 'COMPLETED'` where `completedAt >= today midnight UTC`

Use `useQuery(trpc.workflows.instance.list.queryOptions({ take: 20 }))` to fetch instances.

Classify each instance client-side:
```typescript
function classifyInstance(instance: InstanceWithSteps): 'attention' | 'progress' | 'completed' | null {
  const todayMidnight = new Date()
  todayMidnight.setHours(0, 0, 0, 0)
  
  if (instance.status === 'BLOCKED') return 'attention'
  if (instance.status === 'COMPLETED') {
    return instance.completedAt && new Date(instance.completedAt) >= todayMidnight ? 'completed' : null
  }
  if (instance.status === 'IN_PROGRESS') {
    const hasOverdue = instance.stepInstances.some(
      (s) => s.status !== 'COMPLETE' && s.status !== 'SKIPPED' && s.dueDate && new Date(s.dueDate) < new Date()
    )
    return hasOverdue ? 'attention' : 'progress'
  }
  return null
}
```

**Each swimlane card contains** (user-facing names only — naming lint enforced):
- Entity name (linked to entity profile — `/drivers/[id]`, `/trucks/[id]`, or `/crm/[id]`)
- Playbook name from `playbookSnapshot.name` + an icon
- Circular progress indicator showing `completionPercent`% 
- Next/overdue step label (first NOT_STARTED or overdue step name from `stepInstances`)
- A `"View Checklist"` button linking to `/checklists/instances/[id]`

**Empty state:** If no instances exist across all three buckets, render a single centered message:
```
"When you start a checklist, it'll show up here."
```
Do NOT render the three column headers in the empty state.

**2. Add "Start Checklist" button** above the Work Board section:

A full-width button (or top-right aligned) labeled `"+ Start Checklist"`. Clicking opens a Dialog (shadcn `Dialog` component) with:
- A playbook selector: use `useQuery(trpc.workflows.playbook.list.queryOptions())` (already exists from Phase 42) to list active playbooks; render as a `<select>` or ComboBox
- An entity type selector (select: Driver, Vehicle, Partner)
- An entity ID picker: when entity type changes, load the appropriate list (drivers via existing API, trucks via existing API). Use a `<select>` for simplicity.
- A `"Start Checklist"` submit button that calls `useMutation(trpc.workflows.instance.generate.mutationOptions())` with the selected values. On success, close the dialog and invalidate the instance list query to refresh the Work Board.
- Loading + error states handled inline

**User-facing names in all JSX text:**
- Never write `PlaybookInstance`, `StepInstance`, `PlaybookTrigger` in rendered text
- "Active Checklist" for instances, "Task" for step instances, "Playbook" for playbooks (user-facing per spec)

After implementing, run `cd apps/web && npm run test -- --run workflows-naming-lint` to confirm the naming lint still passes.
  </action>
  <verify>Run `cd apps/web && npm run test -- --run workflows-naming-lint` — must pass. Navigate to /checklists in the browser (or read the page source) and confirm the Work Board section exists above the Playbook grid. Run `cd apps/web && npx tsc --noEmit` to confirm no type errors.</verify>
  <done>Work Board renders three swimlane columns. Empty state renders correctly when no instances exist. Start Checklist button opens a dialog. Dialog calls instance.generate tRPC mutation on submit. Naming lint passes. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create Active Checklist Detail screen at /checklists/instances/[id]</name>
  <files>
    apps/web/src/app/(owner)/checklists/instances/[id]/page.tsx
    apps/web/src/app/(owner)/checklists/instances/[id]/_components/ChecklistDetailClient.tsx
  </files>
  <action>
Also run UI/UX Pro Max before implementing:
```
python .claude/skills/ui-ux-pro-max/scripts/search.py "checklist detail step list progress tracker dispatch" --design-system -p "DriveCommand"
```

**Create `apps/web/src/app/(owner)/checklists/instances/[id]/page.tsx`:**

A Next.js 15 App Router server component:
```typescript
import { ChecklistDetailClient } from './_components/ChecklistDetailClient'

type PageProps = { params: { id: string } }

export default async function ChecklistDetailPage({ params }: PageProps) {
  return <ChecklistDetailClient instanceId={params.id} />
}
```

**Create `apps/web/src/app/(owner)/checklists/instances/[id]/_components/ChecklistDetailClient.tsx`:**

A `'use client'` component. Fetches instance via `useQuery(trpc.workflows.instance.get.queryOptions({ id: instanceId }))`.

**Header card (spec Section 8.3):**
- Breadcrumb: `Checklists > [Playbook Name]`
- Entity name — derive from snapshot data + a link to the entity profile
- Playbook name from `instance.playbookSnapshot.name`
- Status badge (color-coded): `NOT_STARTED` (gray), `IN_PROGRESS` (blue), `BLOCKED` (red), `COMPLETED` (green)
- Completion ring: circular progress indicator — use Tailwind `conic-gradient` via inline style or a simple `<svg>` ring showing `completionPercent`%
- Dispatch readiness banner:
  - If `instance.isDispatchReady === true`: green banner `"Dispatch Ready"`  
  - If `false` and there are blocker steps not complete: red banner listing step names `"Blocked — N steps required: [step names]"`

**Phase sections (collapsible):**

Group `stepInstances` by `stepSnapshot.playbookPhase`. For each phase group, render a collapsible section (use shadcn `Collapsible` or a simple `useState` toggle):
- Header: `"Day 1 (3/5 complete)"` (never use "Phase 1" or internal names)
- Phase label mapping: `PRE_START → "Pre-Start"`, `DAY_1 → "Day 1"`, `WEEK_1 → "Week 1"`, `ONGOING → "Ongoing"`, `NONE → "General"`

**Step rows:**

Each row renders:
- Status icon: `COMPLETE` → green checkmark, `FAILED` → red X, `SKIPPED` → gray dash, `IN_PROGRESS` → yellow spinner, `NOT_STARTED` → gray circle
- Step name from `stepSnapshot.name` (user-facing — already a user-friendly name)
- Assignee: `assignedUserId` → show user name if available; otherwise fall back to `stepSnapshot.assigneeRole` displayed as readable text (DRIVER → "Driver", ADMIN → "Admin", etc.)
- Due date: formatted as "Due Jan 15" — show in red text if overdue (`dueDate < now && status not COMPLETE`)
- Result summary: truncated to 60 chars from `result.note` or `result.formData` JSON preview
- Contextual action button (right-aligned):

| Step status | Action |
|------------|--------|
| NOT_STARTED | `"View Details"` — links to instance detail (same page, scroll to step) |
| COMPLETE | `"View Result"` — opens a Sheet/Dialog showing full result JSON |
| FAILED | `"View Issue"` — red button |
| SKIPPED | Muted text `"Skipped — [reason]"` |

- Admin skip: every NOT_STARTED or IN_PROGRESS step row has an overflow menu (three-dot) with a `"Skip with Reason"` option. Clicking opens a small Dialog with a reason text input and a `"Skip"` button that calls `useMutation(trpc.workflows.stepInstance.skip.mutationOptions())`. On success, invalidate the instance query.

**Loading state:** show a skeleton while `isLoading`. **Error state:** show an error message with a "Back to Checklists" link.

**Naming rules (strictly enforced by lint):**
- Never write `"PlaybookInstance"`, `"StepInstance"`, `"PlaybookTrigger"` in any rendered text
- Use `"Active Checklist"` for instances, `"Task"` for step instances
- Phase labels use human-readable strings (see mapping above)

After writing both files, run:
```
cd apps/web && npm run test -- --run workflows-naming-lint
cd apps/web && npx tsc --noEmit
```
  </action>
  <verify>Run `cd apps/web && npm run test -- --run workflows-naming-lint` — must pass (covers instances/[id] automatically). Run `cd apps/web && npx tsc --noEmit`. Navigate to /checklists/instances/[any-id] to confirm the page renders (will show loading or not-found state without real data — that's acceptable).</verify>
  <done>page.tsx and ChecklistDetailClient.tsx created. Instance data fetched via tRPC. Header card shows entity, playbook name, status badge, completion ring, dispatch readiness banner. Step rows grouped by phase with collapsible sections. Admin skip action available on each open step. Naming lint passes. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npm run test -- --run workflows-naming-lint` passes
- `cd apps/web && npx tsc --noEmit` exits 0
- /checklists page: Work Board section exists above Playbook grid
- /checklists: empty state shows when no instances
- /checklists: "Start Checklist" button exists and triggers a dialog
- /checklists/instances/[id]: renders header card with status badge + completion ring + readiness banner
- /checklists/instances/[id]: step rows grouped by phase, each with status icon + action button
- No JSX text contains "PlaybookInstance", "StepInstance", or "PlaybookTrigger"
</verification>

<success_criteria>
Active Work Board swimlanes live on /checklists dashboard. Active Checklist Detail screen live at /checklists/instances/[id]. Both use user-facing names exclusively. Naming lint passes. TypeScript compiles. Start Checklist dialog calls instance.generate. Skip with Reason calls stepInstance.skip.
</success_criteria>

<output>
After completion, create `.planning/phases/43-workflow-engine-2-execution/43-04-SUMMARY.md`
</output>
