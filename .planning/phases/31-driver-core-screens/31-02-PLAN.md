---
phase: 31-driver-core-screens
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - packages/api-client/src/driver.ts
  - packages/api-client/src/index.ts
  - apps/mobile/context/QueryProvider.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(driver)/index.tsx
  - apps/mobile/components/driver/StatChip.tsx
  - apps/mobile/package.json
autonomous: true
must_haves:
  truths:
    - "driverApi.getDashboard, getLoads, getLoad, updateLoadStatus functions exist in api-client and make correct HTTP calls"
    - "TanStack Query QueryClientProvider wraps the app with staleTime 30000 and retry 2"
    - "Driver dashboard screen shows active load card (full-width, prominent) when driver has an active load"
    - "Driver dashboard screen shows empty state with 'No active load' and 'View All Loads' button when no active load"
    - "Dashboard shows 3 stat chips: Miles Today, Stops Done, HOS Remaining"
    - "Dashboard has pull-to-refresh that triggers data refetch"
    - "Dashboard shows recent alerts list below stats"
  artifacts:
    - packages/api-client/src/driver.ts
    - apps/mobile/context/QueryProvider.tsx
    - apps/mobile/app/(driver)/index.tsx
    - apps/mobile/components/driver/StatChip.tsx
  key_links:
    - "api-client driverApi functions are used by dashboard screen via TanStack Query"
    - "QueryProvider wraps app in root _layout.tsx"
---

<objective>
Extend the api-client with driver endpoint functions, set up TanStack Query, and build the driver dashboard screen.

Purpose: The dashboard is the driver's home screen — the first thing they see after login. It shows their active load prominently, today's stats, and recent alerts. The api-client and query provider are shared infrastructure needed by all mobile screens.
Output: api-client driver module, QueryProvider, and a fully functional dashboard screen with real data.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-driver-core-screens/31-CONTEXT.md
@.planning/phases/31-driver-core-screens/31-01-SUMMARY.md

# Reference for api-client pattern
@packages/api-client/src/client.ts
@packages/api-client/src/index.ts
# Reference for existing mobile layout
@apps/mobile/app/_layout.tsx
# Reference for shared UI components
@packages/ui/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend api-client with driver endpoints + install TanStack Query</name>
  <files>
    packages/api-client/src/driver.ts
    packages/api-client/src/index.ts
    apps/mobile/context/QueryProvider.tsx
    apps/mobile/app/_layout.tsx
    apps/mobile/package.json
  </files>
  <action>
**packages/api-client/src/driver.ts:**
Create a driverApi object following the existing client.ts pattern:
```typescript
export const driverApi = {
  getDashboard: (token: string) => /* GET /api/mobile/driver/dashboard with Bearer token */,
  getLoads: (token: string, status: 'active' | 'history') => /* GET /api/mobile/driver/loads?status={status} */,
  getLoad: (token: string, id: string) => /* GET /api/mobile/driver/loads/{id} */,
  updateLoadStatus: (token: string, id: string, status: string) => /* POST /api/mobile/driver/loads/{id}/status with { status } body */,
}
```
Export from packages/api-client/src/index.ts.

**Install TanStack Query:**
Run `cd apps/mobile && npm install @tanstack/react-query`

**apps/mobile/context/QueryProvider.tsx:**
Create QueryClient with defaultOptions: `{ queries: { staleTime: 30_000, retry: 2 } }`. Export QueryProvider component wrapping children in QueryClientProvider.

**apps/mobile/app/_layout.tsx:**
Add QueryProvider as a wrapper inside the existing layout (wrap around AuthProvider or at the same level). Import from `../context/QueryProvider`.
  </action>
  <verify>
- `cd packages/api-client && npx tsc --noEmit` compiles
- `cd apps/mobile && npx expo export --dump-sourcemap 2>&1 | head -5` or just verify no import errors
  </verify>
  <done>driverApi has 4 methods matching the REST endpoints. QueryProvider wraps the app. TanStack Query installed.</done>
</task>

<task type="auto">
  <name>Task 2: Build driver dashboard screen</name>
  <files>
    apps/mobile/app/(driver)/index.tsx
    apps/mobile/components/driver/StatChip.tsx
  </files>
  <action>
**apps/mobile/components/driver/StatChip.tsx:**
Small component: large number (H3/text-xl font-bold), small label below (text-sm text-muted). Background bg-slate-800 (dark) or bg-slate-100 (light), rounded-lg, p-3. Takes props: `{ value: number | string, label: string }`.

**apps/mobile/app/(driver)/index.tsx:**
Replace placeholder with full dashboard screen:

1. Use `useQuery({ queryKey: ['driver-dashboard'], queryFn: () => driverApi.getDashboard(token) })` — get token from useAuth hook
2. ScreenWrapper with scrollable content and RefreshControl (pull-to-refresh triggers refetch)
3. **Active load card** (most prominent element per user decision):
   - If activeLoad exists: full-width Card with brand-colored left border, showing load number (H3 bold), origin -> destination, status badge, customer name, "View Details" pressable that navigates to load detail
   - If no activeLoad: EmptyState card with "No active load" text + "View All Loads" button that navigates to loads tab
4. **Stats row**: horizontal row of 3 StatChip components — "Miles Today" (todayMiles), "Stops Done" (stopsCompleted), "HOS Remaining" (hosHoursRemaining)
5. **Recent alerts section**: "Recent Alerts" heading + list of alert items (icon + text + timestamp). If empty, show subtle "No recent alerts" text.
6. Loading state: show ActivityIndicator while query is loading
7. Error state: show error message with retry button

Use NativeWind classes for styling. Status badges should match web app color scheme (reference existing badge colors from shared UI or constants).
  </action>
  <verify>
- Run `cd apps/mobile && npx expo start` — navigate to driver tab, dashboard renders
- Active load card appears if driver has loads, empty state if not
- Pull down to refresh triggers data refetch
- Stats row shows 3 chips with values
  </verify>
  <done>Dashboard screen shows active load card (prominent, full-width) or empty state, 3 stat chips, and recent alerts. Pull-to-refresh works. Data fetched via TanStack Query from REST API.</done>
</task>

</tasks>

<verification>
- api-client exports driverApi with getDashboard, getLoads, getLoad, updateLoadStatus
- QueryProvider wraps the app in root layout
- Dashboard screen renders with real data from /api/mobile/driver/dashboard
- Active load card is the most prominent element (full-width, larger than stats)
- Empty state shows "No active load" + "View All Loads" when no active load
- 3 stat chips display correctly in a row
- Pull-to-refresh triggers refetch
</verification>

<success_criteria>
Driver dashboard screen fully functional with real API data. Api-client and query infrastructure in place for subsequent screens (loads list, load detail).
</success_criteria>

<output>
After completion, create `.planning/phases/31-driver-core-screens/31-02-SUMMARY.md`
</output>
