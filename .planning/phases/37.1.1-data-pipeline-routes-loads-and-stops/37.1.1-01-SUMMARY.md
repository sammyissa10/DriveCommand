---
phase: 37.1.1-data-pipeline-routes-loads-and-stops
plan: "01"
subsystem: database
tags: [prisma, postgresql, schema, migration, routestop, load, geocoding, backfill]

# Dependency graph
requires:
  - phase: 36-owner-fleet-messaging
    provides: stable mobile schema foundation this builds upon
provides:
  - RouteStop.loadId FK linking stops to their serving load
  - RouteStop contact/bol/po metadata fields
  - Load.pickupStopId and deliveryStopId FK links to RouteStop
  - Backfill migration creating RouteStops for all existing loads with routeId + coordinates
affects:
  - 37.1.1-02 (geocoding trigger — uses Load schema with pickupStopId/deliveryStopId)
  - 37.1.1-03 (RouteStop auto-creation on load-route assignment)
  - 37.7 (driver map — reads route.stops ordered by position with now-populated loadId)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Circular FK pattern: RouteStop.loadId → Load and Load.pickupStopId/deliveryStopId → RouteStop with DEFERRABLE INITIALLY DEFERRED constraints"
    - "Stop sequencing: all PICKUP stops first (1..N ordered by pickupDate), then DELIVERY stops (N+1..N+M ordered by deliveryDate) per route"
    - "Named Prisma relations for disambiguation: LoadPickupStop, LoadDeliveryStop, RouteStopLoad"

key-files:
  created:
    - apps/web/prisma/migrations/20260404000001_route_stop_load_links/migration.sql
  modified:
    - apps/web/prisma/schema.prisma

key-decisions:
  - "DEFERRABLE INITIALLY DEFERRED FK constraints used for circular Load↔RouteStop relationship — allows inserting RouteStops first then updating Load FKs in same transaction"
  - "Backfill uses idempotent NOT EXISTS guard — safe to re-run, won't duplicate stops"
  - "Sequencing: all PICKUPs first (1..N) then all DELIVERYs (N+1..N+M) per route — matches locked decision from CONTEXT.md"

patterns-established:
  - "Named Prisma relations required when multiple FKs exist between same two models"
  - "Backfill migrations should be idempotent with NOT EXISTS guards"

# Metrics
duration: 4min
completed: 2026-04-04
---

# Phase 37.1.1 Plan 01: Route Stop Load Links — Schema and Backfill Summary

**Prisma schema extended with circular Load↔RouteStop FK links (loadId, pickupStopId, deliveryStopId, contact/bol/po fields) and migration applied with idempotent backfill creating RouteStops for all existing loads with routeId and coordinates**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-04T05:51:00Z
- **Completed:** 2026-04-04T05:54:18Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Updated RouteStop model with 5 new fields: loadId (FK to Load), contactName, contactPhone, bolNumber, poNumber — plus 3 named back-reference relations
- Updated Load model with 2 new fields: pickupStopId and deliveryStopId (FK to RouteStop) — plus named relations using LoadPickupStop/LoadDeliveryStop/RouteStopLoad
- Created and applied migration 20260404000001_route_stop_load_links with DEFERRABLE FK constraints and idempotent backfill SQL (all PICKUPs first, then DELIVERYs)

## Task Commits

Each task was committed atomically:

1. **Task 1: Update Prisma schema with RouteStop and Load field additions** - `537e156` (feat)
2. **Task 2: Create migration SQL with schema changes and RouteStop backfill** - `078fb8c` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `apps/web/prisma/schema.prisma` - RouteStop: +loadId, contactName, contactPhone, bolNumber, poNumber, load relation, pickupForLoads/deliveryForLoads back-refs, @@index([loadId]). Load: +pickupStopId, deliveryStopId, pickupStop/deliveryStop relations, routeStops back-ref
- `apps/web/prisma/migrations/20260404000001_route_stop_load_links/migration.sql` - ALTER TABLE statements + backfill INSERT/UPDATE with CTE for pickup counts and pickups-first sequencing

## Decisions Made
- Used `DEFERRABLE INITIALLY DEFERRED` on both FK constraints for the circular dependency — allows inserting RouteStop (with loadId) first, then updating Load (with pickupStopId/deliveryStopId) without constraint violations within the same transaction
- Backfill guarded with `NOT EXISTS` to make migration idempotent — safe to re-run if needed
- Resolved pre-existing migration drift (`20260329000001_add_load_sequence` was failing due to column already existing in DB) by marking it applied before running our migration

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Resolved pre-existing migration drift before applying new migration**
- **Found during:** Task 2 (prisma migrate deploy)
- **Issue:** Migration `20260329000001_add_load_sequence` was marked failed in `_prisma_migrations` because the `sequence` column already existed in the DB. This blocked `prisma migrate deploy` from applying any subsequent migrations.
- **Fix:** Ran `npx prisma migrate resolve --applied 20260329000001_add_load_sequence` to mark it as already applied, then re-ran deploy which succeeded
- **Files modified:** None (DB-side migration table only)
- **Verification:** `prisma migrate deploy` succeeded, both pending migrations applied
- **Committed in:** 078fb8c (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Pre-existing drift was a blocker for all future migrations. Fixing it was necessary to apply the new migration. No scope creep.

## Issues Encountered
- `prisma migrate dev --create-only` failed (shadow database can't access `_prisma_migrations` on Supabase-hosted DB). Resolved by creating migration directory and SQL manually, then applying with `migrate deploy`. This is standard practice for this project.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Schema foundation complete: RouteStop and Load are linked with proper FKs
- Backfill applied: existing loads with routeId + coordinates now have RouteStops in DB
- Ready for Plan 02: geocoding trigger (geocode origin/destination on load create/update → populate pickupLat/pickupLng/deliveryLat/deliveryLng)
- Ready for Plan 03: RouteStop auto-creation when load is assigned to a route

---
*Phase: 37.1.1-data-pipeline-routes-loads-and-stops*
*Completed: 2026-04-04*

## Self-Check: PASSED

- FOUND: apps/web/prisma/schema.prisma
- FOUND: apps/web/prisma/migrations/20260404000001_route_stop_load_links/migration.sql
- FOUND: .planning/phases/37.1.1-data-pipeline-routes-loads-and-stops/37.1.1-01-SUMMARY.md
- FOUND commit: 537e156 (Task 1)
- FOUND commit: 078fb8c (Task 2)
