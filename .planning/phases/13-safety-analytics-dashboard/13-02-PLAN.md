---
phase: 13-safety-analytics-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/app/(owner)/safety/page.tsx
  - src/components/safety/safety-score-card.tsx
  - src/components/safety/event-distribution-chart.tsx
  - src/components/safety/safety-trend-chart.tsx
  - src/components/safety/driver-leaderboard.tsx
  - src/components/safety/threshold-config.tsx
autonomous: true
must_haves:
  truths:
    - "Owner can view fleet-wide safety dashboard with composite safety score (0-100 scale)"
    - "Owner can view safety events with distribution charts (bar chart by type, donut chart for severity percentages)"
    - "Owner can view safety score trends over time (30-day line chart showing daily average scores)"
    - "Owner can view driver/truck safety performance rankings (leaderboard showing top and bottom performers)"
    - "Owner can configure safety alert thresholds (g-force sensitivity settings per vehicle class)"
  artifacts:
    - path: "src/app/(owner)/safety/page.tsx"
      provides: "Safety dashboard server component page with parallel data fetching"
      contains: "Promise.all"
    - path: "src/components/safety/safety-score-card.tsx"
      provides: "Large composite score display with color coding and label"
      contains: "use client"
    - path: "src/components/safety/event-distribution-chart.tsx"
      provides: "Bar chart by event type + donut chart for severity percentages"
      contains: "BarChart"
    - path: "src/components/safety/safety-trend-chart.tsx"
      provides: "30-day line chart of daily safety scores"
      contains: "LineChart"
    - path: "src/components/safety/driver-leaderboard.tsx"
      provides: "Ranked list of trucks by safety score"
      contains: "use client"
    - path: "src/components/safety/threshold-config.tsx"
      provides: "G-force threshold configuration form with localStorage persistence"
      contains: "localStorage"
  key_links:
    - from: "src/app/(owner)/safety/page.tsx"
      to: "src/app/(owner)/safety/actions.ts"
      via: "import server actions"
      pattern: "import.*from.*actions"
    - from: "src/app/(owner)/safety/page.tsx"
      to: "src/components/safety/*"
      via: "renders chart components with data props"
      pattern: "<(SafetyScoreCard|EventDistributionChart|SafetyTrendChart|DriverLeaderboard|ThresholdConfig)"
    - from: "src/components/safety/event-distribution-chart.tsx"
      to: "src/components/ui/chart.tsx"
      via: "ChartContainer and ChartConfig for theming"
      pattern: "import.*ChartContainer.*from.*ui/chart"
    - from: "src/components/safety/safety-score-card.tsx"
      to: "src/lib/safety/score-calculator.ts"
      via: "getScoreColor and getScoreLabel for display"
      pattern: "import.*getScoreColor.*from.*score-calculator"
---

<objective>
Build the complete safety dashboard UI: server component page with parallel data fetching and 5 client chart/widget components covering all SAFE requirements.

Purpose: Deliver the fully functional safety analytics dashboard at /safety with score card, event distribution charts, trend line, leaderboard, and threshold configuration.
Output: Safety dashboard page with 5 interactive chart/widget components rendering real data from server actions.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-safety-analytics-dashboard/13-RESEARCH.md
@.planning/phases/13-safety-analytics-dashboard/13-01-SUMMARY.md
@src/app/(owner)/dashboard/page.tsx (parallel data fetching pattern)
@src/app/(owner)/live-map/page.tsx (server component + client wrapper pattern)
@src/lib/safety/score-calculator.ts (from plan 01)
@src/app/(owner)/safety/actions.ts (from plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create safety score card, event distribution charts, and trend chart components</name>
  <files>
    src/components/safety/safety-score-card.tsx
    src/components/safety/event-distribution-chart.tsx
    src/components/safety/safety-trend-chart.tsx
  </files>
  <action>
    All three components are `'use client'` components that receive pre-aggregated data as props.

    **Component 1: `src/components/safety/safety-score-card.tsx`** (SAFE-01)
    - Props: `{ score: number, totalEvents: number, eventsBySeverity: Record<string, number>, period: number }`
    - Display the composite score (0-100) as a large number with color coding via `getScoreColor()` and label via `getScoreLabel()` from `@/lib/safety/score-calculator`
    - Use shadcn Card (CardHeader, CardContent) for layout
    - Show a circular/radial indicator: Use Recharts `RadialBarChart` with a single bar showing the score percentage, OR a simpler approach using a large styled number with a descriptive label
    - Simpler approach recommended: Large score number (text-6xl font-bold), colored by score range, with label below (e.g., "Excellent"), total events count, and a small breakdown showing events by severity (4 small badges: LOW, MEDIUM, HIGH, CRITICAL with counts)
    - Use shadcn Badge component for severity counts with appropriate colors: LOW=default/outline, MEDIUM=secondary, HIGH=destructive variant, CRITICAL=destructive with stronger emphasis
    - Handle empty state: if totalEvents === 0, show "No safety events recorded" with a Shield icon instead of a misleading score of 100
    - Card title: "Fleet Safety Score" with Shield icon from lucide-react
    - Card description: "Last {period} days"

    **Component 2: `src/components/safety/event-distribution-chart.tsx`** (SAFE-02)
    - Props: `{ byType: Array<{eventType: string, label: string, count: number}>, bySeverity: Array<{severity: string, label: string, count: number, fill: string}> }`
    - Two charts side by side (or stacked on mobile via responsive grid):
      1. **Bar chart** showing event count by type (horizontal bar chart for readability of long labels):
         - Use Recharts `BarChart` with `layout="vertical"`
         - XAxis type="number", YAxis dataKey="label" type="category" width={140}
         - Wrap in shadcn `ChartContainer` with `className="min-h-[300px] w-full"` (CRITICAL: without min-h, chart collapses to 0px)
         - Use `ChartTooltip` with `ChartTooltipContent` for themed tooltips
         - Single Bar with `fill="var(--color-count)"` and radius={4}
      2. **Donut chart** showing severity distribution:
         - Use Recharts `PieChart` with `Pie` having `innerRadius={60}` for donut shape
         - `Label` component in center showing total events count (see research Pattern 3)
         - ChartConfig maps severity keys to colors: low=chart-2 (green-ish), medium=chart-3 (yellow-ish), high=chart-4 (orange), critical=chart-5 (red)
         - Each data item has `fill` property matching its severity color from ChartConfig
    - Wrap both charts in a single Card with title "Safety Events" and description "Distribution by type and severity"
    - Use a `grid grid-cols-1 md:grid-cols-2 gap-4` layout inside CardContent for the two charts
    - Handle empty state: if both arrays are empty, show "No safety events recorded" message

    **Component 3: `src/components/safety/safety-trend-chart.tsx`** (SAFE-03)
    - Props: `{ data: Array<{date: string, score: number, events: number}> }`
    - Line chart showing daily safety scores over 30 days
    - Use Recharts `LineChart` with:
      - `CartesianGrid strokeDasharray="3 3"`
      - XAxis dataKey="date" with tickFormatter to show MM/DD format
      - YAxis domain={[0, 100]} for consistent score scale
      - Single `Line` type="monotone" dataKey="score" with stroke="var(--color-score)", strokeWidth=2, dot=false, activeDot={{ r: 6 }}
    - Wrap in ChartContainer with `className="min-h-[300px] w-full"`
    - Wrap in Card with title "Safety Score Trend" and description "Daily fleet safety score over the last 30 days"
    - Handle empty state: if data array is empty, show message instead of empty chart
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all three components compile without type errors. Verify each file has `'use client'` directive and imports from `@/components/ui/chart` and `@/components/ui/card`.
  </verify>
  <done>Three chart components compile without errors: SafetyScoreCard shows 0-100 score with color coding, EventDistributionChart renders bar + donut charts, SafetyTrendChart renders 30-day line chart. All use shadcn ChartContainer with proper min-h classes.</done>
</task>

<task type="auto">
  <name>Task 2: Create leaderboard, threshold config, and safety dashboard page</name>
  <files>
    src/components/safety/driver-leaderboard.tsx
    src/components/safety/threshold-config.tsx
    src/app/(owner)/safety/page.tsx
  </files>
  <action>
    **Component 4: `src/components/safety/driver-leaderboard.tsx`** (SAFE-04)
    - `'use client'` component
    - Props: `{ rankings: Array<{truckId: string, name: string, licensePlate: string, score: number, totalEvents: number}> }`
    - Display as a ranked list inside a Card (title: "Vehicle Safety Rankings", description: "Ranked by safety score, best to worst")
    - For each truck, show: rank number, truck name (make + model), license plate, safety score with color coding via `getScoreColor()`, total events count, and score label via `getScoreLabel()`
    - Use a table-like layout with alternating row backgrounds (odd:bg-muted/50)
    - Top 3 get a visual indicator (trophy or medal icon, or just bold styling)
    - Bottom performers (score < 60) get a warning indicator
    - Handle empty state: if rankings is empty, show "No vehicles found" message
    - Sort should already come from server action (score descending), but ensure display order is best score first

    **Component 5: `src/components/safety/threshold-config.tsx`** (SAFE-05)
    - `'use client'` component
    - Three vehicle classes: Light Duty, Medium Duty, Heavy Duty
    - For each class, three g-force threshold fields: Harsh Braking, Harsh Acceleration, Harsh Cornering
    - Default values from research: Light (0.45, 0.35, 0.30), Medium (0.35, 0.30, 0.25), Heavy (0.25, 0.20, 0.20)
    - **Storage: localStorage** (per research recommendation for mock data context)
      - Key: `drivecommand-safety-thresholds`
      - On mount: load from localStorage, fall back to defaults
      - On save: write to localStorage, show success toast/feedback
    - UI: Card with title "Alert Thresholds" and description "G-force sensitivity settings per vehicle class"
    - Use shadcn Tabs component for vehicle class selection (Light Duty | Medium Duty | Heavy Duty)
    - Each tab shows 3 input fields (shadcn Input) with labels: "Harsh Braking (g)", "Harsh Acceleration (g)", "Harsh Cornering (g)"
    - Input validation: 0.1 to 5.0 range, step 0.05
    - Save button per tab or single "Save All" button
    - Show a brief "Saved" confirmation when thresholds are persisted
    - Use `useState` for form state, `useEffect` for loading from localStorage on mount

    **Page: `src/app/(owner)/safety/page.tsx`** (assembles everything)
    - Server component (NO `'use client'` directive)
    - Import and call `requireRole([UserRole.OWNER, UserRole.MANAGER])` first
    - Import all 4 server actions from `./actions`
    - Parallel data fetching with `Promise.all` (same pattern as dashboard/page.tsx):
      ```typescript
      const [score, distribution, trends, rankings] = await Promise.all([
        getFleetSafetyScore(),
        getEventDistribution(),
        getSafetyScoreTrend(30),
        getDriverRankings(),
      ]);
      ```
    - Add `export const fetchCache = 'force-no-store';` for fresh data on every load
    - Layout structure (responsive grid):
      ```
      <div className="space-y-6">
        {/* Page header */}
        <div>
          <h1 className="text-3xl font-bold tracking-tight">Safety Dashboard</h1>
          <p className="text-muted-foreground">Fleet-wide safety performance and driver rankings</p>
        </div>

        {/* Top row: Score card + Event distribution */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <SafetyScoreCard score={score} />            {/* 1 col */}
          <div className="lg:col-span-2">
            <EventDistributionChart data={distribution} /> {/* 2 cols */}
          </div>
        </div>

        {/* Trend chart: full width */}
        <SafetyTrendChart data={trends} />

        {/* Bottom row: Leaderboard + Thresholds */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <DriverLeaderboard rankings={rankings} />
          <ThresholdConfig />                           {/* No server data needed */}
        </div>
      </div>
      ```
    - Import all 5 components from `@/components/safety/*`
    - Pass data as props to each component (ThresholdConfig gets no props - manages its own localStorage state)
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no type errors. Then run `npm run build` to verify the page builds successfully (server component renders, client components are properly split). Visit `/safety` in the browser to confirm the dashboard renders with data from seed (this is the build verification, not a checkpoint).
  </verify>
  <done>Safety dashboard page at /safety renders with all 5 sections: composite score card (SAFE-01), event distribution bar + donut charts (SAFE-02), 30-day trend line chart (SAFE-03), vehicle safety leaderboard (SAFE-04), and g-force threshold configuration with localStorage persistence (SAFE-05). Page uses parallel data fetching and all charts display seed data.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `/safety` page renders for OWNER/MANAGER roles (auth-gated)
- Safety score card shows 0-100 score with appropriate color and label
- Bar chart shows event counts by type (8 event types from schema)
- Donut chart shows severity distribution (LOW, MEDIUM, HIGH, CRITICAL)
- Line chart shows 30-day score trend with dates on x-axis, 0-100 on y-axis
- Leaderboard shows trucks ranked by safety score (best first)
- Threshold config loads defaults, persists changes to localStorage
- All charts handle empty state gracefully (no crashes with empty data)
</verification>

<success_criteria>
- All 5 SAFE requirements (SAFE-01 through SAFE-05) have corresponding UI sections
- Dashboard fetches data in parallel (Promise.all) following established pattern
- Charts render with real seed data (not hardcoded mock data)
- Threshold configuration persists across page reloads (localStorage)
- Page builds and renders without SSR errors (all Recharts in 'use client' components)
- Responsive layout: stacks vertically on mobile, grid on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/13-safety-analytics-dashboard/13-02-SUMMARY.md`
</output>
