---
phase: 11-navigation-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/navigation/sidebar.tsx
  - src/app/(owner)/layout.tsx
  - src/components/navigation/user-menu.tsx
  - components.json
  - src/lib/utils.ts
  - src/components/ui/sidebar.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/input.tsx
  - tailwind.config.ts
  - src/app/globals.css
  - package.json
autonomous: true

must_haves:
  truths:
    - "Owner portal displays Samsara-style collapsible sidebar with grouped menu sections"
    - "Sidebar shows company logo + name at top (collapses to logo icon when collapsed)"
    - "Menu items are grouped: Dashboard (top), Fleet Intelligence (Live Map, Safety, Fuel), Fleet Management (Trucks, Drivers, Routes), Maintenance"
    - "Active page is highlighted in sidebar based on current route"
    - "Sidebar persists collapse state across page navigation (localStorage)"
    - "Sidebar is responsive: persistent on desktop, drawer on mobile"
    - "Role-based menu filtering: Fleet Intelligence only visible to OWNER/MANAGER"
  artifacts:
    - path: "src/components/navigation/sidebar.tsx"
      provides: "Samsara-style sidebar component with grouped sections"
      contains: "SidebarGroupLabel"
    - path: "src/app/(owner)/layout.tsx"
      provides: "Owner layout with SidebarProvider wrapping sidebar + main content"
      contains: "SidebarProvider"
    - path: "src/components/ui/sidebar.tsx"
      provides: "shadcn/ui sidebar primitives"
      contains: "SidebarContent"
  key_links:
    - from: "src/app/(owner)/layout.tsx"
      to: "src/components/navigation/sidebar.tsx"
      via: "AppSidebar import"
      pattern: "import.*AppSidebar"
    - from: "src/components/navigation/sidebar.tsx"
      to: "src/components/ui/sidebar.tsx"
      via: "shadcn Sidebar primitives"
      pattern: "import.*@/components/ui/sidebar"
---

<objective>
Replace the owner portal's top navigation bar with a Samsara-style collapsible sidebar using shadcn/ui components. The sidebar has grouped menu sections (Dashboard, Fleet Intelligence, Fleet Management, Maintenance) with role-based visibility, company branding at top, and persistent collapse state.

Purpose: Establish the navigation foundation for v2.0 fleet intelligence features (Live Map, Safety, Fuel dashboards) while matching industry-standard Samsara UX patterns.
Output: Working sidebar navigation in owner portal with all existing pages accessible via sidebar menu items.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-data-foundation/11-RESEARCH.md
@src/app/(owner)/layout.tsx
@src/components/navigation/user-menu.tsx
@src/lib/auth/roles.ts
@package.json
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui and add sidebar component with dependencies</name>
  <files>
    components.json
    src/lib/utils.ts
    src/components/ui/sidebar.tsx
    src/components/ui/sheet.tsx
    src/components/ui/separator.tsx
    src/components/ui/tooltip.tsx
    src/components/ui/input.tsx
    tailwind.config.ts
    src/app/globals.css
    package.json
  </files>
  <action>
    Initialize shadcn/ui in the project and add the sidebar component with all its dependencies.

    **Step 1: Install shadcn/ui prerequisites**
    - Run `npx shadcn@latest init` (choose: New York style, Tailwind CSS, src/ directory, TypeScript, default colors, CSS variables: yes).
    - If interactive prompts are problematic, manually create `components.json` config file and install required packages: `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `@radix-ui/react-slot`.
    - Create `src/lib/utils.ts` with the `cn()` utility function (combines clsx + tailwind-merge).
    - Update `tailwind.config.ts` to include CSS variable-based color system that shadcn expects (extend colors with `border`, `input`, `ring`, `background`, `foreground`, `primary`, `secondary`, `destructive`, `muted`, `accent`, `popover`, `card` using CSS variables like `hsl(var(--primary))`). Also add the `sidebar` color config that the sidebar component requires (sidebar-background, sidebar-foreground, sidebar-primary, sidebar-primary-foreground, sidebar-accent, sidebar-accent-foreground, sidebar-border, sidebar-ring).
    - Update `src/app/globals.css` to define CSS variables in `:root` and `.dark` for the color system, including `--sidebar-*` variables.

    **Step 2: Add shadcn/ui sidebar component**
    - Run `npx shadcn@latest add sidebar` to add the sidebar component and its dependencies (sheet, separator, tooltip, input, skeleton).
    - If CLI doesn't work, manually install the component files from the shadcn/ui sidebar source:
      - `src/components/ui/sidebar.tsx` — The full sidebar component (SidebarProvider, Sidebar, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarGroupContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarHeader, SidebarFooter, SidebarTrigger, useSidebar hook)
      - `src/components/ui/sheet.tsx` — For mobile drawer behavior (wraps @radix-ui/react-dialog)
      - `src/components/ui/separator.tsx` — For visual dividers
      - `src/components/ui/tooltip.tsx` — For collapsed icon tooltips (wraps @radix-ui/react-tooltip)
      - `src/components/ui/input.tsx` — For search input in sidebar (optional but sidebar component imports it)
    - Also install required Radix UI packages: `@radix-ui/react-dialog`, `@radix-ui/react-separator`, `@radix-ui/react-tooltip`, `@radix-ui/react-slot`.

    NOTE: shadcn/ui is NOT currently installed in this project. The research incorrectly stated it was. You need to initialize it from scratch. Be careful — the existing tailwind.config.ts and globals.css may need significant updates to support the CSS variable color system.

    IMPORTANT: lucide-react is also not installed yet. Install it as a dependency.

    IMPORTANT: Preserve existing Tailwind classes and styles used by the v1.0 UI. The color system additions should be ADDITIVE, not replace existing utility classes. Test that `npm run build` succeeds after all changes.
  </action>
  <verify>
    - `npm run build` succeeds without errors
    - `src/components/ui/sidebar.tsx` exists and exports SidebarProvider, Sidebar, SidebarContent, etc.
    - `src/lib/utils.ts` exists and exports `cn()` function
    - `components.json` exists with shadcn configuration
    - All required @radix-ui packages in package.json dependencies
    - lucide-react in package.json dependencies
  </verify>
  <done>shadcn/ui is initialized with sidebar component and all dependencies installed. Build passes. CSS variable color system is configured in tailwind.config.ts and globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Create AppSidebar component and integrate into owner layout</name>
  <files>
    src/components/navigation/sidebar.tsx
    src/app/(owner)/layout.tsx
    src/components/navigation/user-menu.tsx
  </files>
  <action>
    Create the Samsara-style sidebar component and replace the owner portal's top navigation.

    **Step 1: Create AppSidebar component** at `src/components/navigation/sidebar.tsx`

    This is a "use client" component that uses:
    - `usePathname()` from next/navigation for active state highlighting
    - shadcn/ui Sidebar primitives (Sidebar, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarGroupContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarHeader, SidebarFooter)
    - Lucide icons: LayoutDashboard, MapPin, Shield, Fuel, Truck, Users, Route (aliased as RouteIcon), Wrench
    - Role-based menu filtering using `useUser()` from @clerk/nextjs

    **Menu structure (per locked user decision — Samsara-style grouping):**

    1. **Header:** Company logo (Truck icon in blue rounded square as placeholder) + "DriveCommand" text. When collapsed, only show the icon.
    2. **Dashboard** (ungrouped, at top): icon LayoutDashboard, links to `/dashboard`
    3. **Fleet Intelligence** (group with SidebarGroupLabel):
       - Live Map: icon MapPin, links to `/live-map` (page created in Phase 12 — link should still work)
       - Safety: icon Shield, links to `/safety` (Phase 13)
       - Fuel: icon Fuel, links to `/fuel` (Phase 14)
       - This group is ONLY visible to OWNER and MANAGER roles
    4. **Fleet Management** (group with SidebarGroupLabel):
       - Trucks: icon Truck, links to `/trucks`
       - Drivers: icon Users, links to `/drivers`
       - Routes: icon RouteIcon, links to `/routes`
    5. **Maintenance** (standalone, not in a group): icon Wrench, links to `/trucks` (maintenance is accessed through truck detail pages — keep as a conceptual entry that links to trucks for now)

    **Footer:** Keep the existing UserMenu (Clerk UserButton) in the sidebar footer area.

    **Active state:** Use `pathname.startsWith(path)` for matching (not exact match) so that `/trucks/123` still highlights the Trucks menu item. For Dashboard, use exact match `pathname === '/dashboard'`.

    **Role-based filtering:**
    - Import `useUser` from `@clerk/nextjs`
    - Check `user?.publicMetadata?.role` against OWNER/MANAGER for Fleet Intelligence visibility
    - All other sections visible to all owner portal users (OWNER and MANAGER both have access since layout already enforces this)

    **Discretion decisions (Claude's discretion from CONTEXT.md):**
    - Theme: Match current app theme (light bg, dark text) rather than dark navy — consistent with v1.0 styling
    - Collapse behavior: Icon rail (shows icons when collapsed, not fully hidden) — matches Samsara pattern
    - Section headers: Always visible (not collapsible) — simpler, less UI complexity
    - User profile: Move UserMenu to sidebar footer
    - Driver portal: Keep current top nav (driver portal not changed in this phase)
    - System admin portal: Keep current layout (not changed in this phase)
    - Mobile: Use shadcn's built-in drawer behavior (automatic based on SidebarProvider)

    **Step 2: Update owner layout** at `src/app/(owner)/layout.tsx`

    Replace the current header-based layout with sidebar layout:
    - Wrap children in `SidebarProvider` from shadcn/ui
    - Add `AppSidebar` component
    - Main content area: `flex-1 overflow-y-auto` with padding
    - Add `SidebarTrigger` button at top of main content for collapse toggle
    - Keep the auth checks (they're server-side and must stay)
    - The layout is an async server component. SidebarProvider is a client component. So the layout needs to render SidebarProvider (client boundary) with AppSidebar and children inside.

    IMPORTANT: The layout.tsx is a server component with `await auth()`. The SidebarProvider and AppSidebar are client components. Create a client wrapper component or use the pattern where the server component renders client components as children. The simplest approach: create an `OwnerShell` client component that wraps SidebarProvider + AppSidebar + SidebarTrigger + main area, and render it from the server layout after auth checks.

    **Step 3: Update UserMenu placement**
    - The UserMenu component stays in `src/components/navigation/user-menu.tsx` unchanged
    - It gets rendered in the sidebar footer (inside AppSidebar) instead of the old header

    Ensure ALL existing owner portal routes still work: /dashboard, /trucks, /trucks/new, /trucks/[id], /trucks/[id]/edit, /trucks/[id]/maintenance, /drivers, /drivers/invite, /drivers/[id], /drivers/[id]/edit, /routes, /routes/new, /routes/[id], /routes/[id]/edit.
  </action>
  <verify>
    - `npm run build` succeeds
    - `src/components/navigation/sidebar.tsx` exists and exports `AppSidebar`
    - `src/app/(owner)/layout.tsx` uses SidebarProvider and AppSidebar
    - Grep for "SidebarGroupLabel" in sidebar.tsx returns matches for "Fleet Intelligence" and "Fleet Management"
    - Grep for "usePathname" in sidebar.tsx confirms active state logic
    - Grep for "SidebarProvider" in layout.tsx confirms integration
  </verify>
  <done>Owner portal displays Samsara-style collapsible sidebar with Dashboard, Fleet Intelligence (Live Map, Safety, Fuel), Fleet Management (Trucks, Drivers, Routes), and Maintenance menu items. Active page is highlighted. Sidebar collapse state persists via localStorage. UserMenu is in sidebar footer. All existing pages remain accessible.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Owner layout uses SidebarProvider with AppSidebar
3. Sidebar has grouped sections matching Samsara pattern (Dashboard, Fleet Intelligence, Fleet Management, Maintenance)
4. Company branding (logo + "DriveCommand") appears in sidebar header
5. Active state highlighting works via pathname matching
6. Role-based filtering hides Fleet Intelligence for non-OWNER/MANAGER roles
7. All existing owner portal routes accessible via sidebar links
</verification>

<success_criteria>
- Owner portal has fully functional Samsara-style sidebar navigation replacing the old top bar
- Sidebar collapses to icon rail and persists state
- Menu items correctly link to all existing pages plus placeholder paths for future fleet intelligence pages
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-navigation-data-foundation/11-01-SUMMARY.md`
</output>
