---
phase: 25-sysadmin-invoicing-module
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/billing/page.tsx
  - src/app/(admin)/billing/new/page.tsx
  - src/app/(admin)/billing/new/new-invoice-form.tsx
  - src/app/(admin)/billing/[id]/page.tsx
  - src/app/(admin)/billing/[id]/invoice-actions.tsx
  - src/app/(admin)/billing/[id]/edit/page.tsx
  - src/app/(admin)/billing/[id]/edit/edit-invoice-form.tsx
  - src/app/(admin)/tenants/[id]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Admin nav has a 'Billing' link pointing to /billing"
    - "Admin can see all invoices at /billing with status badges, tenant name, amount, due date"
    - "Billing page shows summary stats: total unpaid count, total overdue count, total paid this month"
    - "Admin can create a new invoice at /billing/new by selecting a tenant, adding line items, setting a due date"
    - "New invoice form computes line-item amounts and subtotal in real time"
    - "Admin can view invoice detail at /billing/[id] with full line-item table"
    - "Invoice detail page shows status badge and context-appropriate action buttons (Mark Paid, Void, Send, Edit)"
    - "Admin can edit a DRAFT invoice at /billing/[id]/edit"
    - "Tenant detail page /tenants/[id] has a Billing History card below the two-column grid"
    - "Billing History card lists that tenant's invoices with number, amount, due date, status"
    - "npx tsc --noEmit passes"
  artifacts:
    - path: "src/app/(admin)/billing/page.tsx"
      provides: "Invoice list with summary stats and status badges"
    - path: "src/app/(admin)/billing/new/new-invoice-form.tsx"
      provides: "Client form: tenant select, line items editor, due date, submit"
    - path: "src/app/(admin)/billing/[id]/page.tsx"
      provides: "Invoice detail with line-item table and action buttons"
    - path: "src/app/(admin)/billing/[id]/invoice-actions.tsx"
      provides: "Client component for Mark Paid / Void / Archive buttons with confirmation"
    - path: "src/app/(admin)/tenants/[id]/page.tsx"
      provides: "Tenant detail with new Billing History section"
  key_links:
    - from: "src/app/(admin)/billing/page.tsx"
      to: "getSysAdminInvoices"
      via: "server action import"
      pattern: "getSysAdminInvoices"
    - from: "src/app/(admin)/billing/new/new-invoice-form.tsx"
      to: "createSysAdminInvoice"
      via: "form submit"
      pattern: "createSysAdminInvoice"
    - from: "src/app/(admin)/billing/[id]/invoice-actions.tsx"
      to: "markInvoicePaid, voidInvoice, archiveInvoice"
      via: "button onClick to server action"
      pattern: "markInvoicePaid|voidInvoice|archiveInvoice"
    - from: "src/app/(admin)/tenants/[id]/page.tsx"
      to: "getSysAdminInvoices"
      via: "server component data fetch filtered by tenantId"
      pattern: "getSysAdminInvoices.*tenantId"
---

<objective>
Build all admin UI for the invoicing module: billing list page, new invoice form, invoice detail page, edit page, and billing history section on the tenant detail page. Also adds the Billing nav link.

Purpose: Gives the DriveCommand team the full interface to create, view, and manage tenant invoices.
Output: 8 new/modified files. No external dependencies beyond what plan 01 created.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-sysadmin-invoicing-module/25-CONTEXT.md
@.planning/phases/25-sysadmin-invoicing-module/25-01-SUMMARY.md

# Reference patterns
@src/app/(admin)/layout.tsx
@src/app/(admin)/admin-dashboard/page.tsx
@src/app/(admin)/tenants/[id]/page.tsx
@src/app/(admin)/actions/tenants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav link + billing list page + summary stats</name>
  <files>
    src/app/(admin)/layout.tsx
    src/app/(admin)/billing/page.tsx
  </files>
  <action>
    **src/app/(admin)/layout.tsx** — Add a "Billing" nav link after the "Support" link.
    Inspect existing links: href="/tenants", href="/admin-dashboard", href="/admin-support" (no /admin prefix — the (admin) route group uses segment paths directly). Add:
    ```tsx
    <Link href="/billing" className="text-white hover:text-gray-300 font-medium">
      Billing
    </Link>
    ```

    **src/app/(admin)/billing/page.tsx** — Server component:

    Add `export const dynamic = 'force-dynamic';` at top.

    Fetch data:
    - Call getSysAdminInvoices() (no filter — all non-archived)
    - Derive summary stats from the returned invoice list:
      - unpaidCount: invoices where status is DRAFT or SENT
      - overdueCount: invoices where status is OVERDUE
      - paidThisMonth: invoices where status is PAID and paidAt is within current calendar month; sum their total fields using Decimal.js, formatted as $X,XXX.XX

    Layout (admin gray palette — bg-white, border-gray-200, text-gray-900):

    1. Page header: "Billing" h1 + "Invoice tenants for subscription and setup fees" subtitle
    2. Action bar (flex justify-between): left = "Mark Overdue" button (outline style, calls markOverdueInvoices() via a small 'use client' MarkOverdueButton component), right = "New Invoice" Link to /billing/new
    3. Summary stats row: 3 Cards matching admin-dashboard/page.tsx pattern
       - Unpaid Invoices (count, gray text)
       - Overdue (count, red text — text-red-600)
       - Paid This Month (dollar amount, green text — text-green-600)
    4. Invoice table (full width, Card wrapper): columns — Invoice #, Tenant, Status, Amount, Due Date, Actions
       - Invoice # links to /billing/[id], styled as blue link
       - Status badge: DRAFT=bg-gray-100 text-gray-700, SENT=bg-blue-100 text-blue-700, PAID=bg-green-100 text-green-700, OVERDUE=bg-red-100 text-red-700, VOID=bg-gray-100 text-gray-400
       - Amount: $X,XXX.XX (use Number(inv.total).toLocaleString('en-US', { style: 'currency', currency: 'USD' }))
       - Due Date: toLocaleDateString()
       - Actions: "View" link to /billing/[id]
    5. Empty state: "No invoices yet. Create the first one."

    MarkOverdueButton: small 'use client' component in same file or billing/mark-overdue-button.tsx. On click, calls markOverdueInvoices() server action then shows a brief toast or status update. Use router.refresh() after call.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Nav should show "Billing" link. Billing page should render with table.
  </verify>
  <done>
    Billing nav link renders in admin header.
    /billing page renders invoice list table and 3 summary stat cards.
    npx tsc --noEmit exits 0 for these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: New invoice form + invoice detail + edit page + tenant billing history</name>
  <files>
    src/app/(admin)/billing/new/page.tsx
    src/app/(admin)/billing/new/new-invoice-form.tsx
    src/app/(admin)/billing/[id]/page.tsx
    src/app/(admin)/billing/[id]/invoice-actions.tsx
    src/app/(admin)/billing/[id]/edit/page.tsx
    src/app/(admin)/billing/[id]/edit/edit-invoice-form.tsx
    src/app/(admin)/tenants/[id]/page.tsx
  </files>
  <action>
    **src/app/(admin)/billing/new/page.tsx** — Server component:
    - Accept searchParams?: Promise<{ tenantId?: string }> to pre-select tenant
    - Fetch all tenants via getAllTenants() for the select dropdown
    - Render NewInvoiceForm with tenants and optional preselectedTenantId
    - Page title: "New Invoice", back link to /billing

    **src/app/(admin)/billing/new/new-invoice-form.tsx** — Client component ('use client'):

    State: selectedTenantId (string, defaults to preselectedTenantId prop), dueDate (string), notes (string), items (array of { id, description, quantity, unitPrice }), error (string|null), isSubmitting (boolean).

    Line item logic:
    - "Add Line Item" button appends { id: crypto.randomUUID(), description: '', quantity: '1', unitPrice: '0.00' }
    - Each row: description text input, quantity number input (step='0.01'), unit price number input (step='0.01'), computed amount shown as read-only (new Decimal(qty).times(new Decimal(price)).toFixed(2)), remove button (disabled if only 1 item)
    - Subtotal shown below table using Decimal.js sum of all item amounts
    - Import Decimal from 'decimal.js'

    On submit: call createSysAdminInvoice({ tenantId: selectedTenantId, dueDate, notes, items: items.map(i => ({ description: i.description, quantity: parseFloat(i.quantity), unitPrice: parseFloat(i.unitPrice) })) })
    On success: router.push('/billing/' + result.invoice.id)
    On error: show red error banner

    Props: { tenants: Array<{ id: string; name: string }>, preselectedTenantId?: string }

    Form layout: tenant select (required), due date input type='date' (required), notes textarea (optional), line items section with Add Line Item button, subtotal display, "Create Invoice" submit button + "Cancel" link to /billing.

    **src/app/(admin)/billing/[id]/page.tsx** — Server component (export const dynamic = 'force-dynamic'):
    - Fetch via getSysAdminInvoiceById(id), handle not found with error card
    - Render: back link to /billing, page header with invoice number (font-mono) + status badge, two-column info (left: tenant name + owner contact, right: issue date + due date + notes)
    - Line items table: Description | Qty | Unit Price | Amount columns, total row at bottom with bold styling
    - Owner contact: ownerUser.firstName + ' ' + ownerUser.lastName + ' — ' + ownerUser.email, or "No owner found" if null
    - Render InvoiceActions component passing { id, status, tenantId }

    **src/app/(admin)/billing/[id]/invoice-actions.tsx** — Client component ('use client'):
    Props: { invoice: { id: string; status: string } }

    Render context-aware buttons:
    - DRAFT: disabled "Send Invoice" button (text "Send Invoice — configure email in Plan 03", disabled: true), "Edit" Link to /billing/[id]/edit, "Archive" button (window.confirm before calling archiveInvoice(id), then router.push('/billing'))
    - SENT or OVERDUE: "Mark as Paid" button (window.confirm, calls markInvoicePaid(id), then router.refresh()), "Void Invoice" button (window.confirm, calls voidInvoice(id), then router.refresh())
    - PAID: read-only status display only
    - VOID: read-only status display only

    Import all needed actions from @/app/(admin)/actions/sysadmin-invoices.

    **src/app/(admin)/billing/[id]/edit/page.tsx** — Server component:
    - Fetch invoice via getSysAdminInvoiceById(id)
    - If not DRAFT, redirect('/billing/' + id)
    - Render EditInvoiceForm with invoice data

    **src/app/(admin)/billing/[id]/edit/edit-invoice-form.tsx** — Client component:
    - Same shape as NewInvoiceForm but pre-populated: dueDate from invoice.dueDate (ISO to YYYY-MM-DD for date input), notes from invoice.notes, items from invoice.items
    - Tenant displayed as read-only text (cannot be changed)
    - On submit: call updateSysAdminInvoice(id, { dueDate, notes, items: ... })
    - On success: router.push('/billing/' + id)

    **src/app/(admin)/tenants/[id]/page.tsx** — Modify existing file:
    - Add import for getSysAdminInvoices from @/app/(admin)/actions/sysadmin-invoices
    - After fetching tenant, add: const billingHistory = await getSysAdminInvoices({ tenantId: id }).catch(() => [])
    - After the closing div of the lg:grid-cols-2 grid, add a full-width Billing History Card:
      - CardHeader: "Billing History" title + "+ New Invoice" link to /billing/new?tenantId={tenant.id}
      - CardContent: empty state paragraph if no invoices, otherwise a table with columns Invoice # (link), Amount, Due Date, Status badge
      - Use same getStatusBadge helper inline function for status badge classes
  </action>
  <verify>
    Run: npx tsc --noEmit
    Expected: 0 errors across all modified files.
  </verify>
  <done>
    npx tsc --noEmit exits 0.
    /billing renders with invoice list.
    /billing/new renders with form (tenant select, line items, due date).
    /billing/[id] renders with line item table and action buttons.
    /tenants/[id] renders Billing History card below the two-column grid.
    Invoice actions show appropriate buttons based on status.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Admin billing UI end-to-end</name>
  <what-built>
    Full admin billing UI:
    - "Billing" nav link in the admin header
    - /billing — invoice list with status badges and summary stats
    - /billing/new — create invoice form with live line-item subtotal
    - /billing/[id] — invoice detail with line items and action buttons
    - /billing/[id]/edit — edit DRAFT invoices
    - /tenants/[id] — billing history section at the bottom
  </what-built>
  <how-to-verify>
    1. Visit /admin-dashboard — confirm "Billing" appears in the nav bar
    2. Click Billing nav link — list page loads, shows "No invoices yet" or existing invoices
    3. Click "New Invoice" — form loads with tenant dropdown, line items, due date
    4. Add a line item, fill in description/qty/unit price — subtotal updates in real time
    5. Select a tenant, set a due date, submit — redirected to the invoice detail page
    6. Invoice detail shows SINV-0001, line item table, total, status badge DRAFT
    7. Visit /tenants — click any tenant — Billing History card appears at the bottom
    8. From tenant detail, click "+ New Invoice" — form pre-selects that tenant
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. npx tsc --noEmit — 0 errors
2. npx next build — build succeeds (no missing imports)
3. Billing nav link visible in admin header
4. /billing page renders without server errors
5. Invoice detail page shows all line items and correct total
6. Tenant detail billing history section visible
</verification>

<success_criteria>
- All 8 files created/modified
- tsc --noEmit exits 0
- Human verification approved
- Invoice status badges visually distinct (DRAFT gray, SENT blue, PAID green, OVERDUE red, VOID gray)
- Line item subtotal math uses Decimal.js (no floating point)
</success_criteria>

<output>
After completion, create `.planning/phases/25-sysadmin-invoicing-module/25-02-SUMMARY.md`
</output>
