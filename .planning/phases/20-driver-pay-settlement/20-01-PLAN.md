---
phase: 20-driver-pay-settlement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/migrations/20260227000001_add_driver_settlement/migration.sql
  - prisma/schema.prisma
autonomous: true

must_haves:
  truths:
    - "DriverPayConfig table exists with RLS tenant isolation"
    - "DriverSettlement table exists with unique constraint on (driverId, periodStart, periodEnd)"
    - "SettlementLine table exists with tenantId for RLS (no repeat of InvoiceItem bug)"
    - "Load model has distanceMiles Decimal column for per-mile pay calculation"
    - "Prisma client can create/read/update all three new models"
    - "PayrollRecord has optional settlementId FK to DriverSettlement"
    - "Cascade delete: deleting a DriverSettlement deletes its SettlementLines"
  artifacts:
    - path: "prisma/migrations/20260227000001_add_driver_settlement/migration.sql"
      provides: "DriverPayType + SettlementStatus enums, DriverPayConfig + DriverSettlement + SettlementLine tables, distanceMiles on Load, settlementId on PayrollRecord, all indexes + FKs + RLS"
      contains: "tenant_isolation_policy"
    - path: "prisma/schema.prisma"
      provides: "DriverPayConfig, DriverSettlement, SettlementLine models, DriverPayType + SettlementStatus enums, distanceMiles on Load, settlementId on PayrollRecord"
      contains: "model DriverSettlement"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260227000001_add_driver_settlement/migration.sql"
      via: "schema matches migration SQL"
      pattern: "model DriverSettlement"
---

<objective>
Create the database foundation for driver pay settlement: three new models (DriverPayConfig, DriverSettlement, SettlementLine), two new enums (DriverPayType, SettlementStatus), add distanceMiles to Load, add settlementId FK to PayrollRecord, migration SQL with RLS, and Prisma schema updates.

Purpose: All settlement engine logic (Plan 20-02) and PDF generation (Plan 20-03) depend on these models existing.
Output: Migration SQL file + updated schema.prisma with all new models, enums, relations, and back-references on Tenant/User.
</objective>

<execution_context>
@C:/Users/sammy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sammy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-driver-pay-settlement/20-RESEARCH.md
@prisma/schema.prisma
@prisma/migrations/20260218000002_add_crm_invoice_payroll_models/migration.sql
@prisma/migrations/20260226000002_add_rls_missing_tables/migration.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration SQL for settlement models + Load.distanceMiles + PayrollRecord.settlementId</name>
  <files>prisma/migrations/20260227000001_add_driver_settlement/migration.sql</files>
  <action>
Create the migration SQL file following exact project conventions (enums first, tables, indexes+FKs, RLS block). Include:

**Enums (with DO/EXCEPTION idempotent pattern):**
- `DriverPayType` AS ENUM ('PER_MILE', 'PERCENTAGE', 'FLAT')
- `SettlementStatus` AS ENUM ('DRAFT', 'APPROVED', 'PAID')

**New tables:**

1. `DriverPayConfig` — id UUID PK, tenantId UUID NOT NULL, driverId UUID NOT NULL, payType "DriverPayType" NOT NULL, rateValue DECIMAL(10,4) NOT NULL, effectiveFrom TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, createdAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updatedAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. FK to Tenant(id) ON DELETE CASCADE, FK to User(id) ON DELETE CASCADE. Indexes on tenantId, driverId, effectiveFrom.

2. `DriverSettlement` — id UUID PK, tenantId UUID NOT NULL, driverId UUID NOT NULL, periodStart TIMESTAMPTZ NOT NULL, periodEnd TIMESTAMPTZ NOT NULL, status "SettlementStatus" NOT NULL DEFAULT 'DRAFT', totalPay DECIMAL(10,2) NOT NULL, paidAt TIMESTAMPTZ nullable, notes TEXT nullable, createdAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, updatedAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. FK to Tenant(id) ON DELETE CASCADE, FK to User(id) ON DELETE CASCADE. UNIQUE constraint on (driverId, periodStart, periodEnd). Indexes on tenantId, driverId, status, periodStart.

3. `SettlementLine` — id UUID PK, settlementId UUID NOT NULL, tenantId UUID NOT NULL, loadId UUID NOT NULL, description TEXT NOT NULL, miles DECIMAL(10,2) NOT NULL DEFAULT 0, grossRate DECIMAL(12,2) NOT NULL, payAmount DECIMAL(10,2) NOT NULL, createdAt TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP. FK to DriverSettlement(id) ON DELETE CASCADE, FK to Tenant(id) ON DELETE CASCADE, FK to Load(id) ON DELETE CASCADE. Indexes on settlementId, tenantId.

**Alter existing tables:**
- `ALTER TABLE "Load" ADD COLUMN IF NOT EXISTS "distanceMiles" DECIMAL(10,2);` (nullable, no default — dispatchers can fill in manually)
- `ALTER TABLE "PayrollRecord" ADD COLUMN IF NOT EXISTS "settlementId" UUID;`
- Add FK from PayrollRecord.settlementId to DriverSettlement(id) ON DELETE SET NULL
- Add index on PayrollRecord.settlementId

**RLS block (for all 3 new tables):**
Each table gets:
- `ALTER TABLE "X" ENABLE ROW LEVEL SECURITY;`
- `ALTER TABLE "X" FORCE ROW LEVEL SECURITY;`
- `CREATE POLICY tenant_isolation_policy ON "X" FOR ALL USING ("tenantId" = current_tenant_id()) WITH CHECK ("tenantId" = current_tenant_id());`
- `CREATE POLICY bypass_rls_policy ON "X" FOR ALL USING (current_setting('app.bypass_rls', TRUE)::text = 'on');`

IMPORTANT: SettlementLine MUST have tenantId and RLS from the start (learned from InvoiceItem bug in migration 20260226000002).
  </action>
  <verify>Read the migration file and confirm: 2 enum types, 3 CREATE TABLE statements, 2 ALTER TABLE statements (Load + PayrollRecord), RLS policies on all 3 new tables (6 CREATE POLICY statements total), unique index on DriverSettlement(driverId, periodStart, periodEnd).</verify>
  <done>Migration SQL exists with all tables, enums, FKs, indexes, RLS policies, and ALTER statements for Load.distanceMiles and PayrollRecord.settlementId.</done>
</task>

<task type="auto">
  <name>Task 2: Update Prisma schema with new models, enums, and relation back-references</name>
  <files>prisma/schema.prisma</files>
  <action>
Update prisma/schema.prisma to add:

**Two new enums:**
- `enum DriverPayType { PER_MILE PERCENTAGE FLAT }`
- `enum SettlementStatus { DRAFT APPROVED PAID }`

**Three new models** (exact field definitions from research, copy the schema verbatim from 20-RESEARCH.md "Prisma schema for new models" section):
- `DriverPayConfig` — with tenant/driver relations, @@index on tenantId/driverId/effectiveFrom
- `DriverSettlement` — with tenant/driver relations (named "DriverSettlements"), lines SettlementLine[], payrollRecords PayrollRecord[], @@unique on [driverId, periodStart, periodEnd], @@index on tenantId/driverId/status/periodStart
- `SettlementLine` — with settlement/tenant relations, settlement onDelete: Cascade, @@index on settlementId/tenantId

**Update Load model:**
- Add `distanceMiles Decimal? @db.Decimal(10, 2)` field
- Add `settlementLines SettlementLine[]` relation

**Update PayrollRecord model:**
- Add `settlementId String? @db.Uuid`
- Add `settlement DriverSettlement? @relation(fields: [settlementId], references: [id])`

**Update User model:**
- Add `driverPayConfigs DriverPayConfig[]` relation
- Add `driverSettlements DriverSettlement[] @relation(name: "DriverSettlements")` relation

**Update Tenant model:**
- Add `driverPayConfigs DriverPayConfig[]`
- Add `driverSettlements DriverSettlement[]`
- Add `settlementLines SettlementLine[]`

After schema update, run `npx prisma generate` to regenerate the Prisma client and confirm no errors.

CRITICAL: DriverSettlement -> User relation MUST use `@relation(name: "DriverSettlements")` to avoid ambiguity with other User relations. Match the naming pattern used by existing relations like "DriverLoads".
  </action>
  <verify>Run `npx prisma generate` — must succeed with no errors. Run `npx prisma validate` — must succeed. Check that schema.prisma contains: `model DriverPayConfig`, `model DriverSettlement`, `model SettlementLine`, `enum DriverPayType`, `enum SettlementStatus`, `distanceMiles` on Load, `settlementId` on PayrollRecord.</verify>
  <done>Prisma schema has all 3 new models, 2 new enums, distanceMiles on Load, settlementId on PayrollRecord, all back-references on Tenant/User, and prisma generate succeeds.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes with no errors
2. `npx prisma generate` succeeds
3. Migration SQL contains exactly 2 enum types, 3 new tables, 2 ALTER TABLE statements
4. All 3 new tables have RLS (tenant_isolation_policy + bypass_rls_policy = 6 CREATE POLICY)
5. SettlementLine has tenantId column (no repeat of InvoiceItem bug)
6. DriverSettlement has unique constraint on (driverId, periodStart, periodEnd)
7. Load model has distanceMiles Decimal field
8. PayrollRecord has settlementId FK to DriverSettlement
</verification>

<success_criteria>
- Migration SQL file exists with correct structure
- Prisma schema validates and generates successfully
- All new models, enums, and relations are correctly defined
- RLS policies cover all 3 new tables
- Back-references on Tenant and User models added
</success_criteria>

<output>
After completion, create `.planning/phases/20-driver-pay-settlement/20-01-SUMMARY.md`
</output>
