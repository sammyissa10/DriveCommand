---
phase: quick-198
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/support/support-ticket-modal.tsx
  - apps/web/src/components/carrier/loads/LoadForm.tsx
autonomous: true
---

<objective>
Bug 38: Fix floating support button overlapping form action buttons on smaller screens.
Bug 39: Redirect to /carrier/loads after creating a load instead of staying on detail page.

Purpose: Fix UI overlap and navigation flow issues.
Output: Support FAB repositioned, load create redirects to list.
</objective>

<context>
@apps/web/src/components/support/support-ticket-modal.tsx (line 288: fixed bottom-20 lg:bottom-6 right-6 z-50)
@apps/web/src/components/carrier/loads/LoadForm.tsx (line 282-283: router.push to detail page on create)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move support FAB to bottom-left to avoid overlapping form action buttons</name>
  <files>apps/web/src/components/support/support-ticket-modal.tsx</files>
  <action>
On line 288, the support FAB button has className `fixed bottom-20 lg:bottom-6 right-6 z-50 ...`.

Change `right-6` to `left-6` to move the button to the bottom-left corner. This avoids overlapping the Create/Cancel buttons that are typically right-aligned at the bottom of forms.

Keep `bottom-20 lg:bottom-6` and `z-50` unchanged — the bottom-20 on mobile already accounts for bottom nav, and z-50 is needed for layering.

Also update the comment on line 285 from `{/* Fixed support button — bottom-right */}` to `{/* Fixed support button — bottom-left */}`.
  </action>
  <verify>Open any form page (e.g., /carrier/loads/new). Confirm the support FAB appears in the bottom-left corner and does not overlap the Create/Cancel buttons. Test on both desktop and mobile viewport widths.</verify>
  <done>Support FAB is positioned bottom-left, no longer overlaps right-aligned form action buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect to /carrier/loads list after creating a load</name>
  <files>apps/web/src/components/carrier/loads/LoadForm.tsx</files>
  <action>
In LoadForm.tsx, the submit handler (line 282-283) currently redirects to the load detail page after creation:

```
if (mode === 'create') {
  router.push(`/carrier/loads/${savedId}`);
}
```

Change this to redirect to the loads list instead:

```
if (mode === 'create') {
  router.push('/carrier/loads');
}
```

The `savedId` variable can remain (it's used for the json parsing) but the redirect target changes. The edit mode redirect on line 285 (`router.push('/carrier/loads')`) already goes to the list, so this makes both consistent.
  </action>
  <verify>Create a new load via /carrier/loads/new. After clicking "Create Load" and seeing the success toast, confirm you are redirected to /carrier/loads (the loads list) instead of the load detail page.</verify>
  <done>Creating a load redirects to /carrier/loads list page.</done>
</task>

</tasks>

<verification>
- Support FAB is in bottom-left on all pages
- Load creation redirects to /carrier/loads
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Bug 38 and Bug 39 are resolved. FAB no longer overlaps action buttons. Load create flow ends at the list page.
</success_criteria>
