---
phase: quick-198
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/carrier/templates/RouteTemplateForm.tsx
  - apps/web/src/app/(owner)/carrier/templates/[id]/page.tsx
  - apps/web/src/lib/carrier/route-templates.ts
autonomous: true
---

<objective>
Bug 25: Replace raw RRULE text input with day picker UI.
Bug 32: Add active/inactive status indicator + toggle to route template detail page.

Purpose: Make recurrence rules user-friendly and show template activation status.
Output: Updated RouteTemplateForm with day picker, updated template detail page with status toggle.
</objective>

<context>
@apps/web/src/components/carrier/templates/RouteTemplateForm.tsx
@apps/web/src/app/(owner)/carrier/templates/[id]/page.tsx
@apps/web/src/lib/carrier/route-templates.ts
@apps/web/prisma/schema.prisma (RouteTemplate model — has `active Boolean @default(true)`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace RRULE text input with visual day picker in RouteTemplateForm</name>
  <files>apps/web/src/components/carrier/templates/RouteTemplateForm.tsx</files>
  <action>
Replace the plain `<Input>` for recurrenceRule (lines 419-442) with a visual RRULE builder UI. The builder should appear when scheduleType is `fixed_days` or `frequency`.

**Frequency selector:**
- Add a `<Select>` dropdown with options: Daily, Weekly, Monthly
- Default to Weekly when scheduleType is `fixed_days`, Daily when scheduleType is `frequency`

**Day picker (shown when frequency is Weekly):**
- Render 7 toggle buttons in a row: Mon, Tue, Wed, Thu, Fri, Sat, Sun
- Each button toggles on/off with visual active state (e.g., `bg-primary text-primary-foreground` when active, `bg-muted` when inactive)
- Use standard border-radius and consistent sizing with the rest of the form

**Monthly day picker (shown when frequency is Monthly):**
- Show a text input for comma-separated day numbers (e.g., "1,15")

**RRULE auto-generation:**
- Build the RRULE string automatically from the UI selections:
  - Daily: `FREQ=DAILY`
  - Weekly with days: `FREQ=WEEKLY;BYDAY=MO,TU,WE` (map day indices to RRULE day codes: MO,TU,WE,TH,FR,SA,SU)
  - Monthly with days: `FREQ=MONTHLY;BYMONTHDAY=1,15`
- Store the generated string in the existing `recurrenceRule` state variable
- Show the generated RRULE string as read-only helper text below the day picker: `<p className="text-xs text-muted-foreground font-mono">Generated: FREQ=WEEKLY;BYDAY=MO,WE,FR</p>`

**Initialization from existing RRULE (edit mode):**
- Parse the existing `recurrenceRule` string on mount to set the frequency and selected days
- Parse FREQ= to set frequency dropdown
- Parse BYDAY= to set selected day toggles
- Parse BYMONTHDAY= to set monthly days input
- If parsing fails, fall back to showing the raw input as editable text

**Remove:**
- Remove the old `handleRecurrenceRuleBlur` validation function
- Remove `recurrenceRuleError` state (validation is now implicit since UI builds valid RRULE)

**Keep:**
- Keep the timezone selector and departure time fields as-is (they follow the recurrence section)
  </action>
  <verify>Open the route template create/edit form. Confirm: frequency dropdown appears, day toggle buttons render, clicking days updates the generated RRULE string shown below, the RRULE string is well-formed (e.g., FREQ=WEEKLY;BYDAY=MO,WE,FR). In edit mode, existing RRULE is parsed into the correct UI state.</verify>
  <done>RRULE builder renders with frequency selector + day toggles, auto-generates valid RRULE strings, and round-trips existing RRULE data in edit mode.</done>
</task>

<task type="auto">
  <name>Task 2: Add active/inactive status indicator and toggle to template detail page</name>
  <files>apps/web/src/app/(owner)/carrier/templates/[id]/page.tsx</files>
  <action>
The template detail page (`[id]/page.tsx`) currently shows the edit form + dispatch preview. Add an active/inactive status indicator to the page header area.

**Status indicator in header:**
- After the `<h1>Edit Route Template</h1>` and template name `<p>`, add a status badge:
  - Active: green badge `<span className="inline-flex items-center rounded-full bg-green-100 text-green-700 dark:bg-green-900 dark:text-green-200 px-2.5 py-0.5 text-xs font-medium">Active</span>`
  - Inactive: red/slate badge `<span className="inline-flex items-center rounded-full bg-red-100 text-red-700 dark:bg-red-900 dark:text-red-200 px-2.5 py-0.5 text-xs font-medium">Inactive</span>`

**Inline toggle:**
- Next to the badge, add a `<Switch>` component (from shadcn/ui, already imported in the form) that toggles active/inactive
- The toggle should call `PATCH /api/v1/carrier/route-templates/{id}` with `{ active: true/false }`
- Since this page is a server component, extract the toggle into a small client component (e.g., `TemplateStatusToggle`) defined inline or at the top of the file
- The client component receives `templateId: string` and `initialActive: boolean` as props
- On toggle: call `fetch(\`/api/v1/carrier/route-templates/${templateId}\`, { method: 'PATCH', body: JSON.stringify({ active: newValue }) })`, show toast on success/error
- Use `useRouter().refresh()` after successful toggle to update server-rendered content

**Verify the PATCH endpoint exists:**
- Check `apps/web/src/app/api/v1/carrier/route-templates/[id]/route.ts` — it should already have a PATCH handler that calls `updateRouteTemplate`. The `active` field should be accepted since `RouteTemplateUpdateInput = Partial<RouteTemplateCreateInput>` — but `active` is NOT in `RouteTemplateCreateInput`. So also update `apps/web/src/lib/carrier/route-templates.ts`:
  - Add `active?: boolean` to `RouteTemplateUpdateInput` (cannot just use Partial since active isn't in create input)
  - Change `RouteTemplateUpdateInput` from `Partial<RouteTemplateCreateInput>` to `Partial<RouteTemplateCreateInput> & { active?: boolean }`
  </action>
  <verify>Open a route template detail page. Confirm: active/inactive badge shows correct state, toggle switch works, toggling updates the badge and persists to DB (refresh page to confirm).</verify>
  <done>Template detail page shows active/inactive badge with inline toggle that persists via PATCH API.</done>
</task>

</tasks>

<verification>
- Route template form shows visual day picker instead of raw RRULE input
- Generated RRULE string appears as read-only helper text
- Edit mode correctly parses existing RRULE into UI state
- Template detail page shows active/inactive badge with working toggle
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Bug 25 and Bug 32 are resolved. RRULE entry is user-friendly with auto-generation. Template status is visible and toggleable inline.
</success_criteria>
