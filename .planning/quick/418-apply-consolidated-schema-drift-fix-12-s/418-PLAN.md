# Quick Task 418 — Apply Consolidated Schema Drift Fix

**Description:** Apply consolidated schema drift fix — 12 soft-delete columns across 6 models via Supabase MCP

**Source:** apps/web/scripts/audit/417-DRIFT-FIX-DRAFT.sql (generated by quick-417 full-schema-drift-scan)

**Models affected:**
- Route (table: `Route`) — deletedAt, deletedById
- CarrierClient (table: `clients`) — deleted_at, deleted_by_id
- CarrierContract (table: `contracts`) — deleted_at, deleted_by_id
- CarrierDriver (table: `carrier_drivers`) — deleted_at, deleted_by_id
- CarrierTruck (table: `carrier_trucks`) — deleted_at, deleted_by_id
- CarrierLoad (table: `loads`) — deleted_at, deleted_by_id

---

## Task 1 — Create migration.sql and rollback.sql

**File:** `apps/web/prisma/migrations/20260530000002_add_soft_delete_columns_drift_fix/migration.sql`

Contents = draft SQL + self-validation DO $$ block before COMMIT.

**File:** `apps/web/prisma/migrations/20260530000002_add_soft_delete_columns_drift_fix/rollback.sql`

Contents = 12 DROP COLUMN IF EXISTS statements wrapped in BEGIN/COMMIT with data-loss warning.

## Task 2 — Apply migration via Supabase MCP

Apply migration.sql to production Supabase via `mcp__claude_ai_Supabase__apply_migration`.

## Task 3 — Re-run drift scan and confirm zero drift

Run `npx ts-node --project apps/web/tsconfig.json apps/web/scripts/audit/full-schema-drift-scan.ts` and confirm 0 drifted models.
