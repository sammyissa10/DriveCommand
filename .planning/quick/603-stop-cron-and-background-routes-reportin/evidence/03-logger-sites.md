# 03 — `logger.error` object-literal-in-slot-2 sites, by name

**Generated by** `evidence/scripts/scan-logger-arity.mjs`, run from `apps/web`.

**76 sites across 36 files**, out of 621 total `logger.error` calls.

Each row is `file:line`, then the second argument as written. The bucket column is filled
in `05-logger-classification.md` (Task 5).

### `src/app/(admin)/actions/tenants.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 116 | `{ tenantId: tenant.id, error: seedError instanceof Error ? seedError.message : String(seedError), }` |

### `src/app/(driver)/error.tsx` — 1

| line | slot-2 argument as written |
|---|---|
| 16 | `{ message: error.message, digest: error.digest, stack: error.stack, }` |

### `src/app/(owner)/actions/customers.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 69 | `{ customerId: customer.id, err }` |

### `src/app/(owner)/actions/drivers.ts` — 2

| line | slot-2 argument as written |
|---|---|
| 158 | `{ invitationId: invitation.id, err }` |
| 207 | `{ invitationId: invitation.id, err }` |

### `src/app/(owner)/actions/trucks.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 124 | `{ truckId: truck.id, err }` |

### `src/app/(owner)/error.tsx` — 1

| line | slot-2 argument as written |
|---|---|
| 16 | `{ message: error.message, digest: error.digest, stack: error.stack, }` |

### `src/app/api/admin/tenants/[id]/users/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 39 | `{ error: message }` |

### `src/app/api/admin/users/[id]/role/route.ts` — 3

| line | slot-2 argument as written |
|---|---|
| 65 | `{ userId, error: authError.message, }` |
| 73 | `{ userId, error: message }` |
| 80 | `{ error: message }` |

### `src/app/api/auth/admin-reset-password/route.ts` — 4

| line | slot-2 argument as written |
|---|---|
| 35 | `{ email, error: error.message }` |
| 52 | `{ error: listError.message }` |
| 64 | `{ authUserId: authUser.id, error: error.message }` |
| 75 | `{ error: message }` |

### `src/app/api/cron/carrier-compliance-alerts/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 100 | `{ tenantId: tenant.id, error: err, }` |

### `src/app/api/cron/cleanup-quarantine/route.ts` — 2

| line | slot-2 argument as written |
|---|---|
| 87 | `{ key: obj.Key, error: String(err), }` |
| 97 | `{ error: String(err) }` |

### `src/app/api/cron/purge-deleted/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 45 | `{ error: String(err) }` |

### `src/app/api/cron/workflow-digest/route.ts` — 3

| line | slot-2 argument as written |
|---|---|
| 62 | `{ error: err }` |
| 161 | `{ error: emailErr }` |
| 194 | `{ error: err }` |

### `src/app/api/cron/workflow-notifications/route.ts` — 4

| line | slot-2 argument as written |
|---|---|
| 106 | `{ err }` |
| 110 | `{ err }` |
| 156 | `{ err }` |
| 163 | `{ err }` |

### `src/app/api/driver/gps-ping/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 122 | `{ error }` |

### `src/app/api/driver/notifications/mark-read/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 72 | `{ orgId, error: err }` |

### `src/app/api/driver/notifications/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 65 | `{ orgId, error: err }` |

### `src/app/api/driver/stops/[stopId]/documents/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 168 | `{ status: uploadRes.status, stopId, }` |

### `src/app/api/email-confirm/[token]/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 61 | `{ error: err }` |

### `src/app/api/mobile/carrier/driver/stops/[stopId]/documents/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 149 | `{ status: uploadRes.status, stopId, }` |

### `src/app/api/v1/carrier/clients/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 118 | `{ clientId: client.id, err }` |

### `src/app/api/v1/carrier/fleet/drivers/route.ts` — 2

| line | slot-2 argument as written |
|---|---|
| 103 | `{ driverId: carrierDriver.id, err }` |
| 113 | `{ driverId: carrierDriver.id, err }` |

### `src/app/api/v1/carrier/fleet/trucks/route.ts` — 2

| line | slot-2 argument as written |
|---|---|
| 110 | `{ truckId: carrierTruck.id, err }` |
| 121 | `{ truckId: carrierTruck.id, err }` |

### `src/app/api/v1/carrier/loads/[id]/cancel/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 85 | `{ loadId, err }` |

### `src/app/api/v1/carrier/loads/[id]/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 102 | `{ error: err instanceof Error ? err.message : JSON.stringify(err, Object.getOwnPropertyNames(err as object)), stack: err instanceof Error ? err.sta...` |

### `src/app/api/v1/carrier/notifications/mark-read/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 56 | `{ orgId, error: err }` |

### `src/app/api/v1/carrier/notifications/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 63 | `{ orgId: session.tenantId, error: err }` |

### `src/app/api/v1/carrier/trips/[id]/loads/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 50 | `{ tripId, loadId, err }` |

### `src/app/api/v1/carrier/trips/[id]/reorder/route.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 50 | `{ tripId, err }` |

### `src/lib/carrier/dispatch-generator.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 469 | `{ templateId, dateStr, err }` |

### `src/lib/carrier/in-app-notifications.ts` — 1

| line | slot-2 argument as written |
|---|---|
| 81 | `{ orgId, type, entityType, entityId, error: err, }` |

### `src/lib/carrier/notifications.ts` — 9

| line | slot-2 argument as written |
|---|---|
| 339 | `{ orgId, loadId, error: err }` |
| 472 | `{ orgId, stopId, error: err }` |
| 561 | `{ orgId, payRecordId, error: err }` |
| 688 | `{ orgId, loadId, error: err }` |
| 783 | `{ orgId, error: err }` |
| 965 | `{ orgId, loadId, error: err }` |
| 1104 | `{ orgId, loadId, error: err }` |
| 1208 | `{ orgId, loadId, error: err }` |
| 1315 | `{ orgId, dispatchId, changeType, error: err }` |

### `src/lib/carrier/trips.ts` — 3

| line | slot-2 argument as written |
|---|---|
| 728 | `{ dispatchId: id, err }` |
| 938 | `{ dispatchId: id, err }` |
| 1115 | `{ loadId, err }` |

### `src/server/services/workflows/computeDispatchReadiness.ts` — 2

| line | slot-2 argument as written |
|---|---|
| 81 | `{ instanceId, err }` |
| 94 | `{ instanceId, err }` |

### `src/server/services/workflows/fireEvent.ts` — 2

| line | slot-2 argument as written |
|---|---|
| 58 | `{ event, tenantId, err }` |
| 81 | `{ event, triggerId: trigger.id, playbookId: trigger.playbookId, err, }` |

### `src/server/services/workflows/notifications.ts` — 15

| line | slot-2 argument as written |
|---|---|
| 142 | `{ err, ...data }` |
| 201 | `{ err, assignedUserId }` |
| 215 | `{ err, stepInstanceId }` |
| 286 | `{ err, recipientId }` |
| 301 | `{ err, stepInstanceId }` |
| 370 | `{ err, dispatcherId: dispatcher.id }` |
| 385 | `{ err, playbookInstanceId }` |
| 418 | `{ err, dispatcherId: dispatcher.id }` |
| 432 | `{ err, userId, playbookInstanceId }` |
| 488 | `{ err, dispatcherId: dispatcher.id }` |
| 503 | `{ err, stepInstanceId, recipientRole: args.recipientRole }` |
| 540 | `{ err, approverUserId }` |
| 554 | `{ err, stepInstanceId }` |
| 620 | `{ err, adminEmail: admin.email }` |
| 635 | `{ err, playbookInstanceId }` |
