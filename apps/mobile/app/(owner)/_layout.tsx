import { Stack } from 'expo-router'

// Placeholder owner tab navigator — full implementation in Phase 35.
export default function OwnerLayout() {
  return <Stack screenOptions={{ headerShown: false }} />
}
