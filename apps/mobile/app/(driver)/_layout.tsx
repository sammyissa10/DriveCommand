import { Stack } from 'expo-router'

// Placeholder driver tab navigator — full implementation in Phase 31.
export default function DriverLayout() {
  return <Stack screenOptions={{ headerShown: false }} />
}
