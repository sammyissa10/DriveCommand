import { View, Text } from 'react-native'

// Placeholder login screen — full implementation in Phase 30.
export default function LoginScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-slate-900">
      <Text className="text-white text-2xl font-semibold">DriveCommand</Text>
      <Text className="text-slate-400 mt-2">Login coming in Phase 30</Text>
    </View>
  )
}
