# 406b — RLS Blocker Resolution Findings
Date: 2026-05-27T16:47:23.023Z

> Generated by `apps/web/scripts/audit/406b-resolve-blockers.ts` — READ-ONLY diagnostic.
> No DDL or DML was executed against the database.

## Blocker A — Tenant Table SELECT Policy

**Findings:**

- Tenant table stats: seq_scan=12572, idx_scan=760, n_live_tup=18
- NOT DETERMINABLE: pg_proc functions referencing Tenant — 
Invalid `prisma.$queryRawUnsafe()` invocation:


Raw query failed. Code: `42809`. Message: `"array_agg" is an aggregate function`
- Codebase grep: no prisma.tenant.* calls found (likely camelCase prisma.tenant or via $queryRaw)
- Codebase grep: no prisma.tenant calls found
- Codebase grep: no raw SQL FROM "Tenant" found
- WHERE clause analysis: no code-level Tenant reads found — callers appear to use explicit id

**Verdict:** `TENANT_ALL_EXPLICIT_ID`

**RECOMMENDED ACTION:** No ORM or raw SQL Tenant reads found — all reads appear to pass explicit id. Safe to use id-based RLS policy.

**Recommended Policy SQL:**

```sql
-- Recommended policy for Tenant table
CREATE POLICY tenant_select_own ON "Tenant"
  FOR SELECT
  USING (id = (auth.jwt() ->> 'tenant_id')::uuid);
```

---

## Blocker B — User.id vs auth.users.id

**Findings:**

- JOIN public.User ↔ auth.users (LIMIT 20): 20 rows — ids_match=true: 20, ids_match=false: 0, no auth row: 0
- FK: User.tenantId → public.Tenant.id
- No non-internal triggers found on auth.users

**Verdict:** `IDS_MATCH`

**RECOMMENDED ACTION:** All sampled public.User.id values match auth.users.id — safe to use auth.uid() in User-scoped RLS policies.

**Recommended Policy SQL:**

```sql
-- User-scoped SELECT policy (safe — ids confirmed to match):
CREATE POLICY user_select_own ON "User"
  FOR SELECT
  USING (id = auth.uid());
```

---

## Blocker C — JWT org_id Claim Population

**Findings:**

- Codebase grep auth.jwt(): no matches found in apps/web/src or packages
- Codebase grep org_id: no matches found — org_id is NOT used anywhere in the codebase
- Codebase grep app_metadata/user_metadata: no matches found
-   sam***@gmail.com: app_metadata keys=[role, provider, tenantId, providers, permissions, isSystemAdmin] user_metadata keys=[lastName, firstName, email_verified]
-   own***@test.com: app_metadata keys=[role, provider, tenantId, providers] user_metadata keys=[email_verified]
-   own***@test.com: app_metadata keys=[role, provider, tenantId, providers] user_metadata keys=[email_verified]
-   man***@test.com: app_metadata keys=[role, provider, tenantId, providers] user_metadata keys=[email_verified]
-   dri***@test.com: app_metadata keys=[role, provider, tenantId, providers] user_metadata keys=[email_verified]
- org_id in app_metadata: 0/5 users, in user_metadata: 0/5 users

**Verdict:** `JWT_ORG_ID_MISSING`

**RECOMMENDED ACTION:** org_id is NOT populated in any JWT metadata and is NOT used in any codebase policy. The existing codebase uses tenant_id (not org_id) in app_metadata. Carrier-table policies should reference tenant_id, NOT org_id. Action: use (auth.jwt() ->> 'tenant_id')::uuid in all carrier policies.

**Recommended Policy SQL:**

```sql
-- Carrier-table policy using tenant_id (which IS in app_metadata):
CREATE POLICY carrier_select_own ON "carrier_documents"
  FOR SELECT
  USING (
    "tenantId" = (auth.jwt() ->> 'tenant_id')::uuid
  );

-- Backfill tenant_id into app_metadata if not already present:
-- (run via Supabase Admin API or admin server action, not direct SQL)
```

---

## Final Verdict

**RESULT: GO**

All three blockers are resolved. The FORCE RLS migration design can proceed.

Next step: draft the migration SQL using the recommended policies above.
